blocking: 0

# r1 verdict — hide-and-seek

Head: `5c819abc2b95b7a12ac49e3ebc8272321c5b027e` (main, current checkout HEAD; review was written at `a6d3a86`, 15 fix commits earlier)
Checklist: run brief §ACCEPTANCE CHECKLIST (prompts/30-review-loop.md shape, 15 items + parallel-batch rule)
Independent read written before reading fixes: **yes** — I read the repo, the design note, the CI run and logs, and wrote my own notes before opening `r1-review.md`. Per the coordinator's brief I did **not** read `r1-fixes.md` at all, so the fixer-report audit below is against the reviewer's findings re-verified at head, not against the fixer's self-report.

CI evidence: run **33131110037** on `main` at the reviewed sha, conclusion **success**, all four jobs green (`test`, `docker-smoke`, `wasm-viewer` incl. "Load the bundle in a real browser", `renderer-fixture` incl. "Render a full-cap remark on every seat"). None is `continue-on-error`; `wasm-viewer` has `needs: docker-smoke` (`ci.yml:243`).

## Standing blocking findings

None. Every reviewer finding is either fixed at the current head or was non-blocking; my own checklist pass found no item that fails at head.

## Refuted

All four of the reviewer's blocking findings were true at `a6d3a86` and are **fixed at head** — refuted as standing findings, per the rule that a finding fixed since the review is refuted, not standing.

### F1 — `knownEnemy` has no success path → REFUTED (fixed at head)
- Evidence: `src/hns/control.nim:339-340` at `5c819ab` — the proc now ends
  `(true, ctl.lastSeenX[cogIndex], ctl.lastSeenY[cogIndex], ctl.lastSeenIndex[cogIndex], age)`.
  New test `tests/test_hns_control.nim:139-181` (`knownEnemyReportsASighting`) asserts a seeker looking at a hider gets `known == true`, the right index/position/freshness, that the memory expires after `HuntMemoryTicks`, and that the baseline then emits `intChase` at the sighted point. The tuning record was re-measured with the fix (`tools/ci/baseline_tuning.json`: `chaseRadius` now moves the margin; grid margins are no longer flat triples).

### F2 — shout bubble placed unclamped above the cog → REFUTED (fixed at head)
- Evidence: `src/hns/global.nim:589-612` — `shoutBubblePlacement` (the starter's proc, ported) clamps both axes into the board and flips the bubble below the cog when it does not fit above (`if tailTipY - bubbleH >= 0: … else: tailTipY + ShoutFloat`, then `clamp(…, 0, maxY)`); the draw site uses it (`global.nim:794`). Test `tests/test_hns_viewer.nim:222-251` (`shoutBubblesStayInsideTheBoard`) feeds a full-cap 10-rune say at all four board corners, the centre, and every committed pocket anchor (incl. the y = 40 ones the review named) and asserts the placed box is wholly inside the board.

### F3 — no worst-case renderer fixture → REFUTED (fixed at head)
- Evidence: `tools/ci/renderer_fixture.html` (315 lines) + `tools/ci/renderer_fixture_state.json` (frame built by the shipped `src/hns/broadcast.nim`, regenerated/pinned by `tests/test_hns_renderer_fixture.nim`) + `ci.yml:412-459` `renderer-fixture` job driving it with `viewer_smoke.mjs --strict-text-bounds`. The fixture loads the real `client/replay_broadcast.html` in an iframe, shims only the wasm entry, asserts its own strings are full-cap (6 × 10-rune says, 6 × 96-rune radios — `renderer_fixture.html:235-254`), and at 360/720/1280 px asserts each of the twelve strings reaches the DOM full-length, inside the frame, un-ellipsized (`checkFullLength`/`checkInsideFrame`), setting `data-replay-loaded`/`data-replay-error` accordingly. CI log at head: `{"loaded":true,…}` and `ok at 360px/720px/1280px` implied by the loaded signal (step green, run 33131110037).

### F4 — `results.stopDetail` untruncated → REFUTED (fixed at head)
- Evidence: `src/hns/sim.nim:645-651` — `forceFaultStop` now does `sim.stopDetail = sanitizeLine(detail, MaxFallbackDetailRunes)` (200-rune, rune-boundary cut), applied on record and playback alike since both go through this proc.

### Reviewer's non-blocking findings, re-verified at head
- **F5** (byte slice at `decide.nim:475-476`) — still present; reviewer's own trace stands: the broken tail cannot reach the replay (a byte-cut string is ≥1024 runes, every error path cuts to ≤160 runes first). Non-defect, agreed.
- **F6** (tuning) — partially fixed: the sweep was re-measured post-F1, the second axis is now `chaseRadius` (which moves the margin), the test reads the band from the harness (`MarginLo/MarginHi`, `tools/tune_baselines.nim:30-31`) instead of the record itself, asserts ≥3 distinct grid margins, and `ci.yml:169-177` re-runs `tune_baselines.nim --check` in CI. **Residue, stated plainly below** (§Non-blocking observations 1): the recorded margin is **−178** — `burrow` still loses to `scatter` — against the note's [+80, +400] target. Not a checklist item; the band has been [-400, 400] since the fork commit, so no tolerance was widened during the run.
- **F7** (held object not dropped at phase change / game end) — fixed: `src/hns/sim.nim:333` `dropAllHeld`, called at game end (`:349`) and at the prep→hunt transition (`:544`).
- **F8** (blocked-push velocity) — fixed: `src/hns/motion.nim:221-231` zeroes `velX`/`velY` on the blocked axis for the held pair.
- **F9** (`broadcast_core.js` note-vs-code) — still true (file is starter's ± 4 rename lines; the note describes a fork that doesn't exist). Doc drift only; byte-near-identity is the *stronger* provenance for checklist 14.
- **F10** (page is not "starter bytes then append") — still true as a note-vs-test drift; checklist 14 itself passes (see pass table).
- **F11** (unclipped cones, per-frame rebuild) — fixed: `src/hns/global.nim:482-530` clips the wedge against the cog's own fov cache (`fovVisibleAt` per cell) and the dedup key is the fov cell + aim, paying for the sprite once per change (commits `f66cc7d`, `5c819ab`); `tests/test_hns_viewer.nim:253-285` (`theVisionConeStopsAtAWall`) asserts alpha is present before and zero beyond a wall.
- **F12** (airborne float feeding hashed state, outside the determinism grep) — addressed: `tests/test_hns_determinism.nim:55-118` now sweeps `src/hns/vision.nim` line-by-line and fails on any float-bearing line not in the enumerated whitelist. The native↔wasm behaviour of the whitelisted airborne expression remains hypothetical (observation 5 below).
- **F13** (note's test-12 clauses unasserted) — fixed: `tests/test_hns_sim.nim:528-548` asserts `win == (scorePermille > 0)` and the all-zero-margin draw.
- **F14** (dead paintbot chrome) — addressed: commit `630701d` asserts the state stream/fixture frame never carries the paintball keys (`lives`, flags, `capture` beats…), so the surviving inherited branches cannot light up.
- **F15** (missing CI gates) — fixed: `ci.yml:160-167` runs the installed coworld CLI validator (`tools/ci/validate_manifest.py`, coworld==0.1.43), `:169-177` the tuning `--check`, `:152-158` `page_smoke.mjs` directly, `:352-366` `tools/wasm_replay_smoke.cjs` over the smoke replay. All green at head.
- **F16** (`docker_smoke.sh` doesn't fail on `reason == "fault"`, no expected-key set) — **still true at head** (`docker_smoke.sh:306-308` prints the reason). Non-blocking, agreed: the script is the shared template with only the three sanctioned substitutions, which is what checklist 6 requires; the note over-claims. The head smoke ended `reason=complete` (log line, run 33131110037).
- **F17** (no shout jitter) — fixed: `src/hns/sim.nim:653-669` `shoutHeardAt` (deterministic, pure, clamped), used at `decide.nim:193`; tested `tests/test_hns_control.nim:184-225` including "no seat is told the exact pixel".
- **F18** (re-derive never covers a shout) — fixed: `tests/test_hns_engine_support.nim:51-65` forces a shout per cog on turn 1, applied and written exactly as `server.nim` does; `tests/test_hns_replay.nim:35-40` asserts the recording carries ≥6 shouts so `mismatch < 0` covers the hashed shout path.
- **F19** (three weak sim assertions) — fixed: the vault block searches all three rooms for a clear span and fails if none exists (assertions no longer conditional); the sealed scan has the walled-in positive case, the unlocked-wall case and the gap case (`aWalledInHiderIsSealedAndOnlyWhileTheWallHolds`); keep-clear is tested through driven pushes.
- **F20** (cosmetics) — fixed in `04965da` (no `holdline` comment survives; `installOrder` no longer takes-and-discards).

## Checklist pass (independent)

| item | status | evidence (path:line or run) |
|---|---|---|
| 1. CI green, no test loosened | PASS | `gh run list`: run **33131110037**, `ci.yml`, branch main, headSha `5c819ab…`, conclusion **success**. `git log -p --since=2026-08-27T19:33Z -- tests/` (19 commits, the repo's whole test history): every deleted assertion was replaced by a strictly stronger one in the same hunk (vault: conditional→unconditional; tuning: self-referential band→harness band + distinctness; summary: same check, richer message). No `skip`/`xfail`, no widened tolerance, no test file removed. The tuning band `[-400,400]` is unchanged since the fork commit `501a9f5`. |
| 2. Replay re-derivation | PASS | `src/hns/replays.nim` `stepReplay`→`checkReplayHash` compares `gameHash` at every recorded tick; `tests/test_hns_replay.nim:26-70` record→re-derive for `full_time`, `wall_clock`, `sim_fault`, asserting `mismatch < 0`; viewer builds frames from the same re-simulation (`replay-viewer/hns_replay.nim` imports the same sim; `initReplayRuntime`/`advanceReplayFrame`). |
| 3. Static viewer | PASS | `coworld_manifest_template.json` `game.replay_viewer = {"bundle":"static-replay-viewer"}`; `tools/build_replay_viewer.sh` mode 100755, exec bit asserted `ci.yml:256-267`, invoked by path `:280`; no `/client/replay` in the manifest; the bundle fetches only its assets + the `?replay=` URL (`static_replay_worker.js:113`). |
| 4. Both name spaces | PASS | `src/hns/roster.nim:46-52` `cogAlias` = role + `IdentityNames`; observations carry aliases only (`decide.nim:126-…`, doc comment + grep); real names in `results.names`, replay joins, and the viewer (endcard policy subheaders `replay_broadcast.html:2235` `teamHeadline(pol)`, pip titles `:1769`, `rosterName` fallbacks). `showPlayerLabels:false` in both variants + fixture. |
| 5. Degrade-never-hang | PASS | `attempt1Ms`/`retryMs` → `CURLOPT_TIMEOUT` (`decide.nim:465-466`); ≤2 attempts (`:439`); monotonic `turnBudgetMs` (`:442`); rate floor sleeps ≤ `turnSpacingMs` (`:418-421`); rolling rate guard never sleeps (`:318-331`); budget guard settles early (`:375-382`, tested `test_hns_engine.nim:138-166`); lobby bounded, dead seats don't stop the clock (tested `:85-125`); engine stop at 660 s ≤ 720 s written as the load-bearing stop record (`server.nim:1352-1379`); frame limiter sleeps ≤2 ms/iter (`server.nim:949-960`). |
| 6. num_agents | PASS | `num_agents: 6` in both variants' `game_config` and `certification.game_config` (read from the JSON); `docker_smoke.sh:106-151` enforces all four invariants + `SMOKE_SEATS` cross-check; `grep 'SEAT-COUNT'` over the full head-run log: **0 hits**; `smoke OK: seats=6 … reason=complete`. |
| 7. Scripted baseline full episodes | PASS | `tests/test_hns_engine.nim:10-37` all-scripted episode to natural end, `reason == "complete"`, `endRule == "full_time"`, zero-sum; `tests/test_hns_control.nim:35-107` bounds every baseline order and every compiled mask; tuned with a grid harness: `tools/tune_baselines.nim` + `tools/ci/baseline_tuning.json`, re-run in CI with `--check` (`ci.yml:177`, step green). Margin-sign residue noted below. |
| 8. LLM reply handling | PASS | `extractJsonObject` tolerant of prose/fences (`directives.nim:118-157`); retry exactly once (`decide.nim:439` `attempt < 2`); fallback to `burrow` via the same proc the baseline uses (`burrowFor`, `:308-316`); every fallback recorded with a cause (`fallbackRecord`, causes {timeout, transport_error, throttled, parse_error, no_credentials, budget_guard, rate_guard}) and counted in `results.fallbackTurns`. |
| 9. Rune-safe truncation | PASS | `truncateRunes`/`runeSubStr` the single cut (`directives.nim:70-77`); `say`/`radio`/`notes`/prompt/policy label/fallback detail/provider bodies all cut on runes; `stopDetail` now cut in `forceFaultStop` (`sim.nim:645-651`); multi-byte-at-cap tests `test_hns_control.nim` (emoji at each cap, `validateUtf8`) and `test_hns_replay.nim:95-149` (full-cap emoji round-trip through `replay_summary.py` under a strict parser). |
| 10. Manifest validates | PASS | `game.docs` = readme(text) + 3 pages(id/title/content text); `game.protocols` carries both `player` and `global` as `{"type":"uri","value":…}` objects (read from the JSON); the installed coworld CLI (0.1.43) validates it in CI (`ci.yml:160-167`, green). |
| 11. Viewer legible at 360 px | PASS | `.plate-name { flex: 1 1 auto; min-width: 3.2em; overflow: hidden; text-overflow: ellipsis }` (`replay_broadcast.html:2678-2683`); labels hidden under `.tiny` (`:2725-2727`), toggled at `boardW <= 620` (`:2620`, the starter's inherited threshold — nuance noted below); at 360 px the board letterboxes whole (fixed 720×400, `#viewpanel` removed). |
| 12. Release order and scaffold | PASS | `coworld-release.yml`: build (`:159`) → certify (`:173`) → upload-policies (`:216`) → upload-coworld (`:314`) → secret put (`:410`); all three workflows present; both scripts mode 100755 (`git ls-files -s`); `policies.json`: 2 `PLAYER_PROMPT` champions + 2 scripted fillers, champion #2 carries `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`; the placeholder gate grep over the five named files **finds nothing** (I ran it at head — exit 1 from grep, gate exits 0). |
| 13. Viewer executes | PASS | `wasm-viewer` green at head incl. "Load the bundle in a real browser" (run 33131110037); `needs: docker-smoke` (`ci.yml:243`); loads the smoke replay; `soak: 10s of playback kept advancing ("1 / 948" -> "161 / 948" -> "199 / 948")`; scrub readouts differ at 0/50/100 %. Markers: `data-replay-loaded` in the `'loaded'` branch (`static_replay.js:161`), `data-replay-error` in `showFailure` (`:14-20`), both the shell's own paths. Link flags/bootstrap same starter: `config.nims` has no `MODULARIZE`/`EXPORT_NAME` (diff vs starter = identifier renames only), worker uses `Module.onRuntimeInitialized` + `importScripts(…, './hns_replay.js')` — the correct non-modularized pairing, and the smoke's `loaded: true` is the proof. |
| 14. Chrome is the starter's | PASS | `client/chrome_common.js` **byte-identical** to `/workspace/starters/coworld-ctf/client/chrome_common.js` (sha256 equal, 40 022 B, I diffed it; pinned by `tests/test_hns_viewer.nim:28-35`). `replay_broadcast.html` = starter page with the note's enumerated removals (I diffed the pre-banner region: the large hunks are exactly `#povBadge`/`#fpv` CSS+JS, the FPV raycaster, the zoom/minimap wiring, and the starter's own appended paintball block; the small hunks are the enumerated re-labelings) + the HNS block appended under the banner at `:2652`, installed through the starter's `window.PaintballChrome.install(PB_CTX)` hook. Transport: `relayout()` measures `#scorebug`/`#transport` and sets `--band`/`--topband`/`--hudscale` on `:root` (`:2596-2626`); the one absolutely-positioned game overlay rides `bottom: calc(var(--band, 0px) + 1px)` (`:2760`); `#endcard { top: var(--topband); bottom: var(--band) }`, shown via `#endcard.on` (`:739`, `:2277`) and dismissed on every seek (`:1678`). Beats are labelled `<button>`s built by `hnsBeat` (`:2840-2860`) that seek on click, with CSS for exactly the seven emitted kinds (`:2778-2790`, asserted `test_hns_viewer.nim:103-131`). `#viewpanel`/minimap/zoom removed (markup, CSS, wiring, and asserted absent, `test_hns_viewer.nim:72-81`) — correct for a fixed 720×400 board. |
| 15. Every drawn string fits | PASS | This game draws **no** canvas `fillText`/`strokeText` anywhere (I grepped `client/`, `replay-viewer/`, `src/` — zero hits), so `canvas_text` reports `total: 0` in both smoke steps — the canvas check legitimately covers nothing, and `--strict-text-bounds` stays on in both steps (`ci.yml:350`, `:447`). The actual gates for this game's two text surfaces: (a) the board's only text sprite, the shout bubble, is clamped/flipped into the board by `shoutBubblePlacement` and unit-tested at every corner and pocket with a full-cap say (`test_hns_viewer.nim:222-251`); (b) all model text in the DOM (say + radio feed rows) is measured by the worst-case renderer fixture — real page, real renderer, 6 full-cap says + 6 full-cap radios, at 360/720/1280 px, full-length/inside-frame/un-ellipsized, gated by its own `ci.yml` step (green at head; `{"loaded":true,…}`, `canvas text: 0 drawn`). The fixture asserts its own strings are at their caps (`renderer_fixture.html:235-254`), and its frame is regenerated from the shipped `broadcast.nim` by `tests/test_hns_renderer_fixture.nim`. |
| Parallel batch (simultaneous game) | PASS | One `curl.makeRequests` batch per attempt containing every open seat (`decide.nim:450-466`); never per-seat sequential calls; prep-phase seekers are not live and take no call. |

## Non-blocking observations

1. **Baseline margin sign (F6 residue).** The re-measured `burrow`-vs-`scatter` hiding margin is **−178 permille** (`tools/ci/baseline_tuning.json:8`); the design note's target was [+80, +400] ("tool use beats no tool use"). The deviation is declared in `tools/tune_baselines.nim:30-49` and the harness/`--check` gate is real, so checklist 7 ("tuned with a grid harness, not guessed") holds — but the note's autocurriculum story (fort beats roaming) is currently not what the shipped baselines demonstrate. Named a v2 driver problem in-tree.
2. **`docker_smoke.sh` does not fail on `reason == "fault"`** (F16, still true) and has no expected-key set — the note over-claims relative to the shared template. The head smoke ended `complete`.
3. **`.tiny` threshold is `boardW <= 620`**, the starter's inherited value, vs the checklist's "labels hidden under 640px" figure. At the operative 360 px width the labels are hidden and the plate-name rule is exact; nuance only.
4. **Note-vs-code doc drift** (F9, F10, F13 residue): `broadcast_core.js` is the starter's file rather than the described fork; the page's inherited region is edited (enumerated deletions/re-labelings), not byte-prefix-identical; the note names test files (`shard_1..4.nim`, `test_hns_scoring.nim`, `test_hns_tuning.nim`) whose content lives elsewhere. All content located; nothing missing against the checklist.
5. **Airborne visibility float** (F12 residue): the new `cos`/`sqrt` expression in `playerVisibleTo`'s airborne branch feeds hashed exposure counters. It is now whitelisted line-by-line by the determinism test, and `wasm_replay_smoke.cjs` runs in CI over the smoke replay, but no committed fixture forces an airborne visibility check across the native↔wasm boundary. Checklist 2's required test passes; the residual risk is hypothetical.
6. **Endcard-vocabulary test is phrase-level** (F14 residue): inherited dead chrome (`buildFlag`, `.ec-heart` CSS, `.squad-pip`) survives above the banner, with a head-commit test asserting the state stream can never carry the keys that would light it up.

## Fixer report audit

Not performed against `r1-fixes.md` — the coordinator's brief for this verdict explicitly forbade reading it. In its place, every reviewer finding F1–F20 was independently re-verified against the head tree and the head CI run (table above and §Refuted): F1–F4, F7, F8, F11–F15, F17–F20 fixed at head; F5 correctly non-defect; F6 partially fixed with a declared residue; F9, F10, F16 unchanged and non-blocking.

| finding | reviewer said | I verified at head | agrees |
|---|---|---|---|
| F1 | blocking | fixed (`control.nim:339-340` + new test) | yes (was real; now fixed) |
| F2 | blocking | fixed (`global.nim:589-612` + corner test) | yes (was real; now fixed) |
| F3 | blocking | fixed (fixture + CI job, green) | yes (was real; now fixed) |
| F4 | blocking | fixed (`sim.nim:651`) | yes (was real; now fixed) |
| F5–F20 | non-blocking | states verified individually above | yes |

BLOCKING: 0
