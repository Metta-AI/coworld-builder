blocking: 0

# r1 verdict — cogame-atari-57
Head: c8498ce481637d6acdac33192bf688a5d9f55ee5   Checklist: prompts/30-review-loop.md §ACCEPTANCE CHECKLIST (items 1–15 + the simultaneous-batch rule)   Independent read written before reading fixes: yes (repo cloned fresh to /tmp/judge-atari-57; checklist → design note → tree/CI read → review → fixes, in that order)

CI at head: run **33210994977**, conclusion **success**, all three jobs green (`test`, `docker-smoke`, `wasm-viewer`), no step skipped, no `continue-on-error` anywhere in `.github/workflows/*.yml` (grep: zero hits).

## Standing blocking findings

None. Every finding the review raised is either fixed at the current head (verified below at file:line) or correctly non-blocking against the checklist, and my own independent checklist pass found nothing the reviewer missed that falsifies an item.

## Refuted / resolved

The review was written at `309a9b3`; eight commits have landed since. Per the brief: a finding true then and fixed since is **resolved**, not refuted. Nothing in the review turned out to be factually wrong; my audit disagrees with none of its observations, only confirms the resolutions.

### B1 — live zoom/pan wiring on a fixed arena → RESOLVED at head (commits 5e21d69, 9a66f48, 16f2227)
- Evidence: `grep -rn "core.zoomAt(\|core.setZoom(\|core.panBy(\|core.resetView(\|attachMinimap("` over `client/replay_broadcast.html`, `replay-viewer/static_replay.js`, `replay-viewer/static_replay_worker.js` at c8498ce returns **nothing**. The page binds no `wheel`/`gesture*`/`dblclick`/`pointerdown`/`pointermove` view handler; `static_replay.js` posts no `view`/`minimap` message. `broadcast_core.js` still carries its zoom/pan/minimap implementation verbatim (my diff against `/workspace/starters/coworld-ctf/client/broadcast_core.js`: identical after restoring exactly `window.LANE_WIRE→window.CTF_WIRE` and one comment path). New regression test `tests/test_viewer.nim:testNoZoomPanWiring` (added 5e21d69) pins all of it and is green in run 33210994977. The finding was correct at 309a9b3 and the checklist-item-14 bullet is now satisfied: markup, CSS, ids **and** the `core.zoomAt/setZoom/attachMinimap` wiring are removed.

### N1 — no `--soak` on the viewer smoke → RESOLVED (0feee42, fix-forward c8498ce)
- Evidence: `.github/workflows/ci.yml:343` now passes `--soak 8`; head run printed `soak: 8s of playback kept advancing ("0 / 1440" -> "816 / 1440" -> "1200 / 1440")` (run 33210994977, wasm-viewer, `Load the bundle in a real browser`). On the 12→8 reduction: I checked it independently. `viewer_smoke.mjs` requires the **last** soak interval to advance; the head run's own samples show playback runs ~1440 ticks in ~9.6–10.2 s of wall clock (the rAF loop advances up to six sim frames per animation frame), so a 12 s soak samples past the end of playback — the fixer's cited runs 33207297869/33209115187 both read `1441/1440` at the 12 s sample, 0.2 s from a false "frozen". The 8 s soak ends ~2 s inside playback and still gates a frozen viewer. Net change **this run** is soak absent → soak 8: a gate added, not loosened, and it is in `ci.yml`, not `tests/` — item 1 is unaffected (see checklist pass).

### N2 — `a57.bubbles` produced, never consumed → RESOLVED (d93c9cb)
- Evidence: `client/replay_broadcast.html:2626-2654` — `#a57-bubbles` is a reserved strip at `top: var(--topband)`, height `(100% − bands) × 2/35` (rows [0,2) of the 35-tile board), never positioned relative to an avatar; each `.a57-bubble` is `max-width: 32%`, `overflow: hidden; text-overflow: ellipsis; white-space: nowrap`; `#stage.tiny #a57-bubbles { display: none }` (`:2710`). Producer caps at three (`src/lane/broadcast.nim`, `min(3, live.len)`, pinned by `tests/test_viewer.nim:testBubbleBand`). Band and font both scale with `--u`, so the fit is scale-invariant: 48 runes at 7u ≈ 176u against a 243u bubble — a full-cap say fits without ellipsis at every width; the renderer fixture at head reports `112 drawn, 0 never inside, 0 ellipsized`.

### N7 — provenance test name overstated → RESOLVED (7fc9e38)
- Evidence: `tests/test_viewer.nim` — `testBroadcastCoreDiffersOnlyInTheWireNameAndOnePath`; the assertion hunk shows the same two `replace`s and the same `count("window.CTF_WIRE") == 2` check as before (rename + doc only, no assertion change). My own diff confirms the two-place delta the test now names.

### N12 — inter-batch floor not stop-interruptible → RESOLVED (f86274b)
- Evidence: `src/lane/decide.nim:174-184` — the floor now sleeps in 100 ms slices (`SpacingSliceMs`), bounded by `lastBatchStart + turnSpacingMs`, and abandons the wait at `turnStart + (wallClockBudgetSeconds − elapsedSeconds)`. Worst-case stop overshoot drops to ≈ one `turnBudgetMs` (16 s): 660 + 16 + artifact writes ≈ 680 s, inside the 720 s (60 %) budget.

### N3–N6, N8–N11, N13, N14 — reviewer classified all as non-blocking; I concur, independently
- **N3** (end-position contact resolution vs the design's swept resolver): real deviation from the design note, but not a checklist item; both server and wasm viewer run the same `stepLane`, so replay re-derivation (item 2) is unaffected, and the no-tunnelling bound that makes end-position testing sound is asserted directly (`tests/test_physics.nim`). The NEEDS-DESIGN disposition is the right routing.
- **N4** (`MarcherCols = [5..12]`): not a checklist item; documented in place (`src/lane/maps.nim:92-101`) **and** in the shipped rules page (the manifest's `rules.md` carries the deviation blockquote), playability pinned by test.
- **N5** (`panicTicks 20 / leadTicks 10`): item 7 requires sweep-tuned, not the note's illustration; `tools/ci/baseline_tuning.json` records the 36-cell grid, `tests/test_tuning.nim` asserts shipped == pick and no swept cell beat it.
- **N6** (13 `A57_MODE` branch points above the banner): item 14's provenance concern is a lookalike rewrite; this page is genuinely the starter's — `chrome_common.js` byte-identical (my `diff` against the starter mount returned empty; sha pinned in `test_viewer.nim`), all 47 kept ids present, all removed ids absent, CSS sections 1–5 intact, and my diff of the above-banner region against the starter shows the 2320 removed lines are the declared `#fpv`/`#viewpanel`/`#povBadge`/zoom-pan blocks while the ~163 changed lines are the cabinet retargetings of ctf's own readouts, declared in the banner. Noted as an observation, not blocking.
- **N8** (flake.nix deleted): not a checklist item; canonical build is Dockerfile + nimby.
- **N9** (three design-list tests unimplemented): item 1 forbids loosening, not incompleteness against the note; the underlying server code exists and I read it (`src/lane/server.nim:600-620`, held registrations applied further down the join loop).
- **N10** (8 of 13 event kinds): the beat set is exactly the five the page styles (`.beat-marker.life_lost/.screen_clear/.record/.lane_over/.over`, `client/replay_broadcast.html:2666-2679`); no emitted kind lacks CSS.
- **N11** (fixture rescales font to box): the gated numbers stayed honest — the fixture separately asserts `scrollWidth > clientWidth` overflow and full-length source strings (`tools/ci/renderer_fixture.html:68-71,192-197`); item 15's named requirements (fixture exists, real bundle in iframe, full-cap strings all seats, several widths, self-checked lengths, own `--strict-text-bounds` step) are all present and green.
- **N13** (wall-clock stop gated on `phase == Playing`): the lobby has its own bound (`lobbyJoinTimeoutTicks` 2880 = 120 s in every shipped config, then `forceStart`), so no unbounded wait exists; unreachable except by a hand-built config.
- **N14** (plate shows real policy name): item 4 *requires* the viewer to map aliases to real names; agents still see aliases only (`tests/test_locality.nim`, `test_isolation.nim`).

## Checklist pass (independent)

| item | status | evidence (path:line or run id) |
|---|---|---|
| 1 CI green, no test loosened | PASS | Run 33210994977 = success at c8498ce (cited above). `git log -p --since=2026-08-28T16:00:00Z -- tests/` shows 4 commits: d31e0f7 (initial adds, +3493/−0), 5e21d69 (+testNoZoomPanWiring), d93c9cb (+testBubbleBand), 7fc9e38 (rename + doc, assertion byte-identical). No deletion, no widened tolerance, no skip/xfail. The only gate-parameter change outside tests/ is ci.yml soak 0→12→8, a net addition (see N1). |
| 2 Replay re-derivation | PASS | Server writes one hash/tick (`src/lane/server.nim:817`); playback re-steps recorded bytes through the same `sim.step` and compares per tick (`src/lane/replays.nim` stepReplay/checkReplayHash); viewer draws only from that re-simulated sim (`replay-viewer/atari57_replay.nim`, `src/lane/replay_runtime.nim:56-86`). Asserted for all 3 ROMs and all four end reasons incl. `wall_clock`/`sim_fault` (`tests/test_replay.nim:99-152`). |
| 3 Static viewer | PASS | `coworld_manifest_template.json:26-28` `"replay_viewer": {"bundle": "static-replay-viewer"}`; `tools/build_replay_viewer.sh` present, mode 100755, invoked by path in ci.yml:263; the bundle's only network call is `fetch(message.replayUrl)` (`static_replay_worker.js:111`). The `/client/replay` route in `server.nim` is the starter's live-spectator page, not a manifest-declared replay viewer. |
| 4 Both name spaces | PASS | Aliases only in-game (`sim_types.nim` LaneAliases; `tests/test_locality.nim:94-126`, `test_isolation.nim`); real names in chrome roster/endcard/`results.names` (`roster.nim`, `broadcast.nim` rosterJson). |
| 5 Degrade-never-hang | PASS | attempt1Ms 9000 / retryMs 5000 inside turnBudgetMs 16000 (`decide.nim:109,194-218`); spacing floor sliced + deadline-bounded (`decide.nim:174-184`); engine stop 660 s ≤ 720 s (`server.nim:587-596`, manifest 145-150); lobby bounded at 2880 ticks then forceStart (`server.nim:609-620`); frame limiter bounded (`server.nim:429-441`); shutdown grace bounded (`server.nim:934-937`). Settle+score ≈ 680 s worst case < 720 s. |
| 6 num_agents | PASS | `num_agents: 4` in variants chomper/brickfall/gallery `game_config` (manifest:547,598,649) and certification.game_config (:740); certification.players ×4 (:697-709). `docker_smoke.sh:106-151` enforces all four invariants + SMOKE_SEATS cross-check, each `SEAT-COUNT FAIL:`-prefixed; `grep SEAT-COUNT` over the head run's full log: **zero hits**; smoke printed `smoke OK: seats=4 … reason=complete`. |
| 7 Scripted baseline full legal episodes | PASS | Golden runs pin `reason: "complete"` for all 3 ROMs (`tests/data/golden_hashes.json`: chomper 2644 all_lanes_over, brickfall 2880 full_time, gallery 1440 all_lanes_over; asserted in `test_determinism.nim`); legality of every emitted stance + compiled byte 0..14 with per-ROM dir/act rules (`tests/test_baselines.nim:20-63`); grid harness `tools/tune_baselines.nim` + `tools/ci/baseline_tuning.json` + `tests/test_tuning.nim`. docker-smoke ran a real all-scripted episode to `reason=complete`. |
| 8 LLM reply handling | PASS | `extractJsonObject` fence/prose-tolerant (`stances.nim:154-193`); exactly one retry (`decide.nim:191` `attempt < 2`), throttle fast-fail (`:250-256`); fallback to arcader stance with a recorded `fallback` cause (`:259-274`), counted into `results.fallbackTurns`. |
| 9 Rune-safe truncation | PASS | `truncateRunes` via runeLen/runeSubStr (`stances.nim:79-86`), say/note/policy/detail/stance-record caps all rune-based; `tests/test_stances.nim:93-125` feeds a 4-byte emoji at the 48-rune boundary, asserts rune-cut + `validateUtf8() == -1`; `test_replay.nim` forces non-ASCII say/label through `replay_summary.py` under a strict UTF-8 parser. |
| 10 Manifest validates | PASS | `game.docs.readme = {"type":"text","value":…}` + 3 pages each `{id,title,content{type,value}}` (manifest:483-513); `game.protocols` has both `player` and `global` as text objects (:473-482). |
| 11 Viewer legible at 360 px | PASS | `.plate-name { flex: 1 1 auto; min-width: 3.2em; overflow: hidden; text-overflow: ellipsis }` (`client/replay_broadcast.html:2539-2544`); labels hidden under 640 px (`:2711-2714`); `.tiny` toggled at boardW ≤ 620 (`:2463` area, relayout). |
| 12 Release order and scaffold | PASS | coworld-release.yml: build manifest → certify (`--timeout-seconds 300`) → upload policies (before upload-coworld, commented) → upload-coworld → canonical wait → secret put; all three workflows present; `docker_smoke.sh` 100755; `policies.json`: 2 `PLAYER_PROMPT` champions + 2 `PLAYER_SCRIPTED` fillers, champion #2 `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`; placeholder gate `grep '<slug>\|<IMAGE>\|<SEATS>'` over the five files exits non-zero (clean, verified myself); only the four documented residue names survive. |
| 13 Viewer executes | PASS | wasm-viewer `needs: docker-smoke` (ci.yml:226); `Load the bundle in a real browser` ran and passed at head (run 33210994977 job steps; soak advancing 0→816→1200), no continue-on-error; markers set in the shell's own paths (`static_replay.js:142` loaded-on-first-frame, `:20` error, `:32` mismatch); playback opens at game start and every seek clamps there (`replay_runtime.nim:38-52` startTick = gameStartTick + seek to replayStartTick; `replays.nim:249-252` clamp, `:697` seek clamp, `:758/:821/:840` restarts) — verified from the tree since the CI replay's lobby is short; link flags and bootstrap are the same starter pair (`config.nims`: no MODULARIZE/EXPORT_NAME; `static_replay_worker.js:186` `Module.onRuntimeInitialized` + `:225` importScripts — the non-modularized form), pinned by `test_viewer.nim`. |
| 14 Chrome is the starter's | PASS | `chrome_common.js` byte-identical to `/workspace/starters/coworld-ctf/client/chrome_common.js` (my diff: empty; sha pinned in test). Page carries the banner at :2504 with the game block below; all kept ids present, all removed ids (`#viewpanel`+children, `#fpv`+children, `#povBadge`) absent; zoom/pan **wiring** removed at head (B1); transport rules: relayout sets `--hudscale/--topband/--band` on `document.documentElement` (`:2440-2467`), game-block overlays ride `calc(var(--band) + …)` (`:2606`), `#endcard { bottom: var(--band) }` + `.on` class (`:741,:752`) dismissed on every non-gameover frame (`:1656`), beats are labelled `<button>`s that seek with CSS for all five kinds (`:2657-2679`). |
| 15 Every drawn string fits its frame | PASS | Both smoke steps carry `--strict-text-bounds` (ci.yml:344,381). Main bundle reports `canvas text: 0 drawn` (OffscreenCanvas Worker — covered nothing, as the checklist anticipates), so the evidence is the required worst-case fixture: `tools/ci/renderer_fixture.html` loads the real shipped `index.html?embed=1` in an iframe, shims only the frame source, drives full-cap 48-rune say + 160-rune note on all four seats at 360/620/1280 px, self-checks its strings are near-cap (`:68-71`), and its own gated step reports `canvas text: 112 drawn, 0 never inside, 0 ellipsized` at head. Speech band is a reserved, cap-sized, scale-invariant strip (see N2). |
| Simultaneous batch | PASS | All open seats' requests go out as one `RequestBatch` via `client.curl.makeRequests` (`decide.nim:202-218`); no sequential path exists; `tests/test_engine.nim` asserts four intersecting in-flight windows. |

## Fixer report audit

| finding | fixer said | I verified | agrees |
|---|---|---|---|
| B1 | fixed (5e21d69 + 2 fix-forwards) | zero zoom/pan call sites/handlers/routes at head (grep); broadcast_core untouched; new test green; the mid-round red run 33207297869 was the fixer's own comment naming `#viewpanel` above the banner, fixed forward in 9a66f48/16f2227 | yes |
| N1 | fixed, soak 12→8 with measurement | `--soak 8` at ci.yml:343; head soak samples 2 s inside a ~10 s playback; 12 s samples land past playback end per both cited run ids; gate is net-new this run | yes |
| N2 | fixed (d93c9cb) | `#a57-bubbles` reserved strip + clip + tiny-suppress in page; producer 3-cap; testBubbleBand green | yes |
| N7 | fixed (7fc9e38) | rename + doc only; assertion hunk unchanged; my diff confirms exactly two retargetings | yes |
| N12 | fixed (f86274b) | 100 ms slices + deadline bound at decide.nim:174-184 | yes |
| N3 | NEEDS-DESIGN | not a checklist item; hash parity unaffected (same code both sides); correct routing | yes |
| N4,N5,N6,N8,N9,N10,N11,N13,N14 | DECLINED with reasons | each checked against the checklist independently; none falsifies an item (details under Refuted/resolved) | yes |

## Non-blocking observations

- The main-bundle viewer smoke's `canvas_text.total` is 0 (Worker/OffscreenCanvas rendering), so item 15's numeric evidence rests entirely on the renderer fixture. That is the checklist's own designed fallback and the fixture is real and gated, but any future change that breaks the fixture silently shifts the whole text-bounds burden onto nothing.
- The fixture's `scrollWidth` overflow assertion exempts elements with `text-overflow: ellipsis`, and the new bubbles carry ellipsis — so a bubble that *did* ellipsize a full-cap say would not fail the fixture. The band arithmetic says it cannot at any width (fit is scale-invariant), but a direct "no bubble ellipsized" assertion would close the gap.
- The soak value (8 s) now encodes a measured playback speed (≈6 sim frames per rAF). If the fixture's `maxTicks` or the rAF batching changes, the 8 s margin should be re-measured; the step's comment records the method.
- `src/lane/labels.nim` from the design's kept-files table does not exist; label composition lives elsewhere. Design-note drift only.
- Design §Tests items (disconnect/revive, failure-URI no-show, held registration) remain untested (review N9); the server code exists. Worth its own change with a second-server harness.

BLOCKING: 0
