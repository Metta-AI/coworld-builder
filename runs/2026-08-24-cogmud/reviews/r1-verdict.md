blocking: 0

# r1 verdict — cogmud

Head: 575c86bccd842d656a32fbc27039f51e4032167f   Checklist: prompts/30-review-loop.md §ACCEPTANCE CHECKLIST (items 1–14 + the simultaneous-batch rule)   Independent read written before reading fixes: yes (r1-fixes.md not read at all, per brief; the review was read only after my own pass of the tree, the manifest, the workflows and the CI logs)

The review was written against dd6f018 (the original build sha). Seventeen fix commits (F1–F17)
landed since; every claim below is verified at the current head 575c86b, not at the review's
snapshot.

## Standing blocking findings

None.

## Refuted

### F1 — "grid harness has no artefact anywhere in the tree" → REFUTED (fixed at head)
- The reviewer's claim was true at dd6f018. It is false at 575c86b: commit dfb740a ("F1: a real
  grid harness for the baselines' five thresholds") added `tests/test_tuning.nim` (314 lines) and
  `docs/tuning/baseline-grid.md`.
- Evidence at head:
  - `src/cogmud/llm.nim:58-105` — the five thresholds are now a `BaselineParams` object
    (`factorSellMargin`, `factorPickupValue`, `magpieHawkPeriod`, `magpieSellMargin`,
    `magpieBuyMargin`) with the shipped point in `const TunedParams* = BaselineParams(...)`,
    threaded through `scriptedSentence(sim, seat, kind, params = TunedParams)` (llm.nim:374-381).
  - `tests/test_tuning.nim:52-56` — the grid: 5×6 = 30 factor points, 4×4×4 = 64 magpie points;
    each point plays a full 14-turn all-scripted episode of the shipped mixed table on eight seeds
    (`Seeds = [1, 3, 7, 11, 19, 23, 31, 42]`, line 47) and scores with the game's own
    `sim.score(seat)` (line 100). Feasibility predicates keep each baseline the thing it ships as
    (`evalFactor:120-142`, `evalMagpie:144-167`), and the winner is re-derived
    (`bestOf:169-178`). Assertions: the shipped point is on the grid and feasible (272-280);
    nothing on the factor grid outscores it (`check factorBest.label == shippedFactor.label`,
    282-289); magpie's regret stays inside one standard error (291-302); and the committed report
    is byte-equal to the one the harness just produced
    (`check readFile(path) == text`, 304-314).
  - `docs/tuning/baseline-grid.md` — the committed sweep surface (`<!-- GENERATED by
    tests/test_tuning.nim -->`; factor: shipped `sell 0 · pickup 8` = 1.4417 = the grid's best
    feasible point, regret 0.0000).
  - It runs in CI, not just in principle: run 32690212886 test job 97322402743 executed
    `nim r … tests/test_tuning.nim` in both debug and `-d:release` (confirmed in the job log,
    all 7 test files × 2 modes, zero failures).
- Checklist item 7's third clause ("tuned with a grid harness, not guessed") is therefore
  satisfied at head. Refuted as a standing finding.

The review's sixteen non-blocking findings (F2–F17) were accurate observations at dd6f018 and are
outside the blocking count by the reviewer's own classification. For completeness I verified each
against head; every one that named a defect has a landed fix, and I found no fix that papers over
rather than repairs:

| review finding | state at head 575c86b |
|---|---|
| F2 dead ROBBED chip | fixed (27fb13e): `tableStateJson` emits `recentRobbed` (sim.nim:1136-1152, victim marked for the theft turn and the next); renderer reads it (renderer.js:1284-1286 region); global-protocol text documents it |
| F3 missing tether | fixed (4135e22): `drawTether` exists and reads `seat.retainerOf` (test_viewer.nim:230-235 asserts it) |
| F4 `salienceOf(iSay)` reads the wrong string | fixed (ec805ce): the speech class fills `acts[seat].say` from the lifted line before `emitAct` (sim.nim:862-874), so `event.say` is what the seat spoke; threshold asserted via `event.say.runeLen > 40 → 30` (sim.nim:309-312) |
| F5 unreachable 25 branch | fixed (8a0b41f): `salienceOf`'s `iGive` arm is now ok/npc≥0 → 90/60, else 35, else 5 (sim.nim:287-297), with the reachability argument in the comment |
| F6 chart re-computes score in JS | fixed (f3bfe9f): chart reads `seat.score` off the payload; `test_viewer.nim:237-248` asserts `[6, 7, 8, 9, 11, 14]`, `- 40) / 40` and `event.npc === 4` are all absent from renderer.js |
| F7 tautological observation-split check | fixed (f8d1ea4): replaced by a structural `questNodes` walk asserting exactly `Quests` commission-shaped objects, all the seat's own |
| F8 restock array never asserted | fixed (7e34493) |
| F9 prose-coverage vs production | fixed (e8eca8b): a new test_sim suite drives 25 of 26 reasons off recorded act events and asserts `oRejected` absent with the reason recorded in place |
| F10 rules.md restock timing | fixed (e55af65): shipped rules.md now says "at the open of every turn *after the first*" and labels the table "(opening stock)" |
| F11 hire clamp vs refusal | fixed (74fca20): test renamed and STRENGTHENED — the empty-purse case is kept and a clamp case added (employer holding 10 asking 15 posts at 10); code comment now states the clamp |
| F12 same-turn bodyguard | pinned by a test (34ef456); a consequence of the design's class order, not a defect |
| F13 non-traded goods enter stock | pinned by a test (9ed9e18); design says "the goods enter its stock", which is what the code does |
| F14 live pages' banner overstates | fixed (e5d3096): global.html/player.html banners now describe the live pages ("ONE element is APPENDED - #townbar … A live page has no transport") |
| F15 `wander` missing from parse.nim header | fixed (dbb61e1): the header now lists `off`, `wander`, `have`/`has`, `picks`/`asks`, `put`/`read` unguarded, `bargain`/`proposal`, and the verbless purchase (parse.nim:10-30) |
| F16 closing snapshot reads WAITING ON 6 | fixed (33b7d62): `matchHeader` treats `turnsPlayed >= total` as settled; `test_viewer.nim:264-268` asserts `(state.turnsPlayed || 0) >= total` is in the renderer |
| F17 fallback recorded only on stdout | fixed (575c86b): `LlmClient.fellBack` (llm.nim:93-97, set at 741) is folded into the act event's `scripted` flag by `server.nim:299-300` (`scripted[seat] != skNone or client.disabled or client.fellBack[seat]`); `test_bot.nim:219-251` drives a dead endpoint (127.0.0.1:9) and asserts every seat played legally and is marked |

Item 1's "no test loosened" audit of these fixes: `git log -p -- tests/` at head shows the only
deleted test lines are the tautology F7 replaced, the prose-coverage test F9 renamed-and-split
(both halves now stronger), and the hire test F11 renamed with its original assertion kept and a
new one added. No assertion deleted without a stronger replacement, no tolerance widened, no
skip/xfail anywhere, no test file removed.

## Checklist pass (independent)

| item | status | evidence (path:line or run) |
|---|---|---|
| 1 CI green, no test loosened | PASS | `gh run list -w ci.yml --branch main`: run 32690212886, conclusion success, headSha 575c86b (the reviewed sha); jobs test/docker-smoke/wasm-viewer all success. `git log -p -- tests/`: 15 commits, hunks read — only strengthenings (table above); initial suite c8ffb6c all-adds |
| 2 replay re-derivation | PASS | `replayMatch` re-runs act events and checks every recorded `turn` event via `sameWorld` (sim.nim:1304-1390), raising on mismatch; pre-seeds recorded `deadline` reason (sim.nim:794-798, 1332-1340); tests: frames.len == events.len+1 + final-frame equality (test_sim.nim:992-997), tamper +1 coin raises (1033-1043), both deadline landings (1046-1073). The viewer draws from that same re-derivation: wasm `cogmud_replay.nim:41` `for frame in replayMatch(...)`, shell hands `states` to `attachReplay`, `currentState()` reads `states[index]` (renderer.js:1671-1673 region) — no parallel recording |
| 3 static viewer | PASS | manifest:17-19 `"replay_viewer":{"bundle":"static-replay-viewer"}`; `tools/build_replay_viewer.sh` present, git mode 100755, `mkdir -p "$(dirname …)"` before containment; release workflow refuses a non-static liveness line ("a pod-served /client/replay viewer is not acceptable", coworld-release.yml Certify step); bundle contacts only the `?replay=` URL (static_replay.js:76 fetch, assets `./assets`); no pod path in the manifest |
| 4 both name spaces | PASS | aliases from `tableNames` (sim.nim:54-65) in every seat-facing string; `resultsJson` uses policy names (sim.nim:1018); replay carries `names` + `policyNames` (server.nim:124-133); `makeNameMap` swaps at render, keeps `Baseline` fillers' alias, `nameMap.text()` rewrites the chronicle's verbatim sentences (renderer.js:887-911); final frame to players hands aliases back (server.nim:169-181) |
| 5 degrade-never-hang | PASS | connect wait bounded 180 s (server.nim:213-221); one batch/turn bounded 24 s + retry 12 s (`for attempt in 0..1`, `makeRequests(batch, budget)`, llm.nim:708-725); rate floor 12 s, skipped when disabled (server.nim:280-285); deadline checked before every batch (server.nim:263-268 → `endEarly`); `sampleEpisode` fits 14 turns, 704 s < 720 s = 0.6×1200 (sim.nim:73-86); player loop try/except + quit(0) (cogmud_player.nim:59-91); docker-smoke: game exit 0, `all 6 player containers exited 0`, `reason=complete` (run 32690212886) |
| 6 num_agents | PASS | 6 in `variants[0]`, `variants[1]`, `certification.game_config` (manifest:394, 427, 458); cert players ×6, game_config.players ×6; schema pins integer 6..6 (manifest:71-76); docker_smoke.sh enforces all four invariants with `SEAT-COUNT FAIL:` prefixes (lines 110-151) and `SMOKE_SEATS=6` as the independent second declaration (line 54); grep of the full docker-smoke log of run 32690212886: 0 hits for `SEAT-COUNT FAIL`; log shows `seats=6`, `results.names/scores` length checks pass |
| 7 scripted baseline + grid | PASS | test_bot.nim:66-94: all-factor/all-magpie/mixed × seeds [1,7,11,42] end with `reason == "complete"`, `intent != iNone` for every act, qty ≥ 1 on ok trades, coin ≥ 0, carry ≤ 8; grid harness `tests/test_tuning.nim` + committed `docs/tuning/baseline-grid.md`, byte-equal-asserted, runs in CI both modes (see F1 refutation) |
| 8 LLM reply handling | PASS | `extractJsonObject` strips fences/prose (llm.nim:591-602); invalid = non-object / missing / non-string / empty action (parseDecision:669-683); exactly one retry with the hint (llm.nim:708-717); then `scriptedAction(..., skFactor)` (739-742); fallback recorded on the act event via `fellBack` → `wasScripted` (server.nim:299) AND the stdout line; test_bot.nim:219-251, 254-324 |
| 9 rune-safe truncation | PASS | `cutRunes` (sim.nim:171-176) on sentence/say/notes/room-log/spoken; `cleanText` runeSubStr (llm.nim:660-667); prompt cap runeSubStr (server.nim:441-442); tests feed "é"×900 at the caps and assert runeLen == 240/160/600 and `validateUtf8() == -1` (test_sim.nim:839-851, test_bot.nim:272-277, 311-314); docker_smoke decodes the replay as strict UTF-8 (`SMOKE_REQUIRE_REPLAY_JSON=1` default, lines 311-318) — green |
| 10 manifest validates | PASS | `game.docs.readme` = {"type":"text","value":…} (manifest:268-271); `pages` = 3 × {id,title,content:{type:text,value}} (272-297); `game.protocols.player` and `.global` both present (257-265) |
| 11 viewer legible at 360 px | PASS | `.plate-name { … min-width: 3.2em; flex: 1 1 auto; }` — the starter's own rule inside the byte-identical prefix (chrome.css:280-292); labels hidden under 640 px (chrome.css:460-461, starter's rule kept); appended block adds the 6-plate grid, 2-column at 480 px, reel capped at 4 buttons (chrome.css:611-630); test_viewer.nim:93-104 asserts all of it |
| 12 release order + scaffold | PASS | coworld-release.yml: Build manifest (153) → Certify (167) → Upload policies (206) → Upload coworld (304) → Put secret (342); ci.yml docker-smoke builds the image in the same job before the smoke (177→185); all three workflows present; docker_smoke.sh git mode 100755; policies.json: 4 distinct policies, 2 `PLAYER_PROMPT` champions + 2 scripted fillers, champion #2 (`cogmud-broker`) carries `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`; the placeholder gate ran and exits 0 (grep over the five files: no `<slug>`/`<IMAGE>`/`<SEATS>`) |
| 13 viewer executes | PASS | run 32690212886 wasm-viewer job 97322683004 success, `needs: docker-smoke` (ci.yml:212); `Load the bundle in a real browser` ran (not skipped, no continue-on-error anywhere in ci.yml) and printed `{"loaded":true,"ms":289,…}`, `soak: 15s of playback kept advancing`, `scrub readouts: 0%="TURN 4 / 8 · WAITING ON 6" 50%="TURN 5 / 8 · WAITING ON 6" 100%="FINAL · RATCHET 1.73"` — three distinct readouts (the `null -> null -> null` in the soak line is the absent `#tick` field only; I read viewer_smoke.mjs:387-422 — `moved` requires clock/tick/scorebug to differ across BOTH intervals or the run fails "frozen", so the pass is not vacuous); `data-replay-loaded="true"` set at the end of `attachReplay`'s makeRenderer callback after the first synchronous `renderer.draw` (renderer.js:1780-1783), `data-replay-error` set on every failure path and removed on retry (static_replay.js:56, 107, 149); MODULARIZE=1 + EXPORT_NAME=CogmudReplayModule (config.nims:38-39) matched by the shell's `CogmudReplayModule()` factory call (static_replay.js:153), no `onRuntimeInitialized` anywhere; both files bullwhip-lineage (diffs are renames only); test_viewer.nim:166-202 asserts the matched pair |
| 14 chrome provenance | PASS | chrome.css: first 467 lines byte-identical to `/workspace/starters/cogame-bullwhip/client/chrome.css` (cmp clean), one appended `/* ---------- Cogmud ---------- */` block; `tests/fixtures/starter_chrome.css` is itself byte-identical to the real starter file (cmp clean), so the CI provenance test diffs against the real thing; replay.html/index.html = the starter's page + wordmark/title/namespace renames + two appended elements + the banner'd relayout block, nothing removed (full diffs read; every starter id survives); (a) relayout measures `#transport.offsetHeight` into `--band` + `--hudscale` on `document.documentElement`, calls `fit()`, bound to load/resize (replay.html:89-107); (b) overlays live inside `#board-wrap`, `#loading { bottom: var(--band); }` (chrome.css:574), `#transport` in normal flow; (c) `#endscreen` absolute inset:0 inside `#board-wrap` (bottom edge = var(--band)), shown via `.show` and `classList.toggle("show", !!show)`, `updateEndscreen` called from `setIndex` on every index change, and every seek that exists (scrub drag/click, beat buttons, reel buttons, play restart) routes through `setIndex` — no keyboard/back-forward seeks exist in this lineage (grepped); (d) beats are labelled `<button type="button">` with title + aria-label + onclick (markCogmudBeat, renderer.js:1558-1573), `beatKind` emits exactly {rob, commission, deal, market, end} (renderer.js:959-974) and the appended CSS defines `.beat-marker.<kind>` + `.treel button.<kind>` for all five (chrome.css:568-596); zoom/minimap: `viewpanel` absent from markup, CSS, wiring; test_viewer asserts `"viewpanel" notin text` |
| batch rule | PASS | one `RequestBatch` per turn, all open seats posted into it, one `curly.makeRequests` (llm.nim:708-725); server calls `decideAll` once per turn on the full `pendingSeats()` snapshot (server.nim:290); no per-seat request loop exists |

## Fixer report audit

Not read, per the brief ("form your own read first; only the review file below is your input").
The table under Refuted is my own commit-by-commit verification of the F1–F17 fix commits against
the review's findings; it is derived from `git log -p` and the tree at head, not from the fixer's
self-report.

## Non-blocking observations (mine, not counted)

- The 0% scrub readout in the viewer smoke is `TURN 4 / 8`, not `TURN 1 / 8` — `setIndex(0)`
  renders states[0] but the smoke samples after ~15 s of autoplay before scrubbing to 0%…
  actually the harness's 0% sample is taken after the soak, so the first sample reflects
  playback position at that moment. Cosmetic harness behaviour (template-verbatim), three
  readouts still distinct, gate satisfied.
- llm.nim's captured-error truncations are byte slices (e.g. `response.body[0 .. min(…, 400)]`,
  llm.nim:635-649); those strings reach stdout only, never the replay, so item 9's "every string
  that reaches the replay" is unaffected (the reviewer noted the same).
- `magpie`'s dark-walk ramble and the parse-table additions (`wander`, `off`, `have/has`,
  `picks/asks`, unguarded `put`/`read`, `bargain`/`proposal`, verbless purchase) are all
  documented in parse.nim's header (post-F15) and in the shipped sentences.md, and each is
  exercised by test_parse.nim.

BLOCKING: 0
