# r1 review — tandem

Range: `4b78981e77210a5f910dd679c81a32983e0a333d` (main, whole tree read against the design note; the repo has four commits and the tests arrived in one, `68e39b0`)
Repo read at: `/tmp/cogame-tandem` (fresh clone), starter at `/workspace/starters/coworld-ctf`
Design note: `/workspace/coworld-builder/runs/2026-08-23-tandem/design.md` (repo copy `docs/plans/2026-08-23-tandem-design.md` is byte-equal in substance; not re-reviewed separately)
Files read: 46 (all of `src/tandem/*.nim`, `src/tandem.nim`, `src/tandem_player.nim`, `replay-viewer/*`, all 13 `tests/*.nim` + `tests/lib/helpers.nim`, `client/{chrome_common.js,broadcast_core.js,replay_broadcast.html}`, `coworld_manifest_template.json`, all three workflows, `tools/ci/{docker_smoke.sh,policies.json}`, `tools/build_replay_viewer.sh`, `tools/replay_summary.py`, `Dockerfile`, `compose.yaml`)
Checklist: `prompts/30-review-loop.md` §ACCEPTANCE CHECKLIST (items 1–14 + the simultaneous-decision batching rule)

Method note: everything below is **observed** (read in the tree at that sha) unless labelled *inferred* (reasoned, not executed) or *untested* (needs a run). CI evidence is cited by run id and step. The sandbox has no Nim, Docker, emsdk or browser, so no code was executed here.

---

## Blocking

### B1/F1 — the baseline tuning harness the code names does not exist in the tree
- Where: `src/tandem/baselines.nim:20-22` ("The tuning constants are `{.intdefine.}` so the grid harness can sweep them from the command line (`-d:TandemLookahead=2800000`) … The harness is `tools/tune_baselines.nim`.") and `src/tandem/baselines.nim:37` ("See tools/tune_porter.nim."); `tools/` contains `build_manifest.py, build_replay_viewer.sh, ci/, expand_replay.nim, extract_events.nim, gen_trig_table.nim, gen_wire_constants.nim, record_fixture.sh, replay_summary.py, wasm_replay_smoke.cjs` — neither `tune_baselines.nim` nor `tune_porter.nim` is present, and `git log --all -- tools/tune_baselines.nim tools/tune_porter.nim` returns nothing (they were never committed).
- Observed: the 24 porter/mule constants at `baselines.nim:24-105` are all `{.intdefine.}`-sweepable, and their docstrings quote measured outcomes ("measured: the heading swung ±90 degrees every turn", "porter x mule scored exactly what mule x mule scored (0.016)", "four of twenty seeds stopped 0.45 m from the start"). `tests/test_baselines.nim:71-93` pins the resulting behaviour over 20 committed seeds. So there is strong *indirect* evidence of measurement, but the harness the tree points at is absent, and no sweep output, log or fixture is committed.
- Checklist item: 7 — "Scripted baseline plays full episodes legally. … **The baseline's parameters were tuned with a grid harness, not guessed.**" (The rest of item 7 is satisfied: `tests/test_baselines.nim:20-49` asserts bounded/legal orders over 500 states × 2 baselines; `tests/test_engine.nim:141-160` and `:208-236` run all-scripted episodes to a natural end and assert `sim.endReason == reasonComplete`.)
- Why blocking: the judge's rule is that a checklist item that cannot be verified from the tree or from cited CI evidence counts as blocking. Here the only artefact of the "grid harness" half of item 7 is a docstring pointing at two files that do not exist.
- What would settle it: committing `tools/tune_baselines.nim` (or whatever the sweep actually was), or citing a run/log of the sweep, or removing the two dangling references and stating in the design note how the constants were fixed.

No other checklist item is falsified by anything I read. Items 1–6 and 8–14 are each traced below with evidence.

---

## Non-blocking

### F2 — `damage_last_turn` in the seat view is structurally always 0
- Where: `src/tandem/decide.nim:322` (`engine.damageAtTurnStart = sim.damage`, first statement of `turn()`) and `src/tandem/decide.nim:203` (`"damage_last_turn": int(max(0'i32, sim.damage - engine.damageAtTurnStart))`, read while composing the user message at `decide.nim:376`).
- Observed: `turn()` snapshots `damageAtTurnStart` before it builds either seat's message, so the subtraction is always `sim.damage - sim.damage`. No other writer of `engine.damageAtTurnStart` exists (`grep damageAtTurnStart` → `sim_types.nim:461` unused field, `decide.nim:84,203,322`).
- Note says: §Server "the per-seat view given to the LLM" shows `"damage_last_turn": 31`, i.e. the damage taken during the previous turn.
- Consequence: a documented observation field is dead; policies cannot see how much condition the last two seconds cost. Not a checklist item.

### F3 — captured error text is byte-sliced on a path that reaches the replay
- Where: `src/tandem/llm.nim:169` (`body[0 .. min(body.high, 400)]`), `:177` (`body[0 .. min(body.high, 300)]`), `:181-182`, `:191-193` (`result[0 .. min(result.high, 160)]`), `:203-204` (`head[0 ..< 160]`). These strings become `failure.msg`, which `decide.nim:436-437` writes as `fallback.detail` via `clipRunes(detail, MaxDetailRunes)`.
- Observed: the *final* cap is rune-based (`orders.nim:72-86`, `runeLen`/`runeSubStr`), and `clipRunes` rebuilds the string rune by rune (`for rune in result.runes: clean.add($rune)`), so a byte-split multi-byte sequence is re-encoded rather than passed through (*inferred* — Nim's `runes` iterator decodes an invalid lead byte as a single-byte rune and `$rune` re-encodes it; I could not execute Nim to confirm).
- Note says: §Reply schema — "Slicing a `string` by byte index on any path to the replay is forbidden." Checklist 9 names "captured errors" among the strings that must be truncated on rune boundaries.
- Why non-blocking as I read it: the observable requirement (valid UTF-8 in the replay, with a test feeding multi-byte input at the cap) is met for `say`/`note` (`tests/test_orders.nim:96-120`) and the error path is re-encoded before it is written. A judge applying item 9 literally to "captured errors" could take a different view. What would settle it: a test that feeds a byte-split multi-byte `fallback.detail` through `clipRunes` and asserts `isValidUtf8`.

### F4 — `drive` repair prefers the scripted fallback's vector over last turn's drive
- Where: `src/tandem/orders.nim:299-310`. `order` starts as a copy of the porter fallback (`:279`), so when `drive` is missing or non-numeric (`okX and okY` false) the `elif hasPrevious and (fallback.driveX == 0 and fallback.driveY == 0)` guard is false for any real porter order (porter always emits a non-zero drive, `baselines.nim:306-309`) and the porter doorway vector is kept. The `[0,0]` case *does* fall back to last turn's drive (`:305-307`).
- Note says: §Reply schema table — `drive` "non-finite / missing / `[0,0]` → last turn's `drive`, else the vector to the next doorway".
- Consequence: for the missing/non-finite case the precedence in the note is inverted. `tests/test_orders.nim:60-73` only exercises the `[0,0]` case, so nothing catches it. (The repo's own `docs/PROTOCOL.md` / manifest protocol text already describes the shipped behaviour — "→ last turn's `drive`, else the scripted fallback's" — so the docs and the code agree with each other and not with the note.)

### F5 — a disconnecting LLM seat does not degrade to `porter`
- Where: `src/tandem/server.nim:615-633` (close handling: `recordGameAbandon`, `writeLeave`, `removePlayerAt`, table cleanup — `engine.policies` is not touched) and `src/tandem/decide.nim:346-376` (a seat is queried whenever `policy.kind == pkLlm` and a batch exists; `SeatPolicy.connected` at `decide.nim:76` is written by `registrationOf` and never read by `turn`).
- Note says: §Decisions "Degrade, never hang" — "A seat that disconnects mid-run keeps playing: its order source degrades to `porter` and revives on reconnect."
- Observed consequence: the seat keeps playing (no cog is ever unactuated — `sim.initSimServer:129-132` seeds an order at tick 0), but its orders keep coming from the LLM after the socket dies, i.e. the episode keeps paying LLM latency for a seat nobody is watching. No hang: every call is bounded by `attempt1Ms`/`retryMs`/`turnBudgetMs` (`decide.nim:324-327, 381-398`).
- The test named for this (`tests/test_engine.nim:186-206`, "a seat with no transport plays porter and revives") asserts a different property: it constructs `newTurnEngine(nil, nil)` (no batch at all) and asserts the `no_credentials` fallback, then swaps in a batch. It does not exercise a disconnect.

### F6 — a registration payload over 65 535 bytes is dropped, not truncated
- Where: `src/tandem_player.nim:46-51` writes the Sprite v1 chat length as `text.len and 0xff` / `(text.len shr 8) and 0xff` with no cap; `src/tandem/server.nim:212-217` reads that u16 length and returns `""` when `3 + length > message.len`, so the frame is discarded and `registrationOf` is never called (`server.nim:689-693` drops it).
- Note says: §Reply schema — "`register.prompt` is capped at **≤ 4000 runes** at the transport (over-long is truncated, never rejected)". The server does truncate what it receives (`server.nim:427`, `clipRunes(..., MaxPromptRunes)`), but only if the packet arrives intact.
- Consequence: a `PLAYER_PROMPT` above ~64 KiB silently makes the seat a `porter` (its registration never lands) instead of being truncated. `tests/test_server.nim:41-52` tests a 12 000-byte prompt through `registrationOf` directly, not through the packet framing.

### F7 — `docker_smoke.sh` asserts the game container's exit code only
- Where: `tools/ci/docker_smoke.sh:237-243` (`docker inspect -f '{{.State.ExitCode}}' "${prefix}-game"`); the only other player-side assertion is the absence of `player_failure.json` (`:256-258`). No `docker inspect` of `${prefix}-player-N` exists in the file.
- Note says: §Tests, CI jobs — "`docker-smoke` … asserts **every player container's exit code** as well as the game's".
- Not a checklist item; checklist 6's four seat-count invariants plus the `SMOKE_SEATS` cross-check *are* all present (`docker_smoke.sh:106-150`) and the CI log shows `SMOKE_SEATS: 2`, `seats=2`, and no `SEAT-COUNT FAIL` anywhere.

### F8 — three `tests/test_physics.nim` assertions are weaker than §Tests 1
- Where and what:
  - `tests/test_physics.nim:115-131` (`contactsPush`) asserts `contact.approachMmS >= 0` and `contact.slideMmS >= 0`. Both quantities are non-negative *by construction* at `src/tandem/sim.nim:324-327` (`approachMmS: umPerTickToMmS(max(0'i32, -vn))`, `slideMmS` is an `isqrt` magnitude), so those two assertions cannot fail; the only load-bearing line in that proc is `sim.contactTicks > 0`. The note's item ("contact normal force is never negative (contacts never stick)") is about the clamped `normal` at `sim.nim:305-311`, which no test observes.
  - The note's "friction never reverses the slide direction within one substep" has no assertion anywhere in `tests/`.
  - `tests/test_physics.nim:111` bounds penetration at `InnerWall` (300 000 µm); the note says "its penetration never exceeds 60 000 µm".
  - `tests/test_physics.nim:49` runs 200 ticks and `:64` runs 240 where the note says 480.
- No test was loosened *during this run* — `git log --stat -- tests/` shows all 17 test files added in `68e39b0` and untouched since — so checklist 1's second half is not falsified; this is a note-versus-test gap, not a regression.

### F9 — the chrome removals go beyond the note's declared list
- Where: `client/replay_broadcast.html` is 2 252 lines against the starter's 4 165 (`/workspace/starters/coworld-ctf/client/replay_broadcast.html`). `diff` hunks: `528,833d527` (the `#povBadge`/`#fpv`/`#viewpanel` CSS — declared), `1506,1549d1358` (their markup — declared), and then `2068,3395c1811`, `3403,3473d1818`, `3537,3761d1881`, `3830,4103d1927` — ~1 900 lines of ctf's **game block**: `ingestFpMap`, `renderSquad`/`ensureScorebug`/`renderScorebug` (lives pips, flags, handicaps), `renderPov`/`renderFpv` + the whole raycaster, paint-splat FX, `onKill`/`onSteal`/`onReturn`/`onCapture`, `endcardWinCondition`/`renderEndcardRows`/`capturedHeartsHtml`.
- Note says: §Chrome provenance — "**Removed starter elements (exactly these):** `#viewpanel` … `#fpv` … and `#povBadge`" and "Nothing above them is rewritten".
- What I verified is intact (checklist 14's "sections 1–5"): `#viewport/#stage/#board/#lightpool/#grain` CSS (`:110-130`), `#lockerroom` (`:970-1072`), the scorebug band, `#mmwarn`, `#bannerlane`, `#killfeed`, the full `#transport` (every button, `#speedchips`, `#scrub`, `#momentum`, `#lulls`, `#scrub-win`, `#scrub-head`, `#tick-clock`) and `#endcard` markup (`:1336-1420`), the endcard CSS block (`:729-943`, byte-equal to the starter's for the rules I diffed), `relayout()` (`:1934-1979`), the feed/banner queues (`:1819-1869`) and the transport wiring (`:1886-1926`). The appended tandem block sits under the required banner (`:1984-1992`) and the CSS additions under a second banner (`:1146-1155`).
- Reading: the deleted JS is ctf gameplay rendering that has no tandem counterpart, and the chrome the checklist enumerates is present and unmodified, so I do not read this as the cogame-gridlock "rewrite that reuses the ids" failure. It is nonetheless more than the note declares, and the note's phrase "exactly these" is now inaccurate.

### F10 — no test exercises any HTTP/websocket route
- Where: `tests/test_server.nim` tests `registrationOf` (`:9-63`), `playerJoinAllowed` (`:65-76`), a `file://` results write (`:78-90`) and the two name spaces (`:92-121`). `grep -rn "healthz\|/client/\|runServerLoop\|ShutdownGrace" tests/` returns nothing.
- Note says: §Tests 9 — "`/healthz`; `/global` snapshot → ticks → game over; `/client/global` and `/client/player` serve real pages and neither opens the player socket; `/healthz` and `/global` still answer 15 s after the artifacts are written; artifact writes to `file://` URIs."
- Observed in the code (unasserted): the routes exist and are registered ahead of the catch-all (`src/tandem/server.nim:222-302`, `/client/global` and `/client/player` at `:285-295` respond with the page and never call `upgradeToWebSocket`), and the grace loop is `server.nim:847-853` (`ShutdownGraceSeconds = 20`, `server.nim:85`). Only the last of the note's five clauses (`file://` writes) is actually asserted.

### F11 — the scrape assertion in `tests/test_replay.nim` is an "or"
- Where: `tests/test_replay.nim:182` — `doAssert "scrape" in rough or "impact" in rough`.
- Note says: §Tests 8 — "the stream contains at least one `scrape`, one `doorway`, one `order` per seat per turn, and exactly one `result` record". The doorway (`:184`), per-seat order counts (`:169-171`) and single `result` (`:166`) assertions are exact; the scrape one is not.

### F12 — step-10 end-check order differs from the note
- Where: `src/tandem/sim.nim:643-654` — `Delivered` → `damage >= damageCap` → `physicsGuardTripped()` → `tickCount + 1 - gameStartTick >= maxTicks`. The wall-clock stop is checked in the server loop before the tick (`server.nim:733-738`), which matches edit 4 and is finer-grained than the note's "top of every loop iteration".
- Note says: §Resolution order 10 — Delivered → wrecked → wall_clock → out_of_time → fault.
- Consequence: on the single tick where the run would end `complete/out_of_time` *and* an invariant trips, the code reports `fault/sim_fault`. Both endings are legal enum values; the difference only decides which one wins a tie.

### F13 — the ≥20-point filter on `impact` beats lives only in the page
- Where: `src/tandem/broadcast.nim:113-116` emits `{"k":"impact","dmg":…}` whenever `sim.impacts` increments, and the sim increments it at `dmg >= ImpactEventFloor` = 8 (`sim.nim:440-441`); `replays.nim:92-93` puts `"impact"` in `BeatKinds`, so every such event also becomes a scrubber beat in the precomputed `beatEvents`. The page filters at `client/replay_broadcast.html:2186` (`if ((e.dmg || 0) >= 20)`) for live events but `tandemIngestBeats` (`:2164-2172`) places **every** beat from `s.beats` unfiltered.
- Note says: §Record vocabulary B — "**Beats** (scrubber markers): `doorway`, `impact` (≥ 20 points), `drop`, `wrecked`, `delivered`, `gameover`."
- Second, related observation: tandem's markers are created directly by the page (`:2137-2152`) rather than through `chrome_common.markBeat`, so chrome_common's spoiler gate (`client/chrome_common.js:489-503`, which iterates only its own `markerEls`) does not hide tandem beats ahead of the playhead when spoilers are off. Every marker is still a labelled `<button>` that seeks (checklist 14d satisfied).

### F14 — `AGENTS.md` is absent
- Where: repo root contains `README.md`, `LICENSE`, `config.json`, `compose.yaml`, … but no `AGENTS.md`.
- Note says: §Packaging "Repo layout: … `docs/{RULES.md, PROTOCOL.md, CARRYING.md, plans/}`, **`AGENTS.md`**, `README.md`, …".

### F15 — match-feed rows are double-escaped
- Where: `client/replay_broadcast.html:2122` — `row.textContent = esc(line.text);`. `esc` (`client/chrome_common.js:385`) returns HTML entities for `& < > "`, and `textContent` then displays them literally.
- Consequence: an LLM `say`/`note` containing a quote or an ampersand renders as `&quot;` / `&amp;` in the feed — the one surface where a spectator reads the model's words (§Viewer readout 9). Apostrophes are unaffected (`esc` does not escape `'`), so the shipped baseline strings ("you lead, I'll follow") render correctly.

---

## Observations (noteworthy, conformant)

- **O1 — the seven declared deviations, traced.** (1) `mule` ships at `MuleEffort = 140` (0.55) with the reasoning at `baselines.nim:65-73`, and `tests/test_baselines.nim:95-131` replaces the note's "≥14/20" with strict dominance of porter×mule over mule×mule in both progress and score — the substitution is stated in the test's own docstring and is a *property the filler exists for*; sound, though it is a weaker claim than the note's. (2) `SpinFine = 256` (`sim_types.nim:118-126`) plus the `spinRem` remainder (`sim.nim:370-380`) — `spinRem` is an `int32` field of `SimServer` (`sim_types.nim:439`), is mixed into `gameHash` (`sim_state.nim:100`) and rides in the flatty keyframe by construction, so seek/replay determinism is preserved; sound. (3) porter's conflict rule estimates the partner force from own strain (`baselines.nim:135-152`, `F_partner = 4H + 3F_self`) and requires `align < −ConflictCos` (60°) before leading — the derivation is correct for `m_cog/M = 1/4` and the note's own sign convention would indeed read cooperation as conflict; sound. (4) porter is a four-state machine (`baselines.nim:224-369`); it still satisfies every property the note's five steps assert (drive at the next gap, strain-arbitrated yield/effort, twist toward the through direction with a dead band, brace near a doorway or over 600 N); sound. (5) `tests/test_viewer.nim:127-146` scopes the no-`ctf_` grep to a file list that omits `client/chrome_common.js` — necessary, because that file is pinned byte-for-byte and contains `window.CTF_WIRE`; sound, and the exclusion is documented in the test. (6) the `"q"` array (`orders.nim:150-151`, installed at `orders.nim:154-178` and `broadcast.nim:168-174`) is what makes the note's "no new replay record kind" claim work; it is the only path an order takes into hashed state, on both the live and the replay side. (7) art is ctf's shipped rigs + pixie bakes (`global.nim:371-470`, `data/rig_real/{blue,red}`), as the note pins.
- **O2 — `InertiaMilliKgM2` is 139 050**, computed from the mass table at `sim_types.nim:86-103`; the note says "139 000". `tests/test_physics.nim:22-42` re-derives it in floats and pins the exact integer. The note's figure was the rounded one.
- **O3 — `playerResultsJson` writes 23 keys** (`roster.nim:157-182`); the note's §Manifest says "exactly the 24 keys in §Server" but the JSON block it shows has 23. `results_schema` declares the same 23 (`coworld_manifest_template.json`), and `tests/test_manifest.nim:57-99` asserts set equality, so the contract is self-consistent.
- **O4 — `scrapeTicks` counts per damaging disc per tick** (`sim.nim:451-454`), so a tick with three scraping hull discs adds 3. It is a spectator meter and is not in the score.
- **O5 — comment/value drift.** `baselines.nim:46-52` sets `TwistGain = 5` while the comment argues for "3 units per brad"; `baselines.nim:389` gives `mule` the note string "straight at the goal, full effort" although its effort is 0.55; `server.nim:477` says "the 690 s engine stop" where the constant is 660.
- **O6 — `/client/replay` exists as a game-pod route** (`server.nim:273-284`, via `bitworldClient.ReplayClientRoute`) and is documented in the manifest's `global` protocol text and `docs/PROTOCOL.md:19`. This is inherited verbatim from the starter (`/workspace/starters/coworld-ctf/src/ctf/server.nim:821`); the manifest declares `"replay_viewer": {"bundle": "static-replay-viewer"}` and `coworld-release.yml:190-202` hard-fails certification unless it reports the static bundle. I read checklist 3 as satisfied — the *viewer* is the static bundle and contacts only the replay URL — but the literal string does appear in the tree, so I record it rather than leave it silent.
- **O7 — `game.tags` is absent**; the five tags live at the manifest top level (`tags: ["physics","cooperative","carrying","continuous","llm"]`). The note's §Packaging asks for both a top-level `tags` ≥ 3 and `game.tags`. `tests/test_manifest.nim:159` asserts the top-level one only. Whether the 0.1.42 upload schema wants `game.tags` I could not determine from the tree.
- **O8 — `NIM_TESTS_RELEASE_ONLY` is evidently unset**: the CI log for run 32667457401 shows `tests/test_perf.nim` running in both debug and release. The note said the repo variable would list it. Both modes pass (2 400 ticks well inside the 60 s bound).
- **O9 — the viewer smoke's JSON reports `"feed_lines":0`** (run 32667457401, step "Load the bundle in a real browser"). The scorebug, clock and soak advance are all populated. Whether the match feed ever renders a row in the bundle is not settled by that snapshot (feed rows expire on a wall-clock dwell floor, `replay_broadcast.html:1826-1832`); see "Could not determine".

---

## Traced and consistent

**Resolution rules**
- Tick order matches §The game 1–10: turn boundary before the step (`server.nim:740-755`), regrip gate (`sim.nim:609-614`), control compile in seat order (`control.nim:82-93`), four substeps (`sim.nim:627-628`), contacts → sum of forces → semi-implicit Euler + drag → pose → felt strain inside each substep (`sim.nim:329-394`), damage (`sim.nim:405-479`), grip/drop (`sim.nim:481-526`), progress/doorway/delivery (`sim.nim:549-575`), stats (`sim.nim:637-640`, `applyGrip`), hash (`server.nim:761`), end checks (`sim.nim:643-654`).
- Every constant in §4.1–4.3 matches the source: `ContactStiffness 200`, `ContactDamping 29`, cap `40 000 000`, `FrictionNum/Den 354/1024`, `FrictionViscous 200`, `LinearDragNum 171`, `AngularDragNum 341`, `DragDen 4096`, `MaxSpeedUm 145 833`, `MassStepDen = 120 000*96*24`, `SpinStepNum 28 294` (`sim_types.nim:108-132`), and the code applies them exactly as written (`sim.nim:305-320, 352-368`).
- Sum-of-forces is literal: `F = F_contact + F₀ + F₁`, `τ = τ_contact + r₀×F₀ + r₁×F₁ div 1e6` (`sim.nim:341-362`); `tests/test_physics.nim:76-89` asserts `(a,b)` and `((a+b)/2,(a+b)/2)` give the identical velocity.
- Damage: impact `((approach−250)*40) div 1000` clamped 0..200 with an event floor of 8 (`sim.nim:436-450`); scrape `slide div 800` clamped 1..4 per tick, event throttled one per disc per 6 ticks (`sim.nim:451-465`, `ScrapeThrottleTicks = 6`); cog discs excluded (`for disc in 0 ..< HullDiscs`, `sim.nim:431`); blame by nearer handle (`sim.nim:400-403`) and explicitly out of the score (`roster.nim:174-176`, `scoreMicros` reads only par/tick/damage/progress).
- Grip/drop: `slip += max(0,(strain−limit) div 100 000) − 4`, threshold 240, drop adds `60 + clamp(speed_mm_s div 60,0,40)`, zeroes `vel`/`spin`, sets `Regrip` for `regripTicks`, resets both seats' slip and strain, increments `drops` (`sim.nim:481-526`) — every number as the note states.
- Course generation follows the note's draw order exactly (`course.nim:268-452`): `rand(0..4)` twice, the 60/20/20 self-avoiding walk with backtracking (`course.nim:106-164`), 9..15 length with 64 attempts then the monotone fallback (`:278-290`), width from the five legal values and offset from ±1.2 m clamped to a 200 000 µm edge margin (`:308-321`), the final door forced to 1 050 000/0 (`:317-319`), non-route cells solid, non-consecutive boundaries full walls, consecutive boundaries two stubs (`:351-393`), 45 % pillars with a quadrant draw and a through-line rejection (`:396-416`), the interleaved route polyline (`:419-425`), `parTicks = routeLen/1000*24/1600 + 36*narrow` (`:435-437`), the inset goal pad (`:445-448`) and the FNV-1a digest (`:170-199`). `tests/test_course.nim:7-100` checks all of it over 500 seeds.
- Determinism discipline: no `float`/libm in `{sim,sim_types,sim_config,sim_state,course,control,trig}.nim` — enforced by an identifier-level grep after comments and string literals are stripped (`tests/test_determinism.nim:126-160`, `helpers.nim:sourceText`); `SinQ12` is a committed table re-derived from `math.sin` entry by entry (`test_determinism.nim:162-186`); `isqrt` exhaustive below 2¹⁶; `bradsOfVectorI` within ±1 brad of `arctan2` over 100 000 vectors and exactly antisymmetric (`:188-210`). Every hashed field is `int32`/enum/bool (`sim_types.nim:434-449`). RNG is used only at `t = 0` (course generation; `sim.rng` is never drawn from after `initSimServer`).
- `gameHash` (`sim_state.nim:79-122`) mixes tick, phase, verdict, pose, velocity, heading, spin, `spinRem`, damage, slip, both strains, progress, deliveryTick, regripUntil, drops, doorsCleared, `courseDigest` and **both quantised orders** — and no FX, notes, `say`, feed text or policy label.
- Scoring is the note's formula in integer millionths, computed once and copied (`sim.nim:184-202`, `roster.nim:128-131,147-155`): delivered `0.30 + 0.35·speed + 0.35·cond`, otherwise `0.25·progress·cond`; `win = [delivered, delivered]`.

**Decision path (checklist 8 + the simultaneous-decision rule)**
- One `curly.makeRequests` per attempt carrying both seats (`decide.nim:240-270`); `decide.turn` builds `calls` for both seats before any transport call (`:346-379`); `tests/test_engine.nim:44-70` asserts a single batch of two distinct seats.
- Retry exactly once (`decide.nim:382`, `while calls.len > 0 and attempt <= 2`), then the porter order with `source = osFallback` (`:445-449`, `fallbackFor` at `:284-293`), and a `fallback` record per failed seat per attempt with `cause ∈ {timeout, transport_error, parse_error, no_credentials, budget_guard}` (`:357-371, 433-438`); `tests/test_engine.nim:97-124` asserts four fallback records and two `source:"fallback"` orders.
- Parsing is tolerant: fence/prose tolerant `extractJsonObject` (`llm.nim:195-207`), numeric strings, `{"x","y"}` drive, percentage repair, per-field defaults (`orders.nim:232-332`); `tests/test_orders.nim:14-88` covers each.
- Credential ladder Bedrock → `ANTHROPIC_API_KEY` → `ANTHROPIC_API_KEY_URI` → disabled-with-instant-fallback (`llm.nim:57-129`); the sonnet-4-6 profile is absent from `bedrockModelIds()` (`llm.nim:82-85`); `max_tokens` from config (default 900), no `temperature`, no `output_config.effort` on Bedrock or on haiku/4-5 (`llm.nim:143-162`); system prompt demands a leading `{` (`decide.nim:44`).
- Budget guard at the top of the turn (`decide.nim:331-340`), writes a `budget_guard` record and switches every remaining turn to the scripted layer; `tests/test_engine.nim:126-140` asserts the run then ends `complete`.

**Waits and bounds (checklist 5)**
- Turn cap `turnBudgetMs` via a monotonic deadline (`decide.nim:324-327`, `allowedMs = min(want, remaining)`, `:384-388`); attempt deadlines floored to whole seconds before curly (`:398`).
- Inter-batch floor: a bounded `sleep`, capped by both `minBatchSpacingMs` and the remaining wall budget (`decide.nim:295-307`).
- Wall-clock stop inside the tick loop (`server.nim:733-738`) → `deadline/wall_clock` (`sim.nim:230-234`); default 660 s (`sim_types.nim:168`), 660 in variant `default` and 420 in `sprint`, 180 in the cert fixture; `tests/test_manifest.nim:22-23` asserts every variant ≤ 720.
- Lobby: `lobbyJoinTimedOut` at `lobbyJoinTimeoutTicks` lobby ticks (`sim_state.nim:33-39`), after which the no-show is declared and the game starts anyway (`server.nim:708-717`).
- Shutdown grace is a bounded 20 s loop (`server.nim:847-853`). Frame limiter sleeps ≤ 2 ms per pass (`server.nim:364-384`).
- Player container: connect retry bounded at 90 s (`tandem_player.nim:24-32,57-75`), `receiveMessage(ReceiveTimeoutMs = 120 000)` (`:33-40,112`), every send/receive wrapped, exits 0 on a dead socket (`:107-140`).
- Startup network reads: `mummy.waitUntilReady` raises after 10 s (`/root/.nimby/pkgs/mummy/src/mummy.nim:1548-1563`); `readCogameUri`'s HTTPS branch uses curly's `get` whose default timeout is 60 s (`/root/.nimby/pkgs/bitworld/src/bitworld/runtime.nim:106-115`, `/root/.nimby/pkgs/curly/src/curly.nim:642-646`).
- Arithmetic: 660 s stop + 20 s grace + the game-over hold ≈ 680 s < 720 s (60 % of `episode_timeout_minutes: 20`). The wall-clock clock starts before the board bake and before the listener opens (`server.nim:479`), so setup is charged against it.

**Replay writer / re-derivation (checklist 2)**
- Header carries the resolved config *including the fully expanded course*, every physics constant, the roster with real names, slots/aliases, tokens, `num_agents`, `fastMode` (`sim_config.nim:213-265`); the writer is opened with it (`server.nim:506-507`); `tests/test_replay.nim:132-152` asserts the recorded course round-trips and equals `generateCourse(seed)`.
- Records are written and folded back through the *same* `applyRecord` on both sides (`server.nim:566-575`, `replays.nim:259-262`), so live and playback install bit-identical integers; keyframes exclude the course and restore it from the donor (`replays.nim:142-155`).
- `stepReplay` = apply records → `control.stepSim` → `checkReplayHash` per tick, with `mismatchTick` surfaced (`replays.nim:264-304`); `tests/test_replay.nim:104-127` re-simulates a recorded episode and asserts `hashMismatchTick == -1` plus equal final damage/progress/deliveryTick.
- Strict-UTF-8 forensics path: `tools/replay_summary.py` (stdlib only, `ensure_ascii=False`, strict decode first with a counted repair, `:76-83, 294-297`), asserted with a non-ASCII `say` and policy label (`tests/test_replay.nim:129-146`).

**Viewer (checklist 13)**
- `replay-viewer/config.nims` is ctf's with only the `ctf_*` → `tandem_*` renames (4 hunks), no `MODULARIZE`, no `EXPORT_NAME`; `static_replay_worker.js` is ctf's non-modularized `var Module = {}` + `Module.onRuntimeInitialized` + `importScripts('./…tandem_replay.js')` (`:8, 166, 214`) — same starter, matched pair; `tests/test_viewer.nim:169-190` pins both halves.
- `data-replay-loaded='true'` on the first drawn frame (`static_replay.js:152`, the starter's line 144 + the 8 added lines) and `data-replay-error` set in `showFailure` before `#status` (`:15-22`) — both from the shell's own code paths.
- `tandem_replay.nim` keeps the stage-note buffer, `ABORTING_MALLOC` diagnostics, the `predictedViewerRenderBytes` capacity check and `emscripten_exit_with_live_runtime` (`:14-34, 66-87, 151-164`) and imports the same `tandem/sim` the server runs.
- CI: run **32667457401** on `main` at `4b78981`, conclusion `success`, jobs `test`/`docker-smoke`/`wasm-viewer` all success; `wasm-viewer` `needs: docker-smoke` (`ci.yml:220`); the step **"Load the bundle in a real browser"** ran and printed `{"loaded":true,"ms":2340,…}` plus `soak: 12s of playback kept advancing ("0 / 948" -> "239 / 948" -> "287 / 948")`; the `Native/wasm determinism gate` step (`tools/wasm_replay_smoke.cjs`) also ran green. No `continue-on-error` anywhere in `ci.yml`.

**Chrome provenance (checklist 14)**
- `client/chrome_common.js` is byte-identical to the starter's: `sha256 7ace7287…d72f7c` on both copies, `diff` empty; `tests/test_viewer.nim:41-56` additionally pins length 40 022 and an FNV digest.
- `client/broadcast_core.js` differs from the starter in exactly the wire-constant identifier (8 diff lines, one hunk; `tests/test_viewer.nim:148-158` asserts `TANDEM_WIRE` appears exactly twice).
- Transport rules: (a) `relayout()` measures `#transport`/`#scorebug` and sets `--hudscale`, `--topband`, `--band` on `document.documentElement` (`replay_broadcast.html:1946-1976`); (b) tandem's overlays ride `bottom: calc(var(--band, 0px) + 10 * var(--u))` inside `#chrome` (`:1237-1258`, markup `:1361-1376`); (c) `#endcard { bottom: var(--band, 0px) }` kept (`:730-752`), shown with `#endcard.on`, removed on every non-gameover frame — i.e. after any seek (`:1802-1803`); (d) beats are `<button class="beat-marker <kind>">` with `aria-label`, `title` and a click that sends `s:<tick>` (`:2137-2152`), and `.beat-marker.{doorway,impact,drop,wrecked,delivered,over}` all have rules (`:1276-1288`).
- `#viewpanel`/`#fpv`/`#povBadge` and all their children are gone from markup and CSS, and nothing wires `core.zoomAt/setZoom/attachMinimap` (`tests/test_viewer.nim:58-77`); the design justifies dropping the panel (fixed 1110×630 board).
- `.plate-name { flex: 1 1 auto; min-width: 3.2em; … }` (`:1170`) and the `@media (max-width: 640px)` block hiding `.strain-num`, `.plate-blame`, `.plate-alias`, `#arrowlegend` (`:1303-1309`), plus the `#stage.tiny` density rules (`:1292-1300`) — checklist 11.

**Manifest (checklists 3, 6, 10, 12)**
- `num_agents: 2` in variant `default`, variant `sprint` and `certification.game_config`; `certification.players` has 2 entries; `game_config.players`/`slots` have 2 each. `SMOKE_SEATS: "2"` in `ci.yml:190`; `docker_smoke.sh:106-150` enforces the four invariants plus the cross-check, each with a `SEAT-COUNT FAIL:` prefix; the docker-smoke log shows `seats=2` and no `SEAT-COUNT FAIL`.
- `game.replay_viewer.bundle == "static-replay-viewer"`; `tools/build_replay_viewer.sh` is present, mode `100755`, and `mkdir -p "$(dirname …)"` precedes the containment check (`:17-26`); `tools/ci/docker_smoke.sh` and `tools/ci/viewer_smoke.mjs` are also `100755`.
- `game.docs` = `readme` (3 451 chars) + three pages `rules.md`/`protocol.md`/`carrying.md` (9 605 / 6 428 / 3 711 chars), all `{"type":"text","value":…}`; `game.protocols` carries both `player` (1 976 chars) and `global` (1 278 chars) as text.
- `game.runnable.env.ANTHROPIC_API_KEY_URI = "secret://coworld/tandem/anthropic_api_key"`; `results_schema` is `additionalProperties: false` with the 23 keys `playerResultsJson` writes, the three-value `reason` enum, the six-value `endRule` enum and `minItems/maxItems: 2` on every per-seat array; `config_schema` covers all 24 fields `sim_config.update` reads plus `slots`/`players`/`tokens`.
- `coworld-release.yml` order: build (`:153`) → certify (`:167`) → **upload policies** (`:206`) → upload coworld (`:304`) → secret put (`:342`). `tools/ci/policies.json` has four entries, two `PLAYER_PROMPT` champions (`tandem-anchor`, `tandem-feather`, the latter carrying `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`) and two `PLAYER_SCRIPTED` fillers (`porter`, `mule`), all `"run": "/bin/tandem-player"`; the prompts are the note's champion texts verbatim.
- The placeholder gate exits 0: `grep -n '<slug>\|<IMAGE>\|<SEATS>'` over the three workflows, `docker_smoke.sh` and `policies.json` returns nothing.
- `coworld-submit.yml` inputs are `player_id`, `policy`, `league_id`, all required strings, with the `daveey`/`daveey-1` ids in the description.

**Two name spaces (checklist 4)**
- In-game vocabulary is `Cobalt`/`Rust` only, structurally: every board label is built from `seatAlias()` (`global.nim:802-832`, `labels.nim`), and the LLM view uses aliases (`decide.nim:177-180`). Real names appear in the replay config roster (`sim_config.nim:222-225`), the chrome roster/scorebug/endcard (`broadcast.nim:199-243, 343-368`) and `results.names` (`roster.nim:113-122,148`). `tests/test_server.nim:92-121` asserts the guarantee with `showPlayerLabels` forced **true**; `tests/test_no_channel.nim:22-79` asserts nothing of the partner's order, note, say or quantised fields reaches a seat over 200 randomised pairs, and `:81-102` that `seatForce` is independent of the partner's order.

**Tests present**: all 13 files the note's §Tests names exist and are wired into `ci.yml`'s matrix (`ls tests/*.nim` → baselines, control, course, determinism, engine, manifest, no_channel, orders, perf, physics, replay, server, startup, viewer = 14 files for the note's 13 items, since §Tests 9 is two files). `git log --stat -- tests/` shows one commit, no later edits: **no test was disabled, skipped, deleted or loosened during this run** (checklist 1, second half).

---

## Could not determine

- **Whether porter×mule could reach the note's 14/20 at some other `mule` effort.** The claim that effort 1.0 makes the mule "immovable" rests on the measured numbers quoted in `baselines.nim:65-73` and `tests/test_baselines.nim:95-113`; the sandbox has no Nim, so I could not reproduce the sweep. A committed sweep log (see B1) would settle both this and item 7.
- **Whether `clipRunes` on a byte-split multi-byte sequence yields valid UTF-8** (F3). Settled by running `tests/test_orders.nim` with a case that byte-slices a multi-byte `detail` before clipping, or by a one-line Nim REPL check — neither available here.
- **Whether the match feed ever renders rows in the static bundle** (O9). The viewer smoke reported `feed_lines: 0` at its sampling moment; rows expire on a wall-clock dwell floor, so a zero there is consistent with both "rendered and expired" and "never rendered". A `--soak` screenshot taken right after a turn boundary, or a smoke assertion on `#killfeed` children, would settle it.
- **Whether the 0.1.42 upload schema requires `game.tags`** (O7). Settled by `coworld build`/`validate_upload_manifest` output — the release workflow runs it, but no such run exists yet for this repo.
- **Whether any hosted episode has actually exercised the LLM path** (`source == "llm"` orders, `fallbackTurns` counts). Everything in CI is scripted (no credentials in the smoke), so the batch transport is only covered by the fake in `tests/test_engine.nim`. Phase 60's `replay_summary.py` check is the intended settler.
