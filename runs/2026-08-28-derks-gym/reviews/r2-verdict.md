blocking: 0

# r2 verdict — derks-gym

Head: `7c87e98d332223a37103f474ca0a4d7a4f90d92e`   Checklist: `prompts/30-review-loop.md` §ACCEPTANCE CHECKLIST   Independent read written before reading fixes: yes
Review judged: `runs/2026-08-28-derks-gym/reviews/r2-review.md` (written at `624f1cb`; **six fix commits landed after it** — `6373057`, `6a43ba1`, `636e747`, `3b1f78e`, `8b1fa2a`, `eb077ee`, plus `7c87e98` — every finding is verified at the current head, from the code and `git log -p`, never from a fixer claim).
`r2-fixes.md` and `r1-fixes.md` were **not read**, per the brief. `r1-verdict.md` was read (permitted) — after my independent read of the tree.
CI evidence: run **33172526475**, conclusion `success`, headSha `7c87e98d…` (`gh run list -R Metta-AI/cogame-derks-gym --branch main -w ci.yml --json databaseId,headSha,conclusion`); jobs `test` ✓, `docker-smoke` ✓, `wasm-viewer` ✓ (browser-load step ran: `{"loaded":true,"ms":556,…}`; `Assert the derks-gym chrome` → `all derks-gym chrome checks passed`), `upload-coworld` ✓. `grep -c "SEAT-COUNT FAIL"` over the docker-smoke job log = **0**.

---

## Standing blocking findings

None. Every one of the review's eight findings is resolved at head or was correctly filed non-blocking and remains non-blocking; my own checklist pass found nothing new that ties to a checklist item.

---

## Refuted / resolved review findings

All eight findings were **true at the review's sha (`624f1cb`)** — I reproduced each from that commit's code via `git show` — and every one that named a code defect is fixed by a post-review commit. A finding that was true and has since been fixed is refuted at head, not standing.

### F1 — prose-wrapped JSON rejected (the r1 blocking finding, item 8)  → RESOLVED by `6373057`
- Evidence: `players/derk_player.py:168-195` at head — `first_json_object` scans every `{` and parses with `json.JSONDecoder().raw_decode`, so nesting/strings/escapes are the JSON parser's problem, not a brace counter's; `legal_picks` (`:198-228`) runs it after `strip_one_fence`. Prose before, after, both sides, prose+fence, fence+trailing prose, and `[{…}]` are all accepted — `tests/test_llm_player.py:100-113` parametrises exactly those six shapes ("Checklist item 8" named in the docstring); `:116-123` pins first-object-wins; `:126-133` pins that braces inside a `note` string cannot truncate the object. `test_a_prose_wrapped_legal_reply_never_reaches_the_fallback` (`:359-366`) proves the prose case is a draft, not a fallback. Item 8's tolerance clause is satisfied.

### F2 — LLM call starves the websocket heartbeat (~30 s close vs 40 s worst case)  → RESOLVED by `6a43ba1`
- Evidence: `players/client.py:205-217` at head — the draft decision runs as `asyncio.create_task(_answer_draft(ws, policy, data))`, **not** awaited inside `async for msg in ws`, so `receive()` keeps servicing ping/pong while the call runs; the task sends the reply when the decision resolves (`:253-287`), a send onto a dead socket is logged not raised, and a decision still in flight at connection end is cancelled in the `finally` (`:242-249`). Test `tests/test_players.py:382+` (`test_a_slow_draft_keeps_answering_the_ws_heartbeat`) drives a 1.2 s draft against a 0.4 s server heartbeat (production shape scaled, `block > heartbeat * 1.5`) with `max_connect_attempts=1` so a reconnect cannot mask a regression; the commit message records it fails on the pre-fix code.

### F3 — player ignores the observation's `deadline_ms` (cert fixture: 5 s deadline vs up-to-40 s player)  → RESOLVED by `636e747`
- Evidence: `players/derk_player.py:239-259` at head — `call_timeout(observation, elapsed)` caps each call at `min(20.0, deadline_ms/1000 − 1.5 − elapsed)` and returns `None` (no call) below a 1 s floor; `on_draft` (`:325-332`) recomputes it before each attempt and skips with a logged reason. Tests: `test_call_timeout_is_capped_by_the_servers_draft_deadline` (`tests/test_llm_player.py:222-236` — 5000 ms ⇒ ≈3.5 s, then `None`; the schema-minimum 1000 ms ⇒ no call; 2×budget+margin ≤ 45 s), and `test_under_the_certification_deadline_one_short_call_then_scripted` (`:239-253` — one call, no retry, `elapsed < 5.0`, `reason=timeout`). The cert-fixture seat now answers (scripted) inside the server's 5 s deadline instead of being resolved `timeout` while still off-socket.

### F4 — player-side fallback recorded only in stderr  → RESOLVED (documented + tested) by `3b1f78e`; item 8 ruling: PASS
- Evidence: `docs/DRAFT.md` §"Two kinds of fallback (and where each one is counted)" now pins the two records explicitly: `results.draft_fallbacks` counts server-side substitution *only*; the player-side LLM→scripted fallback's record is the single stderr line `draft_fallback=scripted reason=<…> picks={…}`, emitted from exactly one code path (`derk_player.py:308-314`, `_scripted_fallback`), with the counting recipe stated ("count LLM usage from the player logs … not from `results.draft_fallbacks`"). `tests/test_llm_player.py:338-346` asserts the doc names every reason and both records. My ruling on the reviewer's open question: item 8 says "the fallback is **recorded** so phase 60 can count it" — a stable, closed-vocabulary, single-emission-site log line with a documented counting recipe is a countable record; the reply the player then sends is a legal pick, so widening the closed protocol/results schema for it would be the wrong fix. Not blocking.

### F5 — fallback log vocabulary wider than documented; prose failure mislabelled `illegal`  → RESOLVED by `3b1f78e` + `6373057`
- Evidence: `derk_player.py:65-66` — `FALLBACK_REASONS = ("no_key", "no_time", "timeout", "parse", "illegal", "transport")`, a closed tuple; `_scripted_fallback` asserts membership (`:311`); `transport` is now the bare word (exception type logged on its own line, `:342-344`); `parse` vs `illegal` is decided by whether an object was *extracted* (`:356-358`), not by `"{" in text`. Tests: `test_every_fallback_logs_its_own_reason` parametrises **all six** reasons and asserts one line, one shape (`tests/test_llm_player.py:300-335`); `test_a_broken_object_is_parse_not_illegal` (`:349-356`) pins the reviewer's exact mislabel case; `test_the_fallback_vocabulary_is_closed_and_documented` pins the tuple and the doc.

### F6 — vacuous `assert transport.bodies == []`  → RESOLVED by `8b1fa2a`
- Evidence: `tests/test_llm_player.py:276-295` at head — `_anthropic_call` is monkeypatched to a function that appends to `calls` and raises `AssertionError("an LLM call was made without an API key")`; the test asserts `calls == []` **and** `p.last_request is None` (the body was never even built) alongside the picks and the stderr line. The dead `Transport` construction is gone. `git log -p` confirms the removed line was the vacuous one, replaced by strictly stronger assertions — not a loosening.

### F7 — worst-case-note fixture at one viewport only  → RESOLVED by `eb077ee`
- Evidence: `tools/ci/derk_viewer_checks.mjs:407-462` — the worst-case replay (all six seat notes = `"W".repeat(120)`, unbreakable) is loaded through the real bundle at **both** `1280×800` and `360×640`, asserting per size: 6 notes rendered, every note still exactly 120 runes (the anti-silently-shortened guard), zero clipped, no sideways overflow, overlay stops above `var(--band)`. CI run 33172526475 logs all ten `worst case <size>:` checks `ok` at both sizes. On the reviewer's other literal deviation (bespoke driver vs `viewer_smoke.mjs --strict-text-bounds`): the checklist's item 15 as given to this run says `total: 0` (WebGL) is not evidence and asks whether the repo "gates the worst case **where the text is actually drawn**" — here the only model-authored string is DOM-rendered, so the DOM fixture is the required gate and `--strict-text-bounds` (still passed, reporting `canvas text: 0 drawn`) is correctly treated as covering nothing. PASS.

### F8 — commit-message overstatement + in-repo design note still declaring the 7-value enum  → RESOLVED by `7c87e98`
- Evidence: `docs/plans/2026-08-28-derks-gym-design.md:703-704` at head declares the **six**-value `fallback_cause` enum, with an editor's note (`:707-711`) recording that the planned seventh value `"malformed"` was unreachable and the divergence resolved in the code's favour; `draft.py:48-49` and `coworld_manifest_template.json:473-481` agree, cross-checked by `tests/test_manifest.py:100`. The commit-message-overstatement half of F8 was a wording fact about `450c798`, not a code defect; the end state was already consistent.

---

## Checklist pass (independent)

| item | status | evidence (path:line or run) |
|---|---|---|
| 1 CI green, no test loosened | **PASS** | Run `33172526475`, `conclusion: success`, headSha `7c87e98d…`; jobs test/docker-smoke/wasm-viewer/upload-coworld all ✓. `git log -p -- tests/` (all 21 commits): every removed line read — the vacuous assertion replaced by stronger ones (`8b1fa2a`), the single-seat timeout assertion replaced by an all-six-seats assertion (`5adc034`), `loadout_digest == 0` replaced by a C↔Python digest cross-check (`74fcfc7`, FNV-1a of 80 zero f32s is 2545393349, not 0), the drafter-env assertion widened to include the secret URI (`70db559`), inline test clients taught to answer/ignore the draft phase. No deleted test file, no widened tolerance, no `skip`/`xfail` added (the wasm skips hard-fail under `COGAME_REQUIRE_WASM_BUILD=1`, which `ci.yml:81` sets). |
| 2 replay re-derivation | **PASS** | `tests/test_replay.py:171-208` — fresh sim fed the replay's seed+actions reproduces final tick, winner, and final obs bytes; `tests/test_viewer.py:195-270` — the **viewer core** re-simulates the recorded replay and its `state_digest`, ancient healths, per-pid stats and hero positions equal the recording sim's at the same tick. The display chrome reads `viewer_ancient_health`/`viewer_agent_stat`/`viewer_hero_positions` from the re-simulated wasm each frame (`viewer/derk_chrome.js:524-555`), not a parallel recording; `viewer_set_loadout` + `loadout_digest` cross-check (`derk_chrome.js:132-164`). |
| 3 static viewer | **PASS** | `coworld_manifest_template.json`: `"replay_viewer": {"bundle": "static-replay-viewer"}`; `tools/build_replay_viewer.sh` exists, mode 100755, exercised by `ci.yml:183`. The bundle fetches only its `?replay=` URL and its own co-shipped assets (`derk_items.svg`, same directory). The server's `/client/replay` route (`server.py:988`) is the starter's legacy local-view route, byte-inherited from cogame-moba (`cogame-moba/server/cogame_moba/server.py:733-765`), documented in AGENTS.md, and never declared in the manifest — the platform serves the static bundle. `coworld-release.yml:204-212` hard-fails certification unless the STATIC-bundle liveness marker appears ("a pod-served /client/replay viewer is not acceptable"). |
| 4 both name spaces | **PASS** | Draft observation and `/global`/`draft_result` are alias-only (`draft.py:125-163`, `spectator_records` strips `player_name`, `server.py:621-632`); real names in `results.names` + replay-header `config.players`; viewer shows alias **and** real name per row (`derk_chrome.js:176-177, 263-265`). Tests: `test_draft.py:287` (no real name in any draft observation), `test_llm_player.py:370-383` (no real name in the LLM request body), `derk_viewer_checks.mjs` ("every draft card's real player name differs from its alias" — `ok` in CI). |
| 5 degrade-never-hang | **PASS** | Connect: `wait_for(..., player_connect_timeout_seconds)` (`server.py:489-491`). Draft: one `asyncio.gather`, each seat under `wait_for` against one shared deadline instant (`draft.py:284-294`). Ticks: per-seat `wait_for(deadline)` (`engine.py:421-423`); dead-seat probes never awaited (`engine.py:370-393`) and cancelled in `finally`; `fail_waiter` resolves waiters on disconnect (`server.py:253-269`). LLM: 20 s per call, capped by the server's own `deadline_ms` (`derk_player.py:239-259`). Done-sends bounded at 3 s each. Budget: 60 + 45 + 6000×0.1 = 705 s < 720 s; engine hard stop `wall_clock_budget_seconds` 645 timed **from the draft** (`server.py:513-515`, `config.py:148-151` derives draft+play); `episode_timeout_minutes: 20`. `tests/test_server.py::test_wall_clock_budget_writes_artifacts`. |
| 6 num_agents | **PASS** | `num_agents: 6` inside `variants[0].game_config`, `variants[1].game_config`, `certification.game_config`; absent from variant top level (`tests/test_manifest.py:127-137`). `docker_smoke.sh:106-151`: the four seat-count invariants hard-fail with `SEAT-COUNT FAIL`; `SMOKE_SEATS` default 6 is the independent cross-check (`:54,141-151`). Smoke log of run 33172526475: zero `SEAT-COUNT FAIL`, `smoke OK: seats=6`, `derks-gym smoke OK: end_reason=tick_cap … final_tick=1200`. |
| 7 scripted baseline full legal episodes | **PASS** | `tests/test_baseline.py:106-151` — full websocket episode to natural end (pretrained side must beat random; completion + winner + zero dead seats asserted); `:158-186` — 300 live-sim ticks per baseline, every row six ints in `0..ACT_HIGH-1` and `_sanitize` non-None; `:187+` — both draft rules legal for all ten hero/role combos. On tuning: the micro layer is the vendored pretrained network (trained, not guessed) and the draft rules are fixed catalog tables / structural stat splits with no free numeric parameter a grid could tune — the r1 ruling holds on my own read. |
| 8 LLM reply handling | **PASS** | Tolerant extraction (`first_json_object`, F1 above); exactly one retry at `temperature: 0` with the reminder line (`derk_player.py:325-362`, `for attempt in (1, 2)`); scripted fallback `forge_picks`; fallback recorded via the closed-vocabulary `draft_fallback=scripted reason=<…>` line with the phase-60 counting recipe documented and tested (F4/F5 above). |
| 9 rune-safe truncation | **PASS** | Server: `draft.truncate_note` (`draft.py:76-93`) — scalar slice + C0/C1/lone-surrogate strip; player pre-send trim `note[:120]` (`derk_player.py:227`). Tests: 4-byte emoji straddling index 120 and combining sequences (`test_draft.py:218-243`); 3000-emoji note → exactly 120 rockets, valid strict UTF-8, frame ≤ 4096 B (`test_llm_player.py:142-161`); replay header strict-UTF-8 parse + flipped-byte and wrapping-header_len negatives (`test_replay.py:250-296`). |
| 10 manifest validates | **PASS** | `game.docs` = `readme` + two `pages` (DRAFT.md, PORTING.md); `game.protocols` has **both** `player` and `global` (URIs to PROTOCOL.md). `tests/test_manifest.py:206-219`; results_schema/`_results_doc`/docker-smoke key sets asserted equal (`:70-92`); `end_reason` and `fallback_cause` enums synced (`:93-104`). |
| 11 legible at 360 px | **PASS** | Lineage-equivalent of the pinned selectors: `#derk-roster .derk-name { flex: 1 1 auto; min-width: 3.2em; … ellipsis }` (`derk_chrome.css:118-125`); labels hidden under the 720 px breakpoint (`:286-294` — scorebug stats, roster role/deltas). CI at 360×640: scorebug 11.2 px ≥ 10, feed 10.4 px ≥ 10, zero band overlaps (`derk_viewer_checks.mjs` step, `ok` ×3). |
| 12 release order and scaffold | **PASS** | `coworld-release.yml`: build (`:159`) → certify (`:173`, with the static-bundle liveness gate) → upload-policies (`:216`, explicitly BEFORE upload-coworld) → upload-coworld (`:314`) → secret put (`:410`, explicitly AFTER). Three workflows present (`ci.yml`, `coworld-release.yml`, `coworld-submit.yml`). `docker_smoke.sh` mode 100755, invoked by path, and CI asserts the exec bit (`ci.yml:94-102`). Smoke steps build their own binaries in-job (`docker build` at `ci.yml:105`; bundle built at `:183`). `policies.json`: 4 distinct policies — 2 `PLAYER_PROMPT` champions (champion #2 carries `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`) + 2 scripted; the `/bin/derks-gym-player` run target exists (`Dockerfile:87-88`). Placeholder grep `<slug>|<IMAGE>|<SEATS>` over the five files: zero hits (gate exits 0); the only angle-bracket residue is the documented runtime names in comments (`<cow_id>`, `<sha>`, `<run_id>`, `<name>`). |
| 13 viewer executes | **PASS** | (i) `wasm-viewer` green in run 33172526475 including the browser-load step (`viewer_smoke.mjs`, headless chromium, loading docker-smoke's replay via the `smoke-replay` artifact): `{"loaded":true,"ms":556,…}`; `needs: docker-smoke` (`ci.yml:146`); no `continue-on-error` anywhere in the workflow. (ii) `data-replay-loaded="true"` set on the first drawn frame with `Module.canvas.width > 0` (`derk_chrome.js:605-611`), `data-replay-error` set from every failure path — load `catch`, `onAbort`, `onExit` (`index.html:275-293`, `derk_chrome.js:613-615`). (iii) Recording starts at tick 0 (`ReplayWriter.append_tick` sequential-from-0, `test_replay.py:123`), and the chrome checks assert `tick 0 / <total> · 0:00` after an explicit pause+seek-0 (`derk_viewer_checks.mjs:214-267`, `ok` in CI). (iv) Browser link line has no `MODULARIZE`/`EXPORT_NAME` (`sim/build_viewer.sh:66-69`), shell keeps the matching `onRuntimeInitialized` bootstrap (`index.html:285`); node test build (`-sMODULARIZE=1 -sEXPORT_NAME=createViewerCore`, `:98-99`) is a separate artifact never loaded by the shell; `tests/test_viewer.py::test_browser_build_flags_stay_paired` pins the pairing. |
| 14 chrome is the starter's | **PASS** | My own `diff` against `/workspace/starters/cogame-moba/viewer/index.html`: the inline transport script is the starter's with additive hooks only (`derkOnFrame` in `refreshUi`, `derkOnLoad` in `loadReplay`, `derkDismissEndcard` in each of the three seek paths, `derkFail` wrapping the existing failure paths) plus the note-mandated renames (`MOBA`→`DERK`, v1→2, `moba_viewer.js`→`derk_viewer.js`, title/header) and the `seat_hero_pids` mapping forced by the removal of `heroes_per_seat` — same two lineage-necessary edits r1 ruled non-blocking; no starter id reused/redefined/removed (the game block is one appended `#derk` div, every id `derk-`-prefixed; `test_viewer.py::test_bundle_index_links_the_game_block`). Transport rules: (a) `relayout()` sets `--band`/`--hudscale` on `document.documentElement` (`derk_chrome.js:485-507`); (b) nothing fixed sits inside the band — CI asserts zero overlaps at 360 px and both overlays ≤ `controls.top` (the `#derk-beats` strip is positioned off `#seek`'s own box and *is* transport, per item 14(d)); (c) `#derk-endcard` is `inset: 0 0 var(--band) 0` and every seek dismisses it (three call sites + `derkSeekTo` reuses the starter's `change` path) — CI: "the seek dismissed #derk-endcard" and "stops above the transport band" both `ok`; (d) beats are labelled `<button>`s with `aria-label`, seek on click, CSS for all seven kinds + the `+n` chip (`derk_chrome.css:196-203`). Zoom/minimap: the board is pannable (41×23 camera over 128×128), and `#derk-viewpanel` is present — seven camera buttons with `aria-pressed` + a clickable 128×128 minimap with the camera window rectangle (`derk_chrome.js:281-371`). |
| 15 every drawn string fits its frame | **PASS** | `viewer_smoke.mjs` reports `canvas_text` — `canvas text: 0 drawn` (WebGL renderer), correctly not taken as evidence. The gate where the text **is** drawn: the worst-case DOM fixture (six unbreakable 120-rune notes injected into the real smoke replay) at **both** 1280×800 and 360×640, asserting full-length (no silent shortening), zero clipping, no sideways overflow, band respected — all `ok` in run 33172526475 (F7 above). `.derk-note { overflow-wrap: anywhere }` is the CSS under test. |
| batch rule (sequential LLM calls) | **PASS** | Server side: all six draft observations sent as one `asyncio.gather` under one shared deadline instant (`draft.py:284-294`; `test_draft.py:201` proves six 5 s seats cost one deadline, `elapsed < 3.0`). Player side: one seat = one process = one call; no cross-seat sequencing exists anywhere. |

---

## Fixer report audit

`r2-fixes.md` was not read (per the brief), so this audits the **fix commits' messages against their diffs** instead:

| commit (claim) | I verified | agrees |
|---|---|---|
| `6373057` "extract the first balanced JSON object from a prose-wrapped reply" | `raw_decode` scan + 6 prose-shape tests + first-object-wins + brace-in-string test | yes |
| `6a43ba1` "decide the draft off the websocket read loop, so the heartbeat is answered" | draft as own task, cancelled on connection end; scaled heartbeat test with `max_connect_attempts=1` | yes |
| `636e747` "cap the LLM call budget with the server's own draft deadline" | `call_timeout` with safety margin + floor; cert-deadline test (one call, `< 5.0 s`) | yes |
| `3b1f78e` "one shape and one closed vocabulary for the player-side fallback, documented" | `FALLBACK_REASONS` tuple, single emission site, DRAFT.md §Two kinds of fallback, all-six-reasons parametrised test | yes |
| `8b1fa2a` "make test_missing_api_key_makes_no_call_at_all assert something" | monkeypatched `_anthropic_call` raises; `calls == []`, `last_request is None` | yes |
| `eb077ee` "run the worst-case-note fixture at both canvas sizes" | loop over `[1280×800, 360×640]`; CI shows both green | yes |
| `7c87e98` "correct the in-repo design note's fallback_cause enum to the six reachable values" | note now six values + editor's note; code/manifest already agreed | yes |

---

## Non-blocking observations

- The **run-directory** design note (`runs/2026-08-28-derks-gym/design.md:703-704`) still lists the seven-value `fallback_cause` enum including the unreachable `"malformed"`; only the in-repo copy (`docs/plans/…`) was corrected, so the two copies now diverge. The in-repo copy is the shipped doc and the one the review's F8 named; no checklist item covers the run-dir copy.
- `ci.yml` keeps `--strict-text-bounds` on `viewer_smoke.mjs` despite the pannable board; it is harmless here (the renderer is WebGL, `total: 0`, so the gate can never fire) and the real worst-case gate lives in `derk_viewer_checks.mjs`.
- The reviewer's "could not determine" on whether F2's heartbeat interaction ever fired in a hosted episode is now moot: the mechanism is removed (the draft no longer blocks the read loop) and the scaled-timing regression test covers the shape.

BLOCKING: 0
