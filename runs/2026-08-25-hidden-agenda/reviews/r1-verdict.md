blocking: 0

# r1 verdict — hidden-agenda

Head: `dd84d91871ecb6f0eb94d8b6bd54b43a15fddf89` (origin/main, checked out and verified)
Checklist: `prompts/30-review-loop.md` §ACCEPTANCE CHECKLIST (as quoted in the judge brief)
Independent read written before reading fixes: **yes** — I read the design note, the manifest, all three workflows, `docker_smoke.sh`, `policies.json`, `server.nim`, `llm.nim`, the sim/driver code, all page/viewer files, the test tree, the git history since run start, and the head CI run's full log **before** opening `r1-review.md` or `r1-fixes.md`.

Review was written against `5fb4368` (the initial commit). Fourteen fix commits landed since; every claim below is verified at `dd84d91`.

## Standing blocking findings

**None.** All three of the reviewer's blocking findings were true at `5fb4368` and are resolved at the current head; my own checklist pass found no new blocking finding.

## Refuted (resolved at head)

### B1 — `/client/replay` pod route → REFUTED AT HEAD (fixed by `eea1c85`)
- Evidence: `src/hidden_agenda/server.nim:409-418` at `dd84d91` — `buildRouter` registers exactly `/healthz`, `/client/player`, `/client/global`, `/client/@name`, `/global`, `/player`; the comment reads "There is no replay page route". No `ReplayPage` staticRead, no `replayPageHandler` anywhere in `src/`. Repo-wide grep for `/client/replay` finds only negative references: `tests/test_manifest.nim:105-111` (a test asserting the string is **absent** from `src/`, `docs/`, `client/`, README and the manifest), `coworld-release.yml:201` (an error message), and `tools/build_page.py:46` (the starter's source path). The route the reviewer cited is gone and a test now pins its absence.

### B2 — page a 47 % rewrite (CSS inherited, script re-authored) → REFUTED AT HEAD (fixed by `44d190c` + `dd84d91`)
- Evidence: `tools/build_page.py` at head slices **all three halves** out of the starter — CSS (`src[0:704]+src[833:1451]` minus POV/mmwarn blocks, every cut asserted line-by-line), the starter's body markup, and the starter's page script minus the raycaster/zoom/minimap/mismatch families — and appends the game block under the banner comment at `client/replay_broadcast.html:2395`. I ran `python3 tools/build_page.py` against the mounted starter at `/workspace/starters/coworld-ctf/client/replay_broadcast.html` and `git status` stayed **clean**: the committed page is bit-identical to the generator's output, i.e. mechanically derived from the starter. Page is now 3,170 lines vs the starter's 4,660 (68 %); the delta is the removals the design note lists (the ~1,100-line first-person raycaster, the zoom/minimap cluster, `#povBadge`, `#mmwarn`). Section headers above the banner diff against the starter's only in the removal of `4b. VIEW CONTROLS (zoom + minimap)` — mandated, since the 1080×760 board is fixed. `client/chrome_common.js` diffs byte-identical to the starter's. `tests/test_broadcast.nim:227` (`theScriptIsTheStartersToo`) pins 29 inherited script functions, the removed identifiers, and that `window.AgendaChrome = {` appears after the banner.

### B3 — no per-tick re-derivation test → REFUTED AT HEAD (fixed by `8ac004c`)
- Evidence: `tests/test_replay.nim` block `eventsReDeriveEveryFrame` folds the replay's own event rows forward one tick at a time and asserts **every** frame's `d` (against `deposit` rows and each row's running `total`), `g` (against `mine`/`seam` rows from `seamCapacity`), `ph` (against `meeting` rows through the four offsets) and `v` (recomputed via `seesCog` from that frame's own `c` array), then walks every tick through `global.replayFrame` and asserts the viewer's packet equals that same frame field-for-field. `tests/test_vision.nim:recordedMaskMatches` now recomputes every slot's mask on every frame (the old `frames[^1]`-only loop is gone — the deletion in the test history is a replacement by a strictly stronger check, not a loosening). Both ran green in run 32925353796.

## Checklist pass (independent)

| item | status | evidence |
|---|---|---|
| 1 CI green, no test loosened | **pass** | run **32925353796** on `main` at `dd84d91`, conclusion `success`, jobs `test`/`docker-smoke`/`wasm-viewer` all `success`. `git log -p --since=2026-08-25T23:20:00Z -- tests/`: every hunk read; no skip/xfail/removed test. Two deletions, both strengthenings: `test_manifest.nim` `seconds > 12.0` → `> 20.0` (tightened), `test_vision.nim` terminal-frame mask check → per-tick check (B3). `ci.yml:115-150` runs `ls tests/*.nim` (all 12 files, debug + release), no exclusions, no `continue-on-error`. |
| 2 Replay re-derivation | **pass** | `tests/test_replay.nim:eventsReDeriveEveryFrame` + `tests/test_vision.nim:recordedMaskMatches` (see B3). Viewer half: `global.nim:replayFrame` indexes `replay.frames[tick]`; packet equality asserted per tick. |
| 3 Static viewer | **pass** | manifest `game.replay_viewer == {"bundle":"static-replay-viewer"}` (parsed); `tools/build_replay_viewer.sh` present, mode 100755; no `/client/replay` pod path (see B1); bundle fetches only the `?replay=` URL. |
| 4 Both name spaces | **pass** | `llm.nim:seatView`/prompts use `Aliases` only; `broadcast.nim:76` puts the policy name in `roster[].pol`; replay header carries `names[]` + `policyNames[]` + `roles[]`; endcard role table and roster chips show policy names; `results.names` are policy names, `results.aliases` the aliases. |
| 5 Degrade-never-hang | **pass** | `sim_config.nim:97-102` `playDeadlineSeconds = 0.6 × episodeTimeoutSeconds` (720 s of 1200); checked in `runEpisode` at every tick and again at meeting open (`sim.nim:687-696`) → `endEarly()`. LLM batch bounded by `llmTimeoutSeconds=14` (`llm.nim:734-736`); retry is one extra iteration (`for attempt in 0..1`); `maxDecisionBatches=20` → `dsBudget` reuse (`llm.nim:811-822`); connect wait ≤ `playerConnectTimeoutSeconds=120` + 3 s register grace (`server.nim:188-207`); final broadcast on a 3 s/socket allowance (`server.nim:146-150`); `validate` statically refuses configs where `20×2×14+120 > 721` (`sim_config.nim:145-148`). mummy sockets are event-driven; no blocking read found. |
| 6 num_agents | **pass** | `num_agents: 5` in all three `variants[].game_config` and in `certification.game_config` (parsed); `certification.players` len 5; `game_config.players` len 5. `docker_smoke.sh:106-149` enforces all four invariants with the `SEAT-COUNT FAIL` prefix; `SMOKE_SEATS` default substituted to 5 as the second declaration. `grep 'SEAT-COUNT FAIL'` over run 32925353796's full log: **0 hits**; log prints `game=hidden_agenda seats=5`, `all 5 player containers exited 0`, `smoke OK: seats=5 results=429B replay=93247B reason=complete`. |
| 7 Scripted baseline full episodes | **pass** | `tests/test_baseline.nim`: 3 variants × 16 seeds × 4 pairings = 192 episodes to natural end, per-decision legality vs the role's enum, per-tick board audit, `sim.done` asserted; `tests/test_manifest.nim:246` asserts the all-scripted cert episode's `reason == "complete"` (docker-smoke log confirms `episode end reason: complete`). Tuning: `tests/test_feasibility.nim` gates (a)–(g) print measurements; `sim_config.nim:66-76` records the measured awarenessRadius 3-vs-4 sweep; fixes record a seeds-1..60 × 5-cooldown sweep for the fixture. Not guessed. |
| 8 LLM reply handling | **pass** | `extractJsonObject` (`llm.nim:543`) takes `find('{')..rfind('}')` — prose/fences tolerated; `decideAll` retries failed seats **once** in a second batch with `RetryHint`, then `scriptedDecision(skMiner)` recorded as `dsFallback` on the `order` event; `tests/test_llm.nim` (commit `7beb614`) now drives transport error / 429 / 403 / junk through a stubbed `sendBatch`, asserting `dsRetry`, `dsFallback`, exactly-one-retry-batch, and the retry batch carrying only the failed seats. |
| 9 Rune-safe truncation | **pass** | `sim_types.nim:58-62` `cleanText` = strip → `runeSubStr(0, limit-1) & "…"`; applied to say/hunch/notes, error text (200), prompt (4000), and — since `29cffb4` — policy names / `config.variant` / `config.model` at `MaxPolicyLen`. Tests: `test_llm.nim:capsAreRuneSafe`, `test_replay.nim:runeTruncation` (multi-byte at the caps → `validateUtf8 == -1`), `policyNamesAndVariantAreCapped`. |
| 10 Manifest validates | **pass** | parsed: `game.docs.readme` `{type:"text",value}` (5,747 ch) + `pages` `[{id,title,content:{type:"text",value}}]` × 2; `game.protocols.player` (6,969) **and** `.global` (7,200), both `{type:"text",…}`. |
| 11 Viewer legible at 360 px | **pass** | `.plate-name { flex: 1 1 auto; min-width: 3.2em; … }` at `client/replay_broadcast.html:2419-2425`; `#stage.tiny` hides `.lives-label`/`.ha-sub` (`:2634-2636`); tiny toggles at `boardW <= 620` (`:2346`, the starter's own constant) so labels are hidden at the 360 px embed width. Renderer fixture drew at 360 px with 0 `never_inside`. See non-blocking note on the 620/640 literal. |
| 12 Release order and scaffold | **pass** | `coworld-release.yml` step order: Build manifest (:153) → Certify (:167) → **Upload the policies** (:206) → Upload the Coworld (:304) → Put the Coworld secret (:342), sequential steps of one job. All three workflows present; `docker_smoke.sh` 100755; `policies.json` = 2 × `PLAYER_PROMPT` champions (both `USE_BEDROCK`) + 2 × `PLAYER_SCRIPTED` fillers; champion #2 (`hidden-agenda-shadow`) carries `"player":"ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`. I ran the item's gate verbatim over the five files: **no matches, exit 0** (GATE-CLEAN). docker-smoke builds the image in-job before running it. |
| 13 Viewer executes | **pass** | run **32925353796**, `wasm-viewer` `success`, `needs: docker-smoke` (`ci.yml:212`), downloads `smoke-replay`, Playwright pinned 1.55.0 both places, no `continue-on-error`. Log: `{"loaded":true,"ms":317,"clock":"TICK 242 / 900 MEETING 2 — VOTING",…}`; three distinct scrub readouts (`TICK 242…VOTING` / `TICK 321…DEPOSITS 4 / 12` / `TICK 607 / 900 FINAL`); `soak: 10s of playback kept advancing ("2 / 607" -> "194 / 607" -> "242 / 607")`. `config.nims` diffs against the starter's in rename hunks only — **no** MODULARIZE/EXPORT_NAME — and `static_replay_worker.js:181` bootstraps `Module.onRuntimeInitialized`: matched pair from one starter. `data-replay-loaded` set at `static_replay.js:153`, `data-replay-error` at `:20`, both the starter's own paths. |
| 14 Chrome provenance | **pass** | `client/chrome_common.js` **byte-identical** to `/workspace/starters/coworld-ctf/client/chrome_common.js` (diff: no output). Page: regenerating via `tools/build_page.py` from the mounted starter reproduces the committed file byte-for-byte (git status clean after run); CSS sections 1–5 present and unmodified above the banner except the note's removals (only header removed: 4b zoom+minimap — the board is fixed, so `#viewpanel` **must** go, and it is gone from markup, CSS, wiring and the test id list). Transport: `relayout()` (inherited, `:2310`) measures and sets `--band`/`--topband`/`--hudscale` on `:root`; `#rosterstrip` rides `--topband`, `#voteboard` is band-clipped; `#endcard { inset: var(--topband) 0 var(--band) 0 }` shown via `.on` and removed on every non-gameover frame (`:1544`), so every seek takes it down; `buildAgendaBeats` + `upgradeBeatButtons` produce labelled `<button class="beat-marker <kind>">`s wired to seek, with CSS for all six emitted kinds (`:2586-2624`); `noScopeDuplication` guards the alias list. |
| 15 Drawn strings fit | **pass** | `--strict-text-bounds` on **both** smoke steps (`ci.yml:324`, `:345`). Bundle smoke reports `canvas text: 0 drawn` (worker/OffscreenCanvas — covers nothing, and is not counted as evidence); the mandated mitigation exists and is green: `tools/ci/renderer_fixture.html` loads the real `broadcast_core.js`/`chrome_common.js`, full-cap 90/80/240 strings on all five seats (asserts its own string lengths, `:82-88`), four canvas sizes including 360 px (`:186`), sets `data-replay-loaded`/`data-replay-error` from its own paths, driven by `viewer_smoke.mjs --strict-text-bounds` in its own ci.yml step → `canvas text: 144 drawn, 0 never inside the canvas, 0 ellipsized`. Evidence now uploaded from both paths (`ci.yml:359-363`, N32 fix). |
| batching (simultaneous game) | **pass** | `decideAll` builds ONE `RequestBatch` over every open seat and issues one `makeRequests` call (`llm.nim:722-736`); the comment and the code agree; no per-seat request loop exists; `test_llm.nim:163` asserts `driver.batchSizes == @[Seats, Seats-1]` across a freeze. |

## Non-blocking observations

- **Item 11 literal**: the checklist says labels hide "under `640px`"; the page's inherited switch is `boardW <= 620` (`:2346`). The operative requirement — plates not collapsing at the 360 px embed — holds (tiny is on at 360, `.plate-name` rule present, fixture drew 360 px with 0 `never_inside`). A 620–640 px band shows labels; the design note itself names 620. Not counted as blocking.
- **`tools/_page_markup.html` is orphaned**: the fixes report says it was deleted; it is still committed and referenced by nothing (`build_page.py` at head reads only `_page_css.css` and `_page_script.js`). Dead file, no checklist impact.
- The cert-fixture pins `freezeCooldownTicks: 500` (schema-legal, 30..600) to stretch the episode to 608 ticks for the soak — a fixture-only value diverging from the game default 260. Documented in the fixes; `test_manifest` holds the 20 s floor.
- The bundle-path canvas-text measurement will always read `0 drawn` (worker OffscreenCanvas); the renderer fixture is the only text-bounds coverage. Reviewer N28's point stands: the fixture's DOM half styles the LLM strings with its own CSS and its `overflowing` count is reported, not gated. Every literal clause of item 15 is met; a round-2 improvement, not a blocker.
- `awarenessRadius = 4` deviates from the note's ladder (2→3) but is now documented in-place with the failing measurements at 3 (`sim_config.nim:66-76`) and enforced by the feasibility gates. Reasonable resolution of N1.

## Fixer report audit

| finding | fixer said | I verified at `dd84d91` | agrees |
|---|---|---|---|
| B1 | fixed, `eea1c85` | route/handler/staticRead gone; PROTOCOL.md row replaced; test asserts absence repo-wide | yes |
| B2 | fixed, `44d190c`+`dd84d91` | page regenerates bit-identical from the starter via build_page.py; 3,170 lines; script inherited; `theScriptIsTheStartersToo` present; CREW/IMPOSTOR headlines in the game block | yes (except `_page_markup.html` "deleted" — it is still committed, orphaned) |
| B3 | fixed, `8ac004c` | per-tick event re-derivation + per-frame mask + viewer packet walk, all present and green | yes |
| N1 | DISPUTED, documented not changed | comment with measurements at `sim_config.nim:66-76`; constant stays 4; gates pass in CI | yes |
| N6 | fixed, `a9b7152` | `lastMeetingCounts` walks back to the newest record with an `outcome`; new test | yes |
| N14 | fixed, `7beb614` | `sendBatch` seam + five stubbed-transport cases incl. `dsRetry` and the retry hint | yes |
| N15 | fixed, `29cffb4` | `cleanText(… MaxPolicyLen)` on player names (`sim_state.nim:133`), variant/model (`sim_config.nim:177-179`); capping test | yes |
| N16 | fixed, `9ff9782` | `finalFrame(slot)` built per socket (`server.nim:128-155`) | yes |
| N17 | fixed, `0c7fa26` | `canAct`/`canVote` in the state frame (`llm.nim:280-281`); test | yes |
| N18 | fixed, `aebe9bd` | `replayChrome` derives `freezeCooldown`/`meetingIn` from the recorded rows (`global.nim:264-298`); test | yes |
| N22 | fixed (seed DISPUTED) | fixture `seed:5, freezeCooldownTicks:500` → 608-tick episode; test floor 12→20 s (a tightening); CI soak shows `607` | yes |
| N23 | fixed, `29cfdf4` | docs corrected; `docsQuoteTheRealConstants` parses `<constant> = <n>` from all four docs against `defaultGameConfig()` | yes |
| N32 | fixed, `3f1d35a` | `ci.yml:359-363` uploads `dist/fixture/viewer-smoke.{png,json}` too | yes |
| remaining Ns | noted, not fixed | spot-checked N5/N13/N19/N20/N27/N29/N30/N31 rationales against the code; each holds | yes |

Blocking items: none.

BLOCKING: 0
