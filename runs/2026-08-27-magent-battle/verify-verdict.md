blocking: 0

# Phase-60 verify verdict — magent-battle
Run: `2026-08-27-magent-battle` · VERIFY.md of 2026-08-27T10:31Z · adjudicated fresh 2026-08-27 (judge, new context)
Checklist: `docs/SPEC.md` §Definition of done (phase 60) / `prompts/60-verify.md` — the eight checks.
Independent read: SPEC + prompt + VERIFY.md + committed artifacts read, then live spot-checks fetched, before this verdict was written. I did not read any fixer/verifier self-summary beyond VERIFY.md itself, which is the artifact under adjudication.

## Per-item table

| # | Item | Evidence in/beside VERIFY.md | My verification | Verdict |
|---|---|---|---|---|
| 1 | ≥2 completed rounds after fillers set | Full rounds JSON pasted (rounds 1+2 completed, `error:null`, ids + timestamps); poll table; filler time from log.md 10:07:44Z | **Live re-fetch**: rounds list now shows 3 rounds — r1 completed 10:09:19Z, r2 completed 10:24:29Z, r3 completed 10:38:25Z (created 10:36:02Z); zero failed/discarded. See timestamp note below. | **TRUE** |
| 2 | Both champions ranked, fillers absent/Baseline | Full leaderboard body pasted (bare list) | **Live re-fetch**: identical byte-for-byte — rank 1 daveey vanguard:v3 1030.5304984710244 rp=2 wins=2; rank 2 daveey-1 marshal:v3 969.4695015289755 rp=2; no third row (fillers absent) | **TRUE** |
| 3 | Latest round's episode request completed with replay | 405 on flat route recorded verbatim; nested `GET /rounds/<id>/episode-requests` result + full `GET /episode-requests/<id>` body pasted (participants daveey seat 0 / daveey-1 seat 1, `is_filler:false`, scores +24/−24) | **Live re-fetch**: flat route reproduces `405 {"detail":"Method Not Allowed"}`; nested route reproduces `ereq_3cc278d1… completed` with the same replay_url. Substitute route establishes exactly the required facts — accepted. | **TRUE** |
| 4 | Replay bytes valid, show the game | COWLDMAG magic + size shown; `jq -e` failure on raw bytes recorded honestly; `replay_summary.py` JSON view strict-parsed under jq **and** python strict-UTF-8; protocol/reason/directives/fallbacks pasted; full results doc + a full directive pasted | **Independently reproduced end-to-end**: I re-downloaded the 81 991-byte replay from S3 and ran the repo's own `tools/replay_summary.py` myself: strict parse ok (22 588 bytes), `protocol=magent-battle/v1` (= `src/magent/sim_types.nim:19`), `reason=complete`, 32 directives / 32 `source=llm` / 0 fallbacks, kills [156,132], survivors [30,6], scores [24,−24], directive 0 has 9 real orders + non-empty say. Judgment call accepted — see below. | **TRUE** |
| 5 | Hosted game log clean | HTTP 200 / 67 525 bytes; decoded-repr grep = CLEAN, raw grep cross-check = 0; 32/32 Bedrock `ok:true` 200; decoded container excerpts pasted | **Live re-fetch with elevated header**: 200, 67 525 bytes; my own repr-decode + grep for all four patterns = 0 hits decoded AND raw; Counter shows exactly 32× `"ok":true,"status_code":200` | **TRUE** |
| 6 | Public page uses static replay path | All three sources recorded: raw-HTML grep empty (declared unknown, per prompt), SSR `state.playlist[0]` featured match pasted, `POST /coworlds/replays/session` → static `viewer_url` + `ready:true` | **Live re-fetch**: session POST returns the identical static URL — `/v2/coworlds/replays/static/cow_a5961275…/sha256%3A9f7e22e4…/index.html?replay=<s3>`, sha = STATE and release-result `manifest_sha`; page HTML has **zero** `/client/replay` occurrences and now features `magent-battle.r3.e1` (featured match still present). Substitute is the exact path `playbooks/observatory-api.md` §Featured match prescribes for the client-rendered page — accepted. | **TRUE** |
| 7 | Cert declared static bundle | Read from committed `runs/<run>/release-result.json`; jq output pasted; provenance fields pasted | **Read the committed file myself**: `.certify.replay_liveness` = `Replay liveness: skipped (static replay bundle declared; /client/replay and /replay not required)`; committed in `0210c30` (phase-40 commit, verified in git log); cow_id/manifest_sha match check 6 | **TRUE** |
| 8 | Viewer executed + spectator judgment | Run 33063093381 dispatched by this phase, selected by createdAt (a 10:21Z sibling run explicitly not taken); `viewer-smoke.json` + `viewer-smoke.png` committed under `viewer-check/` (commit `fa248f1`); readouts pasted verbatim; judgment paragraph written from the png | **Verified**: `gh run view 33063093381` → `completed/success`, created 10:27:11Z, workflow viewer-check. Committed json: `loaded:true`, `data_replay_loaded:"true"`, `failure:null`, `console_tail:[]`, three scrub clocks all distinct (`TICK 0/300 · 81 V 81` → `TICK 49/300 · 43 V 32` → `TICK 194/300 · 30 V 0`). My own read of the png below. | **TRUE** |

## Spot-checks performed live (all matched VERIFY.md)

1. `GET /divisions/div_ea7bd527…/leaderboard` — both rows identical to VERIFY.md down to the full-precision scores.
2. `GET /rounds?league_id=league_b56ff3be…` — rounds 1+2 exactly as pasted (ids, timestamps, `error:null`); round 3 has since completed (10:38:25Z); zero failed/discarded.
3. `GET /coworlds` — `cow_a5961275…` v0.1.2 `canonical:true`, sole canonical (0.1.1 / 0.1.0 false), as pasted.
4. Flat `GET /episode-requests?round_id=` reproduces the 405; nested route reproduces `ereq_3cc278d1…` completed + replay_url.
5. Replay bytes re-downloaded (81 991 bytes, `COWLDMAG` magic) and re-summarised with the repo's own tool — every number in VERIFY.md check 4 reproduced.
6. `artifacts/logs` re-fetched (200, 67 525 bytes) — 0 pattern hits decoded and raw; 32/32 Bedrock 200s.
7. `POST /coworlds/replays/session` reproduces the identical static `viewer_url`, `ready:true`.
8. `gh run view 33063093381` → success, timestamps as claimed.

## Judgment calls audited (each accepted on its merits, not on assertion)

- **405 → nested episode-requests route (check 3).** Reproduced live; the nested route returns the same object the flat route would. It establishes every fact the item names (status, replay_url, participants). Sound.
- **Binary COWLDMAG → `replay_summary.py` JSON view (check 4).** The design note declares the binary format and this exact substitute (`design.md:783–808`); the tool is committed in the game repo and is stdlib-only; I ran it myself and the strict-UTF-8 parse holds under two parsers. The item's substance — replay integrity and champions demonstrably playing the game via non-scripted, non-trivial LLM decisions — is fully established (32/32 llm, 0 fallbacks, real verbs, non-empty say, cross-checked against `llmTurns [16,16]` / `fallbackTurns [0,0]`). Additionally the viewer *loaded these exact bytes* in check 8, the strongest possible validity proof for their real consumer. Sound. The "protocol matches the manifest" phrasing was handled honestly: the manifest carries protocol *doc URIs*, not an id string (verified in `coworld_manifest_template.json:20–27`); the replay's `protocol` matches the sim source's `ProtocolId` and the replay's `game` matches manifest `game.name`. That is the strongest match the artifacts permit.
- **SSR payload + replay-session POST for check 6.** This is not an improvisation — it is the documented answer in `playbooks/observatory-api.md` §Featured match (lighthouse, 2026-08-22: page client-rendered, `featured_match` null platform-wide, the session POST is "the call the page's own JS makes"). Reproduced live; sha in the URL equals the manifest_sha in STATE and in the committed release-result.json; no `/client/replay` anywhere in 671 KB of page HTML. Sound.

## The check-1 timestamp tension (examined, not blocking)

VERIFY.md states fillers were registered at 10:07:44Z yet counts round 1 (`created_at` 10:06:01Z) as post-filler — on its face those two timestamps contradict the claim. Resolution: 10:07:44Z is the **log-write** stamp of a batched line covering champion submits → fillers → unpause → trigger-round (the previous log line is 10:04:35Z; the actions in between share the 10:07:44Z stamp), and the narrative order inside that line puts fillers before the trigger-round. Corroboration: the playbook records that a trigger-round issued before any filler exists *fails instantly* (`observatory-api.md` §6), and round 1 completed with `error:null`. Decisive regardless: at adjudication time rounds 2 (created 10:21:02Z) and 3 (created 10:36:02Z, completed 10:38:25Z) are both completed and strictly after 10:07:44Z — the latest possible filler-registration time — so "≥2 completed rounds after the fillers were set" holds under the strictest possible reading. TRUE stands; the ambiguous phrasing in VERIFY.md check 1 is an evidence-hygiene note, not a defect in the world.

## My own read of the spectator judgment against viewer-smoke.png

I viewed the committed 1280×800 png myself. It shows the endcard over the finished battle, exactly as the paragraph describes: headline `ALPHA TAKES THE PAIR 1-1 — 30 SURVIVORS TO 6`; `SCORE +24 / -24` chip; ledger `game 1: 0–6 (wipe, 109 ticks) · game 2: 30–0 (wipe, 194 ticks) · complete`; two commander plates (`COMMANDER | KILLS | LOST | ALIVE | REWARD`: ALPHA 156/132/30/793.56, BRAVO 132/156/6/676.34) with `TROOPS LEFT` 30 and 6. Every number reconciles byte-for-byte with the replay results I extracted independently. The scorebug strip reads blue `ALPHA` / `ALIVE 30`, centre clock `game 2/2 · turn 10/15` / `TICK 194/300 · 30 V 0`, `0 ALIVE` / red `BRAVO`, `HEAT` chip top-right — matching the 100 % scrub readout in the json. The bottom transport strip (restart/step-back/pause/+5s/play/loop/ffwd, `spoilers` toggle, `ALPHA WINS 363 / 363`, 1×–16× speed chips) and the full-width scrubber with coloured beat marks over a momentum sparkline are visible and are recognisably the coworld-ctf starter chrome re-labelled, not a rewrite. The faint 45×45 grid with two clusters of unit chips and a dimmed feed are visible behind the endcard. The paragraph is accurate to the picture; it is legible; it shows the game. Items 8(a) loaded, 8(b) motion (three distinct clocks), 8(c) judgment — all hold.

## Non-blocking observations

- The dimmed banner behind the endcard reads `ALPHA IS ROUTED`, but the final game (game 2) ended with **Bravo** wiped (30–0). It is plausibly the game-1 rout banner persisting/re-rendered at the seek, and it sits dimmed behind a correct endcard, so a spectator is not misled about the result — but a wrong-side banner at the final frame is worth the phase-30 glance the verifier already recommended for the `CONNECTING` chip. Legibility note, tied to no DoD item's truth.
- VERIFY.md check 1 should have said explicitly that 10:07:44Z is a batched log-write stamp rather than asserting registration "at 10:07:44Z" and then counting an earlier-created round — the "therefore" as written does not follow from the two timestamps shown (see the tension section above for why the item nevertheless holds).
- `canvas_text.total` is 0, meaning the probe sampled no canvas text draws at all; VERIFY.md's "0 draws crossing a canvas edge and 0 ellipsized" is literally true but vacuous. No item turns on it.

## Verdict

All eight definition-of-done items are supported by evidence actually pasted in VERIFY.md or committed beside it, and every claim I spot-checked live (eight fetches) reproduced exactly. The three documented judgment calls each establish their item rather than merely substituting for it. No item rests on assertion.

BLOCKING: 0
