blocking: 0

# r1 verdict — snake-royale

Head: `d8652fa2d92b14a6da207d71076027f806f7849e` (`origin/main`, confirmed `git rev-parse`)
Review under judgement: `r1-review.md` (F1–F25, written against pre-fix `f985499c`)
Checklist: `prompts/30-review-loop.md` §ACCEPTANCE CHECKLIST (read in full, first, verbatim)
Independent read written before reading the review: **yes** (repo, design note, starter diff, CI
run 33149313876 + artifacts + job logs, `git log -p -- tests/` across all 26 commits).
`r1-fixes.md` **not read** — this verdict was formed from the head tree and CI evidence only. (The
fixer's commit messages are part of the repo history and were unavoidably visible; the fixes
report itself was not opened.)

CI evidence used throughout: run **33149313876**, workflow CI, `head_sha
d8652fa2d92b14a6da207d71076027f806f7849e`, conclusion **success**, jobs `test` ✓ (2m24s),
`docker-smoke` ✓ (1m11s), `wasm-viewer` ✓ (2m21s), `gameversion-tripwire` skipped (PR-only by
design, `ci.yml:50 if: github.event_name == 'pull_request'`). Artifacts `smoke-replay`,
`viewer-smoke` (root + `fixture/`), `static-replay-viewer` downloaded and read.

---

## Per-finding disposition, F1–F25, verified at d8652fa

Legend: **FIXED** = reproduced the reviewer's claim as true at f985499 (from history) and verified
it no longer holds at head; **NOT BLOCKING** = accurate observation that never falsified a
checklist item. No finding stands.

| # | Reviewer's claim | Verdict at head | Evidence at d8652fa |
|---|---|---|---|
| F1 | `s:<tick>` parsed as a fraction; every non-zero seek lands on the last frame and raises the endcard | **FIXED** (1cab680) | `src/snake/replay_runtime.nim:349-356` — `of 's': … rt.seekTurn(parseInt(text[2 .. ^1]))`; `seekTurn` (`:315-326`) clamps to `[0, snapshots.len-1]` on the turn axis. Test 33b (`tests/test_snake_replay.nim:77-103`) seeks midway, asserts it lands there, is not the last turn, clamps both ends, and asserts the page still sends `send('s:' + (st + Math.round(frac * (mx - st))))`. Head `viewer-smoke.json` scrub rows advance correctly; endcard seek-dismissal is asserted by the renderer fixture (`tools/ci/renderer_fixture.html:311-315`, "the endcard stayed up after a seek back into the match" is a failure) |
| F2 | Shipped tunables differ from the note; `coil` loses the ladder (−0.097); directional test replaced by `abs(margin) ≥ 0.05` | **FIXED** (2e4878b) | Ladder seat-mirrored (`src/snake/engine.nim:94-156`: 4 seeds × 3 modules × 2 seatings, identical pair scores exactly 0.0 — asserted `tests/test_snake_control.nim:289-291`). Re-swept pick `coil = (100, 2, 900, 120, 100, 12)` (`baselines.nim:53-55`) with recorded margin **+0.1805** (`tools/ci/baseline_tuning.json`; CI `test` log line 357: `margin=0.1805`). Directional assertion restored **and** strengthened: `margin > 0.0` (`test_snake_control.nim:294`), `abs(margin) ≥ 0.05` kept, per-module `coil wins the <module> ladder` for all three (`:305-308`), exact integer pins kept. The note's `[+0.30, +1.20]` band remains unmet — recorded as unreachable over the swept matrix; checklist 7 requires a grid harness, not the note's band (see checklist pass, item 7) |
| F3 | Trapped-emission assertion replaced by the tautology `pending.len >= 0` | **FIXED** (66a3239) | `tests/test_snake_sim.nim:437-515` — a hand-built seal-in: flag false → no event (`:457-458`), transition turn → flag true and **exactly one** trapped event with the free/length payload (`:467-474`), next turn still trapped → no second event; plus a whole-episode sweep asserting every emission is a false→true transition and `transitions >= 1` (`:503-515`) |
| F4 | `turnBudgetMs` clock starts before the `turnSpacingMs` sleep, pre-empting the retry in steady state | **FIXED** (389d120) | `src/snake/decide.nim:246-268` — the rate-floor sleep runs first (`:251-257`), then `let turnStart = getMonoTime()` at `:268` **below** it, with the comment explaining exactly this defect; `sim_config.nim:114-119` additionally repairs any `turnBudgetMs < attempt1Ms + retryMs` so the budget always covers both attempts |
| F5 | Bundle draws in a Worker so `canvas_text.total == 0` there; fixture is the only text gate, its evidence not uploaded, and it drove empty roster/beats/lead/duel | **FIXED** (94fd81e) | `ci.yml:411-422` states the coverage split in the step itself; fixture evidence uploaded — artifact `viewer-smoke` now contains `fixture/viewer-smoke.json` + `fixture/viewer-smoke.png` (`ci.yml:480-489`; downloaded and read: `canvas_text: {total: 72, never_inside: 0, ellipsized: 0}`). `tools/ci/renderer_fixture.html` now drives a real 4-seat roster (`:83-96`), all seven beat kinds (`:111-121`), a full-width length series (`:100-107`), `duel: 26`, a full-cap say feed row, a real results document, and mounts the shipped `index.html` in an iframe asserting plates=4, real names, 7 labelled `<button>` markers, momentum path, duel banner, endcard up/relabelled/down-on-seek (`:241-316`) |
| F6 | Say bubble sized from the actual string in `system-ui`, no reserved band, no cap-based layout | **FIXED** (0fbedf5) | `client/broadcast_core.js:91-96` — `MAX_SAY_RUNES = WIRE.maxSayRunes`, `SAY_CAP_SAMPLE = new Array(MAX_SAY_RUNES + 1).join('W')`, `sayBandFor(cell)`; the band is reserved in the layout whether or not anyone speaks (`:184-187`, comment at `:444`). Asserted by test 43 (`tests/test_snake_viewer.nim`: `WIRE.maxSayRunes` and the cap-sample literal present in core; clamp `if (y < 2) y = 2;` inside the canvas). Fixture `never_inside: 0` at 360/640/1024 px across all three boards with full-cap says on all four seats incl. top row |
| F7 | Duel banner shown but the playback rate never halves (`framesPerTurnAt` had no caller) | **FIXED** (00d60dd) | `src/snake/replay_runtime.nim:287-313` — `advance()` walks the frame axis through `framesPerTurnAt` with a sub-frame accumulator; test 33c (`tests/test_snake_replay.nim:105-139`) drives 8 frames outside the duel → +8, 8 frames inside → +4, and asserts the axis stays 1:1 with the scrubber |
| F8 | Step 10 frees the head-on loser's head cell, contradicting the note's step-10 wording | **FIXED as documented divergence** (6db2d04) | `docs/RULES.md:84-94` — divergence 6 records the step-10 head-on-loser exception and why (`longer_wins` would decide nothing); code comment at `rules.nim:518-527` matches; every other corpse cell **including a wall/starve corpse's head** still blocks (`rules.nim:507-529`). No checklist item touches this; the note-vs-code tension is now an enumerated, tested divergence |
| F9 | `headOnOutcome` never returns `tie` under `longer_wins`; contention counted a rival's neck | **FIXED** (8c2cd1c) | `src/snake/rules.nim:224-258` — a rival's own neck is excluded from reachability (`:229-234`); equal-length top → `hrTie` (`:256`), two equal leaders → `hrTie` (`:257`), strictly longer rival → `hrLose`. `tie` is now reachable on the flagship module, matching the observation schema and the system prompt |
| F10 | `MaxReplyBytes = 4096` declared, asserted, never applied | **FIXED** (850a912) | `src/snake/directives.nim:51-67` — `boundedReply` cuts at 4096 bytes backing off UTF-8 continuation bytes; applied at the read site `decide.nim:306` (`boundedReply(engine.client.textOf(…))`). Tests: sample really exceeds cap, cut lands at cap, JSON buried past the cap is not read, JSON inside the cap survives, cut is valid UTF-8, and `decide.nim` contains the `boundedReply(` call (`test_snake_control.nim:166-197`) |
| F11 | `ekFallback` never emitted; tier-2 stream and "MISSED THE CALL" feed row unreachable | **FIXED** (df92012) | `decide.nim:202-206` — `noteFallback` template emits `TurnEvent(kind: ekFallback, …)` on every fallback path (`:239, :278, :353`); the server folds `engine.events` into the episode stream (`server.nim:409`); playback re-derives fallback events from the recorded chat records (`replay_runtime.nim:184-187`), driving the beat and the feed row identically live and in replay |
| F12 | Feed rows carry fewer facts than the note's examples; `deathPhrase`/`wallSideOf` uncalled | **FIXED** (d58ee61) | `src/snake/labels.nim` — `deathPhrase` called at `:54` with `wallSideOf` (named walls: "runs into the north wall" etc., `:105-106`); head-ons carry lengths: `"HEAD-ON — X (8) beats Y (6)"` / `"… both die (7 v 7)"` (`:68-73`, fed by the `headon` event's members payload, `rules.nim:489-497`); decline names the spared rival. `tests/label_manifest.txt` regenerated in the same commit; label-contract test pins the richer set |
| F13 | Ten numbered tests asserted less than the note claims (15, 16, 18, 29, 31, 32, 33, 39, 40, 44) | **FIXED** (88589cc, plus 66a3239/a1b4474) | Spot-verified at head: test 15 ranks independently and compares places (moved to `tests/test_snake_scoring.nim`); test 29 asserts `headons >= 1` (`test_snake_engine.nim:77`); test 31 drives the real engine loop with a silent LLM seat, asserts `fallbackTurns == turns`, `llmTurns == 0`, `deadSeats`, closed-schema payload (`test_snake_engine.nim:101-187`); test 32 drives `decide.turn` past the guard threshold rather than re-deriving the inequality; test 33 **records** wall_clock/sim_fault episodes via `runScriptedEpisodeWith(stopAfterTurn=12, …)` and asserts the hash chain, the stop record and zero-sum on the stopped episode (`test_snake_replay.nim:30-74`); test 39 pins real SHA-256 with a self-test of the digest function (`sha256Hex("abc")`); test 44 asserts exact counts (`count == want`). Head pin `85330`/sha for the inherited head updated in the same commit as the page change it tracks (d8652fa) — a pin move, not a loosening |
| F14 | No fixtures committed; `wasm_replay_smoke.cjs` invoked by nothing | **FIXED** (9772990) | `tests/fixtures/{royale-seed42,geese-seed7,tron-seed13}.replay` committed; `ci.yml:369-390` requires exactly 3 fixtures and runs `node tools/wasm_replay_smoke.cjs` over each; CI log (job 98777771357): `ok: loaded geese-seed7.replay, advanced 300 frames …` ×3. Native staleness sweep now hard-requires `found == 3` (`test_snake_replay.nim:242-262`) |
| F15 | Three "kept" tools absent; design test 38 had no CI step | **FIXED** (91be18d) | `tools/ci/check_gameversion.sh` (100755), `next_coworld_version.py`, `test_next_coworld_version.py` all present; `ci.yml:49-60` gameversion-tripwire job (PR-gated by design), `ci.yml:187-216` "Validate the manifest under the installed coworld CLI" runs `coworld==0.1.43` `_load_template_manifest` — green in this run (`test` log line 919: "manifest validates under coworld 0.1.43: snake-royale 0.1.0 ['royale','geese','tron']"), `ci.yml:218-222` runs the version picker's self-test |
| F16 | `build_replay_page.py` docstring claimed to write a file it does not write | **FIXED** (2dcfd90) | Docstring now says exactly what it does: derives the inherited head + starter chunks into a scratch dir as a provenance audit trail, "It does NOT write client/replay_broadcast.html" (`scripts/build_replay_page.py:1-20`). The reviewer's own run of it (67 diff lines over 1060) is independent evidence the page is the starter mechanically stripped, not a rewrite |
| F17 | Episode clock started after the lobby (worst case ≈ 720 s exactly); display hold ran after the artifact write | **FIXED** (a240a22) | `src/snake/server.nim:319-327` — `let started = getMonoTime()` **above** `waitForLobby`, with the arithmetic in the comment (worst case ≈ 672 s); hold before the writes (`server.nim:479-488`: `sleep(gameOverTurns * 250)` then artifacts), matching the note's order. All waits remain bounded |
| F18 | `#viewpanel` gone from the page but `zoomAt/setZoom/attachMinimap` stubs and the minimap transfer remained in JS | **FIXED** (f3d2314) | `grep -n "zoomAt\|setZoom\|attachMinimap"` over `client/broadcast_core.js` and `replay-viewer/static_replay.js` → only prose comments explaining the removal (`broadcast_core.js:7-8, 532-535`; `static_replay.js:217`). No stub, no transfer block. Checklist 14's "removes … rather than hiding it" now holds in full |
| F19 | `Turns survived` / `Final length` re-labels had no shipped string | **FIXED** (c413b99) | `client/replay_broadcast.html:1775-1782` — both `fl-cap` spans in the endcard headline pair; renderer fixture asserts both present and carrying the winner's numbers (`renderer_fixture.html:297-310`); endcard-labels test pins them exactly once |
| F20 | `drawLengthRibbon` doesn't exist; ribbon is the starter's `#momentum` SVG | **NOT BLOCKING** (design-note internal inconsistency; implementation follows §Readouts 7) | The ribbon ships, draws full-width from the pre-scan on frame 0 (fixture asserts `#momentum path` present), which is the behaviour both note sections agree on. No checklist item names the function. Correctly advisory |
| F21 | Forager's `spaceWeight` (40 vs the note's 400) was never in the swept matrix | **FIXED** (d54890f) | `tools/tune_baselines.nim:38-49` — the sweep now iterates `foragerSpaceWeight in [40, 400]` as the outer axis (36 rows); the recorded pick keeps 40 (`baseline_tuning.json` `best.foragerSpaceWeight`); `test_snake_control.nim:227-250` asserts the harness sweeps it and the shipped values equal the recorded best |
| F22 | `tests/test_snake_scoring.nim` did not exist | **FIXED** (a1b4474) | File exists (111 lines), imported by `tests/shard_1.nim`, swept by `ci.yml`'s `tests/*.nim` glob; the move carried every assertion verbatim (diff shows identical removed/added check lines) — one copy, not two |
| F23 | Smoke replay written to `replay.json` though it is binary `COWLDSNK` | **FIXED** (0cf2e62) | `ci.yml:262-266` — `SMOKE_REPLAY_OUT: …/dist/smoke/episode.replay` with the reasoning in the comment; head docker-smoke log: `replay saved for the viewer smoke: …/episode.replay`; the wasm-viewer glob picks `*.replay` first |
| F24 | Tier-2 stream used the sixteen-kind sim enum, not the note's reduced `SimEventKind`; no `directive` rows | **FIXED** (8bdd553) | `src/snake/events.nim:33-80` — `SimEventKind` is exactly the note's fourteen kinds; `simKindOf` maps `gamestart`/`spawn`/`end` to "not carried"; `DirectiveEvent` rows are fed from the decision layer (`server.nim:434-437`) so `directive` and `fallback` both reach `COGAME_EVENTS_URI`; the trailing summary row survives |
| F25 | `viewer-smoke.json` reported `feed_lines: 0` | **FIXED** (d8652fa) | Cause was the harness selector (`#feed, .feed, #log`) vs the starter id `#killfeed`; the page now carries `class="feed"` on `#killfeed` and the head artifact reads `feed_lines: 8` (downloaded `viewer-smoke.json`, run 33149313876) — the readout now measures something |

**Refuted count: 0 wrong, 23 fixed-since, 2 accurate-but-advisory (F8 → documented divergence, F20).
Standing blocking findings from the review: none.**

---

## Checklist pass (independent, at d8652fa)

| # | Item | Status | Evidence |
|---|---|---|---|
| 1 | CI green; no test disabled/skipped/loosened this run | **PASS** | Run 33149313876 `success` at exactly `d8652fa…` (`gh api …/runs/33149313876` → `head_sha`, `conclusion`), jobs test/docker-smoke/wasm-viewer green; `NIM_TESTS` empty in the job env (test log line 315) so the `tests/*.nim` glob ran — 45 `nim r` invocations, zero `FAILED`. `git log -p -- tests/` read across all 26 commits since bootstrap (512a5ef): no `skip`/`xfail`/`t.Skip`/`--skip` anywhere, no test file removed (one moved intact, a1b4474). Two transient weakenings existed mid-run and are **stronger than original at head**: (a) a8fbdfd replaced `margin > 0.0` with `abs(margin) ≥ 0.05` — 2e4878b restored the direction and added mirror-control + per-module + exact-integer pins; (b) 5537503 left `pending.len >= 0` — 66a3239 replaced it with an exact-transition test. Every other hunk is a compile fix, a pin update tracking a code change in the same commit, or a strengthening (verified hunk by hunk) |
| 2 | Replay re-derivation, frame by frame, viewer reads it | **PASS** | `replay_runtime.nim:149-198` re-runs `resolveTurn` over the recorded bytes checking `gameHash` per turn (`mismatchTurn`); the viewer's frames come from those `snapshots` via `snake_replay.nim → framePacket`. Tests: `test_snake_determinism.nim`, `test_snake_replay.nim:29-74` (all four end rules, **recorded** stops, hash at every turn incl. the stop turn), fixture sweep `:242-262`; wasm module itself runs the three fixtures in CI (log: "advanced 300 frames" ×3, hash-checked) |
| 3 | Static viewer, no pod path | **PASS** | `game.replay_viewer = {"bundle":"static-replay-viewer"}` under `game` (manifest parsed); `tools/build_replay_viewer.sh` mode 100755, invoked by path (`ci.yml:330-331`), exec bit asserted (`ci.yml:307-318`); bundle's only network call is `fetch(message.replayUrl)` (`static_replay_worker.js:111`); `/client/replay` appears nowhere in the manifest (grep: 0 hits), server route documented local-only (`server.nim:141-145`) |
| 4 | Both name spaces | **PASS** | Observations carry aliases only (`decide.nim:68-169`, `cogAlias`); real names live in `results.names`, replay joins, and the scorebug plate (`replay_broadcast.html:1821`, fixture asserts `plate-name == "daveey"`); `showPlayerLabels: false` in all four game_configs; `tests/test_snake_identity_privacy.nim` asserts no real name/notes/spawnDeal in any observation |
| 5 | Degrade-never-hang; inside 720 s | **PASS** | Every wait bounded: lobby ≤ `lobbyJoinTimeoutSeconds` (`server.nim:295-306`), loop stopped at `wallClockBudgetSeconds = 640` **measured from above the lobby** (`server.nim:327, 389-395`), budget guard at `decide.nim:209-217`, rate-floor sleep ≤ `turnSpacingMs`, `curly.makeRequests(batch, deadline)` with whole-second enforcement (`sim_config.nim:106-113`), ≤ 2 attempts, throttle fail-fast, post-settle tail = 0.5 s hold + writes + 20 s grace. Worst case ≈ 672 s < 720 s; certification/CI episodes are milliseconds (docker-smoke job total 1m11s, `reason=complete`) |
| 6 | `num_agents` everywhere + docker_smoke invariants | **PASS** | `num_agents: 4` inside all three variants' `game_config` **and** `certification.game_config` (manifest parsed; none at variant top level); `docker_smoke.sh:110-151` implements all four `SEAT-COUNT FAIL:` invariants + the independent `SMOKE_SEATS` cross-check; **grep of the full docker-smoke log for `SEAT-COUNT FAIL` → 0 hits**; log reads `game=snake-royale seats=4 …` and `smoke OK: seats=4 … reason=complete` (job 98777549129) |
| 7 | Scripted baseline plays full episodes legally; tuned, not guessed | **PASS** | `test_snake_engine.nim` 28/30 run real episodes to natural end, `reason == "complete"`; `test_snake_control.nim:60-111` — 500 random states × both baselines: dir in enum, never the neck, serialized ≤ 1024 B, `-infinity` set exactly equals the resolver's illegal set (same `willOccupy`), fallback proc **is** coil. Grid harness: `tools/tune_baselines.nim` (36-row matrix incl. forager's axis) re-run in CI with `--sweep --check` (`ci.yml:127-135`, green); shipped constants equal the recorded pick; measured margin +0.1805 on a seat-mirrored ladder, coil wins all three modules. The note's `[+0.30,+1.20]` band is not met and is recorded as unreachable — the checklist requires the harness, not the band; noted under Non-blocking observations |
| 8 | LLM reply handling | **PASS** | Tolerant extraction (`directives.nim:91-130`, fences/prose proven in tests); retry **once** as a second parallel batch (`decide.nim:271`, `attempt < 2`) with the clock started below the rate floor (F4 fix) so the retry always fits; fallback to coil recorded per seat per turn with cause enum + `fallbackTurns` in results + the exact `falling back` phrase (`decide.nim:355`), `will retry` on attempt 1 (`:328`); `no_credentials` counted per turn (`:229-240`) |
| 9 | Rune-safe truncation + multi-byte test | **PASS** | One truncation proc (`directives.nim:42-49`, `runeLen`/`runeSubStr`); `boundedReply` backs off continuation bytes (`:62-67`); applied to say/notes/prompt/fallback detail/stopDetail/policy label/provider bodies (verified call sites). Tests: 4-byte emoji exactly on each cap asserting `runeLen == cap` and `validateUtf8() == -1` (`test_snake_control.nim:147-165`); `test_snake_replay.nim:174-212` runs `replay_summary.py` over a cap-filled replay and asserts strict-UTF-8 JSON, no lone surrogates |
| 10 | Manifest validates: `game.docs` shape + both protocols | **PASS** | Parsed at head: `docs.readme {type:text, 4132 chars}` + 3 pages each `{id,title,content:{type:text,value}}` (5732/1514/3965 chars); `protocols.player` **and** `.global`, both `{type:"uri", value:…PROTOCOL.md}` objects. Independently validated by the installed `coworld==0.1.43` in CI (test log line 919) |
| 11 | Viewer legible at 360 px | **PASS** | `.plate-name { … flex: 1 1 auto; min-width: 3.2em; overflow: hidden; text-overflow: ellipsis }` (`replay_broadcast.html:1600-1613`); labels hidden under 640 px (`@media (max-width: 640px)` `:1736-1738`) and under `#stage.tiny` (`:1739-1741`, toggled at `boardW <= 620`); fixture renders 360 px on all three boards, `never_inside: 0`, and asserts the plates carry full policy names |
| 12 | Release order + scaffold + policies + placeholder gate | **PASS** | `coworld-release.yml`: Build manifest (:159) → Certify (`--timeout-seconds`, :173) → **Upload the policies** (:216, with the ordering comment) → Upload the Coworld (:314) → Put the secret (:410). Three workflows present; `docker_smoke.sh` 100755. `policies.json`: 4 distinct policies — 2 `PLAYER_PROMPT` champions + `coil`/`forager` `PLAYER_SCRIPTED` fillers — champion #2 (`snake-royale-glutton`) carries `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`. **I ran the placeholder gate myself: exit 0** (grep of `<slug>\|<IMAGE>\|<SEATS>` over the five files → no match); the four documented angle-bracket residues are the expected runtime values |
| 13 | Viewer executes | **PASS** | `wasm-viewer` green at head **including** "Load the bundle in a real browser" (job 98777771357, step ran at 06:54:58, no `continue-on-error` anywhere in ci.yml, `needs: docker-smoke` at `ci.yml:294`); `viewer-smoke.json`: `loaded: true`, soak `0/40 → 16/40 → 20/40` over 10 s. Markers: `data-replay-loaded` set in the worker's `'loaded'` branch after the first drawn frame (`static_replay.js:142`), `data-replay-error` in `showFailure()` (`:14-20`) — both the starter's own paths. **Lobby**: this replay format records no lobby frames at all — `replay.turns` accumulates only inside the post-lobby loop (`server.nim:447`) and `snapshots[0]` is the spawned board (`replay_runtime.nim:159`), so frame 0 *is* the game start and every seek clamps onto the turn axis (`seekTurn`); a late-gameStart replay cannot exist in this format. **Link flags**: `config.nims` has no `MODULARIZE`/`EXPORT_NAME` (5 rename-only diff lines vs starter) and the worker uses `Module.onRuntimeInitialized` (`static_replay_worker.js:186`) — same starter, consistent, and the smoke's `loaded: true` is the proof |
| 14 | Chrome is the starter's | **PASS** | `chrome_common.js` **byte-identical** (sha256 `7ace7287…ced72f7c` both sides, 40022 bytes, `diff` empty — run myself). `replay_broadcast.html` is 1969 lines vs the starter's 4660 — I checked whether that fraction is a rewrite: **91% of the inherited head's substantive lines are verbatim starter lines** (measured myself), all kept section CSS and all 46 kept ids present (checked by grep, none missing), zero removed ids survive, no fixed-position element, and `scripts/build_replay_page.py` mechanically re-derives the head from the mounted starter for diffing. The reduction is the note's enumerated deletions (`#viewpanel`, `#fpv*`, `#povBadge`, hill/flag/lives/perk/squad chrome, six beat kinds, paintball JS) — removals, not a rewrite. Transport rules: (a) `relayout()` sets `--band`/`--topband`/`--hudscale` on `document.documentElement` (`:1517-1544`); (b) nothing fixed in the band; (c) `#endcard { top: var(--topband); bottom: var(--band) }`, shown/hidden via `.on`, every seek takes it down (fixture-asserted); (d) beats are labelled `<button>`s built by `snakeBeat` (never `markBeat`) with CSS for exactly the seven emitted kinds (`:1713-1719`). `#viewpanel` removed entirely — markup, CSS, wiring and JS stubs (post-F18) |
| 15 | Every drawn string fits its frame | **PASS** | Bundle step: `canvas_text.total: 0` **by construction** (Worker/OffscreenCanvas) — correctly treated as covering nothing, and `ci.yml:411-422` says so in place; the text gate is the renderer fixture, its own ci.yml step with `--strict-text-bounds` (`ci.yml:434-472`): head evidence `canvas_text: {total: 72, outside: 0, never_inside: 0, ellipsized: 0}` (artifact `fixture/viewer-smoke.json` + job log line "72 drawn, 0 never inside"). Say bubble laid out from the server's cap (`MAX_SAY_RUNES`/`SAY_CAP_SAMPLE`) in a reserved band that exists whether or not anyone speaks (`broadcast_core.js:91-96, 184-187`); fixture drives full-cap says on all four seats including a top-row snake, at 360/640/1024 on all three boards, and asserts its own strings are still full-length (`SAY.length !== CAP` fails it; the feed row must contain the full SAY) |
| — | Simultaneous batch rule | **PASS** | One `RequestBatch`, one `batch.post` per open seat, one `curly.makeRequests` per attempt (`decide.nim:282-298`); at most two batches per turn; no per-seat transport loop anywhere (grepped) |

## Fixer report audit

Not performed against the fixer's own file — `r1-fixes.md` was deliberately not read (fresh-context
rule). The table above **is** the audit: each finding was independently re-verified against the
head tree and CI run 33149313876 rather than against any claim about it. All 25 dispositions were
established from code, history and CI artifacts alone.

## Non-blocking observations

- **Design-note deviations that stand, documented, at head** (no checklist item touches them):
  the baseline tunables and margin band differ from the note's table (`[+0.30,+1.20]` recorded as
  unreachable over the swept matrix; +0.1805 measured, direction asserted); step 10's head-on-loser
  exception is a documented divergence (docs/RULES.md §6); the tier-2 `SimEventKind` note-vs-code
  alignment now matches after F24. Phase 40/60 should treat the note's §Baselines table and step-10
  wording as superseded by the recorded measurements.
- The `gameversion-tripwire` job never runs on push builds (PR-only by design); a direct-to-main
  workflow means the cross-branch GameVersion collision guard is exercised only if PRs are used.
- `test_snake_viewer.nim` test 40 pins the inherited head by its own length+sha256 rather than
  against the starter's bytes (impossible directly, given the enumerated removals); provenance
  therefore rests on `scripts/build_replay_page.py` + reviewer/judge diffing, which both did.
- Transient mid-run assertion weakenings (a8fbdfd, 5537503) were each restored **and strengthened**
  within the same run; recorded here so the audit trail is complete.

## Verdict

Every reviewer finding is either fixed at d8652fa (23), or accurate-but-advisory with no checklist
item falsified (2). My independent checklist pass finds all 15 items plus the simultaneous-batch
rule satisfied at the current head, each verified from the tree or from cited CI evidence
(run 33149313876). Nothing was unverifiable from the sandbox.

BLOCKING: 0
