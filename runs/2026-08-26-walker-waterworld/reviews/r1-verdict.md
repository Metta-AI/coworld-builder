blocking: 0

# r1 verdict — walker-waterworld

Head: `f078434aab36e880d189cedcd74ec64883d71cbc` (main)
Checklist: `/workspace/coworld-builder/prompts/30-review-loop.md` §ACCEPTANCE CHECKLIST (items 1–15 + the simultaneous-decision batching rule)
Independent read written before reading fixes: **yes** (and before reading the review; notes were saved to a scratch file before either was opened). The round-1 reviewed sha was `41bae66`; this verdict is against the current head `f078434`, per the judge rule that a finding true at review time and fixed since is refuted, not standing.

CI evidence used throughout: run **32960525769**, workflow `CI`, `headSha = f078434aab36e880d189cedcd74ec64883d71cbc`, conclusion **success** (verified with `gh run view --json headSha,conclusion`), jobs `test` / `docker-smoke` / `wasm-viewer` all `success`, `wasm-viewer` `needs: docker-smoke` (`ci.yml:217`), and both browser steps ran (step list from `--json jobs`).

## Standing blocking findings

None. All four of the reviewer's blocking findings are fixed at the current head (see §Refuted), and my independent checklist pass found no item the tree or the cited CI evidence fails.

## Refuted

All four were **true at the reviewed sha `41bae66`** — the reviewer's citations were accurate — and are **refuted at the current head** because the fix commits landed and CI is green on the result. In each case I checked that the fix honestly restores or derives the bound rather than papering over it (checklist item 1's explicit judging instruction for this round).

### F1 — rock-tangent tolerance widened to cos 0.35 → REFUTED (fixed at head)
- Evidence: `tests/test_control.nim:403` at `f078434` — `bound = int64(0.11 * sqrt(float(toRockX * toRockX + toRockY * toRockY)) * float(Q12))`, with a term-by-term derivation in the comment (half-step 5.625° + sector-boundary skew 0.064° + µm truncation 0.218° → sin(5.907°)·1.0002 = 0.1030 → 0.11). Commit `4f2e00f`.
- Honesty check: the block now echoes the worst cosine observed. CI `test` job, both modes: `rock tangent: worst cosine toward the rock 0.09911919308913099 over 3365 clipping goals` — 0.09912 = sin(5.625° + 0.064°), i.e. the analytic bound is tight and the reviewer's suggested 0.098 would have *failed* (it is 0.5 % below the measured worst case). 0.11 leaves 11 % headroom, not 3.6×. This is a derivation, not a papering-over.

### F2 — on-path poison-repulsion assertion deleted → REFUTED (reinstated at head)
- Evidence: `tests/test_control.nim:214-275` at `f078434` — `deadAhead(standoffMm)` puts the waypoint straight through a pinned bloom (the deleted block's geometry) over all six standoffs `0/500/900/1200/1800/2500 mm` and asserts: closest approach monotone in the standoff, at `MaxStandoffMm` (2.5 m) the bloom is still live after 48 ticks and never touched (`tests/test_control.nim:272`), and at standoff 0 it *is* eaten (non-vacuity). The off-path monotonicity block is kept. Commit `e69f45d`.
- Honesty check: the radial term genuinely cannot sidestep (reviewer confirmed this against `control.nim`), so the note's "every standoff ≥ 0.5 m holds" was unsatisfiable by the shipped controller; the fix derives when braking suffices (steer reverses at d < s/3, contact 0.40 m, stopping distance 0.44 m at throttle 128 ⇒ s ≈ 2.5 m) and the CI numbers match the derivation (`dead ahead at standoff 2500 mm: bloom survived, closest 486771 µm`; eaten at 0–1800). The note deviation is recorded in the repo copy's Errata (`docs/plans/2026-08-26-walker-waterworld-design.md` §"§Tests 4 …").

### F3 — hold-brake threshold widened 100× → REFUTED (fixed at head)
- Evidence: `tests/test_control.nim:97` at `f078434` — `if speed < 27.0 and stopped < 0:` with the derivation in the comment (integer drag `v − (v·39) div 1024` removes nothing below 1024/39 = 26.3 µm/tick; the servo issues level 0 below 372 µm/tick; so 27 is the smallest integer bound the arithmetic admits — an exact zero was never reachable, so the original `1.0` was unsatisfiable, and 100 was 3.8× too loose). Commit `581e528`. The shipped controller settles at 18 µm/tick per the commit's measurement, so the assertion has real margin and a brake regression fails it.

### F4 — near-tangent skip added to the rock ray-cast pin → REFUTED (fixed at head)
- Evidence: `tests/test_tank.nim:105-186` at `f078434` — the `continue` is gone; rays are classified (`pinned` / `nearTangent` / `grazeFlips`), the non-tangent class keeps the original 2 mm pin, near-tangent hits get a 5 mm pin, graze/miss flips are bounded by count, and the split's shape is asserted (`nearTangent·20 < pinned`, `grazeFlips ≤ 5`, `pinned > 25 000`) so the class can never quietly swallow the sample. Commit `0e09f51`. CI: `rock casts: 29972 pinned to 2 mm, 268 near-tangent (1 graze/miss flips)`.

The reviewer's nineteen non-blocking findings (F5–F23) were advisory by the reviewer's own categorisation and falsified no checklist item; I spot-verified the substantive ones and they are additionally **fixed at head** where fixable (see §Fixer report audit). None converts to blocking.

## Checklist pass (independent)

| item | status | evidence (path:line or run id) |
|---|---|---|
| 1 CI green, no test loosened | **pass** | Run 32960525769 `success` on `f078434` (gh `--json headSha,conclusion`). `git log -p --since="2026-08-26T07:31Z" -- tests/`: 17 commits. `b3686ba`'s four loosenings are exactly the reviewer's F1–F4, all restored/derived at head (above). The one other bound change (`test_physics.nim` ramp 2.6–3.3 → 1.8–2.1 m/s) is a **narrower** band correcting an arithmetically impossible note value (v₂₄ = a·r·(1−r²⁴)/(1−r) = 79 738 µm/tick = 1.914 m/s) *plus an added* terminal-speed assertion (3.15–3.25); documented in the errata. Fix-round diff `41bae66..f078434 -- tests/`: only two `check(` lines removed, both replaced by strictly tighter forms; no skip/xfail/deletion. Test job ran all 17 test files, debug+release (perf/baselines release-only per the documented repo variable), no `FAILED`. |
| 2 Replay re-derivation | **pass** | `tests/test_replay.nim:107-130` — fresh sim from the config JSON, stepped from recorded bytes only, "re-simulating reproduced every recorded hash" (`raised.len == 0`), `checked > 100`, final score equality. Viewer runs the same module: `replay-viewer/waterworld_replay.nim` imports `waterworld/sim`, re-steps and `checkReplayHash`s per tick; wasm smoke loaded and soaked it (below). Not a parallel recording: per tick only the command byte and the hash are written. |
| 3 Static viewer | **pass** | `coworld_manifest_template.json:24-26` `"replay_viewer": {"bundle": "static-replay-viewer"}` under `game`; `tools/build_replay_viewer.sh` mode 100755, asserted executable in `ci.yml:230-241`; the bundle's only network call is `fetch(message.replayUrl)` (`replay-viewer/static_replay_worker.js:113`); no `/client/replay` pod path anywhere in the manifest (the string's only hits are the server's inherited live route constant and `coworld-release.yml:207`, which *rejects* a pod viewer). |
| 4 Both name spaces | **pass** | `tests/test_server.nim:65-103` — no real name reaches any composed LLM message; the chrome roster carries all four real names + `SKIM-n` aliases; `results.names` carries the real names. `tests/test_locality.nim` repeats over 200 randomised states. `src/waterworld/global.nim:875`: board labels carry only SKIM-n on the player stream. |
| 5 Degrade-never-hang | **pass** | `decide.nim`: attempt-1 9 s / retry 5 s (`CURLOPT_TIMEOUT`, floored whole seconds), monotonic `turnBudgetMs` 16 s wrapper re-taken **after** the floor (`decide.nim:383`, F5 fix), the floor sleeps in 100 ms slices and is cut short by the wall clock (`decide.nim:282-310`, F6 fix), budget guard (`decide.nim:338-346`); `server.nim:493-501` engine hard stop 660 s → `deadline/wall_clock`; lobby bounded by `lobbyJoinTimeoutTicks` with the no-show reported and the episode continuing (`server.nim:548-562`); bounded spawn sampler (`tank.nim`, 64 attempts + lattice); shutdown grace 20 s. 660 ≤ 720 = 60 % of 1200 in every variant (manifest schema max 720; `test_engine.nim` deadlineArithmetic asserts worst case 416 s < 660). |
| 6 num_agents | **pass** | `num_agents: 4` in `variants[default]` (:226), `variants[sprint]` (:258) and `certification.game_config` (:294); `certification.players` ×4; `docker_smoke.sh:106-151` enforces all four invariants with `SEAT-COUNT FAIL:` prefixes and the `SMOKE_SEATS` (=4, scaffold-substituted default at :54) cross-check; **grep of the full run-32960525769 log for `SEAT-COUNT FAIL`: 0 hits**; the smoke log shows `seats=4`, `all 4 player containers exited 0`, `smoke OK: seats=4 … reason=complete`. |
| 7 Scripted baseline full episodes legally | **pass** | `tests/test_engine.nim:119-121` — a full `runScripted(blShoal)` episode ends `sim.endReason == ReasonComplete`; `tests/test_baselines.nim:19-86` — 500 states × both baselines, every field inside its schema bounds (incl. "target is a CURRENTLY DETECTED plankton", partner never self) and the compiled byte decodes to `dir 0..31, level 0..7`. Tuned, not guessed: `tools/tune_baselines.nim` (4×4×4 grid), `tools/ci/baseline_tuning.json` records the pick (2 400 000 / 1800 / 12), `tests/test_tuning.nim` asserts shipped defaults equal it, and `test_baselines` asserts against both the recorded panel and the note's floor (18/20 at 8+, mean > +70, 16/20 mixed). |
| 8 LLM reply handling | **pass** | `intents.nim:114-153` `extractJsonObject` (balanced-brace, string-aware, fence/prose tolerant, first-`{`..last-`}` fallback); `parseIntent` repairs every bounded field; retry exactly once (`decide.nim:387` `attempt < 2`); throttled fast-fail skips the retry; fallback recorded (`fallbackRecord`, `source = isFallback`, truthful attempt count `clamp(attempt,1,2)` at `decide.nim:467`); phase-60 grep phrases present (`decide.nim:470` "falling back", `llm.nim:131` "LLM provider is unavailable"). Now *tested against a real fake provider* (`tests/test_engine.nim:322-345, 350-395`): exactly one retry, 429 → zero retries, hung provider cut by the budget, two failures → shoal + terminal record. |
| 9 Rune-safe truncation | **pass** | `intents.nim:61-68` `truncateRunes` (runeLen/runeSubStr) is the single shortening path; say/note/policy/detail/intent-record all route through it; provider bodies truncated rune-safe in `llm.nim:176,184,190,199`. Test: `tests/test_intents.nim:127-158` — 4-byte emoji at runes 48/49, truncation lands on 48 runes, `validateUtf8() == -1`, round-trips parseJson; `test_replay.nim` forces non-ASCII label+note through the real replay and `replay_summary.py` under a strict UTF-8 parse. |
| 10 Manifest validates | **pass** | `coworld_manifest_template.json:171-201` — `docs.readme = {"type":"text","value":…}` and three pages `{id,title,content:{"type":"text","value":…}}`; `:161-169` — `protocols` carries both `player` and `global` as text objects. `tests/test_manifest.nim` pins it. |
| 11 Viewer legible at 360 px | **pass** | `client/replay_broadcast.html:2752-2757` — `.plate-name { flex: 1 1 auto; min-width: 3.2em; … }`; labels hide via the starter's `#stage.tiny` toggle at `boardW <= 620` (:2694), which is under 640; `tests/test_viewer.nim:142-149` parses the threshold out of the page and asserts `≤ 640` (F19 fix), plus the `.plate-name` rule (:133-134). |
| 12 Release order and scaffold | **pass** | `coworld-release.yml` steps: Build the Coworld manifest (:153) → Certify locally (:167) → Upload the policies (:212) → Upload the Coworld (:310) → Put the Coworld secret (:348). Smoke steps build their own image/bundle in the same run (`ci.yml:177, 254`). Three workflows present; `tools/ci/docker_smoke.sh` mode 100755; `tools/ci/policies.json` = 2 `PLAYER_PROMPT` champions + 2 scripted fillers, champion #2 (`walker-waterworld-relay`) carries `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`; I ran the placeholder gate over the five named files — **exit 0, no matches**. |
| 13 Viewer executes | **pass** | Run 32960525769 `wasm-viewer` (needs: docker-smoke) — step "Load the bundle in a real browser" ran and printed `{"loaded":true,"ms":591,…}` plus `soak: 12s of playback kept advancing ("0 / 768" → "288 / 768")`; not absent/commented/continue-on-error (step list + log). Markers from the shell's own paths: `static_replay.js:161` sets `data-replay-loaded="true"` on the Worker's `loaded` (first drawn frame), `:20` sets `data-replay-error` in `showFailure()`. Matched pair: `replay-viewer/config.nims` has **no** MODULARIZE/EXPORT_NAME; `static_replay_worker.js:188` uses `Module.onRuntimeInitialized` — both the starter's non-modularized form, pinned by `tests/test_viewer.nim:316-319`. |
| 14 Chrome is the starter's | **pass** | `client/chrome_common.js` **byte-identical** to `/workspace/starters/coworld-ctf/client/chrome_common.js` (I diffed the sha256s: `7ace7287…` both sides; also pinned by test after F21). `client/replay_broadcast.html` carries the banner at :2727; the 2726 lines above it are the starter's page minus the listed removals (`#viewpanel`/`#fpv`/`#povBadge` markup+CSS+wiring gone — test asserts all 19 ids/5 rules/5 wiring calls absent) — regenerated by `scripts/fork_broadcast_page.py`, which asserts 26 boundary anchors against the starter. Transport: `relayout()` sets `--band`/`--hudscale` on `document.documentElement` (:2672, :2699); `#endcard { bottom: var(--band, 0px) }` (:741) shown with `#endcard.on` (:752) and taken down on every non-gameover frame (:1661) so every seek dismisses it; beats are `<button>`s with `aria-label`/`title` that seek (`CTX.send('s:' + tick)`), CSS for every emitted kind (`.capture` :2848, `.poison` :2849, `.target_met` :2850, `.gameover,.over` :2855) matching `broadcast.nim:23 ScrubberBeatKinds`; `#viewpanel` removed, not hidden (board 1200×800 fits the frame). |
| 15 Every drawn string fits its frame | **pass** | Main smoke: `canvas text: 0 drawn …` — total 0 because the board draws from a Worker OffscreenCanvas out of Nim-baked sprites, so per the checklist it is *not evidence*; the compensating gate the checklist demands is present: step "Worst-case text render fixture" (`ci.yml:344-358`) ran `viewer_smoke.mjs --strict-text-bounds` on `tools/ci/renderer_fixture.html` and printed **`canvas text: 2850 drawn, 0 never inside the canvas (0 draws crossed an edge), 0 ellipsized (--strict-text-bounds)`** — full-cap 48-rune `say` + 160-rune `note` on all four seats at 360/620/1280 px, the fixture self-checks its own string lengths (sets `data-replay-error` if not exactly 48/160: `renderer_fixture.html:103-104`), and its duplicated band geometry is pinned from Nim (`test_viewer.nim:227-243` re-derives BOARD_W/H and the band from `global.nim`'s exported `bubbleBandCentreY`/`bubblePillHeight`). The shipped bubble placement is clamped into the board (`global.nim:615-628 bubbleSlotX`, F17 fix, swept over six widths in test_viewer). The checklist's literal `client/renderer.js` cannot exist here (board is Nim-side; AGENTS.md documents it) and the fixture is the substitute the checklist's purpose requires. Speech bubbles live in a reserved band, never positioned relative to a skimmer. |
| Simultaneous batch | **pass** | One `curly.makeRequests(batch, …)` call per turn covering every open seat (`decide.nim:397-412`); `tests/test_engine.nim:296-317` drives the real batch path at a fake provider that holds every reply 300 ms and asserts the in-flight high-water mark is **exactly 4** — all four seats in flight at the same moment, never sequential. |

## Fixer report audit

Read only after my independent pass and my read of the review. Audited commit-by-commit against `git log -p 41bae66..f078434` and the CI log of run 32960525769.

| finding | fixer said | I verified | agrees |
|---|---|---|---|
| F1 | fixed, `4f2e00f`, bound 0.11 derived, worst cosine 0.09912 | `test_control.nim:403` at head; CI log line `worst cosine … 0.09911919308913099 over 3365 clipping goals` (both modes) | yes |
| F2 | fixed, `e69f45d`, six-standoff dead-ahead block + errata | `test_control.nim:214-275`; CI log `dead ahead at standoff 2500 mm: bloom survived, closest 486771 µm` (eaten at 0–1800); errata section present | yes |
| F3 | fixed, `581e528`, 27 µm/tick derived | `test_control.nim:97` `speed < 27.0` with the drag/servo derivation | yes |
| F4 | fixed, `0e09f51`, skip removed, classes counted+bounded | `test_tank.nim:105-186`; CI log `rock casts: 29972 pinned to 2 mm, 268 near-tangent (1 graze/miss flips)` | yes |
| F5 | fixed, `e2553c3`, budget clock re-taken after the floor | `decide.nim:380-383` — `turnStart = getMonoTime()` after the floor sleep, with the comment | yes |
| F6 | fixed, `156ce1e`, 100 ms-sliced stop-interruptible floor | `decide.nim:278-310` `waitOutInterBatchFloor` + `FloorSliceMs = 100`; `test_engine.nim` drives both paths | yes |
| F7 | fixed, `888a1ee`, real localhost fake provider | `test_engine.nim:191-405`: one batch (max in-flight = 4), exactly one retry, 429 → zero retries, hung cut by budget, shoal + records | yes |
| F8 | fixed, `0f13ef2`, imports the server's parser | `test_server.nim:17-22` "THE SERVER'S OWN PARSER, imported from waterworld/server"; `server.nim:193` exports it | yes |
| F9 | NEEDS-DESIGN, no change | `server.nim:569-599` unchanged; the stated extraction cost is plausible; reviewer had it advisory, no checklist item names it | yes (stays advisory) |
| F10 | fixed, `6f6e0e2`, leading-brace-only strip | `intents.nim:85-87` — printable filter keeps braces; `while result[0] == '{'` strips leading only; test pins three cases | yes |
| F11 | fixed, `9871ce7`, truthful attempt count | `decide.nim:467` `clamp(attempt, 1, 2)`; fake-provider tests assert 1 on throttle, 2 on double failure | yes |
| F12 | fixed, `ea2cbee`, comment corrected | `replays.nim` comment now says by-skimmer-index | yes |
| F13/F14/F15/F22/F23 | documented in errata, no code change | Errata section carries all entries (art PNGs, absent "kept verbatim" files, §Tests 1 ramp, swept superset, swept BaselineParams, `speed` key, ray dimming); AGENTS.md points at it | yes |
| F16 | fixed, `a0569b7`, fixture geometry pinned from Nim | `global.nim:603-628` exports band procs; `test_viewer.nim:227-243` re-derives the fixture's literals | yes |
| F17 | fixed, `880449d`, pill clamped | `global.nim:615` `bubbleSlotX` clamps into `[w/2, BoardW − w/2]`; used at :644; test sweeps six widths | yes |
| F18 | documented, `f078434` | `ci.yml:319-328` — "READ THE NUMBER THIS STEP PRINTS CORRECTLY: … 0 is EXPECTED here, not a pass" pointing at the fixture step | yes |
| F19 | fixed, `d5f4bf5`, threshold parsed and asserted ≤ 640 | `test_viewer.nim:142-149` | yes |
| F20 | fixed, `e78837c`, both deltas pinned two-sidedly | `diff client/broadcast_core.js` vs starter: exactly the 2 lines; test asserts presence+absence pairs | yes |
| F21 | fixed, `a3c79a0`, sha256 pin | `test_viewer.nim:33` `ChromeCommonSha256 = "7ace7287…"` = my own `sha256sum` of the file (and the starter's) | yes |
| overall | "no test disabled, skipped, deleted or loosened by any of these commits" | `git log -p 41bae66..f078434 -- tests/` — only two `check(` lines removed, both replaced by strictly tighter forms; no skip/xfail; ~40 assertions added | yes |

## Non-blocking observations

- `client/replay_broadcast.html` carries a small number of in-place script edits above the banner (the `WW_MODE` latch, the plate-rebuild branch, `WaterworldStaticReplay` adapter names, locker-room captions) rather than being a pure append. The page is regenerated as a deterministic function of the starter by `scripts/fork_broadcast_page.py` (26 asserted boundary anchors, named cuts/replacements), the CSS sections item 14 names are intact, and the reviewer verified byte-identical regeneration — so item 14's substance holds; recorded here because "nothing above the banner is rewritten" is not literally true.
- F9 residue: the held-registration path (`server.nim:569-599`) remains untested; the design note §Tests 7 promises it, no checklist item requires it. Reasonable NEEDS-DESIGN.
- `docker_smoke.sh` writes the binary `COWLDWWD` replay to `dist/smoke/replay.json`; `ci.yml`'s loader and the fixture step both depend on the name; works, green, cosmetic.
- `llmTurns`/`fallbackTurns` on a live LLM episode remain unexercised by CI (no key by design); phase 60 is the place that measures it.

BLOCKING: 0
