# r1 review — battlecode-2024 (`bc24` year module of `Metta-AI/cogame-battlecode`)
Reviewed sha: 5e7c8b78a09618daaec01a532dc3391cec1483c3   Range: d2922438..5e7c8b78   Files read: 52   CI run: 34084170288 (success; jobs test / docker-smoke / wasm-viewer / parity-oracle / parity-oracle-bc20 / parity-oracle-bc21 / parity-oracle-bc24 all success, no step skipped)
Checklist: prompts/30-review-loop.md §ACCEPTANCE CHECKLIST   Design note: runs/2026-09-04-battlecode-2024/design.md
Round: 1   Provenance: run from an operator's local Claude Code session (daveey's Mac, 2026-09-10) after the coordinator's three in-sandbox reviewer threads died silently with nothing on disk; this file was NOT produced in the cloud sandbox. It was written incrementally (skeleton first, items appended as traced) and reordered at the end. Nothing was built or run locally; CI run 34084170288 (log saved and grepped) is the run-time evidence, the pinned checkout is the code evidence.

Scope note: this is a MOD run. Only the bc24 delta (103 files, +15000/−79) and the range's changes to shared files were reviewed. bc26/bc20/bc21 behaviour untouched by the range is out of scope. Every claim below is labelled observed (I read it), inferred (I reasoned from what I read) or untested (a run would be needed).

## Blocking

None. No checklist item 1–15 nor the one-batch rule is falsified by the code at 5e7c8b78 or by CI run 34084170288. A clean review is a valid review.

## Non-blocking

### F1 — The canvas-text gate reports `total: 0` on every replay and on the fixture; the effective legibility gate for LLM text is the fixture's own DOM verdict
- Where: CI log lines 7921, 7939, 7956, 7970 (`canvas text: 0 drawn, 0 never inside the canvas (0 draws crossed an edge), 0 ellipsized`) for replay.json / replay-bc20 / replay-bc21 / replay-bc24, and line 8074 (`canvas text: 0 drawn, … (--strict-text-bounds)`) for the fixture step; `tools/ci/viewer_smoke.mjs:141` ("OffscreenCanvas inside a worker reports total: 0 -- which is itself the…"), `:811-813` (the `never_inside > 0` gate), `:688` (`data-replay-error` → failure); `tools/ci/renderer_fixture.html:375-478`.
- Observed: the bc24 block renders `notes`/`motto`/plain-words into DOM (`client/replay_broadcast.html:4033-4044`, `host.innerHTML = … esc(d.notes) …`; scorebug `.plate-sub`), not onto a canvas. The board is drawn by the worker (`replay-viewer/static_replay_worker.js`, `Module.onRuntimeInitialized`), so the page-level `fillText`/`strokeText` hooks see nothing: `total: 0` on all four replays and on the fixture. The fixture's own verdict (`problem()` at 375-462) measures every `#chrome *` rect against the iframe frame, checks `scrollWidth > clientWidth` on the filled readouts (incl. the new `bc24` selector set at 407-409), caps the doctrine panel at 50 % of frame height, and asserts the motto/notes are still full-length (452-459); a failure sets `data-replay-error` (line 80 `fail`) which `viewer_smoke.mjs:688` turns into exit 1. The fixture reported `{"loaded":true,…}` at 360/720/1280 px for the bc24 row (the step passed).
- Checklist item: advisory — bears on item 15. The item's literal gated number (`never_inside` under `--strict-text-bounds`) covers nothing here because `total: 0` ("the check covered nothing"), and the fixture is a DOM fixture rather than "a page that loads the real `client/renderer.js`… renders it at several canvas sizes". This repo has no `client/renderer.js`; that fixture shape is pre-existing (bc26/bc20/bc21 rows at `d2922438`) and bc24 added a row to it. Any text the wasm renderer itself draws on the worker's OffscreenCanvas (design.md:1266-1275 names a level pip and a return countdown) is unmeasured by any gate — untested.

### F2 — CI/design drift in the bc24 docker-smoke substance assertion and its key names (documented in `ci.yml`)
- Where: `.github/workflows/ci.yml:1250-1268` (comment), `:1275-1277` (`SMOKE_REQUIRE_STATS: {"ducks_spawned":10,"crumbs_spent":100,"traps_built":1,"damage_dealt":300}`), `:1287-1320` ("The bc24 episode really played bc24" step).
- Observed: design.md:1811-1817 specifies the per-seat set `{ducks_spawned:10, crumbs_spent:100, traps_built:1, levels_end:3, damage_dealt:300, heal_dealt:80}` plus one across-seats jq line `([.games[0].flag_pickups[]] | add) >= 1`. The shipped step drops `levels_end` and `heal_dealt` from the per-seat set (comment: `examplefuncsplayer24` never heals and its ducks die before earning 15 attack XP) and asserts them across the two seats instead (`levels >= 3`, `healed >= 80`, `picked >= 1`), reading `.result.games[0].flags_picked_up` — the comment notes the design's key `flag_pickups` "is not a key any bc24 results document has ever carried" (`Bc24GameKeys` in `src/battlecode/results.nim:129-139` has `flags_picked_up`). Run 34084170288 printed `across the two seats: flag pickups=3 skill levels=74 health healed=37354` (log 5817). The docker-smoke step also does not print `sim_seconds / rounds` as design.md:1246-1248 says; the perf shard prints `DefaultLarge 2000 rounds in 0.622 s (0.311 ms/round)` in release (log 2215) instead.
- Checklist item: advisory — bears on design.md:1811-1817 and 1246-1248, not on a checklist item.

### F3 — Replay event fields differ from the design's event table (three kinds)
- Where: `src/battlecode/match.nim:160-176` (`setup_end`, `first_action`), `:181-184` (`flag_taken`); `src/battlecode/years/bc24/rules.nim:292-295`; `src/battlecode/years/bc24/flags.nim:68-69`.
- Observed: (a) `first_action` carries `action` rather than the design's `kind`; the code comment at match.nim:167-175 explains `MatchEvent` flattens `fields` into the event object so a field named `kind` would overwrite the event kind, and `tests/test_bc24_replay.nim:306-308` asserts `first_action carries no field called kind` and that `action` is in `Bc24ActionNames`. (b) `setup_end` is emitted with `fields = {"traps": [e.b, e.c], "teleported": e.a}` — the design table (design.md:1103) lists per-clan `flags` (three `[x,y]`), `dug`, `filled`, and a boolean `teleported`; here `teleported` is the integer `setupFlagTeleports` (0–2) and there are no `flags`/`dug`/`filled`. (c) `flag_taken` carries `alias, flag, x, y` and no `escort` (design.md:1105). The `flag_returned` alias is `plan.aliasOfTeam(gameIndex, 1 - e.a)` (match.nim:190) — inferred to be "the owning clan" while the emitter passes the returning team; not verified against the emitter.
- Checklist item: advisory — bears on design.md:1086-1118 (event vocabulary). Every emitted kind has CSS (traced under item 14) and is inside its per-game bound (`tests/test_bc24_replay.nim:278-286`).

### F4 — Test thresholds differ from the design note's, with the substitutions recorded in the test headers; knob signed-deltas are gated in `-d:release` only
- Where: `tests/test_bc24_knobs.nim:14-40` (five substituted thresholds, each with the measurement that forced it); `tests/test_bc24_survival.nim:19-26` (floors raised to measured values — "a strict superset of the note's floor"); `.github/workflows/ci.yml:70-76` (comment: the knob sweep's signed deltas are gated in release only); CI log 2189-2193 (`bc24 knob teeth: DEBUG pass — the sweep ran on 1 map(s); the signed deltas are gated in release` → `ok (5 checks)`; release → `ok (25 checks)`).
- Observed: design.md:1596-1640 (§Tests items 15-16) lists thresholds; the shipped tests replace five knob clauses (e.g. `trap_budget`'s "dig/fill spend −40 %" → "crumbs into traps +100.8 %") and raise the survival floors. Both passes ran in CI and the release pass gates all 25 checks; the inverted control (`-d:bc24BrokenChassis`) ran and the gate failed on it as required (log 2256-2257, `bc24 survival: ok (108 checks)`).
- Checklist item: advisory — bears on design.md §Tests items 15-16; item 7's testable clauses are met (see Traced). Not a loosening of a pre-existing test (all files are new in the range).

### F5 — No grid-harness artefact for the baseline's tuning exists in the tree (item 7's "tuned with a grid harness, not guessed" clause)
- Where: `tools/` (no `grid`/`tune`/`sweep`/`harness` file), `docs/RULES-BC24.md` (no tuning section; headers at 18-387), `tests/test_bc24_knobs.nim:187` ("…as the chassis was tuned").
- Observed: the only evidence of tuning is prose in test headers quoting measured ranges (knobs 14-40, survival 19-26). No script, log or table of the grid is committed. Could not determine whether a grid harness was run (see §Could not determine); an absent artefact does not falsify the clause, so this is filed as advisory.
- Checklist item: advisory — bears on item 7, second sentence.

### F6 — `setup_end` per-game bound in the replay test is 2 for a one-game match
- Where: `tests/test_bc24_replay.nim:273` (`deriveAndCompare(bc24Config(2000), sheets(), 5, @["Occulus"])` — one map), `:278` (`"setup_end": 2`); emitter `src/battlecode/years/bc24/rules.nim:292-295` fires once per game at `currentRound == SetupRounds`.
- Observed: the design table (design.md:1103) bounds `setup_end` at 1/game; the test allows 2 on a single-game match. `game_start`/`game_end`/`game_abandoned` are bounded at 1. Inferred: the bound is one looser than the emitter can reach; harmless, but the assertion is weaker than the table.
- Checklist item: advisory — bears on design.md:1103.

### F7 — `game.docs` uses `type: "uri"` where checklist item 10 spells `type: "text"`
- Where: `coworld_manifest_template.json` — `game.docs.readme = {type:"uri", value:…}`; all six `pages[].content = {type:"uri", value:…}` (jq on the pinned tree); `tests/test_manifest.nim:257-275` asserts the object shape and that each page's file exists.
- Observed: the shape is `{type, value}` objects with both `player` and `global` protocols present. `type` is `uri` for the readme and every page, which is the shape the tree had at `d2922438` (five uri pages) — the range added the sixth page in the same shape. The CI step "The coworld CLI accepts the manifest template" passed (test job). Whether the platform accepts `uri` as equivalent to the checklist's `text` is not decidable from the tree; the certifier accepted it on the three shipped releases.
- Checklist item: advisory — bears on item 10 (category manifest); pre-existing shape, not a change the range made.

## Traced and consistent

- **Item 1 (CI green; no test loosened)** — observed: `gh run view 34084170288` → `conclusion: success`, `headSha 5e7c8b78…`, `headBranch main`, `event push`, `createdAt 2026-09-07T04:43:37Z`; seven jobs all `success`, no step `skipped`; the only `continue-on-error: true` in `ci.yml` is line 623, the pre-existing bc20 "engine-from-source attempt (reported, not blocking)" step, not a bc24 or viewer step. `git diff d2922438..5e7c8b78 -- tests/` removes exactly 24 lines, all in `tests/test_determinism.nim`, `tests/test_manifest.nim`, `tests/test_viewer.nim`, every one a three-year→four-year widening re-asserted on the next line (`chains.len, 3`→`4` with a pairwise-distinct loop; `variants.len, 3`→`4`; `docs["pages"].len, 5`→`6`; `policies.len, 12`→`16`; `prompts 6`→`8`; `scripted 6`→`8`; `owned 3`→`4`; fixture `YEARS` widened). No assertion deleted, no tolerance widened, no test removed, no skip/xfail added to a pre-existing test. The only skip-shaped path in added tests, `tests/test_bc24_survival.nim:123-125` (`findExe("nim")` empty → inverted control skipped), was NOT taken in CI: log 2256 shows the inverted control running with `-d:bc24BrokenChassis`. Every bc24 shard printed `ok` in both passes (baselines 26, combat 64, comms 93, cooldowns 35, end ladder 23, flags 51, knob teeth 5/25, levels 140, maps 805, perf 5/7, replay 96, scoring 19, sensing 28, sheet 91, spawning 33, survival 108, terrain 40, traps 51, upgrades 23), plus test_determinism 66, test_manifest 611, test_viewer 422, test_replay 72.
- **Item 2 (replay re-derivation, frame by frame, viewer uses the same derivation)** — observed: `src/battlecode/replay.nim:268-277` builds one frame per recorded round per game (`frameGame`/`frameRound`), `newDeriver` starts game 0 and `advance()` steps the year's sim through `years/dispatch.nim` (`stepRound … of yBc24: rules24.runRound(s.w24, …)` at dispatch.nim:244); `hashChainHex` (dispatch.nim:265) is compared per round and exposed as `mismatchRound`; the wasm entry `replay-viewer/bc_replay.nim:70-99,152-153` uses the SAME `newDeriver`/`advance` and exports `bc_mismatch_round`. Tests: `tests/test_bc24_replay.nim:123,321-324` ("re-derives with no hash mismatch", `mismatchRound == -1`), `:216-233` record→re-derive for `capture` and the wall-clock `abandoned` stop (playback stops at `plan.abandonAfter[g]`, replay.nim:264-265, "with the recorder's own hash chain at the stop round"), ladder vectors for `more_flag_captures`/`level_sum`/`more_bread`/`coin_flip`; `tests/test_viewer.nim:693-760` drives the real renderer off a re-derived bc24 session. CI: all four browser loads report `mismatch_round: -1` under node (`wasm_replay_smoke.cjs`, log 8019-8031) and `loaded: true` in chromium; the parity job is bit-exact for 2000 rounds on 15 bot×map pairs with an empty ledger (log 535, `tools/ci/parity_ledger_bc24.json` `entries: []`).
- **Item 3 (static viewer)** — observed: `coworld_manifest_template.json` `game.replay_viewer = {"bundle":"static-replay-viewer"}`; `tools/build_replay_viewer.sh` present, mode 100755, invoked by `ci.yml:1405` and required by `coworld-release.yml:213-220` ("tools/build_replay_viewer.sh must emit it; a pod-served /client/replay viewer is not acceptable"); the only network call in the viewer is `fetch(message.replayUrl)` at `replay-viewer/static_replay_worker.js:127`; no `/client/replay` route (`tests/test_seats.nim:77-78` asserts it is not served; the `client/broadcast_core.js:372` mention is the inherited path-strip list). The range did not touch `replay-viewer/` at all (`git diff --stat` empty).
- **Item 4 (both name spaces)** — observed: the brief sent to a seat carries `"alias": aliasFor(slot), "opponent_alias": aliasFor(1 - slot)` (`decide.nim:366-367`) and no `names`; `AliasA/AliasB = "Clan Ash"/"Clan Basil"` (`sim_types.nim:125-126`); real names live only in `results.names` (`results.nim:79`) and `replay.names` (`replay.nim:127`), and the page draws them in the scorebug plate (`replay_broadcast.html:4316 var name = (s.names && s.names[slot]) || ''`), the endcard (`:4478`) and the bc24 doctrine panel (`:4506`). Inherited mechanism; bc24 changed none of it.
- **Item 5 (degrade-never-hang, 60 % envelope)** — observed bounds: seat registration `waitForSeats` bounded by `connectTimeoutMs` (`server.nim:238-257`); doctrine phase `doctrineBudgetMs` monotonic guard with `attempt1Ms`/`retryMs` handed to curl as whole-second `CURLOPT_TIMEOUT` (`decide.nim:465-523`); per-game `budgetSeconds` checked every 32 rounds on the monotonic clock (`years/bc24/rules.nim:413-424`, `aborted` → `abandoned`, discarded by the match) and the match-level `matchBudgetSeconds` guard (`match.nim:255-283`, `reason = epDeadline`); shutdown grace 20 s (`server.nim:468-473`). bc24 variant values (jq): `connectTimeoutMs 25000, attempt1Ms 20000, retryMs 12000, doctrineBudgetMs 45000, perGameBudgetSeconds 110, matchBudgetSeconds 340`. Inferred arithmetic: 25 + 45 + 340 + 20 + write ≈ 435 s ≤ 720 s. Measured: the worst-case configuration on the largest pool map plays 2000 rounds in 0.622 s release / 5.965 s debug (`test_bc24_perf`, log 2209/2215; budget 90 s), so three games are seconds, not minutes. Untested at production scale (CI plays 600 rounds).
- **Item 6 (`num_agents`)** — observed: all four variants carry `game_config.num_agents: 2` and none has a top-level `num_agents`; `certification.game_config.num_agents: 2`, `certification.players` len 2, `certification.game_config.year: bc26` (unchanged). `tools/ci/docker_smoke.sh` (mode 100755, untouched by the range) carries the four `SEAT-COUNT FAIL:` invariants at 145-176 and the `SMOKE_SEATS` cross-check at 176-184 (`seats_expected="${SMOKE_SEATS:-2}"`). `grep -c "SEAT-COUNT FAIL"` over the full run log = **0**; all four episodes logged `seats=2 … "num_agents": 2` and `smoke OK: seats=2 … reason=complete` (log 5643/5659, 5689/5696, 5727/5735, 5767/5775). `tests/test_manifest.nim:179-197` asserts `len(certification.players) == num_agents`, `player[] == [awu, scaffold]`, cert stays on bc26.
- **Item 7 (scripted baseline plays full episodes legally)** — observed: `tests/test_bc24_baselines.nim:62-99` plays six whole 2000-round games (`playGame(…, 2000, 0)` on three maps × both side assignments), asserts `refused == 0` where `refusedActions` is incremented by the sim at every action entry whose precondition fails (`years/bc24/world.nim:677,718,762,794,836,865,910` — spawn, move, attack, heal, build, dig/fill, upgrade), `opsPeak <= DecisionOps` and `opsPeak > 0` (`world.nim:932`), the weak bot spawns/moves/attacks, and gone-sharkin wins 6/6. `results.reason == "complete"` for an all-scripted match is asserted at the match level in `tests/test_bc24_replay.nim:122,141,274` (`r.reason == epComplete` via `playMatch`) and at the container level by docker-smoke (`reason=complete`, `fallbacks == [0,0]`, log 5775). Both scripted replies pass the same `validate` (`baselines.nim:132-143`, `test_bc24_baselines.nim:48-59`). The "grid harness" clause: see F5 / Could not determine.
- **Item 8 (LLM reply handling)** — observed `decide.nim:458-590` (year-neutral, unchanged by the range except the bc24 preamble/payload arms at 254-343, 347, 411-447): fence-tolerant extraction in `llm.nim:158-195` (untouched); `while open.len > 0 and attempt < 2`; on parse/transport failure a `doctrine_retry` event with cause and the log line `failed, will retry`; after the second failure the baseline sheet, `result.fallback[slot] = cause`, a `doctrine_fallback` event and the log line `falling back to the scripted doctrine (<cause>)`; `results.fallbacks` is a top-level results key (`results.nim:156`). Design.md:705-728 matches.
- **Item 9 (rune-safe truncation)** — observed: `MaxNoteRunes 280`, `MaxMottoRunes 48` (`sim_types.nim:115-116`); `truncateRunes` (211), `truncateBytes` cut on a rune boundary (220-223); call sites `sheet.nim:95,109,117`, `llm.nim:169-204`, `decide.nim:556-560`. Test `tests/test_bc24_sheet.nim:154-176`: astral-plane `\u{1F986}` ×80, `notes.runeLen == 280`, `motto.runeLen == 48`, `validateUtf8() < 0`, 16 KB byte cap on a rune boundary; `tests/test_bc24_replay.nim:242` asserts the written replay is valid UTF-8. Passed in CI.
- **Item 10 (manifest validates)** — observed: `game.docs.readme` and six `pages[]` are `{id,title,content:{type,value}}` objects (`rules.md, rules-bc20.md, rules-bc21.md, rules-bc24.md, replay.md, parity.md`); `game.protocols` = `{global, player}`; `config_schema.year.enum = [bc26,bc20,bc21,bc24]`; `end_reason` enum extended with `capture, more_flag_captures, level_sum, more_bread`; `tokens` required. CI test job step "The coworld CLI accepts the manifest template" passed. (`type` is `uri`, not `text` — F7.)
- **Item 11 (legible at 360 px)** — observed: `client/replay_broadcast.html:2571 #scorebug .plate-name { flex: 1 1 auto; min-width: 3.2em; }` and `:2647-2651 @media (max-width: 640px) { … #scorebug .plate-sub { display: none; } … }` — above the bc20/bc21/bc24 banners, untouched by the range. The fixture runs the bc24 row at 360×640 (`renderer_fixture.html:69`) and passed; the bc24 boxes' own 640 px rules were not individually read.
- **Item 12 (release order and scaffold)** — observed: `coworld-release.yml` steps Validate inputs (112) → Build manifest (168) → Certify locally (182) → Upload the policies (225) → Upload the Coworld (323) → Wait canonical (361) → Put the Coworld secret (419); untouched by the range; all three workflows present; `tools/ci/docker_smoke.sh` present and executable; the placeholder gate exits 0; `tools/ci/policies.json` has 16 entries, the bc24 four being two `PLAYER_PROMPT` champions (`battlecode-bc24-fortress`, `battlecode-bc24-flagrush` with `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`) and two `PLAYER_SCRIPTED` fillers (`gone-sharkin`, `examplefuncsplayer24`), asserted by `tests/test_manifest.nim:374-389`. Release itself (0.4.0 upload) is a phase-40 action and is untested here.
- **Item 13 (viewer executes)** — observed: `ci.yml:1363-1368 wasm-viewer … needs: docker-smoke`; step "Load the bundle in a real browser (ALL FOUR years' replays)" (`:1449`) ran in run 34084170288 — not commented out, no `continue-on-error` — and each of the four replays reported `{"loaded":true,…}` with three differing scrub readouts, `scrub selector: #scrub`, `endcard … shown=true`, and for bc24 a 15 s soak (`round 3 / 600 → 315 → 363`), `--timeout 120 --soak 15` (log 7967-7975; endcard text `CLAN ASH TOOK ALL THREE FLAGS AT ROUND 391`). Markers: `replay-viewer/static_replay.js:180 setAttribute('data-replay-loaded','true')` inside the worker's `loaded` message handler (after the first board frame; the `animate` rAF is started on the next line), `:14-20 setAttribute('data-replay-error', …)`. Bootstrap agreement: `replay-viewer/config.nims:53` has `EXPORTED_FUNCTIONS=…` and NO `MODULARIZE`/`EXPORT_NAME`; the worker uses the global `var Module = {}` (`static_replay_worker.js:8`), `Module.onRuntimeInitialized` (218) and `importScripts('./wire_constants.js','./broadcast_core.js','./bc_replay.js')` (274) — a non-modularized build with an `onRuntimeInitialized` bootstrap, the consistent pair; all pre-existing and untouched by the range (`tests/test_viewer.nim:51` asserts the importScripts line). Lobby-skip: this replay format has no lobby frames — `replay.nim:268-277` creates frames only for `1 .. rounds` of each game and `broadcast.nim:318-319,446-447,639-640,691-692` publish `st: 0, mx: totalFrames-1`; pre-match events carry `ms`, not frames (design.md:1086-1088). So playback opens at game 1 round 1 by construction; a "late gameStart" replay cannot be recorded in this format (see Could not determine).
- **Item 14 (chrome is the starter's)** — observed: `git diff d2922438..5e7c8b78 -- client/chrome_common.js client/broadcast_core.js` = 0 bytes; `tests/test_viewer.nim:31-35` pins both sha256s. `client/replay_broadcast.html` 4225→4736 lines; nine hunks, first at old line 2900 (below every section-1–5 rule: `#endcard` 1847-1908, `#killfeed` 1255-1269, `.beat-marker.*` 1717-1732, `.plate-name` 2571, 640 px query 2647); exactly three removed lines, each a widened re-add (`--statrail` id list +`'bc24-crumbs','bc24-levels'`; `!isBc20 && !isBc21` → `… && !isBc24` twice). Banner `BC24 additions to the inherited cogame-battlecode chrome` at 2904/3157/3832. Transport: (a) `relayout()` 4519-4546 sets `--hudscale/--topband/--band/--statrail` via `document.documentElement.style.setProperty`; (b) `#bc24-doctrines { bottom: calc(var(--band, 0px) + 8px) }`, `#bc24-crumbs`/`#bc24-levels` ride `var(--band, 0px) +` (`test_viewer.nim:311-324`); (c) `#endcard { bottom: var(--band, 0px) }` (1858), `#endcard.on` (1869), `function seek(frac) { dismissEndcard(); send('s:'…) }` (4291-4293) — every seek path, the bc24 block's included (`Bc24Block.attach({… seek, dismissEndcard})` 4232-4239), goes through it; (d) `buildBc24BeatButtons` 3904-3925 makes labelled `<button type="button" class="beat-marker <kind>">` with `aria-label`/`title` and a click → `api.seek(b.t / span)` (the identical formula the bc26 4264 / bc20 3314 / bc21 3616 builders use), CSS for all twelve emitted kinds with the five new ones scoped `html[data-year="bc24"]` (3023-3032), no bc24 function named `markBeat` (`test_viewer.nim:335-347`). `#viewpanel` KEPT per design.md:1206-1213 (boards to 944 px > 360 px). The `--statrail` fix stays armed: measured set includes the two bc24 boxes (4532-4534); `--killfeed-overlap` ran on all four replays (`ci.yml:1490-1503`).
- **Item 15 (every drawn string fits)** — observed: `tools/ci/renderer_fixture.html` gained a bc24 row (`YEARS = ['bc26','bc20','bc21','bc24']`, full-cap 280-rune notes and 48-rune motto on both seats, `#bc24-flags/-crumbs/-levels/-doctrines` filled, 360/720/1280 px); `ci.yml:1599-1625` "Render the full-cap doctrine-text fixture" runs `viewer_smoke.mjs --url … --strict-text-bounds` on it and passed (`loaded: true`, log 8073-8074). The fixture asserts its strings are still full-length (452-459), that no element escapes the frame (383-396), that no filled readout hides its own content (416-430), and that the doctrine panel is ≤ 50 % of the frame (440-447). The gated canvas number is `0` because the text is DOM — see F1. `--strict-text-bounds` is deliberately dropped on the replay runs because the board is pannable (`#viewpanel` kept), which is the case the checklist excludes.
- **One-batch rule** — observed: `decide.nim:507-523` builds ONE `RequestBatch` over all open seats per attempt and issues ONE `client.curl.makeRequests(batch, …)`; there is exactly one decision turn per episode (design.md:449-450); bc24 added no per-seat sequential call (its whole `decide.nim` diff is data: `Bc24Preamble`, a `preambleFor` arm, a `briefFor` payload arm, a `chassisNameFor` arm).
- **Shared-file diffs reviewed (each additive, no behaviour change to older years observed)** — `sim_types.nim` (GV06→GV07 with a prepend-only changelog entry; `ReplayCompatibleGameVersions` EXTENDED to `["GV04","GV05","GV06",GameVersion]`; two `ScriptedChassis` values), `sheet.nim` (`YearBc24`, `doctrine24`, `knownKeysFor`/`validate`/`toJson`/`plainWords` arms), `baselines.nim` (`blGoneSharkin`, `blExamplefuncsplayer24`, the `yBc24` resolution table matching design.md:625-633, the all-defaults reply that is also the fallback sheet verbatim), `results.nim` (`Bc24GameKeys`, four end reasons; `more_flags_picked`/`resignation` absent, asserted by `test_manifest.nim:104-108`), `years/registry.nim` (one `YearSpec` line, `maxRounds 2000`), `match.nim` (bc24 event arms only), `render.nim`/`broadcast.nim` (additive; not read line by line), `tools/gen_year_constants.py` (three→four-year choice list; removed lines are the widened help text/dicts), `tools/ci/policies.json` (four appended entries), `coworld_manifest_template.json` (five removed lines, all widened re-adds: description, `year` enum tail, `end_reason` tail, two `player[].description` strings; `player[]` ids unchanged), `tests/test_{determinism,manifest,viewer}.nim` (item 1), `tools/ci/renderer_fixture.html` (bc24 row), `ci.yml` (bc24 fetch/regenerate step, `parity-oracle-bc24` job, fourth smoke episode + across-seats step, four-replay browser loop with the bc24 timeout, node smoke on both bc24 replays; `test` timeout 75→110 min). `Dockerfile:7,63-79` runtime stage installs only `ca-certificates libcurl4` — no JDK/JRE/Node. `tools/ci/check_gameversion.sh` still runs (`ci.yml:287`).
- **Parity evidence (not a checklist item; supports items 2/7)** — observed in the CI log: 15 pairs (3 bots × 5 small maps) traced for 2000 rounds, Java and Nim line counts identical per pair (e.g. `examplefuncsplayer/Yinyang: 211662 java lines, 211662 nim lines` — an early capture on that map, on both sides), peak bytecode 297–940 of 25 000 (1.2–3.8 %), first divergent round `--` for all 15, `constants: 0 disagreement(s) between the 3.0.5 jar and the pinned master sources`, Tier B `skills.json` byte-diff passed, empty ledger; the scenario coverage step confirmed upgrades bought, flag pickups, stun firings, attack level ≥ 4 and six flags confirmed at round 200 in the teleport variant.

## Could not determine

- **Item 7, "tuned with a grid harness"** — no harness script, sweep log or grid table is in the tree; the only trace is measured-range prose in `tests/test_bc24_knobs.nim:14-40` and `tests/test_bc24_survival.nim:19-26`. Evidence that would settle it: a committed tuning script/log, or the build report naming the grid that produced the census splits and thresholds.
- **Item 13's late-`gameStart` probe** — the checklist asks to check playback with a replay whose `gameStart` is late. This replay format records no lobby frames (frames are game rounds only, `st: 0`), so such a replay cannot be produced by this recorder; the requirement is satisfied by construction rather than by a probe. A recording from the platform runner with a long `lobbyJoinTimeoutTicks` would be the only way to observe the opposite, and none exists for bc24 yet.
- **Item 15 for board-drawn text** — whatever text the wasm renderer draws on the worker's OffscreenCanvas (design.md:1266-1275: level pip, return countdown, jail-rail countdown) is invisible to the page-level `fillText` hook (`canvas_text.total = 0` on every run). Evidence that would settle it: a screenshot review of the bc24 board at 360 px, or a `canvas_text` measurement taken inside the worker.
- **Beat-marker seek precision when `st > 0`** — all four year blocks position a marker at `(b.t - st)/span` but seek to `b.t / span` (`replay_broadcast.html:3308/3314, 3610/3616, 3914/3920, 4258/4264`); with `st = 0` in this format the two coincide. Inherited, identical across years, not a bc24 change; a replay with `st > 0` would be needed to observe any drift.
- **The 60 % envelope at production scale** — the arithmetic (≈ 435 s ≤ 720 s) is inferred from config values and the 0.6 s/game release perf measurement; CI plays 600-round episodes. A phase-60 production episode is what would confirm `wall_clock_seconds`.
- **`flag_returned` alias polarity** — `match.nim:190` uses `1 - e.a`; I did not open `flags.nim`'s return emitter to confirm which team it passes, so whether the event names the flag's owner or the returning side is unverified.
