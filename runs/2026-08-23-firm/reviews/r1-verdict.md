blocking: 0

# r1 verdict — 2026-08-23-firm
Head: 62dcd64f06dd18cf4fca8ae3a612598697299afa   Checklist: prompts/30-review-loop.md §ACCEPTANCE CHECKLIST   Independent read written before reading fixes: yes (r1-fixes.md was NOT read at all, per the brief; r1-review.md was read only after my own pass over the tree, the CI logs and the starter diffs)

The review was written against `10fbf896`; five commits have landed since (F1, F2, F4, F5, F6, F7, F12 fix commits, de1982e..62dcd64). I judged every claim at `62dcd64`.

## Standing blocking findings

None.

## Refuted

### F1 — "No grid-tuning harness for the scripted baselines exists anywhere in the tree" → REFUTED (fixed since 10fbf896)
- Evidence: `tests/test_tuning.nim` (201 lines, added in de1982e) is the harness: family S sweeps `run 0..10 × maint 0..(10−run) × payroll {20..60}` (594 candidates) and asserts the shipped tuple is the argmax with a strict runner-up gap (`test_tuning.nim:143-148`: `check best.run == ShippedBaseline.run … check rows[1].score < rows[0].score - Tie`); family R sweeps the nurse (528 candidates) and asserts the shipped repair-shaped argmax (`:175-178`). The constants moved into a swept `BaselineParams` object, `ShippedBaseline* = BaselineParams(run: 6, maint: 3, nurseRun: 0, nurseMaint: 8, nurseBelow: 70, payroll: 40)` (`src/firm/llm.nim:92-96`), and `scriptedAction` takes `params` (`llm.nim:235-236`) so the sweep runs through the shipped code path. It runs in CI's `test` job, debug and `-d:release` — verified in the head run's log (`nim r … tests/test_tuning.nim`, `[OK] family S…`, `[OK] family R…`, run 32682767057). `docs/baseline-sweep.md` records the tables, the pre-registered objective (mean weakest-seat score, two horizons), the disclosed nurse deviation from the design note (0/8 below 70 vs the note's 4/6 below 40, rank 130 of 240), and cites CI run 32682573460 on main at 316f380 — I verified that run: `conclusion: success` (`gh run list`). Item 7's "tuned with a grid harness, not guessed" is now verifiable from the tree and from cited, checked CI evidence. A finding that was true and has since been fixed is refuted, not standing.

### The reviewer's non-blocking F2, F5, F6, F7, F12 — all fixed at head (verified from the code, not the fixer's claims)
- F2 (fallback not recorded in the replay): `src/firm/llm.nim:244` — `scriptedAction` sets `result.scripted = true`; parsers leave it false; `src/firm/server.nim:285-287` — `let wasScripted = decision.scripted or scripted[seat] != skNone or client.disabled`, passed into `applyMemo`/`applyWork` and hence into the `memo`/`work` event. Test: `tests/test_bot.nim:154-164` plus the `check decisions[index].scripted` at `:187`.
- F5 (DEFIED chip lit pre-decision): `src/firm/sim.nim:559-571` — `obeyedNow()` answers post hoc (chosen line once acted, else last resolved shift's verdict, else true); used at `sim.nim:626,651`. Test: `tests/test_sim.nim:207-236`.
- F6 (byte-sliced error text): all five slices now `runeSubStr` (`llm.nim:499-501, :539, :547, :552, :561`). Test: `tests/test_bot.nim:301-313` (`error.msg.validateUtf8() == -1`).
- F7 (notes uncapped at the sim boundary): `sim.nim:448-449, :498-499` — `trimText(notes, MaxNotesLen)`; `MaxNotesLen* = 600` moved to `sim.nim:43`. Test: `tests/test_sim.nim:302-325` (700-rune multi-byte fixture, past every cap).
- F12 (spectator frame hard-codes `"scripted": false`): `sim.nim:79` — `scriptedSeat*: array[Seats, bool]`, set in `applyMemo:451`/`applyWork:500`, emitted at `sim.nim:631`, and `replayMatch` replays it off the event (`sim.nim:910-914`). Test: `tests/test_sim.nim:504-521`, including round-trip through `replayMatch`.
- F4 (sprite residue): `data/soldier_{blue,green,yellow}_front.png` deleted in 7faaac3; `soldier_red_front.png` is retained deliberately as the committed reference for `scripts/art/generate_cog_sheet.py:26` and documented in `README.md:98` — not residue.

### Two of the reviewer's "could not determine" items — settled by me from the pinned dependency sources
- **`curly.makeRequests` bounds the batch**: nimby.lock pins curly 1.1.1 at `a0f42baa`; I read that exact source. `makeRequests` sets `rw.timeout = timeout` per request and the worker thread applies `easy_setopt(OPT_TIMEOUT, request.timeout)` — libcurl's hard cap on the whole transfer; the five requests run in parallel (`newCurly(maxInFlight = 16)` default, batch of ≤5), and `waitGroup.wait()` returns when every request has a response or an error ("Timeout was reached …"). So a batch's wall clock is bounded by `llmTimeoutSeconds` (30), and the 60 s/shift budget (two batches) holds. `makeRequests` is `{.raises: [].}` — it cannot raise out of the loop.
- **`readCogameUri` is bounded**: bitworld pinned at `9af28b41`; its HTTP path is `newCurlPool(1); client.get(value)` with the default `timeout: float32 = 60` applied via `OPT_TIMEOUT` before `easy_perform`. One bounded fetch at `newLlmClient` (and one per artifact write, `curl.post(uri, …, 60)` at `server.nim:124`).

## Checklist pass (independent)

| item | status | evidence (path:line or run id) |
|---|---|---|
| 1 CI green, no test loosened | PASS | Run **32682767057** on main at 62dcd64, `conclusion: success`; jobs test/docker-smoke/wasm-viewer all green, all three test files run in debug and `-d:release` (log groups). `git log -p --since=2026-08-23T21:56Z -- tests/` shows only added tests (F1/F2/F5/F6/F7/F12 commits); the sole edit to an existing test enlarged a fixture 400→700 runes so a new assertion actually bites (316f380 — a strengthening; the 400-rune fixture itself was added minutes earlier in a1b8940, whose run 32682358341 honestly failed and was fixed forward). No skip, no deleted assertion, no widened tolerance, no removed file. |
| 2 Replay re-derivation | PASS | `replayMatch` (`sim.nim:890-923`) replays only `memo`/`work` through the rules; recorded `shift`/`settle` events are only *checked* (`checkShift` :848-858, `checkSettle` :860-888, mismatch raises). Viewer derives display from the same code: `replay-viewer/firm_replay.nim:36-38` builds `states` from `replayMatch(...).tableStateJson()`; `renderer.js:1607` reads `payload.states`. Tests: `test_sim.nim:480-502` (frame count, final-frame equality, deadline honoured), `:523-540` (tampered settle/shift each raise), `:504-521` (provenance survives the round trip). |
| 3 Static viewer | PASS | Manifest `game.replay_viewer = {"bundle":"static-replay-viewer"}` (`coworld_manifest_template.json:16-18`); `tools/build_replay_viewer.sh` present, committed 100755 (`git ls-files -s`), asserted executable by `ci.yml:225-236`; the bundle's only network call is the `?replay=` fetch (`static_replay.js:76,129`) — no WebSocket, no other fetch in the bundle path. The `/client/replay` string appears only in the `protocols.global` prose and the live server route, identically to the starter's manifest and server; no pod replay viewer is declared. |
| 4 Both name spaces | PASS | Aliases from the seed (`tableNames`, `sim.nim:146-157`); prompts, welcome (`server.nim:397-405`), player frames and final frame (`server.nim:172-185`) carry aliases only; `resultsJson` alone carries policy names (`sim.nim:534`). Replay carries both (`server.nim:139-140`); `makeNameMap` (`renderer.js:907-931`) swaps policy names in wherever rendered, keeping `Baseline`-labelled fillers on aliases. |
| 5 Degrade-never-hang | PASS | Connect wait ≤ `playerConnectTimeoutSeconds` (180 s, 200 ms poll, `server.nim:211-217`); LLM batch ≤ 30 s ×2 attempts (`llm.nim:677,689`; bound verified in curly source, above); credential fetch ≤ 60 s (bitworld source, above); pacing ≤ 20 s total (`sim.nim:171`); pre-shift deadline at 60 % (`server.nim:255-262` → `endEarly`); shutdown 2×500 ms + `quit(0)`; viewer fetch ≤ 20 s (`static_replay.js:14`). The loop cannot spin without progress: `decideAll` never raises and returns a decision per seat; every decision or its `except`-path fallback empties `pendingSeats`. Budget: fitted `shifts = (720−180−20)/60 = 8`; worst case 180+480+20 = 680 s < 720 s. |
| 6 num_agents | PASS | `num_agents: 5` in `variants[0].game_config` (:349), `variants[1].game_config` (:378), `certification.game_config` (:405), and `config_schema` min=max=5 (:70-75); `certification.players` ×5 (:411-427), `certification.game_config.players` ×5. `docker_smoke.sh:106-151` enforces all four invariants before any container starts, `SMOKE_SEATS` default 5 (:54) as the independent cross-check. Grepped the full head-run log: **zero `SEAT-COUNT FAIL`**; `game=firm seats=5`, `smoke OK: seats=5 … reason=complete`. |
| 7 Scripted baseline | PASS | `test_bot.nim:61-87`: 4 seeds × 3 tables, `reason == "complete"`, every run/maint/line/payroll/split/say bound-checked per decision (`apply`, :20-45) and per event, under 2000 ms. Tuning: `tests/test_tuning.nim` + `docs/baseline-sweep.md` citing green run 32682573460 (see Refuted §F1). |
| 8 LLM reply handling | PASS | `extractJsonObject` (`llm.nim:489-503`) takes first `{` to last `}`; `for attempt in 0 .. 1` (`llm.nim:677`) retries once with the hint string on parse, transport or legality failure (probe-apply at :698-704); fallback `scriptedAction(…, skSteady)` (:711-714) with the stdout line, and recorded in the replay via `decision.scripted` → event `scripted` (see Refuted §F2). |
| 9 Rune-safe truncation | PASS | `trimText` (`sim.nim:99-105`, `runeSubStr`) on directive/report/notes at the sim boundary; `cleanText` (`llm.nim:563-569`) on parser output; prompt cap 4000 runes (`server.nim:447-448`); error heads runeSubStr (F6 fix). Tests at the caps with multi-byte input asserting `validateUtf8() == -1`: `test_sim.nim:302-331`, `test_bot.nim:278-313`. CI enforces the consequence: strict-UTF-8 replay parse in docker_smoke (`:285-292`), passed at head. |
| 10 Manifest docs/protocols | PASS | `game.docs.readme = {"type":"text","value":…}` (:234-237); `pages` = 2 × `{id,title,content:{type,value}}` (`rules.md`, `scoring.md`, :238-255); `game.protocols.player` and `.global` both present (:223-232). |
| 11 Viewer legible at 360 px | PASS | `.plate-name { … min-width: 3.2em; flex: 1 1 auto; }` — starter chrome, byte-identical prefix (`chrome.css:280-292`); `.plate-label { display: none }` at 640 px (:460-464, starter) and again at 560 px in the appended block (:553); 2-column scorebug with spanning manager plate at 480 px (:556-559). |
| 12 Release order and scaffold | PASS | `coworld-release.yml`: Build manifest (:153) → Certify (:167) → Upload policies (:206) → Upload coworld (:304) → Secret put (:342). All three workflows present; `docker_smoke.sh` mode 100755; `policies.json` = 4 policies, two `PLAYER_PROMPT` champions (`firm-boss`, `firm-hand`) + two scripted fillers, champion #2 `firm-hand` carries `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"` (:15). Placeholder gate run by me: `grep -n '<slug>\|<IMAGE>\|<SEATS>'` over the five files → no matches → gate exits 0. |
| 13 Viewer executes | PASS | `wasm-viewer` `needs: docker-smoke` (`ci.yml:212`); no `continue-on-error` anywhere in the workflows; step "Load the bundle in a real browser" ran and passed at head — run **32682767057**, emitting `{"loaded":true,"ms":288,"clock":"SHIFT 0 / 4 · WAITING ON 5",…}` and `scrub readouts: 0%="SHIFT 0 / 4 · WAITING ON 5" 50%="SHIFT 2 / 4 · WAITING ON 5" 100%="FINAL · PROFIT $1,080.40"` — three distinct readouts, against the replay docker-smoke produced in the same run (artifact sha256 `de117f26…` matches upload and download). Link flags and bootstrap from the same starter: `MODULARIZE=1` + `EXPORT_NAME=FirmReplayModule` (`config.nims:38-39`) matched by the `FirmReplayModule()` factory call (`static_replay.js:139`); no `onRuntimeInitialized` in the tree; exported `_fm_*` symbols match `firm_replay.nim`'s exportc names and the JS call sites. `data-replay-loaded` set after the first synchronous `renderer.draw` (`renderer.js:1678-1682`); `data-replay-error` set by `fail()` (`static_replay.js:56`) and cleared on retry (:107,:135). |
| 14 Chrome provenance | PASS | Bullwhip ships no `chrome_common.js`; the roles are `chrome.css`/`replay.html`. `chrome.css`: starter's 467 lines **byte-identical** (`cmp` of the prefix), one appended `/* Firm … */` block (468-559). `replay.html` / `replay-viewer/index.html`: full `diff` against the starter shows only the title/wordmark/clock text, one appended `#demandbar` under a banner comment, `BullwhipRenderer`→`FirmRenderer`, one `demandbar:` option and one appended `<script>` block — every starter id and element survives, nothing removed, 101 vs 74 lines (no rewrite). Transport: (a) `relayout()` measures `#transport.offsetHeight` into `--band` + sets `--hudscale` on `document.documentElement` and calls `fit()` (`replay.html:87-98`, `index.html:65-76`); (b) no `position: fixed` in chrome.css; overlays live inside `#board-wrap`, `#loading { bottom: var(--band) }` appended (:535); (c) `#endscreen` absolute `inset: 0` inside `#board-wrap`, shown via `classList.toggle("show")` matching `#endscreen.show` (:383), and **every** seek path routes through `setIndex` whose last act is `updateEndscreen(…, index >= events.length …)` (`renderer.js:1632-1651`); neither the starter nor firm binds keyboard transport, so no unhandled seek exists; (d) `markBeat` creates `<button type="button">` with `title`+`aria-label` and an onclick seek (`renderer.js:1492-1506`); markers emitted for exactly `memo`/`work`/`settle`/`end` (:1558-1567) and the appended CSS defines a rule for each of those four (:538-541). No `#viewpanel` in starter or fork — nothing to remove. |
| Simultaneous batch | PASS | One `RequestBatch` over all open seats, one `client.curl.makeRequests(batch, timeout)` per attempt (`llm.nim:680-689`); no per-seat request loop; server passes all five seats in one call on a snapshot outside the lock (`server.nim:266-278`). Bounded and parallel per the curly source (maxInFlight 16 ≥ 5). |

## Fixer report audit

Not read, per the brief ("Do NOT read runs/2026-08-23-firm/reviews/r1-fixes.md"). Every fix was verified from the code at 62dcd64 and from the head CI run directly; the dispositions above are my own.

| finding | fixer said | I verified | agrees |
|---|---|---|---|
| F1 (blocking) | (not read) | fixed — harness + record + cited green run | n/a |
| F2 | (not read) | fixed — `decision.scripted` ORed into the event flag, tested | n/a |
| F4 | (not read) | fixed — residue sprites deleted; the kept one is referenced and documented | n/a |
| F5 | (not read) | fixed — `obeyedNow()` post-hoc, tested | n/a |
| F6 | (not read) | fixed — five `runeSubStr` cuts, tested | n/a |
| F7 | (not read) | fixed — notes trimmed at the sim boundary, tested past the cap | n/a |
| F12 | (not read) | fixed — per-seat provenance in the frame, survives `replayMatch`, tested | n/a |

## Non-blocking observations

- **Stale baseline description in the manifest.** `coworld_manifest_template.json:283` (`firm-steady` player description) still says the baseline "nurses a machine under condition 40 at run 4 / maint 6" — the shipped nurse is now run 0 / maint 8 below 70 (`llm.nim:92-96`, the F1 sweep's disclosed deviation). A player fielding against the published description will model the filler wrong. Not tied to a checklist item (item 10 is structural); worth a one-line fix before phase 40 uploads.
- **The credential fetch is outside the budget arithmetic.** `readCogameUri` at `newLlmClient` is bounded (60 s, verified from bitworld source) but the design's 680 s worst case does not include it; a fully adversarial stack (180 s connect + 60 s key fetch + all batches timing out) could push the last shift's settle past the 720 s play deadline by up to one shift budget, since the deadline check is pre-batch by design. The episode still settles and writes artifacts ~480 s before the platform kill, so this is a tail-risk note, not a hang.
- The reviewer's F3 (disclosed test-vs-design deviation), F8 (no rate floor vs the Bedrock 30 req/min cap), F9 (player exit codes unchecked — template state), F10 (no `--soak` — template state), F11 (no schema maximum on `player_connect_timeout_seconds`) remain true at head and remain advisory; none names a checklist item.
- The only end-to-end episode CI executes is the no-credential scripted path (docker-smoke log: "no ANTHROPIC_API_KEY…"). The 680 s LLM figure is arithmetic from verified bounds, not a measured hosted episode — that measurement is phase-60 evidence by construction, not a tree-verifiable checklist item.

BLOCKING: 0
