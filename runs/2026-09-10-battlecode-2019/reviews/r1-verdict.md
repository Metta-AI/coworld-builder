blocking: 0

# r1 verdict — 2026-09-10-battlecode-2019
Head: `16b424ffcf9fbeeaf90c109b7fd04dfb52753cdc`   Checklist: `prompts/30-review-loop.md` §ACCEPTANCE CHECKLIST (items 1–15 + simultaneous-decision batch rule)   Independent read written before reading fixes: **yes** (and before reading the review)

**Judged sha:** `16b424ffcf9fbeeaf90c109b7fd04dfb52753cdc` — the newest commit on `main` with a **concluded** `ci.yml` run at the time I started: run **`34463989522`**, conclusion **`success`**, 12/12 jobs `success` (`test`, `docker-smoke`, `wasm-viewer`, `parity-oracle`, `parity-oracle-bc16/19/20/21/22/23/24/25`), verified from `gh api …/runs/34463989522` by `headSha`. `main`'s tip `f5de5bd` (the authorised r1-F6b doc correction, PR #16) had run `34472691904` still `in_progress` at start, so it is not the judged tree; its content is noted below. The whole bc19 change is `git diff 6e89d0fe57cb..16b424f` (+35 842 / −89, 123 files).

Reading order followed: checklist → design note (full, once) → my own read of the tree/diff/CI at the head (notes written) → `r1-review.md` → `r1-fixes.md`.

## Standing blocking findings

**None.** Every checklist item was verifiable from the tree or from cited CI evidence at the judged head, and none is falsified. No item had to be reported unverifiable.

## Refutation pass over the reviewer's 24 findings

I attempted to refute each finding by reproducing it from the code and the CI logs at the judged head (the review was written at `d2f5d3d`; four of its findings were since fixed by PR #15, which is part of the judged tree).

| # | reviewer's claim | my verdict | evidence at `16b424f` |
|---|---|---|---|
| F1 | CI green, 12/12 jobs, only `continue-on-error` is pre-existing bc26 Gradle | **UPHELD** | Run `34463989522` `success`, 12/12 (jobs API, ids 102828140747…102829594770). `ci.yml:906` `continue-on-error: true` is bc20's engine-from-source *reporting* step, pre-existing; the browser-load and smoke steps carry none. |
| F2 | no test loosened/skipped/deleted | **UPHELD** | `git log -p 6e89d0fe57cb..16b424f -- 'tests/*.nim'`: 36 deleted lines total, every one an eight→nine-year count/arity extension (`year.enum` 8→9 ids, variants 8→9, pages 10→11, policies 32→36, guard strings eight-way→nine-way, `--statrail` list) or a comment correction (survival header re-measurement, floors column unchanged; r1-F17 comment). No assertion deleted, no tolerance widened, no `skip`/`xfail`/`when false` added, no test file removed. |
| F3 | GV12 fixture regen changed exactly two `game_version` leaves per file | **UPHELD** | `replay-{bc16,bc22,bc23}.json` each `2 ++--` in the diff stat; `sim_types.nim` `GameVersion = "GV12"`, `ReplayCompatibleGameVersions` extended `GV04…GV11, GameVersion` — never reset. Authorised cross-year edit 1 of 2. |
| F4 | fixture emits 11 of 12 beat kinds; all 12 have scoped CSS; 14(d) met | **UPHELD** | Twelve `html[data-year="bc19"] .beat-marker.<kind>` rules at `client/replay_broadcast.html:4158-4169`. `tests/test_bc19_beats.nim` parses every declared selector out of the page source and asserts declared == the twelve-kind vocabulary in both directions, plus a rule for every kind the fixture actually emitted, plus `famine`'s label off two synthetic events (non-empty, ≤120 runes, names its resource). The false page comment the reviewer flagged was fixed at `1124d4d` — `:4144-4152` now says "TWELVE kinds DECLARED here, ELEVEN OF THEM EMITTED". |
| F5 | `fuel_reserve`'s reading is in the cog-facing knob table | **UPHELD** | `docs/RULES-BC19.md` knob table row: "the fuel floor below which the order stops making WAR, not … MONEY", the measured literal-reading deadlock (3 280 karbonite banked, four military units, zero damage), the exact gate scope. Matches `chassis/econ.nim`'s `canSpend(essential=…)`/`fuelGate()`. |
| F6 | RULES-BC19.md:129 said the test asserts raw "attacks down" — false of the committed test | **UPHELD, and FIXED at head** | The row (commit `53d4a677`) now states the truth: raw attacks go UP (1 296→2 331, `MilitaryFuelFloor`: 108 vs 462 military built), and the test asserts **attacks per military unit built** down (−58 %, threshold 20 %) + rounds-at-zero-fuel down (−49 %, threshold 40 %) — exactly what `tests/test_bc19_knobs.nim:351-362` and `:163-164` assert, substitution recorded in the header at `:52-59`. Per coordinator ruling 1 the substitution itself is accepted and not re-litigated. Residual: `:245` (§Divergences item 14) still ends "rounds at zero fuel down, attacks down" at this head — the authorised concurrent r1-F6b (`96d0d6b`, merged in `f5de5bd`, CI in progress at my start) corrects it. No checklist item names doc prose; non-blocking. |
| F7 | `530ccbd`'s `sed 's/^::error:://'` masks nothing | **UPHELD** | Traced independently. `tools/gen_maps_bc19.mjs:248` prints refusals as plain `refused N: <why>` (stdout, no prefix — untouchable by the sed); the `::error::` lines stripped are the main loop's *expected* complaint about a degenerate seed (`:208`) and the summary (`:254`). `--assert-degenerate` (`:240-251`) iterates its own hard-coded 13-seed list independently of `--seeds`. The `|| true` predates the commit; the gate is the workflow's own 13-way `grep -q "^refused ${seed}: " || { …; exit 1; }` loop **outside the pipe**, which still reddens on any missing refusal, on an empty file, or on a node crash. The two positive Tier B checks are separate un-piped invocations under `set -euo pipefail`. The step cannot go quietly green. |
| F8 | parity 54/54 bit-exact, ledger `[]` | **UPHELD** | `tools/ci/parity_ledger_bc19.json` is `[]`; `parity-oracle-bc19` `success` at the judged head (job `102828141062`); `docs/PARITY.md` §Status documents 54/54 `BIT-EXACT`, 13 degenerate seeds refused by name, at the run for `d2f5d3d` (`34454858348`/job `102798775171`) — re-derived by the fixer from the log, consistent with the green job at my head. |
| F9 | three per-pair liveness waivers, none relaxes the comparison; the note's per-pair round-900/8-robots asserts are absent from `ci.yml` | **UPHELD** | `ci.yml:3525-3540`: `--allow-no-build` for `bc19scenariotrade`, `--allow-inert` for `examplefuncsplayer19` on `seed-0017` only (measured reason in the comment and in PARITY.md), `--expect-freeze --allow-inert` only on the separate non-compared `bc19slowbot`. `parity_tiers_bc19.py` runs unconditionally on every pair. `grep -c peak_alive ci.yml` = 0 — the note's §Tests item 4 per-pair asserts are indeed not literal; the anti-vacuity weight is carried by the Tier A/A′ oracle-trace assertions. Design-note divergence, no checklist item; advisory. |
| F10 | one parallel batch, retry once, fallback recorded | **UPHELD** | `decide.nim:1443-1460`: one `RequestBatch`, one `post` per open seat, one `makeRequests` per attempt; attempt loop bounded at 2 (`attempt1Ms` 20 000 / `retryMs` 12 000) inside `doctrineBudgetMs` 45 000; `doctrine_retry` with typed cause; fallback seats baseline sheet, sets `result.fallback[slot]`, emits `doctrine_fallback`, logs `falling back`; no-credentials path (`:1412-1420`) recorded as fallback. |
| F11 | every wait bounded; 305 s ≤ 720 s | **UPHELD** | bc19 `game_config` shipped exactly as the note's arithmetic requires (verified in the manifest); round loop abandon check every 32 rounds; `isOver` unconditional at `round >= 1000`; `llm.nim`/`server.nim`/`seats.nim` unchanged (zero diff) — the bounded-wait machinery is the shipped eight-year machinery. |
| F12 | `drawId`'s `while true` guarded; non-termination unreachable | **UPHELD, and I settle the residual** | `drawId` (`world.nim:358-368`) is called only by `createItem` (`:375`); `createItem`'s call sites are the initial castle roster (`world.nim:497`, ≤6 draws on an empty spent set) and `enactBuild` (`actions.nim:385`), which runs only after validation passed the V4 guard (`actions.nim:217-220`, refuses at `idsSpent.len >= 4095`). So `drawId` is always entered with ≥1 free id of the 4 095. The reviewer's could-not-determine #3 (the 4 094-spent edge) is settled in the code's favour: `id = 1 + int(4095.0 * random())` maps ~2²⁰ distinct 32-bit MT outputs to each id, MT19937's output stream visits every 32-bit value within its period, so the rejection loop provably terminates whenever a free id exists — and the guard guarantees one. Expected cost at the worst edge is ~4 095 cheap draws; measured reality is 43 ids in the smoke. Not an unbounded loop in the item-5 sense. |
| F13 | replay re-derivation frame-by-frame; viewer runs the same `Deriver` | **UPHELD** | `tests/test_bc19_replay.nim` asserts the committed fixture re-derives to its **last** round with `mismatchRound == -1` and `frames >= rounds`, forbids per-tick bulk keys, and covers every bc19 end reason; `replay-viewer/bc_replay.nim` runs the same `Deriver` and exposes `bc_mismatch_round`. Verified in the judged run's own log: `wasm-viewer` printed `{"loaded":true,"game_version":"GV12","frames":200,"mismatch_round":-1}` for **both** `dist/smoke/replay-bc19.json` and `tests/fixtures/replay-bc19.json`. `test_determinism.nim`'s bc19 block proves the MT-state fold is load-bearing. |
| F14 | manifest: bundle, num_agents, docs, protocols, placeholder gate | **UPHELD** | Read out of the file by script myself: `replay_viewer: {"bundle":"static-replay-viewer"}`; `protocols` carries both `player` and `global`; `docs.readme` `{type,value}`, 11 pages all `{id,title,content:{type,value}}`; `num_agents: 2` in all nine variants' `game_config` and in `certification.game_config`, never at variant top level; `player[]` = `[awu, scaffold]` == `certification.players`; `end_reason` enum gained exactly `castles_destroyed`/`more_castles`/`more_unit_health`, reuses `coin_flip`/`abandoned`, omits the two unreachable ones; `year.enum` appended. Placeholder grep over the five named files: zero hits. Coworld version 0.8.2 — correct per ruling 3. |
| F15 | docker_smoke four invariants; no SEAT-COUNT FAIL in the log | **UPHELD** | `docker_smoke.sh` unchanged from base, mode 755, `SEAT-COUNT FAIL:` at `:148,158,166,173,183,201,218,227`. I grepped the judged run's own docker-smoke log (job `102828141145`): **0** occurrences. bc19 episode line: `game=battlecode seats=2 config={…"num_agents": 2, "year": "bc19", "pool": "small", "seed": 13, "gamesPerMatch": 1, "maxRounds": 600 …}`. |
| F16 | baseline plays full legal episodes; grid-tuned | **UPHELD** | Judged run's log: bc19 `episode end reason: complete`, `episode substance OK: ['damage_dealt','moves','units_built']`, across-the-pair `units=39 karbonite=3252 fuel=18590 deposited=3234 pilgrims=11 attacks=20 damage=200 ids=43 queue=38` — all above the committed floors. `test_bc19_baselines.nim`: `refused_actions == 0` for saber both seats (the legality gate — every emitted order legal at emission), weak floor's exemption asserted as `> 0`, saber beats the weak floor on every pair, no CHURCH attack ever; survival gate runs inverted under `-d:bc19BrokenChassis` as a subprocess and must come back red. Tuning evidence in-tree: measured healthy-mirror vs broken-control ranges in `test_bc19_survival.nim:14-31`'s header, and the 12-knob paired-sweep harness with every threshold carrying its measurement. |
| F17 | truncation test is 3-byte multi-byte, not astral; comment was wrong | **UPHELD, and FIXED at head** | `test_bc19_sheet.nim:150-172`: `"\u1F3F0"` = U+1F3F + `'0'` (4 bytes, 2 runes per repetition), caps asserted in runes, `validateUtf8 < 0` on notes, motto, and the 16 384-byte `truncateBytes` cut. Comment corrected at `a7e3908`; literals and all five checks unchanged (check count 215 identical across runs). Item 9 requires multi-byte at the cap + valid UTF-8 — satisfied. |
| F18 | chrome provenance: chrome files untouched; page = starter + appended block; transport rules; viewpanel kept | **UPHELD** | `git diff 6e89d0fe57cb..16b424f -- client/`: `chrome_common.js`/`broadcast_core.js` **zero diff** (ruling 5's binding test); `replay_broadcast.html` +726/−5, the 5 deletions being the statrail-list and two eight→nine-way guard widenings; banner `BC19 additions to the inherited cogame-battlecode chrome` at `:3936` matching the eight sibling banners. Transport: `--band` et al. on `:root` via inherited `relayout()`; `#bc19-units/econ` ride `calc(var(--band,0px)+…)` (`:4066-4067`); `#endcard { bottom: var(--band,0px) }` (`:1847-1858`) and `seek()` calls `dismissEndcard()` first (`:7375-7378`); `buildBc19BeatButtons`/`applyBc19BeatSpoilers` (`:6519,6542`), never `markBeat`. `#viewpanel` KEPT — correct: design note L1697-1708 says bc19 boards render 512–1024 px, all larger than the 360 px frame (coordinator ruling 4 confirmed against the note, not assumed). |
| F19 | viewer executes; needs docker-smoke; markers; bootstrap agreement; lobby clause | **UPHELD** | `wasm-viewer` `needs: docker-smoke` (`ci.yml:4682`); browser-load step (`:4763`) ran on all nine replays in the judged run — bc19: `{"loaded":true,"ms":294,"clock":"0:15 GAME 1 OF 1 — SEED-0043 …",…"feed_lines":7}`; `data-replay-loaded` set on the worker's first-frame `loaded` message (`static_replay.js:180`), `data-replay-error` at `:14-20`. `replay-viewer/` has **zero diff** vs base: `config.nims` has no `MODULARIZE`/`EXPORT_NAME` and the worker bootstraps with `Module.onRuntimeInitialized` + trailing `importScripts` — the matching non-modularized pair from one starter; the lantern deadlock class is excluded. Lobby: this replay format records no pre-game frames — pre-match events carry `ms`, frames are re-derived rounds, every year's frame record sets `"st": 0` (bc19's at `broadcast.nim:2190`) — so playback opens at the game start by construction; a late-gameStart replay cannot exist in this format. No pod path; the bundle's only network call is the S3 replay fetch. |
| F20 | 360 px legibility + worst-case renderer fixture | **UPHELD** | `.plate-name { flex: 1 1 auto; min-width: 3.2em }` at `:2623` (inherited, untouched); bc19 `@media (max-width: 640px)` drops `.lbl` labels, `#bc19-fuel` keeps bars/delta/round counter. Fixture step (`ci.yml:5005-5031`) extracts the page's real `<style>` at run time, serves over HTTP, drives with `--strict-text-bounds`; fixture builds full-cap strings and **fails itself if they are not full-cap** (`renderer_fixture.html:171-173`), measures every element under `#chrome` at 360/720/1280, reports via `data-replay-error`; its astral-plane literals are genuine JS `\u{1F400}`/`\u{1F9C0}` braced escapes (`:167,169`) — I verified, settling the reviewer's could-not-determine #2. The `canvas text: 0 drawn … (--strict-text-bounds)` line is accurate, not a blind spot: `grep -c 'fillText\|strokeText' client/broadcast_core.js` = **0** — this viewer draws no canvas text; the DOM assertions and `--killfeed-overlap` (360/720/1280 at FIT and 2×, on the bc19 replay too) carry the gate. `--strict-text-bounds` correctly dropped on the replay runs (pannable board, viewpanel kept), reason written into the step. |
| F21 | release order, workflows, policies | **UPHELD** | `coworld-release.yml`: build → certify → upload-policies → upload-coworld → (canonical wait) → secret put, in that order; no smoke step in the release workflow; all three workflows present; both scripts mode 755. `policies.json`: 36 entries; bc19's four = 2 `PLAYER_PROMPT` champions + 2 `PLAYER_SCRIPTED` fillers, champion #2 (second `PLAYER_PROMPT`) carrying `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`, image `cogame-battlecode-player:latest`. Verified by script myself. |
| F22 | both name spaces | **UPHELD** | Agent payload carries alias/opponent_alias only; `replay.names[]` drawn by the page at `:7400` (plate sub-line), `:7670` (endcard headline), `:7705` (doctrine panel); `AliasA/AliasB` untouched. |
| F23 | exactly the two authorised cross-year edits; no third | **UPHELD** | My own diff-stat pass: under `src/battlecode/years/` only `bc19/` + additive `dispatch.nim`/`registry.nim`; sibling year modules, maps, atlases untouched; the sibling comparator bugs and `match.nim:480`'s clamp untouched (ruled out of scope); `Dockerfile*` not in the diff. Cross-year edits: the three GV12 fixture regens and `ci.yml` test `timeout-minutes` 150→**180** (`:179`) — the second authorised edit, though the design note said 165 (L2163). The `test` job ran ~90 min at this head (10:03→11:33), so 180 is headroom, not cover for a failing 165; a CI timeout is not a checklist quantity. Two shared files the reviewer did not enumerate are also touched — `src/battlecode/decide.nim` (+150: the bc19 preamble/payload arm, purely additive; I read the diff) and `tools/ci/renderer_fixture.html` (+157: the bc19 row the note's §Tests requires) — both in-scope MOD edits, not cross-year. |
| F24 | PARITY.md §Status cited the branch run | **UPHELD, and FIXED at head** | `docs/PARITY.md:2095-2100` now cites `34454858348` / job `102798775171` on `main` at `d2f5d3d707` — the run at the shipped sha, re-derived from the log; the empty-ledger sentence cites the same run. |

**Dismissed: 0. Recategorised: 0.** The review's tracing reproduced from the code at every point I tested; its four documentation findings were real and are fixed at the judged head. Of its four "could not determine" items: #2 settled (fixture astral literals are genuine 4-byte runes), #3 settled (drawId provably terminates — see F12), #4 settled as far as it matters (job ran ~90 min under the 180 cap; not a checklist quantity), #1 (famine in a real shipped episode) addressed below.

### The famine question (coordinator's open item, checklist 14(d))

Settled: **14(d) is met.** The item requires CSS for every kind the page emits — a kind with no rule is an invisible marker. All twelve kinds carry scoped rules (`:4158-4169`) and `test_bc19_beats.nim` asserts rule-per-emitted-kind, exactly-twelve-declared, and no rule outside the vocabulary (the r1-F26 defect gated in both directions). No emitted kind can lack a rule; the converse (a rule whose kind cannot fire) is not what 14(d) forbids. On reachability itself: `famine` is emitted only when an affordability refusal happens with the store at 0 (`actions.nim:143,179,210-211,254,272` → `noteFamine`, once per resource per side). `saber` pre-checks affordability at every knob setting (`refused_actions == 0` asserted across the gate games), and the weak floor's karbonite bottoms out at 10 (100 − 6×15), never 0, while its fuel income exceeds its ~6 crusaders' drain — so in practice no shipped chassis fires it; an LLM-doctrined saber cannot either, since the chassis, not the sheet, emits actions. The tree says this honestly (page comment `:4144-4152`, fixed by r1-F4), the label is exercised by two synthetic events, and the marker cannot be invisible if it ever does fire. Non-blocking; recorded as a documented design-note divergence (note L1555 claimed the fixture emits all twelve; it emits eleven).

## Checklist pass (independent)

| item | status | evidence (fetched myself at `16b424f` unless noted) |
|---|---|---|
| 1 CI green, no test loosened | **met** | Run `34463989522` `success`, 12/12 jobs, headSha match. `git log -p 6e89d0fe57cb..16b424f -- tests/`: 36 deleted `.nim` lines, all eight→nine extensions or comment corrections; fixture regens = 2 `game_version` lines each; no skip/xfail/deleted assertion; `ci.yml` deletions are the branch list, timeout 150→180 (authorised), and eight→nine step widenings — nothing disabled. |
| 2 replay re-derivation | **met** | `test_bc19_replay.nim` (fixture re-derives to last round, `mismatchRound == -1`, no per-tick bulk keys, every end reason covered); same `Deriver` compiled into `bc_replay.nim` exposing `bc_mismatch_round`; judged run's wasm smoke printed `mismatch_round:-1` for both bc19 replays. |
| 3 static viewer | **met** | Manifest `replay_viewer.bundle = static-replay-viewer`; `tools/build_replay_viewer.sh` present, mode 755, exec-bit asserted then invoked by path in `wasm-viewer`; bundle's only network call is the worker's replay fetch; the sole `/client/replay` hit repo-wide is the release workflow's refusal message. |
| 4 both name spaces | **met** | Alias-only agent payload (`decide.nim`); `names[]` drawn at `replay_broadcast.html:7400,7670,7705`. |
| 5 degrade-never-hang | **met** | All waits bounded (`decide.nim` batch deadlines; abandon checks in `rules.nim`; `connectTimeoutMs`); worst case 305 s of 720 s with every term a shipped config value; the only `while true` in the year module is `drawId`, guarded at every call site (see F12 — provably terminating). Smoke episodes complete in ~20 s. |
| 6 num_agents | **met** | Present in all nine variants' `game_config` + `certification.game_config` (=2), never variant-top-level; `docker_smoke.sh` enforces the four invariants + `SMOKE_SEATS` cross-check + override refusal; **0** `SEAT-COUNT FAIL` in the judged run's docker-smoke log (grepped, job `102828141145`); episode lines print `seats=2`. |
| 7 scripted baseline | **met** | Judged run: bc19 smoke `reason=complete`, per-seat substance OK, across-the-pair floors cleared (`units=39 … damage=200 ids=43 queue=38`); `test_bc19_baselines.nim` legality gate (`refused_actions == 0` for saber, weak-floor exemption asserted `> 0`, 6/6 wins, no CHURCH attack); inverted broken-chassis control must fail; measured tuning ranges recorded in the survival/knob headers with the sweep harness committed. |
| 8 LLM reply handling | **met** | `decide.nim:1440-1520`: tolerant parse via unchanged `llm.nim` extractor + envelope resolver; retry exactly once (`attempt < 2`); fallback to scripted sheet with `result.fallback[slot]`, `doctrine_fallback` event, `falling back` log line; throttle fast-fail. |
| 9 rune-safe truncation | **met** | `test_bc19_sheet.nim:150-172`: multi-byte (3-byte rune) input at the caps, `validateUtf8 < 0` on notes/motto/16 384-byte reply cut; all recorded strings routed through `truncateRunes`/`sanitizeLine` caps in `sim_types.nim`/`decide.nim`. |
| 10 manifest validates | **met** | `game.docs` = `readme {type,value}` + 11 `pages` of `{id,title,content:{type,value}}`; `protocols` carries **both** `player` and `global`. The `type` is `"uri"` rather than the checklist's illustrative `"text"` — this is the repo's shipped shape across eight certified releases, asserted by `test_manifest.nim` (which also checks the workflow validates via `coworld.bundle._load_template_manifest`/`validate_upload_manifest`); the structural requirement the item encodes is met. |
| 11 legible at 360 px | **met** | `.plate-name { flex: 1 1 auto; min-width: 3.2em }` (`:2623`); labels hidden under the 640 px media query; `--killfeed-overlap` exercised at 360/720/1280 at FIT and 2× on the bc19 replay in the judged run. |
| 12 release order & scaffold | **met** | build → certify → upload-policies → upload-coworld → secret put; three workflows present; `docker_smoke.sh` executable; 36 policies with bc19's 2 `PLAYER_PROMPT` champions + 2 scripted fillers, champion #2 carrying `ply_bac48eb1-662e-44f8-973d-f3e016dccf5d`; the three-name placeholder grep over the five files: zero hits (gate exits 0). |
| 13 viewer executes | **met** | `wasm-viewer` green at the judged sha **including** `Load the bundle in a real browser (ALL NINE years' replays)` (no `continue-on-error`), `needs: docker-smoke`; bc19 replay `loaded:true` in 294 ms with advancing clock and endcard/scrub/overlay asserts in-step; both `data-replay-*` markers set from the shell's own paths; `config.nims` (no MODULARIZE) + worker (`onRuntimeInitialized`) are the same starter's agreeing pair, both zero-diff; no recorded lobby exists in this format (`"st": 0` in every year's frame record, frames = re-derived rounds), so playback opens at game start by construction. |
| 14 chrome provenance | **met** | `chrome_common.js`/`broadcast_core.js` zero diff vs `6e89d0fe57cb` (the binding test for this MOD per the coordinator's ruling; the `/workspace/starters/coworld-ctf` copy differs by sha — starter-side drift, not this repo's); page = existing page + bc19 block under the banner, 5 deleted lines all named widenings; transport rules (a)–(d) each verified in the page (`:root` variables, `calc(var(--band))` anchoring, endcard band + seek dismissal, labelled `<button>` beats with CSS for all twelve kinds); `#viewpanel` kept — correct, the note (L1697-1708) says every bc19 board outsizes the 360 px frame. |
| 15 drawn strings fit | **met** | Fixture step `Render the full-cap doctrine-text fixture` (`ci.yml:5005-5031`) with `--strict-text-bounds`; its `canvas_text` line at the judged run: `canvas text: 0 drawn, 0 never inside the canvas (0 draws crossed an edge), 0 ellipsized (--strict-text-bounds)` — accurate because this viewer draws zero canvas text (`fillText|strokeText` count in `broadcast_core.js` = 0); the gate is the fixture's own DOM verdict (full-cap strings self-asserted at `renderer_fixture.html:171-173`, every `#chrome` element measured at 360/720/1280, first failure → `data-replay-error`, which the harness requires absent) plus `--killfeed-overlap` on the real replays; bc19 row present; `--strict-text-bounds` correctly dropped on the pannable replay runs with the reason in-step. |
| batch rule | **met** | One `RequestBatch`, one `makeRequests` per attempt, both seats appended before it (`decide.nim:1443-1460`); one decision turn per episode; no sequential per-seat path exists. |

**Unverifiable items: none.**

## Fixer report audit

| finding | fixer said | I verified | agrees |
|---|---|---|---|
| F6 | fixed at `53d4a677`, `docs/RULES-BC19.md:129` row now states the substituted assertion | Row read at head: raw-attacks-up explanation + per-military-unit assertion with the real thresholds/measurements; `test_bc19_knobs.nim` untouched | **yes** |
| F4 | fixed at `1124d4d`, comment says eleven emitted / twelve declared, no selector text added | Comment read at head (`:4144-4152`); twelve CSS rules byte-identical; beats/viewer check counts unchanged across runs | **yes** |
| F24 | fixed at `1aef1a4`, §Status cites `34454858348`/`102798775171` at `d2f5d3d707` | §Status read at head; both citation sites updated; substance re-derived from the log | **yes** |
| F17 | fixed at `a7e3908`, comment names the real 3-byte rune, literals/checks untouched | Block read at head; literals and all five checks unchanged | **yes** |
| scope | comment/doc-only diff, no behavioural change | `git diff --stat d2f5d3d7..16b424f` = 4 files (+25/−15), exactly the four cited sites; `src/` empty in the diff | **yes** |
| NOTED 1 | `:245` residual left for F6b | Confirmed present at head; F6b merged as `96d0d6b` in `f5de5bd`, CI in progress — authorised concurrent fix | **yes** |
| NOTED 3 | fixture astral literals genuine | Verified `\u{1F400}`/`\u{1F9C0}` at `renderer_fixture.html:167,169` | **yes** |

## Non-blocking observations

1. `docs/RULES-BC19.md:245` (§Divergences item 14) still repeats the pre-F6 sentence at the judged head; the authorised r1-F6b correction is merged on `main` tip (`f5de5bd`) with CI pending. One-line doc residue, no checklist item.
2. The committed fixture emits eleven of the note's twelve beat kinds (L1555 claimed twelve); documented honestly in the page comment, the test, and `ci.yml`. `famine` is practically unreachable for both shipped chassis (analysis above).
3. `test` job `timeout-minutes` is 180 against the note's 165 (authorised); the job ran ~90 min at this head, so the value is headroom.
4. The design note's §Tests item 4 per-pair `round ≥ 900` / `peak_alive ≥ 8` asserts are not in `ci.yml`; equivalent anti-vacuity is asserted off the oracle traces in Tiers A/A′.
5. `pages[].content.type` / `docs.readme.type` are `"uri"` where the checklist's template text reads `"text"` — the repo's certified shipped shape, structurally identical.

No blocking findings; no `- [<category>] <file:line>` lines to list.

BLOCKING: 0
