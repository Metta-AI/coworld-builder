# r1 review — crafter (Metta-AI/cogame-crafter)

Range: `97e907e..71bf90d` (whole history; the repo was created this run)
Repo read at: `/tmp/cogame-crafter`, sha `71bf90d1af107a6c5162a4d7d53f683c9c55cab1`
Starter diffed against: `/workspace/starters/coworld-ctf` (read-only mount)
Design note: `/workspace/coworld-builder/runs/2026-08-28-crafter/design.md` (2209 lines, read in full)
Checklist: `prompts/30-review-loop.md` §ACCEPTANCE CHECKLIST (items 1–15 + the parallel-batch rule)
Files opened: 46 (all of `src/`, all of `tests/*.nim`, `client/{chrome_common.js,broadcast_core.js,replay_broadcast.html,crafter_block.html}`, `replay-viewer/*`, `tools/ci/*`, `tools/*.py`, `.github/workflows/*`, `coworld_manifest_template.json`, `docs/PORTING-CRAFTER.md`, `Dockerfile`, `compose.yaml`)
Evidence executed in-sandbox: `sha256sum`, `diff` against the starter, `python3 tools/build_broadcast_page.py --starter /workspace/starters/coworld-ctf --check`, `python3 tools/replay_summary.py tests/replays/forager-seed42.replay`, a brace-matching scan of the committed replay's `directive` records, `gh run view` on CI run 33228279820 (`test`, `docker-smoke`, `wasm-viewer` job logs).

Findings are numbered F1… sequentially across sections, as the brief asks; each carries the checklist category it touches.

---

## Blocking

### F1 — a `skip()` was added and an assertion was deleted in `tests/` during this run *(category: other — checklist item 1, "no test loosened")*

- Where: `tests/test_crafter_manifest.nim:202-215` (shipped); `tests/test_crafter_manifest.nim:135-151` at commit `d538866` (prior).
- Observed, shipped file:
  ```nim
  test "the manifest loads under the installed CLI, when there is one":
    let probe = execCmdEx("python3 -c 'import coworld' 2>/dev/null")
    if probe.exitCode != 0:
      skip()
  ```
  `git log -p -- tests/` for this run yields exactly one added `skip()` line, this one. `coworld` is not installed on the `test` runner (`ci.yml` never installs it) and there is no other CI step that runs `validate_upload_manifest`, so the note's §Tests item 38 is not executed anywhere in the tree at this sha.
- Observed, the deleted assertion: the prior committed version of the same test asserted
  `check m["game"]["runnable"].hasKey("source_url")` (`d538866:tests/test_crafter_manifest.nim:150`) and
  `check not m["game"].hasKey("image")` (`:149`) and `check player["type"].getStr() == "player"` (`:156`).
  None of the three survives (`grep -c source_url tests/test_crafter_manifest.nim` → `0`). The first one **would now fail**: `coworld_manifest_template.json`'s `game.runnable` keys are `["type","image","run","env"]` — no `source_url` — while the starter's own `coworld_manifest_paintbot.json` `game.runnable` carries `["type","image","run","source_url","env"]`.
- Checklist item: item 1 — "no test disabled, skipped, or loosened during this run … a deleted assertion … or a `skip`/`t.Skip`/`xfail`/`--skip` added … is a blocking finding", verified from `git -C <repo> log -p -- tests/`.
- Why blocking: taken literally the two hunks are exactly what item 1 names. Concretely, the combination means the manifest's `game.runnable` shape is asserted by nothing that runs: the Nim test skips, and `coworld build`/`validate_upload_manifest` is only reached in `coworld-release.yml`, which has never run on this repo.
- **Mitigating context the judge should weigh, stated because I could not resolve it either way:** the prior file was not a passing test of this repo. At `d538866` `tests/test_crafter_manifest.nim` was a scaffold copied from a different game — it sets `norules.xlandRules = 2` (no such field on this `GameConfig`), asserts `m["player"][0]["id"] == "scout"`, and `tests/test_crafter_viewer.nim` at the same commit listed `viewpanel`/`minimap`/`zoombar` as **removed** ids, which contradicts the note. `gh run list -R Metta-AI/cogame-crafter` shows **no CI run at all before `5a509091`**, so those assertions never went green. If the judge reads item 1 as "no *passing* test was loosened", F1 is dismissed and the only live residue is that item 38 is unverified and `game.runnable.source_url` is absent relative to the starter.

Nothing else in this review falsifies a named checklist item.

---

## Non-blocking

### F2 — 30 of 44 `directive` replay records carry `"view": null` *(correctness)*
- Where: `src/crafter/directives.nim:213-235` (`boundedDirectiveRecord`), `src/crafter/sim_types.nim:69` (`MaxDirectiveRunes* = 4000`).
- Observed: the record is serialised once; if `result.runeLen > MaxDirectiveRunes` the whole `view` payload is replaced with `nil` (JSON `null`) and only then is `say` shortened. A brace-matching scan of the committed `tests/replays/forager-seed42.replay` gives 44 `directive` records, record lengths 424…3990 runes, **30 with `"view": null`** and 14 carrying the observation.
- Note says: §Decisions → observation, "The observation is a JSON object … mirrored (minus `notes`) into the replay's `directive` record, **so the replay explains every decision**", and §Record vocabulary lists `view` as a `directive` field. `MaxDirectiveRunes` is a fork-added constant that appears nowhere in the note.

### F3 — `achievementTick[]` is on the absolute tick clock; `survivalTicks`/`finalTick` are run-relative *(correctness)*
- Where: `src/crafter/sim_state.nim:328-331` (`recordUnlock` stamps `sim.tickCount`) vs `sim_state.nim:175-180` (`survivalTicks` returns `gameOverTick - gameStartTick`) and `:939` (`"finalTick": sim.survivalTicks()`). Every other clocked rule reads `sim.runTick()` (`:151-160`).
- Observed in the shipped fixture: `results.finalTick == 949`, `achievementTick == [78,99,337,693,263,100,101,-1,781,…]`, and the first `directive` record is `"tick": 49` — i.e. the achievement ticks are offset by the 48-tick lobby relative to `finalTick`. With a production lobby (`lobbyJoinTimeoutTicks: 2400`, typically ~15 s ≈ 360 ticks) the offset grows.
- Note says: §Server's example has `achievementTick` values inside `finalTick: 812`, and the viewer's checklist/sparkline are described as using "when each lit up". Results identity 3 (`tick >= 0 iff unlocked`) still holds and is asserted (`tests/test_crafter_engine.nim:27-32`); no test pins the clock.

### F4 — the derived-event enum has a 22nd kind, `budget`, that the note's closed enum does not list *(correctness)*
- Where: `src/crafter/sim_state.nim:39` (`evBudget = "budget"`), emitted at `src/crafter/replays.nim:115-117`; `tests/test_crafter_events.nim:8-12` declares 22 kinds including `"budget"` and asserts set equality against the code.
- Note says: §Record and event vocabulary B — "**A closed enum of twenty kinds, plus `end`**" (20 named + `end` = 21), and §Tests item 48 — "the set of kinds `stepEvents` can emit equals exactly the **twenty-one** listed in §Server". `budget` is not among them. Not listed in `docs/PORTING-CRAFTER.md` §"Divergences from this repo's own design note".

### F5 — `fallback.cause` can be `"throttled"`, which is outside the note's closed cause enum *(correctness)*
- Where: `src/crafter/decide.nim:239-240` (`cause = "throttled"` when `error.msg.startsWith("llm throttled")`) and `:264` (`elif engine.client.throttled: "throttled"`), written into the record at `:241` / `:266` through `fallbackRecord` (`decide.nim:69-76`).
- Note says: §Degrade, never hang — `cause ∈ {timeout, parse_error, transport_error, no_credentials, rate_guard, budget_guard, disconnected}`. Conversely `disconnected` is never emitted anywhere in the tree.

### F6 — `forager`'s rules 1 and 4 differ from the note's ladder, and gain a tunable, without being in the divergence list *(correctness)*
- Where: `src/crafter/baselines.nim:194-220` (rule 1), `:244-261` (rule 4), `:32` + `:44` (`restThreshold*: int` / `restThreshold: 5`).
- Observed, rule 1: on a hostile within Chebyshev 2 the code either `do×3` if already facing it, else `move` toward it **and** `do×3`, else backs away by BFS. There is no "if `stone >= 1` and the cell between them is placeable: `move` to face it, `place_stone`" branch.
- Observed, rule 4: fires on `(not daylight and energy <= restThreshold) or energy <= 2` — the note's rule 4 has no energy condition — and the `while` at `:254-259` ends in an unconditional `break`, so **at most one** `place_stone` is emitted per turn, not `min(4, stone)`. It then emits `sleep n = sleepTicks` (8, not 12).
- Note says: §Scripted baselines rules 1 and 4 as quoted; the tunables named are "the thirst/hunger thresholds `3`, the shelter stone budget `4`, the sleep length `12`, the explore step count `3`, and whether the frontier score breaks ties by distance or by `(y, x)`".
- `docs/PORTING-CRAFTER.md` §D documents only the `sleepTicks 12→8` and `exploreSteps 3→2` moves. The rule-shape changes and the new `restThreshold` parameter are explained in code comments (`baselines.nim:246-252`, `:160-162`) but not in the divergence list.

### F7 — the board's cog and creature art are committed image-model renders, not `rig_art.nim` composites of `data/soldier_red.png` *(other)*
- Where: `src/crafter/global.nim:41-59` — `CogArt` loads `data/art/cog_{north,east,south,west}.png` and `CreatureArt` loads `data/art/{cow,zombie,skeleton}.png`; the comment at `:40-45` says "THE BOARD CHARACTER IS A NANO-BANANA RENDER … `scripts/art/source/*.png` is a `gemini-2.5-flash-image` render anchored on the shipped `data/soldier_red.png` master, keyed and split by `scripts/art/split_cog_sheet.py`". `src/crafter/rig_art.nim` does not exist (`find . -name 'rig_art*'` → nothing).
- Note says: §Sim module Kept table lists `src/ctf/rig_art.nim` as **fork**; §Art says "The **cog** is `data/soldier_red.png` composited by `rig_art.nim` into 4 facings × 2 sizes = **8 chips**, with a sleep pose … **Cows**, **zombies** and **skeletons** are composited from the same rig". The terrain bake *is* pixie over `arena_floor.png` / `wall_*.jpg` as the note says (`global.nim:38-40`, `:348`, `:486`). Not in `docs/PORTING-CRAFTER.md`.

### F8 — `src/crafter/roster.nim` does not exist *(other)*
- Where: the note's Kept table maps `src/ctf/roster.nim → src/crafter/roster.nim` (fork, three named edits). In the tree, `IdentityNames`/`seatAlias`/`addPlayer`/`seatName`/`runResultsJson` all live in `src/crafter/sim_state.nim:132-142, 602-629, 858-946`, and `recordAchievement` lives in `src/crafter/achievements.nim:22-30`. The *behaviour* the note names is present; the module split is not.

### F9 — `#viewpanel` is kept but **moved** in the DOM *(static-viewer)*
- Where: `client/replay_broadcast.html:1482-1500` (now a sibling of `#status`, after `#scrub`); starter has it at `:1499-1516`, above `#mmwarn`. Rationale recorded in-page (`:1482-1489`) and in `tools/build_broadcast_page.py:36-45`: `tools/ci/viewer_smoke.mjs` finds the scrubber with `#scrub, #seek, input[type="range"]` `.first()`, resolved in **document order**, so with `#zoom-slider` ahead of `#scrub` the harness's 100 % scrub click became `setZoom(maxZoom)` — the cited cause of CI failures 33225446565 and 33226980062.
- Note says: §Viewer — "`#viewpanel` … (`replay_broadcast.html:1510-1521`) and the page's `core.attachMinimap($('minimap-canvas'))` call (`:4200`) are **all kept, unmodified**." The element, its children, its CSS and the `attachMinimap` call (`:4108`) are all present and unchanged; only its position in the markup moved. It is `position: absolute` (`:632-647`), so I infer (not observed on screen) that rendering is unchanged; the CI screenshot artifact exists but I did not download it.

### F10 — `replay-viewer/config.nims` adds two link flags the note's "identifiers and the output name only" fork does not allow for *(static-viewer)*
- Where: `replay-viewer/config.nims:58-59` — `-s STACK_SIZE=8388608` and `-s INITIAL_MEMORY=33554432`, neither in the starter (`diff` against `/workspace/starters/coworld-ctf/replay-viewer/config.nims` shows exactly these two additions plus the `ctf_`→`crafter_` renames).
- Observed rationale in the file's own comment (`:35-43`): the 64 KB emsdk default stack trapped `crafter_load_replay` with `RuntimeError: memory access out of bounds`, caught by `tools/wasm_replay_smoke.cjs`. Commit `eddcf19` is the fix, and the CI log confirms the fixture now loads (`wasm-viewer` step "Step the emitted wasm module over the committed fixtures": `ok: loaded forager-seed42.replay, advanced 300 frames (1173838 packet bytes, heap 32 MB)`).
- Note says: Kept table — `replay-viewer/config.nims` is "**fork: identifiers and the output name only**". Not in `docs/PORTING-CRAFTER.md`.

### F11 — playback rate and speed chips differ from the note *(legibility)*
- Where: `src/crafter/sim_types.nim:40` — `PlaybackSpeeds* = [1, 2, 4, 8, 16, 32]`; `src/crafter/replays.nim:439-443` — `replayStepBudget` returns `speed` ticks per presentation frame (default index 0 → 1 tick/frame).
- Note says: §Transport rules — "one tick per three animation frames at 30 fps = **10 ticks/second** (speed chips `[0.5, 1, 2, 4, 8]`, default 1) … A 1344-tick episode therefore plays for **134 s**".
- Observed in CI (`wasm-viewer`, "Load the bundle in a real browser"): `soak: 10s of playback kept advancing ("2 / 950" -> "194 / 950" -> "242 / 950")` — ≈24 ticks/s, so the 949-tick smoke replay plays in ~40 s. It still comfortably outlasts `--soak 10`, which is what the note's arithmetic exists to guarantee.

### F12 — the provider reply is byte-sliced at 16384 bytes, not 4096 *(correctness)*
- Where: `src/crafter/llm.nim:199-203`:
  ```nim
  let payload = parseJson(
    if response.body.len > 4 * MaxReplyBytes: response.body[0 ..< 4 * MaxReplyBytes]
    else: response.body)
  ```
- Note says: §Reply schema — "whole reply | bytes | **≤ 4096** read from the provider before parsing".
- Traced consequence: this is a byte slice of the provider's JSON *envelope*, before any extraction. A cut mid-codepoint (or mid-object) makes `parseJson` raise, which the caller catches (`decide.nim:235-249`) and turns into a `parse_error` fallback — nothing byte-truncated reaches the replay. The *text* is separately rune-capped at `MaxReplyBytes` runes at `llm.nim:212-213`. So the rune-safety pin (checklist 9) holds; only the stated byte cap does not.

### F13 — `tests/test_crafter_world.nim` item 1 does not assert what the note's item 1 says *(correctness)*
- Where: `tests/test_crafter_world.nim:11-35`.
- Observed: `a = playScripted(config, blForager)`, `b = playScripted(config, blWanderer)` and `c` (a hand-driven turn) are computed at `:16-20` and then appear in only two assertions: `check a.config.seed == b.config.seed` (`:27`, both come from the same `config`) and `check c.config.seed == reference.cells.len div WorldCells * config.seed` (`:28`, which reduces to `seed == 4096 div 4096 * seed`, i.e. `seed == seed`). The 4096-iteration loop at `:22-26` compares `generate(seed, m).cells[slot]` with `reference.cells[slot]` — `generate` against `generate`. The three worlds `a.world` / `b.world` / `c.world` are never compared to anything.
- Note says: §Tests item 1 — "the same `(seed, variant)` generates a **byte-identical 64 × 64 grid under three different policy behaviours**". The seed-difference half (`differences > 100`, `:31-35`) is real.

### F14 — the six-step post-pass: `carve` can overwrite terrain an earlier step guaranteed *(correctness)*
- Where: `src/crafter/world.nim:293-300` (the loop over `[tTree, tWater, tStone]`), `:228-248` (`carve`).
- Observed: `carve`'s `keep` template protects only `{tBedrock, tCoal, tIron, tDiamond}` and the 3×3 at spawn (`:232-234`); every other cell on the L-path becomes `tSand`. The loop recomputes `landRegion` and re-checks `touches` for each terrain **in order** tTree → tWater → tStone, and never re-checks an earlier one. So a corridor cut toward water can sand over the only tree within 12 that step 2 forced, and nothing revisits it.
- Not observed to happen: `tests/test_crafter_world.nim:37-69` re-checks "a tree within 12, water within 12, stone within 20" over 200 seeds × 2 variants **after** the whole post-pass and passes in CI, and `:79-158` runs a full-knowledge solver over 60 seeds × 2 variants. This is an unexercised ordering hazard, not an observed failure.
- Determinism/seed-purity of the added step, which the brief asks about specifically: `landRegion` (`:159-187`), `touches` (`:189-206`), `nearestOf` (`:208-226`) and `carve` (`:228-248`) read only the grid and integer arithmetic; ties are broken by ascending slot index on both sides (`:220-226`); no `mix64` call and no policy state is consulted. `oreHost` (`:129-157`) reads `noiseAt(seed, SaltMountain, …)` only. **Deterministic and a pure function of `(seed, mountainThreshold)`** — confirmed by reading, and consistent with `tests/test_crafter_replay.nim:155-162` re-generating the world from the replay's seed and matching the played grid to within the cells the cog changed.

### F15 — `docs/PORTING-CRAFTER.md` says "Three things" and then lists five *(other)*
- Where: `docs/PORTING-CRAFTER.md:104` — "Three things in the implementation depart from it, and each is here rather than silently in the code" — followed by §A, §B, §C, §D, §E.
- Each of the five is real and minimal, verified: **A** connectivity carve = `world.nim:279-300` + `oreHost` fallback `:146-157`; **B** 21 glyphs = `sim_types.nim:227-231` (15 + 4 + `@` + `?`), pinned by `tests/test_crafter_world.nim:211`; **C** XOR digest = `world.nim:314-330` (`terrainDigest` XOR-folds, `mixTerrain` XORs old and new), which is what makes `tests/test_crafter_replay.nim:164-169` provable; **D** `sleepTicks 8` / `exploreSteps 2` = `baselines.nim:43-45` matching `tools/ci/baseline_tuning.json` `pick`; **E** `wanderer` rotates past lava = `baselines.nim:441-450`. The undocumented divergences I found are F4, F5, F6, F7, F10 and F16.

### F16 — the geometric-mean documentation is in `ACHIEVEMENTS.md`, not `RULES.md` *(other)*
- Where: `docs/ACHIEVEMENTS.md:66-81` carries the formula, "It needs at least ten episodes to mean anything", and "it is not what the ladder ranks". `grep -i geometric docs/RULES.md` → no match.
- Note says: §Scoring — "`docs/RULES.md` states the formula, states that it needs ≥ 10 episodes to mean anything, and states that it is **not** what the ladder ranks." Both files are shipped and both are linked from `game.docs.pages`.

### F17 — `ci.yml`'s page-derivation check is non-gating *(static-viewer)*
- Where: `.github/workflows/ci.yml:134` — `run: python3 tools/build_broadcast_page.py --check || true`.
- Observed: the `|| true` swallows any failure, and the step is a no-op on the runner anyway (no starter mount). The only unconditional guard on the page's provenance is `tests/test_crafter_viewer.nim:100-142`, which checks markers rather than bytes.
- I ran the real check in this sandbox against the mounted starter: `python3 tools/build_broadcast_page.py --starter /workspace/starters/coworld-ctf --check` → `client/replay_broadcast.html matches the derivation`, exit 0. So the artifact **is** the derivation at this sha; it is simply not CI-enforced.

### F18 — `appState.playerNames` is dead; `results.names[0]` carries the policy label *(other)*
- Where: `src/crafter/server.nim:76` declares `playerNames: Table[WebSocket, string]`; `:413` reads it; nothing in the tree writes it (`grep -n playerNames src/` → those two lines only). `drainSockets` therefore seats the player as `"seat-0"` (`:415-416`) and then overwrites the roster name with the registration label at `:440`.
- Observed in the fixture: `results.names == ["forager"]`, `results.aliases == ["Alpha"]`.
- Note says: §Server's example is `"names": ["daveey"]` — "the seat's **real policy/player name** (`daveey`, `daveey-1`, `Baseline (1)`)". The value shipped is `PLAYER_POLICY_LABEL`, which is a real policy name but not the platform's player name. Both name spaces are present and separate either way (F-see "Traced and consistent").

### F19 — waking by damage never awards `wake_up` *(correctness)*
- Where: `src/crafter/agent.nim:194-199` sets `result.wokeRested` only inside `if primitive != pSleep and cog.asleep`; `src/crafter/sim_state.nim:499-501` wakes the cog on a creature hit (`sim.cog.asleep = false; sim.cog.sleepRun = 0`) **without** going through `applyPrimitive`, so `wokeRested` is never set on that path and `sim_state.nim:430-432` never fires.
- Traced consequence: a sleep run that began below 9 energy, reached 9, and is then ended by a zombie bite does not unlock `aWakeUp`, even though the run "ended with `energy == 9`".
- Note says: §The 22 achievements #16 — "a run of ≥ 1 `sleep` ticks that began with `energy < 9` ends with `energy == 9`"; §The seventeen actions — "any other primitive wakes it, **and so does taking creature/arrow damage**". The healthy path (a non-`sleep` primitive, including the `noop` an empty queue produces) is covered and tested (`tests/test_crafter_sim.nim:481-491`).

### F20 — creature resolution order within a tick is by kind, not by `(spawnY, spawnX)` *(correctness)*
- Where: `src/crafter/sim_state.nim:479-488` attempts spawns in the order cow → zombie → skeleton and `src/crafter/creatures.nim:86-87` appends each to `herd.list` in that order; new arrows are appended after the whole pass (`creatures.nim:215-216`).
- Note says: §Day, night and the creatures — "Three creature kinds live in a single stable array ordered by `(spawnTick, spawnY, spawnX)`". Order is stable and identical on replay (which is what the hash chain needs, and `stepCreatures` iterates by *kind pass* anyway, `creatures.nim:132`), but the within-tick tiebreak is kind order, not `(y, x)`.

### F21 — two small formula/mechanic differences from the note's text *(correctness)*
- `src/crafter/agent.nim:249` draws the sapling with `mix64(seed, 600, idx(front.x, front.y), tick)` (4 mixed words). Note §The seventeen actions writes `mix64(seed, 600, x, y, tick) mod 10 == 0` (5 words). Same 1-in-10 distribution, different draw.
- `src/crafter/creatures.nim:165-168`: when the cell a skeleton would put an arrow into **is** the cog's cell, the code applies 2 damage directly with `by: ckArrow` instead of spawning an arrow that enters the cell next tick. Note §Skeleton shooting says "The arrow spawns in the adjacent cell toward the cog, moving in that direction"; §creature table says the arrow does "2 damage on entering the cog's cell". Damage, amount and `deathCause` (`dcArrow`, `creatures.nim:221`) all match; only the intermediate arrow object is elided.

### F22 — stale docstrings and comments carrying another game's numbers *(other)*
- `src/crafter/decide.nim:4-8`: "one turn every <= `turnTicks` (**12**) ticks, at most **55** turns per episode … at most **110** provider calls per episode". Config is `turnTicks: 24`, `maxTurns: 56` (`sim_config.nim:77-78`) → 112. The code reads config, so behaviour is right.
- `replay-viewer/crafter_replay.nim:77-78`: "at most **660 ticks over a 169-cell grid** — sub-millisecond in wasm". This game is 1344 ticks over 4096 cells.
- `client/replay_broadcast.html:1429`: comment says the panel "now draws the agent's own **7x7** window"; the caption and the sim are 9×9.
- `tests/test_crafter_viewer.nim:132-136`: "the file README.md points a contributor at" — `grep -i block README.md` finds no mention of `client/crafter_block.html`.
- `src/crafter/decide.nim:113-115`: "the SAME proc the **`scout`** baseline uses" — this game has `forager`/`wanderer`.

### F23 — compiled ELF test binaries are committed *(other)*
- Where: `git ls-files -s tests/` shows mode `100755` blobs for `tests/test_crafter_{driver,endcard_labels,engine,events,manifest,replay,sim,viewer,world}` and `tests/shards/tests`, each an `ELF 64-bit LSB pie executable` (≈0.3–4 MB, ~8 MB total). `.gitignore` does not cover them.

### F24 — `tools/ci/baseline_tuning.json` is a 9-row table, not a full matrix *(other)*
- Where: `tools/ci/baseline_tuning.json` `grid` has 9 entries; `thirstThreshold` appears in `pick` but in no `grid` row, and `shelterStones` takes only the values 4 and 2 across the 9 rows.
- Note says: §Scripted baselines — "a parameter object chosen by `tools/tune_baselines.nim`'s sweep, not guessed; `tools/ci/baseline_tuning.json` records the sweep's pick". The mechanism exists (`tools/tune_baselines.nim` is present, `tests/test_crafter_driver.nim:253-265` asserts the shipped defaults equal `pick`); the recorded grid is thinner than "for every cell of the matrix" implies.

### F25 — no CI grep asserts that no `ctf_`/`CTF_` identifier survives *(other)*
- Where: the note's §Sim module opener says "a CI grep asserts no `ctf_` / `CTF_` identifier survives outside comment history". No such step exists in `.github/workflows/ci.yml`. `grep -rn 'ctf_\|CTF_'` over the tree finds `window.CTF_WIRE` in `client/chrome_common.js:72` (unavoidable — that file is byte-identical to the starter's, which the note pins harder) and the deliberate compatibility alias `src/crafter/wire_constants.nim:30` `window.CTF_WIRE=window.CTF_WIRE||window.CRAFTER_WIRE;`. `tests/test_crafter_viewer.nim:141-142` asserts the rename in `broadcast_core.js`.

---

## Traced and consistent

**Checklist 1 — CI green.** `gh run list -R Metta-AI/cogame-crafter --branch main -w ci.yml`: run **33228279820**, head sha `71bf90d1af107a6c5162a4d7d53f683c9c55cab1`, conclusion **success**, jobs `docker-smoke` ✓ 1m12s, `test` ✓ 2m25s, `wasm-viewer` ✓ 3m2s. (The "no test loosened" half is F1.)

**Checklist 2 — replay re-derivation, frame by frame.** `src/crafter/replays.nim:290-303` `stepReplay` calls `checkReplayHash` after **every** tick; `:259-288` compares `sim.gameHash()` against the recorded `hashes[hashIndex]` at the matching tick and latches `hashMismatchTick`. `tests/test_crafter_replay.nim:79-90, 97-110` records an episode for **each** of `death, allUnlocked, turnCap, tickCap, wallClock, fault`, walks the whole replay through `stepReplay`, and asserts `mismatch == -1` plus equal final tick/hash/endRule/endReason/achievements. `:224-238` re-derives the committed fixture the same way. The viewer derives its display from that same re-derivation: `replay-viewer/crafter_replay.nim:107-109` → `replay_runtime.nim:39-66` → `buildReplayViewerPacket` reads the *re-simulated* `sim`, not a recorded frame stream.

**Checklist 3 — static viewer.** `coworld_manifest_template.json` → `game.replay_viewer == {"bundle": "static-replay-viewer"}` (asserted `tests/test_crafter_manifest.nim:94-98`, including `not m.hasKey("replay_viewer")` at top level). `tools/build_replay_viewer.sh` present, mode `100755`, asserted present+executable by `ci.yml:263-276` and invoked by path at `:278`. `src/crafter/server.nim:196-202` serves `/client/replay` locally only; nothing declares a `/client/replay` pod path to the platform, and `coworld-release.yml:173-207` hard-fails unless the certify log reports the STATIC bundle.

**Checklist 4 — both name spaces.** In-game: `sim_state.nim:132-142` `IdentityNames[0] = "alpha"` → `seatAlias(0) = "Alpha"`, and the observation's only identity field is `"you": seatAlias(0)` (`sim_state.nim:796`). Spectator: `broadcast.nim:123-148` `rosterJson` emits `name` (real) **and** `alias` side by side; `showPlayerLabels: false` in both variants. Asserted `tests/test_crafter_replay.nim:141` (`alias == "Alpha"`) and `tests/test_crafter_viewer.nim:274`.

**Checklist 5 — degrade never hang.** Every wait bounded, traced individually:
- LLM attempt: `decide.nim:188` `while open and attempt < 2` — at most two requests per turn; deadline `min(attempt1Ms|retryMs, remaining turn budget)` at `:202-204`, handed to curl as whole seconds at `:222-223`.
- Whole turn: monotonic `turnBudgetMs = 9500` from `turnStart` (`decide.nim:139-140`, `:191-195`).
- Rate floor: `decide.nim:178-181` sleeps at most `turnSpacingMs - since` (≤ 2600 ms) and is inside the turn budget.
- Rate guard: `decide.nim:119-126`, 28 requests / rolling 60 s → `forager` plan, no network wait.
- Budget guard: `decide.nim:145-152` disables the LLM once `elapsed + 2 × ceil(turnBudgetMs/1000) > wallClockBudgetSeconds`.
- Lobby: `sim_state.nim:565-566` `lobbyTicks >= lobbyJoinTimeoutTicks` (2400 @ 24 fps = 100 s), paced in wall clock unconditionally (`server.nim:522-531`).
- Engine stop: `server.nim:487-491`, top of every loop iteration, `wallClockBudgetSeconds = 660` in both shipped variants and the note's 720 s bound = 60 % of 1200 (`tests/test_crafter_manifest.nim:113-124` asserts `<= 660` for both variants and the cert fixture).
- Shutdown grace: `server.nim:629-636`, `max(1, min(30, gameOverTicks div 24))` = 20 s.
- Turn cap: `sim_state.nim:294-305` fires at the *start* of the turn that would exceed `maxTurns`; tick cap `sim_state.nim:530-531` is an independent guard. Each `stepTick` increments `tickCount` (`:340`), so the server loop cannot spin.
- Worst case traced: 100 s lobby + 56 × 9.5 s = 632 s, engine stop at 660 s (+ ≤ 9.5 s in-flight overrun) + 20 s grace ≈ 690 s < 720 s.
- Player container: bounded dial 240 × 500 ms (`crafter_player.nim:27-28`), bounded reconnects (`:31`), exits 0 on a dead socket (`:150`). Observed in CI: `all 1 player containers exited 0`.

**Parallel-batch rule.** One seat, so `decide.nim:214-223` posts a single request into `RequestBatch` and calls `engine.client.curl.makeRequests(batch, …)` once — the starter's batching path carrying a batch of one. No sequential per-cog loop exists.

**Checklist 6 — `num_agents`.** `1` in `variants[0].game_config`, `variants[1].game_config` and `certification.game_config`; absent at every variant top level (verified by parsing the manifest: variant keys are exactly `id,name,description,game_config`). `certification.players` = 1 entry, `certification.game_config.players` = 1 entry. `tools/ci/docker_smoke.sh:102-150` carries all four `SEAT-COUNT FAIL:` invariants plus the `SMOKE_SEATS` cross-check (substituted to `1`). **`grep -n 'SEAT-COUNT' <docker-smoke job log>` → no matches**; the job log reports `game=crafter seats=1 … "num_agents": 1` and `smoke OK: seats=1 … reason=complete`. `sim_config.nim:153-156` additionally rejects any config with `numAgents != 1`.

**Checklist 7 — scripted baseline plays full episodes legally.** `tests/test_crafter_engine.nim:45-63` runs six real all-scripted episodes (2 variants × 3 seeds) to the natural end through `playScripted` (`tests/helpers.nim:43-64`, which drives the same `beginTurn`/`expandPlan`/`installPlan`/`stepTick` sequence `server.nim:536-550` uses), asserting `endReason == erComplete`, `endRule ∈ {death, allUnlocked, turnCap, tickCap}`, and all six §Server results identities. Legality: `tests/test_crafter_driver.nim:54-80` checks 300 pseudo-random states × both baselines for `≤ maxActionsPerTurn`, every `act` in the enum, `goto` inside 0…63, `n` inside 1…12 / 1…24, empty `say`/`notes`, and a ≤ 1024-byte serialised directive; `:82-123` checks no step onto known lava and no BFS step through `?`; `:125-148` checks `≤ turnTicks` primitives, `≤ macroPrimitiveCap` per macro, and that an empty queue yields `noop`. Tuning: `tools/tune_baselines.nim` + `tools/ci/baseline_tuning.json` + `tests/test_crafter_driver.nim:253-265` (see F24 for the grid's thinness).

**Checklist 8 — LLM reply handling.** Tolerant extraction: `directives.nim:40-79` scans for the outermost balanced `{…}` with a string/escape state machine, tolerates fences and trailing prose, and falls back to first-brace..last-brace. Retry: exactly one (`decide.nim:188`, `while … attempt < 2`), with an explicit "your previous reply was not usable" nudge at `:207-209`. Fallback: `decide.nim:112-117` `foragerFallback` calls `foragerPlan` — the same proc, asserted identical to the baseline by `tests/test_crafter_driver.nim:150-158`. Recorded for phase 60: `fallbackRecord` (`decide.nim:69-76`) written to the replay chat stream, `results.fallbackTurns` (`sim_state.nim:943`). Log phrasing: attempt 1 says `"attempt 1 failed, will retry: "` (`decide.nim:246`) and does **not** contain "falling back"; only the second-failure path at `:269` says `"falling back to forager"`, matching the note §Degrade never hang. `llm.nim:129-130` emits "the LLM provider is unavailable" when there are no credentials.

**Checklist 9 — rune-safe truncation.** `sim_types.nim:259-268` `truncateRunes` uses `runeLen`/`runeSubStr`, never a byte index; `sanitizeSay` (160) and `sanitizeNote` (400) at `:270-277`. Applied to `say`/`notes` (`directives.nim:131-132`), the policy label and prompt (`server.nim:313-314`, `crafter_player.nim:43-44`), `fallback.detail` (`decide.nim:75`), `stopDetail` (`decide.nim:99`, `sim_state.nim:281`), the failure payload (`server.nim:288`), and captured provider error text (`llm.nim:181, 190, 196, 216`). `tests/test_crafter_driver.nim:206-221` and `tests/test_crafter_replay.nim:171-210` feed 160/400 four-byte emoji sitting exactly on each cap and assert the replay summary parses under a strict UTF-8 JSON parse with no lone surrogates. CI runs the same check on the real smoke replay (`ci.yml:224-244`).

**Checklist 10 — manifest validates.** `game.docs` = `{"readme": {"type":"uri","value":…}, "pages": [4 × {id,title,content:{type,value}}]}`; `game.protocols` carries **both** `player` and `global` as `{"type":"uri","value":…}` objects. Asserted `tests/test_crafter_manifest.nim:73-92`, which also checks every page file is committed. `$schema` present, `tags` top-level (6 ≥ 3), `game.tags` absent, `episode_timeout_minutes: 20` top-level, no top-level `version`, no `game.display_name`, `game.owner` present. `config_schema` `additionalProperties: false`, `required: ["tokens","players"]`, every array property carries `minItems`/`maxItems` (`tokens` 1/1, `players` 1/1, `slots` 0/1), `num_agents` integer min 1 max 1 default 1; no literal `tokens` in any `game_config`. `results_schema` closed, 53 properties matching `runResultsJson` key-for-key (asserted `tests/test_crafter_engine.nim:54-63`), achievement arrays pinned 22/22, `toolsOwned` 0/6, and the `reason`/`endRule`/`deathCause` enums exactly as the note lists. `player[]` has exactly one entry (`forager`), seated in `certification.players`, `resources.limits.cpu == "1"`.

**Checklist 11 — legible at 360 px.** `client/crafter_block.html:31-36`:
`.plate-name { flex: 1 1 auto; min-width: 3.2em; overflow: hidden; text-overflow: ellipsis; }`.
`relayout()` (`replay_broadcast.html:4190-4233`) toggles `#stage.tiny` at `boardW <= 620` and clamps `--hudscale` to `[0.5, 1.6]`, both the starter's, kept verbatim. The `.tiny` rules the note lists all exist: `:263-313` covers `.cf-alias`, `#cf-vitals` (food/drink/energy collapse), `#cf-inv`, `#cf-checklist` (2 × 11, 9 px chips, captions dropped, gold ring on lit chips), `#zoombar` hidden, `#minimap` pinned 56 px, `#fpv` pinned 56 px with `#fpv-grip` disabled, `#board { --cf-fogsteps: 2; --cf-nightcap: 0.55 }`, `#killfeed` 3 rows. Asserted `tests/test_crafter_viewer.nim:194-233`.

**Checklist 12 — release order and scaffold.** `coworld-release.yml` step order: "Build the Coworld manifest" (`:159`) → "Certify locally" (`:173`, `--timeout-seconds 300`, and a hard fail unless the log names the static bundle) → "Upload the policies" (`:216`, with an in-file comment explaining it must precede upload-coworld) → "Upload the Coworld" (`:314`) → "Wait for canonical" (`:352`) → "Put the Coworld secret" (`:410`). All three workflows present. `tools/ci/docker_smoke.sh` and `tools/build_replay_viewer.sh` both mode `100755`, both asserted present+executable and invoked by path in `ci.yml`. `tools/ci/policies.json` has 4 entries: two `PLAYER_PROMPT` champions (`crafter-techtree`, `crafter-homesteader`) with champion #2 carrying `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"` (asserted `tests/test_crafter_manifest.nim:199-200`), and two `PLAYER_SCRIPTED` fillers (`forager`, `wanderer`). The three-name placeholder gate is implemented as its own `test`-job step (`ci.yml:105-118`, spelled through character classes so it does not match itself) and is green. `diff` against `coworld-builder/templates/`: `viewer_smoke.mjs` is **byte-identical**; `docker_smoke.sh`, `coworld-release.yml` and `coworld-submit.yml` differ only in the `<slug>`/`<IMAGE>`/`<SEATS>` substitutions.

**Checklist 13 — viewer executes.**
- `wasm-viewer` `needs: docker-smoke` (`ci.yml:255`) and is green at the reviewed sha. Its "Load the bundle in a real browser" step **ran and passed**, with no `continue-on-error`: `{"loaded":true,"ms":555,"clock":"DAY 2 · DAY TICK 242/1344 · HP 9 · SCORE 80242", …}`, `soak: 10s of playback kept advancing ("2 / 950" -> "194 / 950" -> "242 / 950")`, three distinct scrub readouts at 0/50/100 %.
- Markers: `replay-viewer/static_replay.js:158-161` sets `data-replay-loaded="true"` on `document.documentElement` in the Worker's `'loaded'` branch, which `static_replay_worker.js:123-131` posts only after `_crafter_load_replay` returned and `ingestPacket()` handed BroadcastCore the first frame; `static_replay.js:8-20` `showFailure()` sets `data-replay-error`. Both are the starter's own code paths — `diff` against the starter shows `static_replay.js` differs by **one line** (the Worker name).
- Playback opens at the game start: `replay-viewer/crafter_replay.nim:82` `replay.seekReplay(game, replay.replayStartTick())` immediately after the pre-scan; `replays.nim:200-201` `replayStartTick` clamps to `startTick`, set from `sim.gameStartTick` at the first `Playing` tick (`replays.nim:355-356`, `:379-380`, `replay_runtime.nim:33-35`); every seek clamps to it (`replays.nim:482` `clamp(tick, replayStartTick(), replayMaxTick())`), and the restart/loop paths seek to it (`:521`, `:555`, `:570`). The hash-checked re-simulation still walks the lobby internally (`replay_runtime.nim:28-32`). See "Could not determine" for the LATE-gameStart caveat.
- Emscripten flags and bootstrap agree, one starter: `replay-viewer/config.nims:51-65` has **no** `MODULARIZE` and **no** `EXPORT_NAME` (non-modularized, output `crafter_replay.js`), and `replay-viewer/static_replay_worker.js:8` `var Module = {};` + `:188` `Module.onRuntimeInitialized = …` + `:192` `self.Module = Module;` + `:239` `importScripts('./wire_constants.js','./broadcast_core.js','./crafter_replay.js')`. `diff` against the starter's two files shows *only* the `ctf_`→`crafter_` renames (plus F10's two link flags). This is exactly the pairing the lantern deadlock inverted, and `loaded: true` in CI is the positive evidence.

**Checklist 14 — chrome is the starter's.**
- `client/chrome_common.js`: 40 022 bytes, sha256 `7ace7287e0d19bf0fddb2362c55e4d76dfb44adcd4fbc8d1743b0557ced72f7c` — **byte-identical** to `/workspace/starters/coworld-ctf/client/chrome_common.js` (verified with `sha256sum` on both). Pinned as a literal at `tests/test_crafter_viewer.nim:9-10, 95-98`.
- `client/replay_broadcast.html`: 5181 lines vs the starter's 4660 — it **grew**, it is not a fraction. `diff` against the starter shows the starter's CSS sections 1–5, `relayout()`, the transport, the scrubber/momentum/beat/lull machinery, the endcard and the locker-room curtain all present above the banner, changed only by (a) the note's removals — `#povBadge` markup+CSS+click handler, `.fpv-hp`/`.fpv-gear`/`.fpv-map`/`#fpv-map-canvas`, the `.beat-marker.steal/.return/.capture` rules — (b) the note's vocabulary re-map table, (c) the blue/green/yellow locker-room bots. The starter's paintball block after `:4344` is replaced by `client/crafter_block.html` under the banner `CRAFTER additions to the inherited coworld-ctf chrome`. **I re-derived the file**: `python3 tools/build_broadcast_page.py --starter /workspace/starters/coworld-ctf --check` → "matches the derivation", exit 0. That is provenance, not id-presence.
- Transport rules: (a) `relayout()` (`:4190-4233`) measures `#scorebug`/`#transport` and does `root.style.setProperty('--hudscale'|'--topband'|'--band', …)` on `document.documentElement`; (b) every element the game block adds is `position: absolute` anchored from `top: calc(var(--topband, 0px) + …)` — the only `bottom:` in `crafter_block.html` is `bottom: auto !important` on `#fpv` (`:163`), and there is no `position: fixed`; (c) `#endcard { bottom: var(--band, 0px) }` (`:950`), shown/hidden with the class its rule uses, and the starter's `else { $('endcard').classList.remove('on'); }` seek path is kept (`:1982`); (d) beats are `<button type="button" class="beat-marker <kind>">` with `title` + `aria-label` that `CTX.send('s:' + tick)` on click (`crafter_block.html:376-395`), and CSS exists for exactly the seven kinds `isBeat` returns — `achievement`, `nightfall`, `daybreak`, `kill`, `death`, `fallback`, `end` (`:183-205`), asserted as a set equality in `tests/test_crafter_viewer.nim:167-192` and cross-checked against `broadcast.nim:115-121`. The block declares `cfBeat`, never `markBeat`, and `tests/test_crafter_viewer.nim:144-165` extracts the chrome alias list and asserts no collision.
- `#viewpanel` **KEPT** — markup, CSS, all eight ids, `core.attachMinimap($('minimap-canvas'))` (`:4108`), plus a follow-cam (`crafter_block.html:404-445`: `core.setZoom(world / cfCameraCells)` with `cfCameraCells = 15`, `core.panTo` on cog movement, `cfDisarm()` on user pan/wheel/minimap drag at `:893-907`, and `armFollow` re-armed from the starter's own `resetView()` dblclick binding at `replay_broadcast.html:4157-4161`). This is the note's own decision: 64 × 64 cells at 24 px = 1536 × 1536 native px against a 15-cell default view.

**Checklist 15 — every drawn string fits its frame.**
- The main smoke reports `canvas text: 0 drawn, 0 never inside the canvas (0 draws crossed an edge), 0 ellipsized (--strict-text-bounds)`. Per the checklist, `total: 0` is not evidence — the board is composited on an OffscreenCanvas inside a Dedicated Worker (`static_replay.js:89`), which the harness's instrumentation does not reach. It is however *consistent* with the design: §Viewer → Art forbids canvas text on the board layer, and `grep -n 'fillText\|strokeText' client/broadcast_core.js client/crafter_block.html client/replay_broadcast.html` returns **nothing** — asserted at `tests/test_crafter_viewer.nim:259-269`.
- The compensating gate the checklist demands **exists and is green**: `ci.yml:335-372` "Drive the shipped chrome's text path (renderer fixture)" serves `dist/static-replay-viewer/` over HTTP, loads `tools/ci/renderer_fixture.html` (which iframes the **shipped** `index.html` and shims only the wasm entry) and runs `viewer_smoke.mjs --strict-text-bounds` against it at 960/640/360 px. Its `canvas_text` line at the reviewed sha: **`canvas text: 167 drawn, 0 never inside the canvas (0 draws crossed an edge), 0 ellipsized`**, `{"loaded":true,"ms":782}`. `--strict-text-bounds` is on in both steps.
- Reserved band: the note's rule is "no text on the board layer at all"; every string lives in a fixed-size gutter panel (`#cf-checklist` left gutter, `#viewpanel` + `#fpv` right gutter, `#cf-vitals`/`#cf-inv` on the plate, `#killfeed`) or in the DOM, and the 56 + 8 + 56 = 120 px right-gutter arithmetic is asserted at `tests/test_crafter_viewer.nim:219-223`.

**Resolution rules — the numbered orders.** `sim_state.nim:333-550` implements the 12 tick steps in the note's order and numbers them in comments: 1 `inc tickCount` (`:340`), 2 pop-or-`noop` (`:345-349`), 3 `applyPrimitive` (`:357`), 4 vitals (`:436-445`), 5 ripen + phase + dawn burn (`:452-475`), 6 bounded spawns cow→zombie→skeleton (`:478-488`), 7 creatures in the order arrows→skeletons→zombies→cows (`:492`, `creatures.nim:132`), 8 achievements (recorded at the moment each predicate fires), 9 death check (`:509-513`), 10 visibility merge (`:521-522`), 11 hash (`:550`), 12 flinch/stop (`:538-548`). **Flinch**: `flinch` is set only by `peLava` (`:426`) and by creature/arrow hits (`:495`); starvation damage (`:441-445`) does not set it, and `tests/test_crafter_sim.nim:493-522` and `tests/test_crafter_engine.nim:181-207` assert both halves and that `interrupts` increments only when `turnTick < turnTicks`. **Vitals**: `agent.nim:329-353` — drains at `tick mod 40 / 30 / 50` with the energy drain skipped while asleep, regen at `mod 25` only when food, drink and energy are all > 0, starvation `-1 health` per zeroed vital at `mod 10` in the order food → drink → energy, mapped to `starvation`/`thirst`/`exhaustion` at `:355-359`. **Creatures**: HP 3/5/3 and periods 4/2/3 (`creatures.nim:23-28`); spawn terrain grass / grass+sand / path (`:59-64`); one attempt per kind per tick with `cx = mix64(seed, 400+k, tick) mod 62 + 1`, `cy = mix64(seed, 410+k, tick) mod 62 + 1`, Chebyshev ≥ 6 (`:66-88`); dawn burn of every zombie not on `path` (`:90-97`); zombie 2 awake / 5 asleep from a 4-adjacent cell with a 5-tick cooldown (`:184-193`); skeleton shot within 6 on a row/column with a clear walkable line and an 8-tick cooldown (`:153-172`), moving only onto `path` (`:180`); arrows one cell per tick, 2 damage, removed on any obstruction (`:137-152`). **17 actions**: `sim_types.nim:106-125`, exactly Crafter's seventeen by name; `move_<dir>` turns then steps only into a walkable creature-free cell, lava zeroing health (`agent.nim:201-223`); `do` resolves creature before terrain (`agent.nim:229-232`); crafting needs a table within Chebyshev 1 and the iron recipes a furnace too (`agent.nim:306-311`, `:101-108` for the six recipes and their exact costs). **22 achievements**: `sim_types.nim:149-174` in the note's canonical order, one-shot and never revoked via `achievements.nim:22-30`, with `achievementTick[]` beside `unlocked[]`. **Terrain table**: `sim_types.nim:227-228` glyphs, `:244-247` walkable (lava walkable), `agent.nim:144-169` mined-by/yields/becomes — all matched line by line against the note's table and asserted by `tests/test_crafter_world.nim:198-248`.

**World generator.** Four integer value-noise fields on a stride-8 lattice, corners `mix64(seed, salt, gx, gy) mod 1024`, bilinear in 16-bit fixed point (`world.nim:55-85`), salts 1/2/3/4 (`:76-79`); the first-match-wins ladder at `:87-101` matches the note's thresholds exactly (830/850, 60/780, 960/900/60, 800/8, 760/12, 640, 590, 700). No float anywhere — `tests/test_crafter_world.nim:182-196` greps `sim_state, world, agent, creatures, achievements, driver, baselines` for `float`/`sqrt`/`.0` outside doc comments, and `:160-180` re-implements the interpolation independently and compares cell for cell. Post-pass steps 1–4 at `:265-278` and the note's step 5 (ore minima) at `:301-307`; the builder's added step is the connectivity carve at `:279-300` (see F14 for its determinism analysis and the one ordering hazard).

**The seat never sees the seed or an unexplored cell.** `observationJson` (`sim_state.nim:769-848`) contains no `seed`, no noise value, no threshold, no creature HP, no score and no `parAchievements`; `view` comes from the live world only inside the 9 × 9 box (`viewRows`, `:672-691`), `region`/`nearest`/`landmarks` read `knownMap` only (`world.nim:374-397`, `sim_state.nim:693-744`), unseen cells render `?` (`world.nim:347-349`), and `threats` is current-only with no known-map write (`sim_state.nim:756-767`; `mergeVisible` writes terrain only, `world.nim:351-368`). Asserted `tests/test_crafter_sim.nim:302-366`.

**Replay writer.** Magic `COWLDCRF`, format version 1, gameName `crafter`, gameVersion `1` (`replays.nim:29-40`) — confirmed on the committed bytes (`b'COWLDCRF\x01\x00\x07\x00crafter\x01\x001…'`). Config JSON carries every field the note's §Server table lists plus the deadlines, and deliberately omits `tokens` (`sim_config.nim:279-329`; asserted key-by-key at `tests/test_crafter_replay.nim:112-128`). Per-tick `gameHash` written by `server.nim:474-479` after every `sim.step()`, in the fixed mixing order of §Determinism point 4 (`sim_state.nim:210-241`), with the terrain digest maintained incrementally through the single `setTerrain` door (`:192-199`). A `stop` record is written for **every** end reason, not just the wall-clock one (`server.nim:579-589`), and re-applied on playback by the same `sim.applyStop` (`replays.nim:171-176`). `results` is embedded once as a `result` chat record (`decide.nim:101-106`). Self-sufficiency is exercised end to end: `python3 tools/replay_summary.py tests/replays/forager-seed42.replay` yields `protocol crafter/v1`, seed 42, variant standard, 44 plans, 14 achievements, `reason complete`, `endRule death`, `finalTick 949` from the bytes alone.

**Two name spaces, in-game vs spectator.** Confirmed on the shipped fixture: `results.names == ["forager"]` (spectator side), `results.aliases == ["Alpha"]`, and the observation's `"you"` is `"Alpha"`. The `register` record is redacted — `tests/test_crafter_replay.nim:143` asserts the record has no `prompt` key, and `server.nim:458-460` writes only label/kind/baseline.

**The 51 test items.** All 51 numbered items in the note's §Tests have a counterpart: 1–5 in `test_crafter_world.nim` (7 tests; see F13 for item 1), 6–18 in `test_crafter_sim.nim` (30 tests), 19–25 in `test_crafter_driver.nim` (11 tests), 26–30 in `test_crafter_engine.nim` (8 tests), 31–36 in `test_crafter_replay.nim` (7 tests), 37–38 in `test_crafter_manifest.nim` (13 tests; item 38 is the `skip()` of F1), 39–48 in `test_crafter_viewer.nim` (8) + `test_crafter_events.nim` (7) + `test_crafter_endcard_labels.nim` (1), and 49–51 as `ci.yml` steps (`viewer_smoke.mjs --soak 10 --strict-text-bounds`, `renderer_fixture.html`, `wasm_replay_smoke.cjs` over the committed fixtures). 92 `test` blocks total, all run twice (debug + `-d:release`) by `ci.yml:139-190` over `tests/*.nim`.

---

## Could not determine

- **Whether playback opens correctly on a replay with a LATE `gameStart`.** The checklist explicitly asks for a replay recorded with a large `lobbyJoinTimeoutTicks` and no joining seats. The CI/committed replay has a 48-tick lobby (first `directive` at `tick: 49`), so the observed `2 / 950` opening readout is consistent but not discriminating. The code path is unconditional (`crafter_replay.nim:82` seeks to `replayStartTick()`, and `beginSeek` clamps at `replays.nim:482`), and `startTick` is `-1` until the first `Playing` tick, at which point `replayStartTick()` clamps `max(0, -1)` → `0`; if the pre-scan never reaches `Playing` the opening tick would be 0. **What would settle it:** record a fixture with `lobbyJoinTimeoutTicks` ≥ 2400 and no seat, and assert `replay.replayStartTick() == gameStartTick` and that the first drawn frame is at that tick.
- **Whether `coworld build` / `validate_upload_manifest` accepts a `game.runnable` without `source_url`.** The starter's manifest carries it, this one does not, and no gate in the tree exercises the real CLI (F1). **What would settle it:** running `uvx --from coworld coworld build --template coworld_manifest_template.json …`, or a `coworld-release.yml` dispatch.
- **Whether `carve` can in practice strand a tree/water/stone it earlier connected (F14).** 200 seeds × 2 variants of generation invariants and 60 × 2 of the reference solver pass, but none of those tests is aimed at the ordering. **What would settle it:** an assertion, after the full post-pass, that a *reachable* tree, water and stone all still exist — the invariants test currently checks existence within a radius, not reachability, and only the solver test (item 3) covers reachability.
- **Whether the `#viewpanel` move changes anything on screen (F9).** The element is absolutely positioned in both locations and I have no rendered evidence either way; the `viewer-smoke.png` artifact from run 33228279820 would settle it, and I did not download it.
- **Whether `results.names` carrying `PLAYER_POLICY_LABEL` rather than a platform player name matters downstream (F18).** The note's example shows `daveey`; the platform's own naming path (whether it injects `players[].name` into the game config) is outside this repo.
