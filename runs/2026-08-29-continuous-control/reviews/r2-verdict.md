blocking: 0

# r2 verdict — continuous-control
Head: b67f8c8673e85b9895165609040a68aae93e8c44   Checklist: prompts/30-review-loop.md §ACCEPTANCE CHECKLIST (items 1–15, quoted verbatim in the judge brief)   Independent read written before reading fixes: yes (notes at /tmp/judge-notes.md; r2-fixes.md was opened only after the checklist pass and every refutation below was already established from the tree and the run artifacts)

The green CI run at this sha: **33252260364**, conclusion `success`, headSha
`b67f8c8673e85b9895165609040a68aae93e8c44` (verified via `gh run view --json headSha,conclusion`),
jobs `test` / `docker-smoke` / `wasm-viewer` all green. All evidence below is from the tree at this
sha and from that run's own logs and uploaded artifacts, which I downloaded and read myself.

**Evidence-handling note.** A stale artifact nearly contaminated this verdict: a leftover
`viewer-smoke.json` from the *pre-fix* run (timestamped before my download) sat at the target
directory root and shows `feed_lines: 0` with twelve pushFeed TypeErrors. The genuine
run-33252260364 artifacts (subdirectories `viewer-smoke/` and `renderer-fixture/`, matching the
step log line `{"loaded":true,"ms":584,…,"feed_lines":2}` at job 99100560059) are what this
verdict cites. Anyone re-checking should download fresh with `gh run download 33252260364`.

## Standing blocking findings

None.

## Refuted / settled at head

The r2 review was written at the pre-fix sha a8db2b32 (run 33249877981). Every finding it filed
was accurate **at that sha** — I re-verified the central one from the pre-fix run's own artifact —
and every one is fixed or moot at the current head. Per the binding rule, a finding that was true
and has since been fixed is refuted (dismissed), not standing.

### F1 (blocking, item 15) — cc feed lines/banners never rendered: `ccFeed` passed a string to the inherited `pushFeed(row: Node)` → FIXED at head, refuted as standing
- Evidence, code: `client/cc_block.html:603-609` at b67f8c86 —
  ```js
  function ccFeed(text, cls) {
    if (!ctx || !ctx.pushFeed) return;
    var row = document.createElement('div');
    row.className = 'feed-row' + (cls ? ' ' + cls : '');
    row.textContent = text;
    ctx.pushFeed(row);
  }
  ```
  Built page `client/replay_broadcast.html:3560-3566` carries the same (regenerated by
  `scripts/build_broadcast_page.py`). This exactly matches the starter's own appended-block shape
  (`coworld-ctf/client/replay_broadcast.html:4508-4513`, `feedRow`) and the starter's callee
  `function pushFeed(row) { feedEl.insertBefore(row, …) }` (`:3568-3579`). `textContent` (not
  `innerHTML`) is correct for model-authored text. The two `ctx.banner(text, '')` calls now pass
  the second argument (`cc_block.html:618-621`, `:644-647`).
- Evidence, run: head run's `viewer-smoke/viewer-smoke.json`: `feed_lines: 2`, `loaded: true`,
  `failure: null`, `console_tail` = 15 lines, **zero** `continuous-control chrome` errors (the
  pre-fix artifact carried twelve `TypeError: … parameter 1 is not of type 'Node' at
  Object.pushFeed`). The head-run screenshot shows the full chrome drawn (machine, ruler, gait
  card, ribbon, pips, scorebug, transport, momentum, beats).
- The fix is guarded three ways, all verified at head: (i) new ci.yml step `The chrome swallowed
  no error while the replay played` (ci.yml:352-366) fails `wasm-viewer` on any
  `continuous-control chrome` line in the smoke's console tail — it ran and printed
  `chrome errors in the smoke's console tail: 0 of 15 lines`; (ii) test 42 pins
  `"ctx.pushFeed(row)" in page`, `"row.className = 'feed-row'" in page`, and
  `"ctx.pushFeed(text" notin page` (tests/test_cc_viewer.nim:81-84); (iii) the renderer fixture
  fails by name if `.feed-row.say` is absent (below). Fix is real, not cosmetic.

### r1-verdict B1 (item 15) — the renderer fixture did not verify the LLM say → SETTLED at head
All three requirements from the brief verified in `tools/ci/renderer_fixture.html` at b67f8c86:
1. **SAY of exactly 140 runes with its own rune-length assertion** — `:43-46` (I counted the
   literal myself: 140), `:53-58` `if (SAY_RUNES !== 140) { …setAttribute('data-replay-error',…);
   throw … }` before any iframe is created; plus a cross-check against the engine-published cap
   `w.CC_WIRE.maxSayRunes` (`:220-224`), so a `MaxSayRunes` drift is red.
2. **Renders and measures the say feed row inside the frame at 360/640/1280 px** —
   `WIDTHS = [[360,203],[640,360],[1280,720]]` (`:38`); the fixture loads the **shipped**
   `dist/static-replay-viewer/index.html` in iframes (`:177`), waits up to 20 s for the page's own
   first real frame (`#cc-ribbon`, created only by the block's `ccEnsureNodes()` on a real
   `CcChrome.frame(s, PB_CTX, …)` — and `frame` does `ctx = context || ctx` at
   `cc_block.html:683`, so the fixture's `null` keeps the page's real context), then drives the
   eleven events and measures `#killfeed .feed-row.say` via `getBoundingClientRect()` at the
   moment the say is newest (`:228-240`), failing on zero box or any edge outside
   `innerWidth`/`innerHeight` (`:256-271`), and failing if fewer than 140 runes survived
   (`:251-255`).
3. **Fails if the row is absent** — `:247-249` `'no `.feed-row.say` rendered at …px: the 140-rune
   say never reached the feed'`.
   Also asserts a banner chip rendered (`:276-278`) and non-empty in-frame boxes for `cc-ribbon`,
   `cc-pips`, `cc-strip`, `fpv-name`, `clock-caption` in the gameover state (`:283-294`).
   Driven by its own ci.yml step with `viewer_smoke.mjs --strict-text-bounds` (ci.yml:380-416),
   which ran at head (`Drive the text path at full cap in the real bundle`, job 99100560059);
   head-run `renderer-fixture/viewer-smoke.json`: `loaded: true`, `data_replay_loaded: "true"`,
   `data_replay_error: null`, `failure: null`.

### F2 (test 42 pinned both halves of the mismatch) → FIXED, `86c57d37` + `38cc254a`
Verified from `git log -p -- tests/`: the buggy call-site pin `check "ctx.pushFeed(text, cls" in
page` became `check "ctx.pushFeed(row)" in page` plus two **new** checks (`row.className =
'feed-row'` present; `ctx.pushFeed(text` absent anywhere). The starter-derived callee pin is
untouched.

### F3 (fixture SAY was 133 runes behind a no-op slice, unasserted) → FIXED, `cd6784bb`
Verified: literal is 140 runes (counted), `slice(0, 140)` gone, loud assertion + CC_WIRE
cross-check (renderer_fixture.html:43-58, :220-224).

### F4 (smoke's feed probe could not match `#killfeed`) → FIXED, `316fbe69`
`tools/ci/viewer_smoke.mjs:433` now reads
`document.querySelector('#feed, .feed, #log, #killfeed, [id$="-feed"]')` with a comment naming
this repo and finding; head run reports `feed_lines: 2` (reported, not gated — correct, since
rows expire on the dwell timer). The template divergence is recorded as `docs/PHYSICS.md`
divergence 16, so the "copied verbatim" claim is not quietly false.

### F5 (measured: the 140-rune row fits the frame; r1-B1's overflow arithmetic did not reproduce) → consistent with my read; no change was needed
The claim is now asserted on every CI run by the fixture at all three widths rather than resting
on a reviewer's local measurement. Correctly left without a `.feed-row.say` max-width override.

### F6 (PHYSICS.md divergence 15 quoted a stale byte count) → FIXED, `739942b1`
`docs/PHYSICS.md:141` now says "about 132 000 B"; the head run's docker-smoke log line is
`replay.json (131999 bytes)`. Consistent.

### F7 (test 11 re-derived fall limits from the fields under test; hopper `fwHigh` unreachable) → FIXED, `97b387e4`
Verified from the diff: the note's literals are pinned (`spec(mHopper).lowY == mmQ(700)`,
`maxPitch == degQ16(20)`, `highY == GuardMaxYQ16` explicitly as a guard; walker 800/2000/57;
`not spec(mCheetah).terminates`) and each branch (`fwLow`/`fwHigh`/`fwNone`/`fwPitched`) is
driven at its own boundary for both terminating bodies. Nothing removed.

### F8 (test 47b greped only the block, not `broadcast_core.js`) → FIXED, `b67f8c86`
Verified from the diff: the same 16 tokens now run over comment-stripped `broadcast_core.js`
too, offenders name their file, no token removed.

### Review's "Could not determine" items
- *Fixture iframes' console invisibility* — explained and made moot: the old fixture drove the
  block before the page handed it `ctx`, so `ccFeed`'s guard returned silently (no throw to see);
  the fixture now waits for `#cc-ribbon` (the page's own first driven frame) and fails by name
  after 20 s. The gate no longer depends on console relay at all.
- *`Unknown sprite protocol message type: 97/34` warnings* — still present at head (15 lines in
  the smoke's console tail). I traced the emitter: `client/broadcast_core.js:1063`, the starter's
  binary sprite-packet dispatch warning on unrecognized bytes (97=`'a'`, 34=`'"'`, consistent
  with a stray text frame on the binary path). Playback is demonstrably unharmed: soak advanced
  0→319→367 ticks over 10 s, scrub works, feed rows render, board fully drawn in the screenshot,
  no hash mismatch. Not tied to any checklist item. Recorded below as a non-blocking observation.

## The changed test pin — judged

Item 1 forbids a test being "disabled, skipped, or loosened". The one contested change this round
is test 42's call-site pin (`38cc254a`, then `86c57d37`). The original assertion
`check "ctx.pushFeed(text, cls" in page` pinned **the bug itself** — a call shape that provably
throws against the starter's own callee (`function pushFeed(row)` doing
`insertBefore(row, …)`), and it is *why* the r2-F1 defect survived a green suite. The replacement
pins the call shape that matches the pinned callee signature and **adds** two assertions,
including the negative pin `ctx.pushFeed(text` notin page that makes the regression red at unit
level. The starter-derived callee pin is byte-untouched. Verdict: **a correction of a wrong
assertion that strengthens coverage, not a loosening.** Everything else in
`git log -p 713e9ac..HEAD -- tests/` is strictly additive (F7: 4 checks → 16; F8: one more file
greped; r1 rounds: assertions added, inputs made harsher). Zero deleted assertions, zero
skip/xfail, zero widened tolerances, zero removed test files.

## Checklist pass (independent)

| item | status | evidence (path:line or run) |
|---|---|---|
| 1 CI green, no test loosened | PASS | run 33252260364 `success` at b67f8c86 (`gh run view --json headSha,conclusion`); job 99100399414 log shows all 13 `tests/*.nim` ran in debug AND `-d:release`; `git log -p 713e9ac..HEAD -- tests/` read hunk-by-hunk — all additive except the corrected pin judged above |
| 2 replay re-derivation | PASS | tests/test_cc_replay.nim:15 (test 34: all four end reasons, per-tick hashes incl. stop tick, keyframes); viewer re-simulates via the same sim module in wasm (`replay-viewer/cc_replay.nim`; `src/cc/replay_runtime.nim:164-169` seek = rewind + re-step; hash consumed per stepped tick at :151-157) |
| 3 static viewer | PASS | manifest `game.replay_viewer = {"bundle":"static-replay-viewer"}`; `tools/build_replay_viewer.sh` present, mode 100755, asserted + invoked by path (ci.yml:244-268); only `/client/replay` hit in the tree is coworld-release.yml:211, a comment forbidding it; bundle fetches only its replay URL |
| 4 both name spaces | PASS | `seatAlias(slot)` in observation/prompt (decide.nim:98, report.nim:144); real name spectator-side only (`roster[].name` in broadcast; results.names); test 28 leak grep (test_cc_obs.nim) ran green |
| 5 degrade-never-hang | PASS | decide.nim:12-19, :151, :226-237 — attempt deadlines clamped to remaining `turnBudgetMs`; `wallClockBudgetSeconds` 690 ≤ 720 in both variants (manifest); tests 31/32/33 (stalled seat, forced guards, forced wall-clock) green in both modes |
| 6 num_agents | PASS | `num_agents: 1` in `ladder` + `bipeds` `game_config` and `certification.game_config`; cert players 1 == gc players 1; `SMOKE_SEATS: "1"` (ci.yml:203); `grep 'SEAT-COUNT FAIL'` over the full 2077-line docker-smoke log of run 33252260364: **0 matches** |
| 7 scripted baseline full episode | PASS | test 29 asserts `reason == "complete"` (test_cc_engine.nim:12) on a real scripted episode; test 22 bounds every order for both baselines; tuning is the swept pick — `tools/tune_gaits.nim --check` + `tools/tune_baselines.nim --check` vs committed `tools/ci/{gait,baseline}_tuning.json` run in CI (ci.yml:104-115) + test_cc_tuning.nim |
| 8 LLM reply handling | PASS | decide.nim:19-22, :220-285 — tolerant extraction, retry ONCE ("will retry" vs "falling back" phrasings), fallback = trotter proc (test 23), `fallbackRecord` written (decide.nim:71-73, :171) so phase 60 can count |
| 9 rune-safe truncation | PASS | `truncateRunes` at MaxSayRunes/MaxNoteRunes (directives.nim:27-44) and in the codec for stop detail (replays.nim:233); test 24 (4-byte emoji at the cap) and test 37 (237 emoji over cap → exactly 200 whole runes, strict UTF-8 parse) green |
| 10 manifest validates | PASS | `game.docs` = readme + 3 pages, each `{"type":"uri","value":…}` content; `game.protocols` carries both `player` and `global` as objects (read from coworld_manifest_template.json) |
| 11 legible at 360 px | PASS | `.plate-name { flex: 1 1 auto; min-width: 3.2em; overflow: hidden; text-overflow: ellipsis; … }` (replay_broadcast.html:2990-2996); `@media (max-width: 640px) { .stage-label, .cc-alias, .fl-cap { display: none; } }` (:3217-3219); the 140-rune say row now measured in-frame at 360 px by the fixture |
| 12 release order and scaffold | PASS | coworld-release.yml: build (:159) → certify `--timeout-seconds 300` (:173-185) → upload-policies (:216, explicitly BEFORE upload-coworld) → upload-coworld (:314) → secret put (:410, explicitly AFTER); smoke image built fresh in-run (ci.yml:189-204); all three workflows present; docker_smoke.sh 100755; policies.json = 2×PLAYER_PROMPT + trotter + plodder, champion #2 `"player":"ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`; I ran the three-name placeholder gate over the five files: clean |
| 13 viewer executes | PASS | wasm-viewer green at head incl. `Load the bundle in a real browser` AND the fixture step (job 99100560059 step list); `needs: docker-smoke` (ci.yml:231); zero `continue-on-error` in ci.yml; `data-replay-loaded` set in the loaded branch (static_replay.js:161), `data-replay-error` in showFailure (:14-20) — both inherited shell paths; lobby is wall-clock only, never recorded (server.nim:228-236), sim enters the replay at phPlaying with tick 0 = game start (replay_runtime.nim:72), seeks clamp to [0, maxTick] (:165) — no dead-lobby dwell is constructible in this format, and the soak shows immediate advancement (0→319→367); config.nims has no MODULARIZE/EXPORT_NAME and the worker bootstraps `Module.onRuntimeInitialized` + `importScripts(…, 'cc_replay.js')` (static_replay_worker.js:188, :239) — flags and bootstrap are both coworld-ctf's and agree |
| 14 chrome is the starter's | PASS | chrome_common.js sha256 `7ace7287…ed72f7c` — I hashed both; byte-identical to the starter's 40,022 bytes; replay_broadcast.html is the starter's page mechanically derived by scripts/build_broadcast_page.py (cut list = the design note's documented removals; anchors asserted) with the game block appended under the banner at :2959 — 3672 lines vs the starter's 4671, not a fraction-size rewrite, and test 42 pins ~48 starter landmarks + kept-proc byte-equality of broadcast_core.js + the pushFeed signature; relayout sets `--band`/`--topband`/`--hudscale` on `:root`; `#endcard { bottom: var(--band, 0px) }` with `.on` and seek-dismissal kept; beats are labelled `<button>`s via `ccBeat` with CSS for exactly the six emitted kinds and none of paintbot's (tests 43/44/45); `#viewpanel`/`#minimap`/`#zoombar`/`#zoom-*` removed, ids asserted absent (test 45) — fixed 1440×960 camera viewport board, so removal is per the pin |
| 15 every drawn string fits its frame | PASS | `canvas_text` reported in both `--strict-text-bounds` steps (ci.yml:333-338, :404-408): `never_inside: 0`; `total: 0` is structurally true (zero fillText/strokeText in the client — all text is DOM) so the gate for the model string is the fixture, which now: feeds a 140-rune SAY it asserts (self + CC_WIRE), measures the rendered `.feed-row.say` box inside the frame at 360/640/1280, fails on absence/shortening/clipping, and runs in its own ci.yml step at head (evidence: renderer-fixture artifact `loaded: true`, `failure: null`) |

Single-seat game: one request per turn via the starter's batch shape (decide.nim builds a
one-request batch); multi-seat batch concerns don't arise.

## Fixer report audit (read only after the above was written)

| finding | fixer said | I verified | agrees |
|---|---|---|---|
| r2-F1 | fixed, 38cc254a | code at cc_block.html:603-609 + built page; head artifact feed_lines 2, zero chrome errors; new gate step ran | yes |
| r1-B1 | fixed, 460e7ddb (+cd6784bb) | fixture measures say row at 3 widths, fails on absence, 140-rune assertion, #cc-ribbon poll | yes |
| r2-F2 | fixed, 86c57d37 | diff read: 2 checks added, callee pin untouched | yes |
| r2-F3 | fixed, cd6784bb | counted the literal: 140 runes; slice gone; loud assertion | yes |
| r2-F4 | fixed, 316fbe69 | viewer_smoke.mjs:433 includes `#killfeed`; divergence 16 in PHYSICS.md | yes |
| r2-F5 | no change (informational) | fixture now asserts the measurement on every run | yes |
| r2-F6 | fixed, 739942b1 | PHYSICS.md:141 "about 132 000 B" vs run's 131999 | yes |
| r2-F7 | fixed, 97b387e4 | literals pinned, four branches driven at boundary | yes |
| r2-F8 | fixed, b67f8c86 | 16 tokens over comment-stripped broadcast_core.js | yes |

The fixer's claim that the six intermediate CI runs were `cancelled` by the concurrency group is
consistent with `gh run list` (cancelled after 3-4 s each on supersession); the two completed runs
(38cc254a and head) are both `success`.

## Non-blocking observations

- **`Unknown sprite protocol message type: 97` / `: 34`** — 15 warnings in the head smoke's
  console tail, from the starter's binary packet dispatch (broadcast_core.js:1063). Playback,
  drawing, feed and scrub all verified working around them. Worth a trace before release, but
  tied to no checklist item and demonstrably not degrading the viewer.
- ci.yml:378 comment says the fixture runs "at 360, 620 and 1280 px"; the fixture's code uses
  640 (renderer_fixture.html:38). Cosmetic comment drift.
- The smoke's scrub probe readouts lag the rewind-and-re-step seek (the `100%` sample read
  `TICK 258/468` while the screenshot moments later shows 762/1488) — a sampling artifact of
  documented divergence 12, not a playback defect; opening-at-start and advancement are verified.
- The fixture's `failed` variable is last-write-wins across checks and widths (fixer noted it
  too); the step still goes red on any failure, only the message may name the last rather than
  the first.
- `ci.yml`'s `NIM_TESTS` / `NIM_TESTS_DEBUG_ONLY` / `NIM_TESTS_RELEASE_ONLY` repo variables could
  narrow the test matrix without a tree change. At head they are demonstrably unset (the run
  executed all 13 files in both modes), but a future silent narrowing would be invisible to
  `git log -- tests/`.
- The champion prompts still describe `brake` optimistically (r1's non-blocking observation,
  unchanged) — a policy-quality nit, not a checklist item.

BLOCKING: 0
