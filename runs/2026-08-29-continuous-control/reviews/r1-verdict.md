blocking: 1

# r1 verdict — continuous-control
Head: a8db2b326b7f7b8f05f10ffdb5c1a7e85f28b2dc   Checklist: prompts/30-review-loop.md §ACCEPTANCE CHECKLIST (items 1–15)   Independent read written before reading fixes: yes
(Reading order kept: checklist → design note → tree + CI at head → r1-review.md → r1-fixes.md. Independent notes were written down before opening the review; the fixer's table was read last and audited, never trusted.)

- [legibility] client/replay_broadcast.html:500-504 + tools/ci/renderer_fixture.html:43-45 — the 140-rune `say` is the one LLM-authored string the viewer draws, and no gate measures it: canvas_text total==0 (void as evidence by item 15's own rule), the feed row is `white-space: nowrap; max-width: none` sized for the starter's 10-char shout (this fork re-pinned the cap 10→140), and the shipped fixture's "Exactly 140 runes" SAY is actually 133 runes with no full-length assertion and no box check on the feed row.

## Standing blocking findings

### B1 — item 15 is unverifiable for the one string class it singles out: the LLM-authored `say` in the feed   (source: judge; sharpens the reviewer's non-blocking F9)
- Where: `client/replay_broadcast.html:496-504` (inherited `.feed-row` CSS), `client/replay_broadcast.html:3562` (`ccFeed(g.alias + ': “' + e.text + '”', 'say')` — full-cap say routed into `#killfeed`), `tools/ci/renderer_fixture.html:40-45` and `:195-206`, CI run 33249877981 wasm-viewer log 11:23:43.385 / 11:23:45.332.
- Verified at head:
  - Both `--strict-text-bounds` steps report `canvas text: 0 drawn, 0 never inside the canvas … 0 ellipsized` (run 33249877981, both steps). Item 15's own rule: "`total: 0` means the check covered nothing and is not evidence." I confirmed why it is 0: `grep -c 'fillText\|strokeText'` = 0 over `chrome_common.js`, `broadcast_core.js`, `replay_broadcast.html`, `cc_block.html` — the viewer draws no canvas 2D text at all. Every string is DOM, which the canvas hook cannot see.
  - The viewer does draw LLM-authored text: `case 'say': ccFeed(g.alias + ': “' + e.text + '”', 'say')` puts the full 140-rune `say` into a `.feed-row` whose inherited CSS is `max-width: none; white-space: nowrap;` with the starter's own justification quoted in the file: "bounded by the small font + **the pre-bounded 10-char name**, so it can't run away" (`replay_broadcast.html:500-501`). This fork re-pinned `MaxSayRunes` from the starter's 10 to 140 (design.md:764-769) and adds no `.feed-row.say` override anywhere in the appended block (grep: none). At 360 px (`--hudscale` 0.5, `--u` = 0.5 px, font 4 px) a ~148-character single-line nowrap row computes to roughly the full frame width, anchored `right: calc(12*var(--u))` under `html { overflow: hidden }` (`:52`) — it clips at the left frame edge rather than wrapping or ellipsizing. From the tree I cannot verify the string fits; my arithmetic says at the cap it does not.
  - The escape hatch item 15 prescribes for exactly this situation — the worst-case renderer fixture — is shipped and runs its own `--strict-text-bounds` ci.yml step (`.github/workflows/ci.yml:352-388`, green at 11:23:45), but it does not verify this string: (a) its SAY is **133 runes, not 140** — `tools/ci/renderer_fixture.html:40-45` claims "Exactly 140 runes: the server's own MaxSayRunes" and then `slice(0, 140)`s a 133-character source string; I measured it (`len == 133`); (b) it contains **no assertion that its own strings are full-length**, which is a property item 15 names in terms ("asserting its own strings are full-length") and which would have caught (a); (c) its DOM assertions check bounding boxes for `cc-ribbon`, `cc-pips`, `cc-strip`, `fpv-name`, `clock-caption` only (`:195-206`) — never the `.feed-row` that carries the say.
  - Net: item 15 ("Every drawn string fits its frame") is not verified by canvas_text (total 0, covers nothing), not verified by the fixture (short string, no length assertion, feed row unmeasured), and not verifiable from the tree (the tree shows an unbounded nowrap row built for a 10-char cap now fed 140 runes). The binding rule — a checklist item that cannot be verified from the tree or from cited CI evidence counts as blocking — applies.
- Checklist item: 15 — "Every drawn string fits its frame. … `total: 0` means the check covered nothing and is not evidence. … a repo whose viewer draws LLM-authored text must ship a worst-case renderer fixture (full-cap remark on every seat, … asserting its own strings are full-length)." *(category: legibility)*
- What would settle it: make the fixture's SAY exactly 140 runes and assert `Array.from(SAY).length === 140`; add the rendered `.feed-row.say` element to the fixture's box checks (inside the frame at 360/640/1280); or bound the say row in the game block (`.feed-row.say { max-width: …; overflow: hidden; text-overflow: ellipsis }` or wrap) so the fit is verifiable from the tree. Any one of the three plus a green run settles it.

## Refuted

### F1 — "the `stop` record's `detail` is the one string on the replay path with no rune cap" → REFUTED (fixed at head)
- Evidence: `src/cc/replays.nim:233` at a8db2b3 — `writer.body.addText(stop.detail.truncateRunes(MaxStopDetailRunes))`, commit e9902ccb ("r1-F1"). The cap is in the codec, so both recorders (server's fault/wall-clock branches and `tests/helpers.nim`) are bounded. Test 37 now feeds `repeat(emoji, MaxStopDetailRunes + 37)` (237 four-byte emoji) through `writeStop` and asserts `summary["stop"]["detail"].getStr().runeLen == MaxStopDetailRunes` and equality with exactly 200 whole emoji (`tests/test_cc_replay.nim:173-200`), alongside the existing strict-UTF-8 assertion. The finding was true at the reviewed sha 4c1b310 and is false at the current head; it stands at zero.

The review's remaining findings F2–F18 were all filed non-blocking; none stands as blocking at head (dispositions verified in the audit table below).

## Checklist pass (independent)

| item | status | evidence (path:line or run id) |
|---|---|---|
| 1 CI green, no test loosened | PASS | `gh run list`: run 33249877981, conclusion `success`, headSha a8db2b3, jobs test/docker-smoke/wasm-viewer all green. `git log -p -- tests/` over the whole history: zero deleted assertions, zero skip/xfail/tolerance widenings; the four post-import test commits added assertions (e9902cc, a8db2b3), widened the forbidden-vocab list from 15 to 16 tokens (b682a19 — stricter), and moved key names with the schema keeping both counts (73b3f44). |
| 2 replay re-derivation | PASS | `tests/test_cc_replay.nim:15-40` (test 34): all four end reasons recorded then re-derived, `hashMismatchTick == -1` at every tick incl. the stop tick, keyframes word-identical. Viewer displays from the same re-derivation: `replay-viewer/cc_replay.nim:3` imports `cc/sim`; `renderCurrent` builds the frame from the re-stepped sim (`cc_replay.nim:39-62`); `replay_runtime.nim:151-157` checks the recorded hash every stepped tick. No parallel recording. |
| 3 static viewer | PASS | `coworld_manifest_template.json:35-37` `"replay_viewer": {"bundle": "static-replay-viewer"}` under `game`; `tools/build_replay_viewer.sh` present, mode 100755 (`git ls-files -s`), builds `Dockerfile.replay-viewer` and copies the dist; no `/client/replay` in the manifest or workflows (the `src/cc/server.nim:668` route is the starter's own developer route — coworld-ctf serves the identical path — and is never declared to the platform; `coworld-release.yml:188-214` fails release without the STATIC-bundle liveness marker). Viewer fetches only the `?replay=` URL and its own relative assets. |
| 4 both name spaces | PASS | `src/cc/report.nim:144` `"you": seatAlias(seat)` (only identity in an observation); real name spectator-side only: `broadcast.nim:17-25` roster, `:206` `cc.name`; `showPlayerLabels: false` in both variants; `tests/test_cc_obs.nim:93-131` greps a whole episode's prompts for the real name. |
| 5 degrade-never-hang | PASS | Every wait bounded: lobby 100 s + 4 s register grace (`server.nim:228-248`); wall-clock stop checked at the top of every loop iteration (`server.nim:311-317`, 690 s ≤ 720 s = 60 % of 1200); `decide.nim` — spacing sleep ≤ 2.6 s, attempt deadlines 6 s/3 s clamped inside the 9 s monotonic turn budget (`:225-237`), rate guard with no sleep (`:139-141,201-205`), budget guard settles early (`:181-190`); fixed 10×12 solver passes, no convergence loop; artifact POST 60 s; 20 s shutdown grace then `quit(0)`. Smoke episode: `reason=complete` in a 1m20s job. |
| 6 num_agents + seat invariants | PASS | `num_agents: 1` in `ladder.game_config` (:516), `bipeds.game_config` (:553), `certification.game_config` (:593); `tools/ci/docker_smoke.sh:110-151` — the four SEAT-COUNT invariants before the first `docker run`; `grep 'SEAT-COUNT'` over the full docker-smoke log of run 33249877981: zero hits; log shows `seats=1 … smoke OK: seats=1 results=835B replay=131999B reason=complete`. |
| 7 scripted baseline full episodes | PASS | `tests/test_cc_engine.nim:8-13` (test 29): all-scripted episode, `reason == "complete"`, `endRule == "ladderComplete"`, seven results identities; `test_cc_baselines.nim:11-36` (test 22): both baselines bounded/legal over 300 states, gait in the closed enum; `:38` fallback IS the trotter proc. Tuning is a sweep harness: `tools/tune_baselines.nim` / `tune_gaits.nim` with committed picks `tools/ci/{baseline,gait}_tuning.json`, `--check` in ci.yml:104-115 and `tests/test_cc_tuning.nim`. |
| 8 LLM reply handling | PASS | `directives.nim:46-85` `extractJsonObject` (fences, prose, balanced-brace, first/last-brace fallback); retry once (`decide.nim:222-277`, `while attempt < 2`, "will retry" on attempt 1 only); second failure → imported trotter order with `fallback` record written (`decide.nim:164-172`) and `results.fallbackTurns` counted (`server.nim:341-343`). |
| 9 rune-safe truncation | PASS | `truncateRunes` (runeLen/runeSubStr) on say 140, notes 320, label/name 48, prompt 4000 (never written to replay), fallback.detail 200, results.stopDetail 200, and — since e9902cc — the replay stop record's detail 200 in the codec (`replays.nim:233`). Tests 24 and 37 feed 4-byte emoji at every cap and assert `validateUtf8 == -1` / strict-UTF-8 JSON out of `replay_summary.py`. |
| 10 manifest validates | PASS | `game.docs` = `{readme: {type,value}, pages: [{id,title,content:{type,value}}×3]}` (`coworld_manifest_template.json:48-79`); `game.protocols` carries both `player` and `global` as `{type,value}` objects (`:38-47`). Note: the content type is `"uri"` rather than the checklist example's `"text"` — the design note pins uri explicitly (design.md:1814-1820) and the `{type,value}` shape the item describes is present; recorded, not counted. |
| 11 legible at 360 px | PASS | `.plate-name { flex: 1 1 auto; min-width: 3.2em; overflow: hidden; text-overflow: ellipsis; …}` (`replay_broadcast.html:2990-2996`); `@media (max-width: 640px) { .stage-label, .cc-alias, .fl-cap { display: none; } }` (`:3217-3219`). (The say feed row at 360 px is B1, filed under item 15.) |
| 12 release order and scaffold | PASS | `coworld-release.yml`: Build manifest (:159) → Certify `--timeout-seconds 300` (:173) → Upload the policies (:216, comment "BEFORE upload-coworld") → Upload the Coworld (:314) → secret put (:410, "AFTER upload-coworld"). All three workflows present; `docker_smoke.sh` and `build_replay_viewer.sh` mode 100755. `policies.json`: 4 distinct policies — 2 `PLAYER_PROMPT` champions, #2 carrying `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"` (:17), plus scripted trotter and plodder. Placeholder gate: `grep '<slug>\|<IMAGE>\|<SEATS>'` over the five files hits nothing (rc 1); only the four documented angle-bracket names survive elsewhere. |
| 13 viewer executes | PASS | (i) run 33249877981 `wasm-viewer` green, `needs: docker-smoke` (ci.yml:231), step "Load the bundle in a real browser" ran and printed `{"loaded":true,"ms":595,…}`, `soak: 10s of playback kept advancing ("0 / 1488" -> "317 / 1488" -> "366 / 1488")`; no `continue-on-error` anywhere in the workflows (grep rc 1). (ii) `static_replay.js:161` sets `data-replay-loaded` in the Worker's `'loaded'` branch (after `ingestPacket` drew frame 0); `showFailure` sets `data-replay-error` (`:14-20`). (iii) recording starts at the first game tick (writer sees no lobby: `server.nim:292-365`), `startTick = 0`, `seekReplay` clamps `clamp(tick, 0, maxTick)` and transport commands clamp to `player.startTick` (`replay_runtime.nim:164-169, 341-344`) — playback opens at the game start for the degenerate reason that no lobby is recorded. (iv) `config.nims` diff vs starter: renames + one documented `--preload-file client/art` only; no MODULARIZE/EXPORT_NAME; worker/shell are the starter's byte-for-byte modulo the `ctf`→`cc` rename (diff shown empty otherwise); `Dockerfile.replay-viewer` asserts the flag set at build time; the smoke's `loaded: true` is the executed evidence. |
| 14 chrome is the starter's | PASS | (i) `sha256(client/chrome_common.js)` == `7ace7287…72f7c` == starter's, 40 022 B both (diffed myself). (ii) `replay_broadcast.html` regenerates **byte-identically** from the starter via the committed `scripts/build_broadcast_page.py` (I re-ran it: sha256 156782… equal) — 174 718 B vs the starter's 234 773 B, every removed span accounted for by the design note's own removal list (raycast FPV pipeline, viewpanel/zoom/minimap, povBadge, fpv-hp/gear/map, .ec-heart PNGs); not a rewrite. (iii) `relayout()` sets `--band`/`--topband`/`--hudscale` on `document.documentElement` (`:2906-2932`); nothing fixed-positioned in the block (test 45 asserts `position: fixed` absent and every added panel anchored to `var(--topband)`); `#endcard { top: var(--topband,0px); bottom: var(--band, 0px) }` (`:819-841`), shown via `#endcard.on`, every non-gameover frame removes `on` (`:1771`); beats are labelled `<button class="beat-marker <kind>" title aria-label>` seeking on click (`:3265-3281`) with CSS for exactly the six emitted kinds (`:3179-3186`) and none of the eight ctf kinds. (iv) `#viewpanel`/minimap/zoom removed — markup, CSS, wiring (`attachMinimap(` appears nowhere in the page), test ids asserted absent by test 45. |
| 15 every drawn string fits its frame | **BLOCKING — B1** | canvas_text total 0 in both steps (covers nothing, by the item's own rule — and truthfully: zero `fillText`/`strokeText` in the client); `--strict-text-bounds` present on both steps (ci.yml:338, :379); the fixture exists and runs (its own step, green) but its SAY is 133 runes not the claimed 140, it asserts no string length, and it never measures the `.feed-row` that carries the model text — the one string whose CSS (`nowrap`, `max-width: none`, sized for a 10-char cap this fork raised to 140) makes overflow at 360 px computable from the tree. |

## Fixer report audit

| finding | fixer said | I verified | agrees |
|---|---|---|---|
| F1 | fixed, e9902ccb | `replays.nim:233` caps in the codec; test 37 asserts 200 whole emoji from 237 in; CI green | yes |
| F2 | fixed, e92e5e8c | `llm.nim:251` now "A brake will NOT save you: it switches the servo off."; no folklore line remains in the constant | yes |
| F3 | fixed, 282f0b6a | `continuous_control.nim:46-47` randomises before `config.update`; pinned seed still wins | yes |
| F4 | fixed (docs), a2fb338f | `docs/PHYSICS.md` §The baseline bands present; numbers match what test 25 asserts | yes |
| F5 | fixed, 3f4bfb64 | `body.nim:140` `highY = mm(20_000)` — the world-box ceiling; inside the reachable world the hopper has exactly the note's two fall conditions | yes |
| F6 | fixed, 73b3f44a | `report.nim:104,111` — `joint_count` scalar, `joints` array; `joints_detail` appears nowhere in the tree | yes |
| F7 | no change, deliberate | `cc_beats` key avoids the inherited `ingestBeats` div markers; labelled buttons satisfy item 14 — correct call | yes |
| F8 | NEEDS-DESIGN, no change | real cause lives in the `fallback` chat record; feed label generic; no checklist item falsified | yes |
| F9 | disputed, item 15 met | facts verified (0 canvas text, both gates on, fixture runs) — but I do **not** accept "item 15 met": the fixture is not the fixture item 15 specifies (133-rune say, no full-length assertion, feed row unmeasured) → B1 | **no** |
| F10 | no change, flag required | `--preload-file client/art` documented in-file; smoke `loaded:true` proves the bundle; no MODULARIZE mixture | yes |
| F11 | no change, documented | replay 131 999 B in the head run; `MaxOrderRecordRunes` bound; PHYSICS.md #15 records it | yes |
| F12 | no change, inherited chrome | dead inherited plate internals overwritten by `ccPlate` every frame; smoke scorebug shows no paintbot residue | yes |
| F13 | fixed, b682a19b | list now carries `Cap<`, `hill`, `kill`; the two remaining narrowings (`flagicon`, dropped `team`) are forced and documented in the test | yes |
| F14 | fixed, fece3c91 | `continuous_control_player.nim:74-89` `ackFrame()` sends 0x85 after every frame, guarded; called at `:114` | yes |
| F15 | disputed, starter's route | the starter serves the identical `/client/replay`; the manifest declares only the static bundle; item 3 gates what is declared — agree | yes |
| F16 | fixed (docs), 04e39c93 | PHYSICS.md now numbers 15 divergences incl. crouch, seek-rewind, tuning `--check`, unwired wasm_replay_smoke.cjs, ~130 KB replay | yes |
| F17 | fixed, a8db2b32 | `replays.nim:235-246` — tick parameter checked against the positional index, raises at the offending write; wire format unchanged | yes |
| F18 | no change, no defect | `isqrtQ16` confined to report.nim as the note requires; unused because slip is reported as \|vx\| | yes |

## Non-blocking observations

- **Scrub 100 % readout lands mid-seek.** Head run: `50%="… TICK 258/468" 100%="… TICK 259/468"` — a 100 % seek of a 1 488-tick replay sampled while the rewind-and-re-step (documented divergence #12) was still converging. Seeks do move (values differ across probes) and the soak proves advancement; no checklist item gates seek latency. A `viewer_smoke.mjs` that re-reads after a bounded settle would make item 13(iii) evidence crisper.
- **`game.docs` content type is `uri`, not the checklist example's `text`** — the design note's own documented choice (design.md:1814-1820); shape `{type,value}` is what the item describes. Test 40 does not run the real CLI validator (documented in the test: the release workflow's `coworld build` is where it runs); local certify in `coworld-release.yml` is the gate that would catch a rejected docs shape before anything ships.
- **The champion prompts still tell a cog a brake saves a fall** (`tools/ci/policies.json`) — operator strategy strings, contradicting documented divergence #9; costs the champions performance, gates nothing.
- **`tests/helpers.nim` records order chat records with `view = nil`**, so tests 34–36 re-derive a replay ~⅓ the size the server writes; the real 132 KB file is exercised by the CI smoke.

BLOCKING: 1
