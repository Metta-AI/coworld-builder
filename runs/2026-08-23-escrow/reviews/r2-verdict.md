blocking: 0

# r2 verdict — escrow

Head: `798d9504155b23b60210a166bd1871a1a4538327` (`Metta-AI/cogame-escrow`, main)
Checklist: acceptance checklist as supplied in the judge brief (prompts/30-review-loop.md §ACCEPTANCE CHECKLIST)
Independent read written before reading fixes: **yes** — all of `src/`, `tests/`, `tools/`, `replay-viewer/`, `client/chrome.css` + `renderer.js` (name map, feed, replay driver), the manifest, all three workflows, `docs/tuning.md`, `git log -p -- tests/`, and the head CI log (run 32648809792, `gh run view --log`, 3509 lines) were read and my checklist notes were complete before I opened `r2-review.md`; `r2-fixes.md` was opened last, only to audit dispositions. No contamination.

CI evidence at head: `gh run list -R Metta-AI/cogame-escrow --branch main -w ci.yml` → run **32648809792**, `headSha 798d9504…`, conclusion **success**; jobs `test` ✓ / `docker-smoke` ✓ / `wasm-viewer` ✓, every step `success` (job/step list pulled via `--json jobs`), including `Re-run the baseline tuning sweep` and `Load the bundle in a real browser`. The four commits since the review's sha (`dac4fc4`) — `4d33973`, `baeecc8`, `555c02f`, `798d950` — each ran CI individually, all `success` (runs 32648479931, 32648585268, 32648683875, 32648809792; verified from `gh run list`, not from the fixer's table).

---

## Standing blocking findings

None. Every checklist item verifies from the tree or from cited CI evidence at this head, including the two carried questions:

- **r1's B1 / r2's F1 (item 7, "tuned with a grid harness, not guessed") is discharged at head** — see Refuted/Resolved below. I verified all four legs the brief names:
  1. *The harness exists and is committed*: `tools/tune_baseline.nim` (new in `4d33973`).
  2. *It drives the shipped sim/baseline code, not a reimplementation*: `tools/tune_baseline.nim:23` `import escrow/[llm, sim]`; its `play` proc (`:46-65`) is `initSim` / `pendingSeats` / `scriptedAction(sim, seat, kinds[seat], params)` / `applyMove` — the same calls `server.nim`'s turn loop makes — with every decision put through `sim.validateMove` (`:57`), the event log scanned for `reject` (`:62-64`), and `liveContracts` checked against `MaxLive` (`:59-61`). The knobs it sweeps are the very object the baseline runs on: `TraderParams` (`src/escrow/llm.nim:36-47`), taken as a defaulted parameter by `bundleValue`/`twoFillNeed`/`traderAction`/`scriptedAction` (`llm.nim:196-308`).
  3. *A committed record exists*: `docs/tuning.md` — the full recorded 45-cell grid (5 seeds, 16 turns), a wider 66-cell sweep (`--units=2..12 --fills=1..6`) proving the argmax is not a range edge, and the reasoning for the choice, including an explicit statement that the price axis is degenerate under all-scripted play rather than a pretended measurement.
  4. *CI wiring + constants match the recorded conclusion*: `.github/workflows/ci.yml:160-164` runs `nim r … tools/tune_baseline.nim --quick --check` in the `test` job; `--check` exits non-zero if the shipped cell is beaten, illegal, out of range, or under the 1.3× canary (`tune_baseline.nim:229-254`). Shipped `DefaultTraderParams` (`llm.nim:191-194`: `housePrice 3/3/3/1, tradeUnits: 6, needFills: 3`) equals the record's `shipped: price=3 tradeUnits=6 needFills=3` (`docs/tuning.md:103,186`); the recorded argmax row (units 6 / fills 3, 1074 minted, 2.27×) is the same cell — the price-2 argmax label is a tie-break over three identical price columns, which the record and the harness both say out loud. At head CI the step printed `argmax: price=3 tradeUnits=6 needFills=3 -> 786.0 hearts minted (2.15x …)` then `check: the shipped cell is still the grid's best legal cell` (log lines 2457–2460).

## Refuted / resolved reviewer findings

### F1 — item 7's tuning sentence had no artefact in the tree → RESOLVED AT HEAD (was true at `dac4fc4`; fixed by `4d33973`)
- Evidence: as itemised above — `tools/tune_baseline.nim`, `docs/tuning.md`, `llm.nim:36-47,169-194` (`TraderParams` / `DefaultTraderParams`, adopted argmax 6/3 replacing the guessed 4/2), `ci.yml:160-164` gate, green at head. The sweep *changed* the shipped values (4/2 → 6/3), which is the strongest possible evidence the process ran and was not decorative. Test 14's canary now shows `traded 1074 vs autarky 474` per seed in the head log. A finding that was true and has since been fixed is resolved, not standing.

### F2 — over-cap reject rendered as a refused "draft" with a raw `over_cap:` token → RESOLVED AT HEAD (advisory anyway; fixed by `baeecc8`)
- Evidence: `client/renderer.js:822-833` — `var overCap = /^over_cap:\s*/.exec(event.text || "")` renders `"<name> tried more actions than one turn allows — <detail>"`, every other reject keeps the draft line. Rendering only; the event, the reason code and test 5d's `startsWith("over_cap")` assertions are untouched. `wasm-viewer` loaded the real smoke replay at head (`{"loaded":true,"ms":280,…,"feed_lines":66}`).

### F3 — over-cap rejects are not re-derived on replay → DISMISSAL VERIFIED, stays advisory
- I checked the fixer's dismissal from the code, not from its prose. Observed: `sim.nim:511-516` emits the reject then `setLen`s; the recorded `move` event carries the **truncated** lists (`sim.nim:533-534`); `replayMatch` rebuilds the `Move` from that event (`sim.nim:950-956`), so the guard cannot re-fire and the re-derived event log lacks the reject. Consequence for item 2: none — item 2 is about **state** re-derivation, and no frame differs: a reject moves nothing, `tableStateJson`'s only event-derived section is `recentJson`, which reads `evSettle`/`evExpire` only (`sim.nim:837-869`); `replayMatch` checks `evTurn` seat state (`:943-948`); the viewer's feed reads the **recorded** `payload.events` (`renderer.js:1319,1338-1341`), so the spectator still sees the reject. And the event is unreachable on every live path (`parseDecision` caps at `llm.nim:645-654`, the baselines never exceed, `replayMatch` feeds truncated moves) — only a hand-built test `Move` reaches it, which test 5d is. No checklist item is falsified. The reviewer's own review says the same (its F3 is filed non-blocking); the fixer's "needs a schema change, buys nothing" is accurate.

### F4 — no test exercised the `scripted == false` line / mixed batch → RESOLVED AT HEAD (advisory anyway; fixed by `798d950`)
- Evidence: `tests/test_bot.nim:12-65` (a real loopback HTTP stub, doubly bounded: stop flag + 10 s idle retirement, every wait a timed `selectRead`/`recv`) and `:366-427` (test 18): seat 0 registered `skTrader`, seats 1–3 model-driven against the stub; asserts `stubServed == Seats - 1` (no retry batch), seat 0 `scripted: true` and equal to `scriptedAction`, seats 1–3 `scripted: false` carrying the stub's `say`/`notes` (proof the reply parsed AND passed `validateMove`), and the recorded move events split 1 scripted / 3 not. This is exactly the mixed batch the review's "could not determine" asked for. Head CI: `[OK] 18.` in both debug and release (log lines 2319, 2357).

### F5 — the player's frame loop was an unbounded blocking read → RESOLVED AT HEAD (fixed by `555c02f`)
- Evidence: `src/escrow_player.nim:36-43` — `FrameTimeoutMs = 5_000`, `IdleTimeoutSeconds = 300.0`; `:63-79` — `socket.receiveMessage(FrameTimeoutMs)` inside a `try` (whisky *raises* on a closed socket; the old `isNone`-means-closed comment was wrong and the close escaped unhandled), `none` re-arms, and 300 s of total silence exits (a turn is bounded at 125 s, so 300 s means the game is gone, not slow). Every wait in the tree now has an explicit bound. The fix also corrected a latent unhandled-exception exit path, i.e. it strengthened rather than merely bounded.

## Checklist pass (independent)

| item | status | evidence (path:line or run) |
|---|---|---|
| 1 CI green, no test loosened | PASS | Run **32648809792** `success` at `798d9504…`, all 3 jobs and every step `success`. `git log -p -- tests/` read hunk by hunk over the whole run history (6 commits touch `tests/`): `19d0959` creates both files; `d68c5ec` corrects hand-computed expectations that were one production step behind the (correct) sim — exact array equalities kept and *added* (`turn == 1/2` checks, per-seat stock for all four profiles); `3b6c3eb` adapts test 15 to the `SeatDecision` type (`decisions[index].offer` → `decisions[index].move.offer`) and **adds** `check decisions[index].scripted` + test 17; `122cf57` adds test 5d; `1ecfa58` adds two `endsWith("…")` assertions; `798d950` adds the stub + test 18. **No assertion deleted, no tolerance widened, no skip/xfail/`--skip`, no test file removed.** 48 `[OK]` lines at head across both files × debug and `-d:release`; the only `FAILED` string in the log is the workflow's own echoed script text (line 2266). |
| 2 Replay re-derivation | PASS | `sim.nim:929-965 replayMatch`: `move` events are ground truth and replayed through `applyMove`; recorded `turn` events are *checked* against the seeded re-derivation (`:944-946`, raises on mismatch); derived events discarded and re-derived; deadline stops honoured (`evEnd` → `closeHorizon` + `settle`). The viewer draws from that same re-derivation: `escrow_replay.nim:9-11` imports `escrow/sim`, `:37-39` builds `states` with `replayMatch`; `renderer.js:1338-1341` draws `payload.states` and never keeps a parallel recording. Asserted: `test_sim.nim:604-630` (frame count = events+1, final frame `tableStateJson` **and** `resultsJson` equality, honoured deadline stop, tampered turn event rejected). |
| 3 Static viewer | PASS | `coworld_manifest_template.json:15-17` `"replay_viewer": {"bundle": "static-replay-viewer"}`; `tools/build_replay_viewer.sh` present, committed 100755 (`git ls-files -s`), is the `coworld build` hook (release workflow's certify gate rejects a non-static viewer, `coworld-release.yml:196-202`); the shell fetches only the `?replay=` URL with a 20 s AbortController bound (`static_replay.js:74-96`), all other assets relative. No pod-served replay viewer is declared anywhere in the manifest; the `/client/replay` route in `server.nim:497` and the protocol-text mention are the starter's live-server pages, identical in `cogame-bullwhip`, and are not the declared viewer. |
| 4 Both name spaces | PASS | Prompts and player frames carry aliases only (`llm.nim:414-419,495-499` build from `sim.names`; `server.nim:421` welcome, `:111` state; final frame swaps results names for aliases, `server.nim:199-211`). Viewer maps alias → policy name for non-baseline seats: `renderer.js:735-762` `makeNameMap`/`isBaselineFiller`, applied to scorebug, feed and endscreen; replay path wires `payload.names`/`payload.policyNames` at `renderer.js:1330`. `results.names` = policy names (`sim.nim:794`). |
| 5 Degrade-never-hang | PASS | Connect wait bounded (`server.nim:243-249`, ≤ `playerConnectTimeoutSeconds`); LLM waits bounded — one `curly.makeRequests(batch, client.timeoutSeconds)` per attempt, ≤ 2 attempts (`llm.nim:707-742`); pre-turn lookahead `epochTime() + maxTurnSeconds > playDeadline` → `endEarly()` + `reason="deadline"` + break (`server.nim:278,293-303`; `maxTurnSeconds` = 125 s, `playDeadline` = start + 0.6 × 1200 = 720 s; `endEarly` idempotent, `sim.nim:541-549`, asserted test 9); pacing clamped by `sampleEpisode` (`sim.nim:74-77`); no-credentials path has zero network waits (test 15, < 2 s bound); the player's read is now explicitly bounded (5 s frame / 300 s idle, `escrow_player.nim:36-43,63-79` — the r2 F5 fix). No unbounded loop or blocking read remains. |
| 6 num_agents | PASS | `num_agents: 4` in `config_schema` (:69-74), `variants.standard` (:361), `variants.sprint` (:387), `certification.game_config` (:411). `tools/ci/docker_smoke.sh:110-151` enforces all four invariants before any container starts (present; positive integer; `len(certification.players)==4`; `len(certification.game_config.players)==4`) with `SEAT-COUNT FAIL:` prefixes, plus the independent `SMOKE_SEATS` cross-check (`:54,146-151`). Grepped the full head log: **zero `SEAT-COUNT FAIL`**; `smoke OK: seats=4 results=267B replay=10664B reason=complete` (log 1940). |
| 7 Scripted baseline full episodes, legal + tuned | PASS | First sentence: `test_bot.nim:77-152` — `validateMove == ""` before every scripted apply, `reason == "complete"`, `turnsPlayed == turns`, no reject/`ok:false` attributed to a scripted seat, offers parse with DUE inside the window and every `n` in bounds, gives/signs ≤ 2, `liveContracts ≤ MaxLive` after every apply, 4 seeds × 3 mixes < 2 s; `[OK] 13.` both modes. Second sentence: **now discharged** — committed harness `tools/tune_baseline.nim` driving the shipped `escrow/llm` + `escrow/sim`, recorded grid + reasoning in `docs/tuning.md`, adopted argmax 6/3 replacing the guessed 4/2 in `llm.nim:191-194`, CI gate `ci.yml:160-164` green at head (`check: the shipped cell is still the grid's best legal cell`, log 2460). |
| 8 LLM reply handling | PASS | Tolerant extract (`llm.nim:531-543`, first `{…}` out of prose/fences, asserted test 16); exactly one retry carrying the exact error (`for attempt in 0 .. 1`, `llm.nim:707,717-719`); terminal fallback to scripted (`llm.nim:743-747`); fallback recorded — `SeatDecision.scripted` travels with the move into `applyMove` → `event.scripted` → replay JSON (`llm.nim:56-65`, `server.nim:317-331`, `sim.nim:531,678`), asserted from **both** sides: tests 15/17 (fallback = `true`) and test 18 (accepted reply = `false`, mixed batch). |
| 9 Rune-safe truncation | PASS | `clip` (`sim.nim:79-89`) and `cleanText` (`llm.nim:606-613`) both `runeLen`/`runeSubStr`; applied to offer/say/notes at `applyMove` (`sim.nim:517-522`), reply fields at parse (`llm.nim:637-641`), prompt cap (`server.nim:466-467`), quoted reply head (`llm.nim:538-540`), retry hint (`llm.nim:740`). Tests 12 and 16 feed multi-byte input with an emoji exactly on the cap and assert `validateUtf8() == -1` on the fields **and** the whole replay bytes plus `parseJson` success. The four byte-index slices in transport error paths (`llm.nim:579,588,593,602`) reach only stdout and the rune-safe hint, never the replay — re-traced at head. |
| 10 Manifest validates | PASS | `game.docs.readme = {"type":"text","value":…}` (:249-251); `pages` = 2 × `{id,title,content:{type:"text",value}}` (`rules.md` :254-261, `dsl.md` :262-269); `game.protocols` carries both `player` (:239-241) and `global` (:243-246). Parsed with `json.load`. |
| 11 Viewer legible at 360 px | PASS | `client/chrome.css:280-292` — `.plate-name { … min-width: 3.2em; flex: 1 1 auto; }` (both exact declarations present); `:484-488` `@media (max-width: 640px) { .plate-label, .plate-stock { display: none; } … }`. |
| 12 Release order and scaffold | PASS | `coworld-release.yml` step order: Build manifest (:153) → Certify (:167) → Upload policies (:206, explicitly before upload-coworld) → Upload coworld (:304) → Put secret (:342, explicitly after). ci.yml's smoke runs against an image built in the same job (`Build image` :191 before `Raw-Docker episode smoke` :193). All three workflows present; `docker_smoke.sh` mode 100755; `tools/ci/policies.json` = 4 distinct policies — `escrow-drafter` + `escrow-swapper` (both `PLAYER_PROMPT`) + `escrow-trader`/`escrow-hoarder` (`PLAYER_SCRIPTED`) — champion #2 (second `PLAYER_PROMPT` entry, `escrow-swapper`) carries `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"` (:15). The three-name placeholder grep over the five files finds no matches, so the gate exits 0 (run in the checkout). |
| 13 Viewer executes | PASS | Run **32648809792** `wasm-viewer` green **including** `Load the bundle in a real browser` (step conclusion `success`; log 3411: `{"loaded":true,"ms":280,"clock":"TURN 0 / 6 · WAITING ON 4",…,"feed_lines":66}` plus three distinct scrub readouts, against the replay docker-smoke produced in the same run); `needs: docker-smoke` (`ci.yml:226`); no `continue-on-error` anywhere in the workflows. Markers: `static_replay.js:133` sets `data-replay-loaded="true"` inside the double rAF on the first drawn frame (alongside the `ready` bridge message); `:63` sets `data-replay-error` in `fail()` (missing `?replay=`, fetch timeout, wasm rejection, outer catch), removed on retry (:146) and success (:114) — both from the shell's own code paths (`renderer.js:1400` setting it too is the declared-deliberate second writer). Matched pair from one starter: `config.nims:44-45` `-s MODULARIZE=1 -s EXPORT_NAME=EscrowReplayModule`; the shell calls the factory `EscrowReplayModule()` and awaits the promise (`static_replay.js:150-158`); `grep -rn onRuntimeInitialized` over the tree returns only the comment warning against it. |
| Simultaneous batch | PASS | All pending seats go out as ONE `RequestBatch` per attempt (`llm.nim:713-722`), one `makeRequests` call; the server decides all pending seats in a single `decideAll` per turn, outside the lock (`server.nim:304-314`). No sequential per-seat call anywhere. |

## Fixer report audit

| finding | fixer said | I verified | agrees |
|---|---|---|---|
| F1 | fixed, `4d33973` | Harness committed, drives shipped `escrow/llm`+`escrow/sim` (import + `scriptedAction`/`applyMove`/`validateMove` call sites read), recorded grid in `docs/tuning.md`, shipped constants = recorded conclusion (6/3, price 3; argmax tie across identical price columns stated openly), CI `--quick --check` step green at head (log 2457-2460). Fixer's headline numbers (1074 vs 474, 2.27×; 4/2 = 834, 1.76×; wider sweep ties only 6/6) match the committed record. | yes |
| F2 | fixed, `baeecc8` | `renderer.js:822-833` over-cap branch present, draft line preserved for parser rejects; viewer smoke loaded the head replay | yes |
| F3 | DISMISSED (design change, buys nothing) | Verified from the code: state frames identical, feed reads recorded events, event unreachable on every live path; item 2 unaffected. Dismissal correct; stays advisory | yes |
| F4 | fixed, `798d950` | Test 18 + bounded stub read in full; asserts the mixed batch and both flag values on recorded move events; `[OK] 18.` both modes at head | yes |
| F5 | fixed, `555c02f` | `receiveMessage(FrameTimeoutMs)` + 300 s idle cap + close-raise handled; docker-smoke at head completed with results + replay | yes |

## Non-blocking observations

- `sim.history` / `TurnRecord.moves` remain write-only dead state (`sim.nim:121,476`, `types.nim:169-171`); no reader anywhere. Inherited starter shape; no checklist item touches it.
- The over-cap `reject` is still not re-derived on replay (F3, dismissed); if anyone ever makes it reachable from a live path, the recorded-vs-derived event asymmetry should be revisited — today it cannot fire outside a hand-built test move.
- `validateMove` does not check an offered contract's `postedTurn == turn − 1` for signs (`sim.nim:220-235`) while `applySign` does (`:274`); unreachable divergence today, since every `csOffered` contract visible at decision time was necessarily posted last turn (offers register at step 5 and expire at step 6 of the following turn).
- The quick CI slice of the tuning sweep (2 seeds, 12 turns, one price column) is a bounded regression gate, not the full recorded grid; the full grid lives in `docs/tuning.md` and is reproducible with the documented flags. This is exactly what the fixer claims, and it is the right trade for a ~1 s CI step.

BLOCKING: 0
