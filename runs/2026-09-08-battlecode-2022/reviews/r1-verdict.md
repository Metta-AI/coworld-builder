blocking: 0

# r1 verdict — 2026-09-08-battlecode-2022 (`Metta-AI/cogame-battlecode`, bc22 year module)

Head: `1fc96b4966661232a00146368b5ff906c0941903` (merge of PR #8; diff judged: `47f36001..1fc96b4`, 119 files, +20573/−117)
Checklist: `prompts/30-review-loop.md` §ACCEPTANCE CHECKLIST (items 1–15 + the parallel-batch rule)
Independent read written before reading the review: **yes** — checklist first, design note in full (one read), then the diff, the head tree, and the CI logs; `reviews/r1-review.md` was read only after the item-by-item pass below was formed. No `r1-fixes.md` exists in the run directory; no fixer report was read.

## Standing blocking findings

**None.** Every checklist item was verified from the tree or from cited CI evidence; nothing was left unverifiable.

## Checklist pass (independent)

| item | status | evidence |
|---|---|---|
| 1 CI green, no test loosened | **PASS** | Run **34288986734** on `main` at `1fc96b4`, `conclusion: success`, all ten jobs green (verified via `gh run view`, not taken from the review). `git diff 47f36001..1fc96b4 -- tests/`: 29 files, +5627/−21. All 21 deletions accounted for: six→seven-year widenings in `test_manifest.nim` (`variants.len 6→7`, `policies 24→28`, `pages 8→9`, `owned 6→7`) and `test_viewer.nim` (five-way→six-way discriminator, statrail list gains bc22), the `replay-bc23.json` fixture regenerated for `GV09→GV10` + new `sheet_envelope` fields with **hash chains byte-identical** (I diffed the two JSON documents structurally: only `game_version` and the three `sheet_envelope` additions changed), and `871a476` (below). No `skip`/`xfail`/`t.Skip`/`--skip` anywhere in `tests/`; the one "SKIP" string (`test_bc22_survival.nim:131-135`) is a hard `check(false)`. Test job log: **all 20 `test_bc22_*` shards ran twice** (debug + release), every one `ok`; `BROKEN-CONTROL: correctly red` twice. `871a476` (+52/−21, `tests/test_bc22_replay.nim` only): the removed six assertions are re-added verbatim one indent deeper under `if haveGame(...)`, and `haveGame` itself calls `check(name, games.len > 0)` (`test_bc22_replay.nim:80-87`) — a failure now prints a FAIL line where it previously crashed with `IndexDefect`. The config change (`perGameBudgetSeconds 0→default 90`) removes a wall-clock flake (`playMatch` clamps 0 up to a ONE-second budget — `max(1, min(...))`), not an assertion. Net: **0 assertions removed, 0 loosened, +7 added (six guards + the guard's own check).** |
| 2 Replay re-derivation | **PASS** | `replay.nim:270-312`: `Deriver.advance()` steps the sim one round and compares `session.hashChainHex()` against the recorded per-round chain — frame by frame, `mismatchRound` is the first divergence. `tests/test_bc22_replay.nim` `record()` re-parses the **written bytes** and asserts `deriver.mismatchRound < 0` on multiple episodes including a 2000-round `annihilated` game and a Singularity mirror. The viewer drives the same `Deriver` compiled to wasm (`replay-viewer/bc_replay.nim`); node smoke on both the smoke replay and the committed fixture reports `mismatch_round: -1`, `game_version: GV10`. Nothing per-round is stored in the replay — the chain values (rules.nim:299-325) are all re-derived. |
| 3 Static viewer | **PASS** | `coworld_manifest_template.json`: `"replay_viewer": {"bundle": "static-replay-viewer"}`. `tools/build_replay_viewer.sh` present, mode 100755, asserted and executed by `ci.yml`'s `wasm-viewer` job. The two `/client/replay` strings (`client/broadcast_core.js:372,385`) are a pathname→websocket mapping table in the inherited **live-broadcast** core, byte-identical to `47f36001` **and** to `/workspace/starters/coworld-ctf/client/broadcast_core.js` (sha256 `226aea03…` at all three); the static bundle's shell reads its replay URL from `location.hash`/`?replay=` and never serves at that path. Pre-existing, untouched by this diff, and not a pod path the viewer contacts — see the reviewer audit (N2). |
| 4 Both name spaces | **PASS** | `decide.nim:625-636`: the brief carries `alias`/`opponent_alias` only, and the doc comment names the opponent's "real player name" as hidden. `replay.names[]`/per-seat `name` recorded; scorebug plate (`replay_broadcast.html:5893` `pl-name`) and endcard draw the real names. Aliases are Clan Ash / Clan Basil. |
| 5 Degrade-never-hang | **PASS** | `decide.nim:1023` doctrine budget as a hard duration; `:1044` `attempt < 2`; `:1045-1060` budget-exhaustion clears `open` with recorded `timeout` fallbacks; `:1081` per-attempt deadline handed to `CURLOPT_TIMEOUT`. `match.nim:470,480` match and per-game budgets; `rules.nim:427-437` monotonic per-game guard every 32 rounds. Committed bc22 variant: `attempt1Ms 20000 + retryMs 12000` inside `doctrineBudgetMs 45000`; `perGameBudgetSeconds 110`, `matchBudgetSeconds 340`, `connectTimeoutMs 25000` → 30+45+340+30 = **445 s ≤ 720 s**. No unbounded loop or blocking read found on the decision or match paths. |
| 6 num_agents | **PASS** | `num_agents: 2` inside **all seven** variants' `game_config` (parsed the template myself), absent at every variant top level; `certification.game_config` keeps `year: bc26`, `num_agents: 2`; `certification.players` = `[awu, scaffold]` = `player[]` exactly (the deliberate bc26-cert pin holds). `tools/ci/docker_smoke.sh:141-227` enforces all four invariants plus the `SMOKE_SEATS` cross-check (the substituted default `2` at line 82) and refuses a `num_agents` override, each exiting non-zero with the `SEAT-COUNT FAIL:` prefix. **`grep -c "SEAT-COUNT FAIL"` on the docker-smoke job log of run 34288986734 → 0**; all seven episodes ran with `seats=2`, and the bc22 episode ended `reason=complete` with all player containers exiting 0. |
| 7 Scripted baseline full episodes, legal | **PASS** | `test_bc22_replay.nim` asserts `reason == epComplete` on all-scripted episodes to the natural end (700 rounds, 2000-round annihilation, clinched best-of-three, Singularity mirror). Legality: I read **all eleven** guarded action procs — `doMove`, `doBuildRobot`, `doAttack`, `doRepair`, `writeSharedArray` (world.nim:631,655,702,731,~757), `doTransform`, `doMutate` (buildings.nim:34,67), `doMineLead`, `doMineGold`, `doTransmute` (economy.nim:132,150,171), `doEnvision` (anomaly.nim:275) — every one has the `if not can…: w.refusedActions += 1; return false` prologue (`doDisintegrate` legitimately has no precondition). `test_bc22_baselines.nim:54-64,136-137` gates `refusedActions == 0` over 9 chassis×map games plus the 6 competence games, and `opsUsedPeak` against the budget. Tuning: `test_bc22_knobs.nim` (paired-seed sweep, 31 checks × 2 modes) and `test_bc22_survival.nim` with measured healthy/broken ranges recorded in its header and the `-d:bc22BrokenChassis` negative control **correctly red** in this run's log. |
| 8 LLM reply handling | **PASS** | Fence-tolerant extraction (year-neutral `sheet_common`) plus the new five-rule envelope resolver (`sheet.nim:97-149`, unwraps at most once, key recorded); retry exactly once (`attempt < 2`) with a typed `doctrine_retry` cause; fallback to the scripted sheet with `results.fallbacks[slot]` set, a `doctrine_fallback` event and the literal `falling back` log line. `fallbacks` is in the smoke's closed key set and asserted `[0,0]`. |
| 9 Rune-safe truncation | **PASS** | `truncateRunes`/`truncateBytes` in `sim_types.nim`; applied to notes, motto, unknown keys, the 16 KB reply cap and provider error text. `test_bc22_sheet.nim:180-207` feeds astral-plane input at the caps and asserts `runeLen` equals the cap, the input really was multi-byte (`runeLen*4 >= len`), and `validateUtf8() < 0` on the byte-cap cut. |
| 10 Manifest validates | **PASS** | `game.protocols` carries both `player` and `global`; `game.docs` is `readme` + nine `pages`, each `{id,title,content:{type,value}}`. `end_reason` enum gains exactly `more_archons`/`more_gold_net_worth`/`more_lead_net_worth`, reuses `annihilated`/`coin_flip`/`abandoned`, has no `resignation`. The `type` discriminator is `"uri"`, not the item's literal `"text"` — pre-existing across all six shipped years, unchanged by this diff for `readme`/`protocols` (the one new page follows the same shape), prescribed at `design.md:1896-1902`, and the manifest **demonstrably validates**: `ci.yml`'s "The coworld CLI accepts the manifest template" step runs the installed CLI's own `validate_upload_manifest` and is green in run 34288986734, and six releases of this manifest have gone canonical. The item's operative clause ("manifest validates") holds; the literal-vs-tree difference is recorded under observations. |
| 11 Legible at 360 px | **PASS** | `replay_broadcast.html:2580` `#scorebug .plate-name { flex: 1 1 auto; min-width: 3.2em; }` verbatim; `@media (max-width: 640px)` hides `.plate-sub` and drops bc22 labels to glyphs; the `--killfeed-overlap` gate ran at 360/720/1280 px at FIT and 2× on all seven replays. |
| 12 Release order and scaffold | **PASS** | `coworld-release.yml` (untouched by the diff): Build manifest → Certify locally → **Upload the policies** → Upload the Coworld → wait-canonical → Put the Coworld secret. All three workflows present; `docker_smoke.sh` executable. The placeholder gate exits 0 — I ran the exact grep from the item over the five files: no `<slug>`, `<IMAGE>` or `<SEATS>`. `policies.json`: 28 entries, four per year; the bc22 four are `battlecode-bc22-rush` (PROMPT), `battlecode-bc22-transmuter` (PROMPT, **`"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`**), `battlecode-wololo` and `battlecode-examplefuncsplayer22` (SCRIPTED); prompts differ; `test_manifest.nim` pins indices 24-27 by name, owner and filler names. |
| 13 Viewer executes | **PASS** | `wasm-viewer` `needs: docker-smoke` (`ci.yml:3138`); the "Load the bundle in a real browser (ALL SEVEN years' replays)" step **ran and succeeded** in run 34288986734 (step-level conclusion checked via `gh run view --json jobs`, not job colour); the only `continue-on-error: true` in `ci.yml` (line 777) is bc20's engine-from-source tier, not the smoke. bc22 runs at `--timeout 120 --soak 15` as designed. `static_replay.js` sets `data-replay-loaded="true"` on the first drawn frame (line 180) and `data-replay-error` on failure (lines 14-20). **Lobby dwell is structurally impossible in this format**: `replay.nim:270-279` builds the frame axis from in-game rounds only (frame 0 *is* game 0 round 1) and every frame packet emits `"st": 0`; there are no recorded lobby frames to dwell through. **Bootstrap agreement**: `replay-viewer/config.nims` has neither `MODULARIZE` nor `EXPORT_NAME`; the worker uses `var Module = {}` + `Module.onRuntimeInitialized` + trailing `importScripts` — a matched non-MODULARIZE pair, statically asserted by `test_viewer.nim:39-52` together with a `Module._bc_*`-vs-`EXPORTED_FUNCTIONS` cross-check. The smoke's `loaded: true` is in the log for all seven replays. |
| 14 Chrome is the starter's | **PASS** | `chrome_common.js` sha256 `f7860b4c…` and `broadcast_core.js` sha256 `226aea03…` **byte-identical at `47f36001` and at head** (computed myself); `git diff 47f36001..1fc96b4 -- client/` lists only `replay_broadcast.html`. (For this MOD the starter is the host repo itself; `broadcast_core.js` additionally matches the coworld-ctf copy byte for byte, and `chrome_common.js`'s divergence from coworld-ctf is a pre-existing pinned fork — the hash pin in `test_viewer.nim:31-33` predates this run and is unchanged.) `replay_broadcast.html` is +709/−26: the bc22 block appended under the banner `BC22 additions to the inherited cogame-battlecode chrome` (line 3415), and I read **all 26 deleted lines**: the bc26-noun `ec-wincond`/`ec-how` endcard block, one doctrine-words render line, the statrail list, and the two six-way discriminator guards — exactly the four shared-endcard fixes plus the seven-way guard the design note authorises as the only non-additive edits (`design.md:2000-2005`). Transport rules: `relayout()` sets `--band`/`--hudscale`/`--topband`/`--statrail` on `document.documentElement.style` (`:6192-6210`); bc22 boxes ride `calc(var(--band,0px) + …)`; `#endcard { bottom: var(--band, 0px) }` shown with `.on` (`#endcard.on { display: flex }` at `:1878`) and dismissed by `seek()`, transport buttons and keyboard; beats are labelled `<button>`s built by `buildBc22BeatButtons` (`:5451`) with spoilers via `applyBc22BeatSpoilers`, and **all fourteen** `html[data-year="bc22"] .beat-marker.<kind>` rules exist (grep count 14) with `test_bc22_beats.nim` (359 checks) asserting emission+label+style from the committed fixture. `--statrail` set gains exactly `bc22-econ`,`bc22-units` (`:6210`). `#viewpanel` **kept** — deliberate and correct: the pool's boards (480–784 px at 16 px/square) exceed the 360 px featured frame. |
| 15 Every drawn string fits | **PASS** | `viewer_smoke.mjs` reports `canvas_text: {total, outside, never_inside, ellipsized}` (lines 417-440) and `--strict-text-bounds` gates `never_inside`. The flag is deliberately dropped on the seven bundle replays — the board is pannable/zoomable, which is the item's own stated exception — with counts still recorded. The worst-case renderer fixture exists and gained a bc22 row: `renderer_fixture.html` `YEARS` includes `bc22`, full-cap 280-rune notes on **both** seats plus the fallback and submitted-vs-applied badges, real page CSS extracted from `replay_broadcast.html` at run time, three widths including 360 px, **and it asserts its own strings are still full-length** (`runes(notes).length !== MAX_NOTE_RUNES` aborts, lines 157-158, plus the per-seat 120-rune `dsub` check at :667) and that no filled element hides its own content (`scrollWidth > clientWidth` sweep). Driven by `ci.yml:3417-3443` with `--strict-text-bounds`; step green in run 34288986734. |
| Parallel batch | **PASS** | `decide.nim:1064-1081`: one `RequestBatch` over every open seat, one `client.curl.makeRequests(batch, …)` per attempt. No sequential per-seat call. |

## Refuted / audited — the reviewer's findings

The review reports **zero blocking**. I tried to break that clean bill and could not — and I independently verified the load-bearing claims it rests on rather than accepting them:

### The four load-bearing claims of the pass — all CONFIRMED
- **`871a476` weakened nothing** — CONFIRMED from `git show 871a476`: the six re-indented assertions are verbatim; `haveGame` asserts (`check(name, games.len > 0)`); the removed lines are test *config* (a `perGameBudgetSeconds = 0` that `playMatch`'s `max(1, …)` clamp turned into a 1-second budget — a debug-only flake) plus a comment. Net **+7 checks, −0**.
- **Chrome byte-identity and the 26 deletions** — CONFIRMED by hashing both files at both ends of the range myself and reading every deleted line of `replay_broadcast.html`; all 26 fall inside the changes `design.md:2000-2005` names (`sheet.nim` resolver aside, "the four shared-endcard fixes … which deliberately change what every year's endcard renders") plus the seven-way discriminator.
- **N1 (`"type":"uri"` vs item 10's `"text"`)** — UPHELD as advisory, tested hard: the shape shipped in all six prior releases, the diff's only docs change is one **added** page in the same shape, and the item's operative clause ("manifest validates") is proven by the green "The coworld CLI accepts the manifest template" step (the platform's own validator). A strict-literal reading would indict the six shipped years, which are out of scope; nothing this diff did moved the tree toward or away from the literal.
- **N2 (`/client/replay` strings)** — UPHELD as advisory, tested hard: the strings sit in a live-broadcast path-mapping table in a file that is byte-identical to the starter **and** to base, untouched by the diff; the static bundle never serves at any pod path (shell reads `location.hash`/`?replay=`), and the third occurrence is inside the release step that *forbids* a pod viewer. Item 3's operative requirements (bundle declared, hook wired, S3-only) all hold.

### The reviewer's four unsettled items — now SETTLED, all in the pass's favour
1. **`#clock-time` positional? (N14)** — SETTLED: it is positional. `chrome_common.js:440-446`: with `mt <= 0` (this repo emits `mt: 0`), `remain = (mx − t)/FPS` — a countdown over the playback position. The identical `0%`/`50%` readouts are an artifact of the harness, not a seek failure: `viewer_smoke.mjs:757-760` records the `0%` entry from the **post-soak** readout without seeking, and a 15 s soak advances ~half of the ~700-frame bc22 replay — so the "0 %" sample was already sitting at ≈50 % when the 50 % seek landed on the same frame. Seeking is independently proven: `scrub_selector == "#scrub"` asserted, the 100 % seek raised `#endcard` with a clan line, and the soak's three-sample `moved` gate proves advancement. Cosmetic; no item falsified.
2. **Trove port vs trove4j on the JVM** — SETTLED from CI evidence: `parity-oracle-bc22` (run 34288986734, job 102271033905) is green through named blocking steps — "Tier B (BLOCKING) — the WHOLE finite arithmetic domain, byte-diffed", "Run both engines on forty pairs (whole 2000-round games)", "Every game really ran (the ASM trap)", "Tier A-prime really exercised the paths Tier A cannot reach", "Tier A, A′ and C (all BLOCKING)". Both trace emitters print `H hashord=<fnv1a64 of robotsArray() order>` every round (`Bc22Trace.java:277`, `parity_trace_bc22.nim:112`), the comparator normalises both sides with the explicit `HEX_FIELDS` allowlist and `zip_longest`, and `tools/ci/parity_ledger_bc22.json` is `{"entries": []}` — the phase-30 exit condition (Tiers A/A′/B pass, **empty ledger**) is met. `tests/test_bc22_trove.nim` additionally replays 500 sequences against `tests/fixtures/trove-bc22.txt`, recorded off the jar's own classes.
3. **Smoke `jq` floors vs `design.md:2649-2653`** — SETTLED: I read the "The bc22 episode really played bc22" step out of the docker-smoke log. Committed floors: `units_built ≥ 45`, `lead_mined ≥ 300`, `builders ≥ 1`, `labs ≥ 1`, `labs_finished ≥ 1`, `transmutes ≥ 1`, anomaly bite ≥ 1 — the note's floors verbatim, each reading the **replay's** `result` block, each `exit 1` on failure with a `::error::` that forbids dropping the clause. Measured in the run: `units=72 lead=3800 builders=1 labs=1/1 transmutes=115 anomaly bite=248`. The episode really made gold.
4. **`refusedActions == 0` vs per-action enumeration** — SETTLED: I read all eleven guarded `do*` procs (list under item 7); every one has the `can*`-check-then-increment prologue, and the `can*` predicates themselves are pinned individually by the units/cooldown/buildings/economy/anomaly/sensing shards. The counter gate is a faithful aggregation of per-action legality, not a substitute for it.

### The advisory findings (N3–N13) — audited
| finding | reviewer said | I verified | agrees |
|---|---|---|---|
| N3 `max(1,…)` vs `> 0` budget spelling | pre-existing, advisory | `match.nim:480` untouched by diff; the trap is real but both bounds enforced | yes — advisory |
| N4 deadline block passes either way | permissive but not a loosening | `reason in [epDeadline, epComplete]` at `test_bc22_replay.nim:258`, present from the file's creation in this run (not introduced by `871a476`); the deadline path fired in debug (85 vs 84 checks) | yes — advisory; item 1 forbids *loosening*, and nothing was loosened |
| N5 record→re-derive not for all six end reasons | advisory vs design test 20 | `annihilated` + a Singularity rung + the deadline branch re-derive; the other rungs covered at world level in `test_bc22_endladder.nim` | yes — item 2 requires *a* frame-by-frame test, which exists |
| N6 `sheet_submitted` records the unwrapped node | pre-existing, advisory | `sheet.nim:150` after the resolver; envelope key separately recorded and rendered | yes — advisory |
| N7 `archon_lost` beat reports level-1 gold drop | display-only | `rules.nim:286-288` passes `goldDropped(rtArchon, 1)`; world credit at `world.nim` uses the real level | yes — advisory (display value; no rule, no checklist item) |
| N8 dead `if …: discard` | advisory | `rules.nim:280-281` confirmed | yes |
| N9 `dfNone` fallback names `more_archons` | unreachable, defensive | code confirmed at `rules.nim:331-334`; reachability trace plausible | yes — advisory |
| N10 reclaim credited to the killer at drop time | a choice, not a contradiction | `world.nim` attribution confirmed; note defines no attribution | yes — advisory |
| N11 beat seek fraction omits `st` | inherited convention, `st` is literal 0 | `:5455-5467` confirmed; `st: 0` in every packet | yes — advisory |
| N12 seeds collapse to two distinct games/map | disclosed in the header | `sideAslotFor(seed,0)` = `(seed shr 8) and 1`; header states it | yes — advisory |
| N13 two survival floors lowered | the note's own sanctioned path | constants + header confirmed: `MinMinersBuilt 4` = half the measured weak 8, `MinSoldiersBuilt 5` = half of 10, measurements inline, every other floor kept or raised (`MinLeadMined 1800` vs the note's 600); `design.md:2308-2314` authorises exactly this and forbids only dropping the clause | yes — **not** a test loosening under item 1: the floors never existed at a higher committed value; they were committed once, from measurement, by the note's rule |

Nothing in the review was found to be wrong at head; nothing it passed was falsifiable from the code. No finding is escalated.

## Non-blocking observations (mine, beyond the review)

- The `NIM_TESTS`/`NIM_TESTS_DEBUG_ONLY`/`NIM_TESTS_RELEASE_ONLY` hooks in `ci.yml:385-391` read **repository variables**, so a future operator could narrow the test set without a tree change. In run 34288986734 they were unset (all 20 bc22 shards plus every prior year's ran, twice each — verified from the job log), so item 1 holds at this head; worth a phase-60 re-check.
- The reviewer's N7 could equally be read against the design's beat-label example ("20 gold is on the ground") — the label is only wrong for a level-2/3 archon, which the fixture does not exercise. Advisory, as filed.

## Verdict

Zero blocking findings stand at `1fc96b4`. All fifteen checklist items plus the parallel-batch rule verified from the tree and from run 34288986734's job- and step-level evidence; the reviewer's zero-blocking report survives adversarial re-verification, and its four declared-unsettled items are settled here, all in the pass's favour.

BLOCKING: 0
