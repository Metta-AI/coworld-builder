blocking: 0

# Phase 60 verdict — battlecode-2022 (bc22 mod on coworld `battlecode`)
Judged: 2026-09-09 · Fresh context · Independent read of the evidence (replay, page, API, CI runs, PNG) formed before relying on VERIFY.md's dispositions. Every load-bearing claim was re-fetched or re-read from committed files, not taken from VERIFY.md's prose.

## Item-by-item pass (SPEC §Definition of done)

### 1. ≥2 rounds completed after fillers set — **TRUE**
Re-fetched `GET /rounds?league_id=league_c7fc991e…` myself: rounds 1, 2, 3 all `status: "completed"`, `error: null`, zero failed/discarded (round 1 completed 2026-09-09T00:33:25Z, round 2 at 00:48:17Z, round 3 at 01:03:27Z — a third round has completed since VERIFY.md was written, strengthening the item). Fillers were registered 00:20–00:29Z per VERIFY.md's log citation; every completion timestamp postdates that.

### 2. Both champions ranked, fillers absent/Baseline — **TRUE**
Re-fetched `GET /divisions/div_c39ea7d1…/leaderboard` myself: exactly two rows — rank 1 `daveey-1` (`battlecode-bc22-transmuter:v1`, win_rate 1.0), rank 2 `daveey` (`battlecode-bc22-rush:v1`, win_rate 0.0), now `rounds_played: 3` each. No filler rows at all — "absent" satisfied.

### 3. Latest round's episode request completed with replay_url, participants named — **TRUE**
Re-fetched `GET /episode-requests/ereq_cc9b003b-e803-4cbf-84ea-0f81ee53bd07`: `status: "completed"`, `replay_url: https://softmax-public.s3.amazonaws.com/replays/fe390134-….replay`, participants position 0 = `battlecode-bc22-rush` v1 / `daveey` / `is_filler: false`, position 1 = `battlecode-bc22-transmuter` v1 / `daveey-1` / `is_filler: false`. Scores [45.0, 454.0] match the hosted game log's `scores=[45.0, 454.0]` line exactly. (Round 2 was the latest completed round at verification time; round 3 completing afterwards does not invalidate the check.)

### 4. Replay bytes valid, complete, champions doing the thing — **TRUE**
Re-fetched the replay from S3 myself. `jq` parses it (strict UTF-8 JSON). `protocol: cogame.battlecode.v1` — matches `design.md` §Server/player/protocol ("Protocol id: cogame.battlecode.v1 — unchanged"). `result.reason: "complete"`. `result.fallbacks: [0, 0]`, `sheet_defaults_applied: [[], []]`, `decision_ms: [6620, 6620]`. Crucially, the replay's `seats[]` contain the full LLM-submitted strategy sheets verbatim (`sheet_submitted` JSON with 11 distinct parameters per seat — seat 0 `soldier_rush`/ratio 90, seat 1 a transmuter build that in fact transmuted 829 gold vs 23 in game 1) plus non-trivial doctrine notes/mottos. The two seats' sheets differ and their game statistics (`result.games[]`) differ accordingly — non-scripted decisions with non-trivial content, zero fallbacks. Both games ran 2000 rounds, winner seat 1 on `more_gold_net_worth`, rich event stream (17 event kinds, 129 events).

### 5. Hosted game log clean — **TRUE**
Re-fetched `/episode-requests/<id>/artifacts/logs` with the elevated header and decoded the `b'…'` reprs myself. Grep for `falling back|LLM provider is unavailable|cut off at max_tokens|rejected`: **CLEAN** — zero matches, verified on my own decode, not the verifier's. Both LLM calls logged `HTTP/1.1 200 OK`. The two non-clean-looking lines are neither of the four strings: the datadog `Connection refused` is a metrics sidecar, and `refused a seat-0 connection: … wrong connection token` is a pre-handshake retry artifact ("refused", not "rejected"; seat 0 subsequently connected and registered `kind=llm label=rush`).

### 6. Public page uses the static replay path — **TRUE** (with one documented deviation, judged non-material)
- I fetched `https://softmax.com/battlecode` myself: HTTP 200, 1,006,306 bytes, **zero** occurrences of `client/replay`, zero `<iframe>` elements, and only 2 occurrences of the string "battlecode" — the page is a client-rendered app shell, so the raw-HTML grep being empty is expected, exactly as VERIFY.md said.
- I re-ran the session API the page's JS calls (`POST /coworlds/replays/session` with this cow_id + the round-2 replay URI) and got the identical `viewer_url`: `…/v2/coworlds/replays/static/cow_ad0e79c5-…/sha256%3A08219b42…/index.html?v=2#replay=<url-encoded s3 url>`, `ready: true`. The sha in the path is byte-identical to `manifest_sha` in the committed `release-result.json`. **Not** a `/client/replay` pod URL — the load-bearing half of this item holds on my own evidence.
- **Deviation:** the SPEC writes `index.html?replay=<s3 url>`; the observed URL carries the replay as a `#replay=` fragment with `?v=2`. This is a documented platform-wide change, which I verified at `playbooks/observatory-api.md:326`: "since 2026-08-28 the session endpoint returns the replay as a URL-encoded **fragment** instead … both are the static route". The fragment form demonstrably works — item 8's harness loaded this exact URL and got `loaded: true`. I judge the deviation non-material: the SPEC's clause is about static-route-vs-pod, and the route is static.
- "Featured match present" cannot be observed in raw HTML for any coworld on this platform (client-rendered); the documented evidence path — precondition of ≥2 ranked players (check 2) plus the page's own session API returning `ready: true` — is satisfied. Accepted on that basis.

### 7. Certification declared the static bundle — **TRUE**
Read from the committed `runs/2026-09-08-battlecode-2022/release-result.json` myself: `.certify.replay_liveness` is exactly `Replay liveness: skipped (static replay bundle declared; /client/replay and /replay not required)`, and the same line appears in `.certify.output_tail` after `Transcript: coworld-executable (10 steps passed)`. `ok: true`, `hosted_certification: "certified"`, version `0.7.0`, cow_id matches.

### 8. Viewer executed, replay advances, spectator judgment — **TRUE** (all three sub-criteria)

**(a) loaded: true** — committed `viewer-smoke.json`: `loaded: true`, `signals.data_replay_loaded: "true"`, `bridge: ["ready"]`, `bridge_ready: true`, `bridge_error: []`, `failure: null`, load in 956 ms. Both required signal paths fired. Not inferred from asset 200s.

**(b) the replay advances** — the committed run's three scrub readouts are literally distinct: `2:47 GAME 1 OF 2 — MONUMENT` → `2:46 GAME 1 OF 2 — MONUMENT` → `1:23 GAME 2 OF 2 — COLLABORATION`.

**The retry question (probed hardest, as briefed).** I did not take VERIFY.md's attempt table on faith: I confirmed all three CI runs exist on `Metta-AI/coworld-builder` workflow `viewer-check` (34296808787, 34296904104, 34297147808 — all `completed`/`success`, timestamps matching the narrative), downloaded all three artifact sets, and confirmed (i) the committed `viewer-check/` files are **byte-identical** (`cmp`) to run 34296904104's artifacts, and (ii) the other two runs' `scrub[]` arrays match VERIFY.md's table exactly. My ruling: **this is not selecting-the-run-that-passes.** The failure mode criterion (b) exists to catch is "a frame that renders once and freezes" — and in **every one of the three attempts**, including the two non-committed ones, the 0% and 100% readouts differ across a whole game boundary (GAME 1/MONUMENT vs GAME 2/COLLABORATION). A frozen viewer produces three identical readouts; no attempt did. The only variance is the 50% sample, and it correlates cleanly with the measured `settle_ms` in the direction autoplay-after-seek predicts: short settle (502–1004 ms) leaves the 50% sample near its seek target in game 1; the 3000 ms settle in attempt 3 lets playback run past the game boundary so 50% collapses onto 100% (not onto 0%, which a stuck frame would produce). With `sim_seconds: 3.665` the whole match renders in a few real seconds, so 0.5–3 s settle windows straddling a game boundary is the expected artifact. The 0%→50% delta of one second (2:47→2:46) is consistent with the clock being a match countdown ticking ~1/s during the settle window — small, but a *change*, and the 50%→100% game transition is unambiguous. The verifier disclosed all three attempts with raw readouts rather than committing attempt 2 silently — the opposite of cherry-picking. Had all three readouts been identical in some attempt, or had an attempt been concealed, I would have ruled the other way.

**(c) judgment paragraph** — present in VERIFY.md, legible, and accurate against the PNG (my own reading of the screenshot is below). Beyond re-reading it, I independently reconciled the rendered frame against the replay JSON, and it reconciles to a degree that excludes a canned frame:
- Feed line "ABYSS strikes, game 1, round 2000/1800/1600" → replay game 0 (displayed "game 1", monument) has `anomaly_struck type:abyss` at exactly rounds 200,400,…,1600,1800,2000 — the feed shows the tail of that schedule.
- Feed "SINGULARITY — round 2000, decided on more gold net worth, game 1" → `singularity {game:0, round:2000}` + `game_end winner Clan Basil`, `end_reason: more_gold_net_worth`. ✓
- Feed "Game 2 begins on collaboration" → `result.games[1].map: "collaboration"`. ✓
- The frame itself is game 2: scorebug shows **4** archons per side (`games[1].archons_per_side: 4`; monument has 1), the map panel reads "397 lead left on **34** squares" (`games[1].lead_squares_start: 34`, `lead_on_map_start: 412` minus early mining), and the anomaly strip reads "ABYSS in 187 · A A A A C F C F C F" — first abyss at round ~189 and the exact abyss/charge/fury sequence of game 2's schedule (189,370,590,801 abyss; then charge/fury alternating). Four independent numbers drawn live from this replay's data.

`canvas_text.total: 0` appears in the committed JSON; per the phase-30 checklist that check covered nothing and is not evidence. **VERIFY.md does not cite or lean on it anywhere** — item 8 is argued entirely from the load signals, scrub readouts, and screenshot reconciliation. No issue.

## My reading of the screenshot (viewer-smoke.png)

Legible, and unmistakably the game. Top strip: match clock `1:23`, `GAME 2 OF 2 — COLLABORATION`, `CLAN ASH` / `CLAN BASIL` with both champions named (`daveey`, `daveey-1`) and their doctrine mottos ("Out-mine, out-soldier, never strip-mine…", "Gold wins wars. Scatter before clumps die.") — genuine LLM content that I located verbatim inside the replay's `seats[].notes`. Scorebug: `ASH 4 ●●●● archons · 0 lost / 0 lost ●●●● 4 BASIL`. Center: a real tile map (rubble-shaded terrain, symmetric layout) with two distinct unit clusters in team colours (blue left, red right) plus small structures at top/bottom. Right: the event feed, five lines, narrating the game-1 resolution and game-2 opening, all reconciled against the replay events above. Bottom-left: per-clan resource cards (`Clan Ash ◆64`, `Clan Basil ◆59`, "no laboratory", "0 dry", map lead counter). Bottom: transport strip with playback controls (⟲ ◀ ‖ +25 ▶ ↻ ▶▶), a "spoilers" toggle, `round 2 / 2000`, speed buttons (1×–16×), and a full-width scrubber with event tick marks. This is the same chrome family as the sibling battlecode year leagues — scorebug top, feed right, transport bottom, endcard-capable frame — with the bc22-specific game inside it. The `#viewpanel` zoom bar (`− [slider] + FIT`) at top-right is present by design for bc22's up-to-60×60 maps in a 360 px frame (design-note decision verified in phase 30) — not a defect, and it does not occlude anything. One neutral observation: the frame captured at 100% scrub shows game 2 near its *start* (round 2) rather than an endcard — consistent with the autoplay/loop behaviour discussed under 8(b), and irrelevant to the criteria, since the readouts and frame content prove advancement.

## Discrepancies with VERIFY.md

None material. Every command output I re-ran matched what VERIFY.md pasted (rounds, leaderboard, episode request, replay fields, decoded log, session API URL, all three CI scrub arrays, committed-artifact identity). Two SPEC-vs-reality notes, both already declared by the verifier and both verified by me independently: the `#replay=` fragment (playbook-documented platform change, see item 6) and the settle-sensitivity of the 50% scrub sample (see item 8b).

## Blocking findings

None.

BLOCKING: 0
