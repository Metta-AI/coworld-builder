blocking: 0

# r2 verdict — minigrid

Head: `8a78a6bfc5e9be0710118d88b2eb37ccc2ccb4c9` (== `main`; `gh run view 33224940969 --json headSha,conclusion` → `success` at this sha)
Checklist: `prompts/30-review-loop.md` §ACCEPTANCE CHECKLIST (items 1–15, verbatim in the brief) + addendum-v2 commitments (`runs/2026-08-28-minigrid/design.md:1975-2851`)
Independent read written before reading fixes: **yes** — repo cloned fresh to `/tmp/cogame-minigrid-judge2`, pinned to `8a78a6bf`; the checklist prompt, the design note (v1 sections + the authoritative addendum at line 1975), `VERIFY.md` §5/6/8, the full `b19bc085..8a78a6bf` diff (77 files, +7034/−2419), the full tests diff (`git log -p b19bc085..8a78a6bf -- tests/`, 2776 lines, read in full), the CI logs for run 33224940969 (jobs 99026594813 docker-smoke / 99026594994 test / 99026799782 wasm-viewer), and the starter mount at `/workspace/starters/coworld-ctf` were all read and my notes formed **before** opening any `r1-*.md`. Per the brief, `r1-review.md` and `r1-fixes.md` were not read before my own pass; `r1-verdict.md` was skimmed only afterwards, for continuity.

**Round shape.** No `r2-review.md` and no `r2-fixes.md` exist in `runs/2026-08-28-minigrid/reviews/` — this round has no reviewer findings to refute and no fixer report to audit. Per the judge brief ("an empty review is not a pass"), this verdict is a full independent checklist pass at head, plus the addendum-specific commitments the brief enumerates.

## Standing blocking findings

None. Every checklist item and every addendum commitment verified at `8a78a6bf`; nothing I could not verify from the tree or cited CI evidence.

## Refuted

No round-2 reviewer findings exist to refute. The three production failures VERIFY (phase 60) recorded — the inputs to this round — are each demonstrably fixed at head:

### VERIFY check 6 — "No featured match: single-seat episodes, structurally" → FIXED at head
- `coworld_manifest_template.json`: `num_agents: 4` in **both** variants' `game_config` (Gauntlet and XLand, read from the file), in `certification.game_config`, with `players` = Alpha/Beta/Gamma/Delta ×3 places and `minPlayers: 4`; `certification.players == [scout, bumper, scout, bumper]`; `player[]` declares **two** players (`scout`, `bumper`), both seated, each with `limits.cpu: "1"`. `sim_config.nim:144-147` **rejects** any other seat count (`numAgents != LaneCount` raises), and `tests/test_minigrid_manifest.nim` (amended test 33) asserts `expect ConfigError` on `numAgents = 1`. docker-smoke log (job 99026594813): `game=minigrid seats=4`, `smoke OK: seats=4 … reason=complete`, `grep -c 'SEAT-COUNT FAIL'` = **0**.

### VERIFY check 5 — "one `falling back` line + mislabelled cause" → FIXED at head
- Ladder re-pinned in both variants: `attempt1Ms 11000 / retryMs 6000 / turnBudgetMs 17000 / turnSpacingMs 11000 / wallClockBudgetSeconds 660` (read from the manifest; defaults match, `sim_config.nim:86-90`). `sim_config.nim:127-140` rejects sub-second deadlines and `attempt1Ms + retryMs > turnBudgetMs`. Worst case `30 × 17 + 121 = 631 ≤ 660` asserted by **two** tests: `tests/test_minigrid_decisions.nim` test 51 (variants + cert fixture, plus `attempt1Ms ≥ 11000`, `retryMs ≥ 6000` = observed p90, spacing keeps steady state ≤ 24 req/min under the 28 guard) and `tests/test_minigrid_manifest.nim` test 33 (same wall-clock inequality per variant).
- Cause truthfulness: `decide.nim:130-148` `transportCause`/`exceptionCause` set the cause **at the point of failure** (`parse_error` reachable only with a body in hand, `:306` initialised then overwritten at `:315/:329/:339`); one `fallback` record **per attempt**, each with its own cause and slot (`fallbackRecord`, `:83-94`, written at `:270-272` and `:342-343`); attempt 1 logs `will retry` (`:348`), only a genuine second failure logs `falling back` (`:367`); `results.fallbackCauses[i]` sums to `fallbackTurns[i]` (`:396-397`, asserted end-to-end by decisions test 50 and engine test 24).

### VERIFY check 8 — four viewer defects → each FIXED at head, each gated in CI
- **Seek/clock**: one axis `mgScrubAxis()` — exactly 1 definition + 5 named consumers (`tests/test_minigrid_viewer.nim:269-304` counts them from the page source); click maps `first + Math.round(f × span)`; caption reads the **playhead** tick (`client/replay_broadcast.html:4919` `'tick ' + Math.max(0, s.t - (s.st || 0))`); `mgFrame(s, ctx, jumped)` recomputes every readout, `if (jumped) return` asserted absent. CI gate (ci.yml step "The viewer defects VERIFY check 8 found stay fixed") requires the 0 %/50 %/100 % readouts **pairwise distinct** — run 33224940969: `0%="…TICK 241/720…" 50%="…TICK 361/720…" 100%="…TICK 720/720…"` — distinct, monotone, and 100 % lands exactly on the last tick (v1 landed at 158 of 315).
- **Feed**: `feed_lines: 9` in the browser smoke (CI gate requires ≥ 4; the addendum's ≥ 1-at-load is subsumed — the phase row is pushed on the first drawn frame, `mgSeedFeed`, asserted by viewer test 54); `say` rows never dropped on overflow (`kept.concat`/`r.kind === 'say'`, test 54; fixture `assertFeed` throws if no say row survives).
- **Layout**: the 27×27 quad (viewer test 57: `SurfaceCells == 27`, non-overlapping lane origins, red/blue/green/yellow in seat order, Alpha/Beta/Gamma/Delta); `tools/ci/renderer_fixture.html` measures ribbon/pips/POV inset/feed/4 plates at **1280, 640 and 360 px** and throws on board intersection, transport-band intrusion, top-band clipping, or pip-caption overlap (`assertLayout`), asserts the seek mapping at f ∈ {0, .25, .5, .75, 1} (`assertSeek`), and asserts its own 140-rune say and full mission survive full-length (`assertFullLength`); its CI step is green: `canvas text: 174 drawn, 0 never inside the canvas, 0 ellipsized (--strict-text-bounds)`.
- **404s / mis-framed packets**: `_front_gun`/`_crown` request sites gone (viewer test 55e asserts absence in page and core, and resolves every constructed image URL against shipped files — all four `soldier_*_front.png` + `soldier_*.png` in `data/`, four lockerroom colour sets shipped); root cause of `Unknown sprite protocol message type: 34` found (U16 label-length wrap past 65 535 bytes) and pinned by `tests/test_minigrid_wire.nim`: exactly the starter's six message types, every emittable packet walked through all three length tables landing exactly on the packet end (55b), a deliberately oversized 70 KB chrome frame **cannot** mis-frame (55c), and the built four-lane worst-case frame stays inside `MaxChromeLabelBytes` (55d). CI gate fails the job on any `[http 404]` or `Unknown sprite protocol message type` console line — none in run 33224940969.

## Checklist pass (independent)

| item | status | evidence (path:line or run) |
|---|---|---|
| 1. CI green, no test loosened | PASS | Run **33224940969**, workflow CI, `conclusion: success`, `headBranch: main`, `headSha: 8a78a6bf` (gh json, cited). Jobs test / docker-smoke / wasm-viewer all green; `continue-on-error` — 0 hits in ci.yml. `git log -p b19bc085..8a78a6bf -- tests/` read **in full** (2776 lines): no skip/xfail/tolerance anywhere; every deleted assertion is a 1-seat fact re-asserted per-lane at the addendum's numbers (e.g. `actions.len <= 12` → `<= 24` = new `maxActionsPerTurn`; `capped.actions.len == 12/overCap 18` → `24/6`; `num_agents == 1` → `== LaneCount` **plus** minPlayers/players/seat-mix checks; `maxTurns == 55` → `== 30`; scoring bounds 515 500 → 515 250 per the new `taskTurnCap 6`; end-rule enum `gauntletComplete` → episode `allLanesComplete` + new per-lane `LaneEndRule`). Net-new coverage: `test_minigrid_decisions.nim` (49/50/51/51b), `test_minigrid_isolation.nim` (46/46b/46c/47/47b/48/58), `test_minigrid_wire.nim` (55/55b/55c/55d), viewer 52/54/55e/57, engine五-identity block. Only narrowing found: driver test 23's tuning-sanity sweep 100 → 60 seeds — each seed now runs 4 lanes with a per-lane fairness check added, assertions unchanged; a runtime tradeoff, not a loosening (noted below). |
| 2. Replay re-derivation | PASS | `tests/test_minigrid_replay.nim:71-95` records a real four-lane episode then re-derives for **all four** end reasons (`allLanesComplete`, `turnCap`, `wallClock`, `fault`), `derived.mismatch == -1` (per-tick hash chain — one `writeHash` per tick), plus endRule/reason/per-lane score/whole-results-JSON equality; test 31 re-derives from the bytes alone. Viewer derives from the **same** re-derivation: `replay_runtime.nim:1-5` is "shared by the native host and the" wasm entry, `replay-viewer/minigrid_replay.nim` imports it; the exact emitted module is executed against the smoke replay (`Headless wasm smoke`: "ok: loaded replay.json, advanced 300 frames"). |
| 3. Static viewer | PASS | `game.replay_viewer == {"bundle": "static-replay-viewer"}` (read at head); `tools/build_replay_viewer.sh` mode 100755, asserted executable by ci.yml before use; the only network call in the shell/worker is `static_replay_worker.js:113` fetching the replay URL (S3); no `/client/replay` declared anywhere in the manifest or compose. |
| 4. Both name spaces | PASS | Observation carries `"you": <alias>` and `"lane": n` only — isolation test 46c asserts no rival alias/score/field and `"you":"Gamma"` for seat 2; `showPlayerLabels: false` in both variants; feed rows built from `mgAlias(` with `rosterName` asserted absent from the row builder (viewer test 54); real names live in `results.names` / joins / plates (engine test 24: names `[scout, bumper,…]`, aliases `[Alpha, Beta, Gamma, Delta]`). |
| 5. Degrade-never-hang | PASS | Every wait bounded: batch deadline `min(configured, remaining turn budget)` (`decide.nim:266-300`), whole-second floor is an identity by validator (`sim_config.nim:127-135`); spacing sleep bounded by `turnSpacingMs` (`:251-254`); rate guard skips rather than sleeps (decisions test 51b drives it: 4 × `rate_guard`, zero requests, turn returns immediately); budget guard settles early with 2-turn headroom (`:193-202`); wall-clock stop at the top of every loop iteration (`server.nim:518-522`); lobby bounded (`lobbyJoinTimeoutTicks` 2400 = 100 s); worst case 631 s < 660 s stop < 720 s = 60 % of 1200, asserted by tests 51 and 33. |
| 6. num_agents + seat invariants | PASS | `num_agents: 4` in both variants' `game_config`, in `certification.game_config`, and absent at every variant top level (read + test 33); `docker_smoke.sh:100-160` enforces all four SEAT-COUNT invariants **before any container starts** with `SEAT-COUNT FAIL:` prefixes, and `SMOKE_SEATS` (default 4, the `<SEATS>` substitution) is the independent cross-check; docker-smoke log grep for `SEAT-COUNT FAIL` = **0 hits**; `seats=4`, episode `reason=complete`. |
| 7. Scripted baseline | PASS | Engine test 24 runs the real binaries with 4 seats `[scout,bumper,scout,bumper]` to `reason == "complete"` and asserts the five results identities of §Results v2 (incl. same-baseline lanes scoring identically); driver tests 17-19 bound every action (≤ 24, enum verbs, legal goto) over 300 poked states and prove never-suicide; `tools/ci/baseline_tuning.json` + ci.yml step "Re-run the baseline sweep with --check" green. |
| 8. LLM reply handling | PASS | `directives.nim` `extractJsonObject` (string/escape-aware balanced-brace scan with first/last-brace fallback); retry exactly once (`while open.len > 0 and attempt < 2`); fallback = the scout proc itself (driver test 20 asserts identity, per lane); recorded countably three ways (per-attempt `fallback` records, `fallbackTurns`, `fallbackCauses` summing — decisions test 50). |
| 9. Rune-safe truncation | PASS | `sim_types.nim:282-291` `truncateRunes` via `runeSubStr`, applied to say/notes/detail/policy labels; driver test 21 feeds 4-byte emoji at the 140/300 caps; replay test 30 pushes every capped field at cap through strict-UTF-8 `replay_summary.py` (`says[0].text.runeLen == MaxSayRunes`). |
| 10. Manifest validates | PASS | `game.docs` = readme + 3 pages of `{id,title,content:{type,value}}`; `game.protocols` carries both `player` and `global` as `{type,value}` objects (read at head); the pinned CLI's own loader runs in ci.yml ("Manifest loads under the pinned coworld CLI", green). |
| 11. Legible at 360 px | PASS | `.plate-name { flex: 1 1 auto; min-width: 3.2em; … }` at `client/replay_broadcast.html:4176-4182`, asserted by viewer test 39; the starter's `.tiny` machinery kept verbatim (`boardW <= 620` toggle asserted), with the game's own `.tiny` shrink rules for alias/carry/mission; the renderer fixture proves the layout contract at 360 px in CI. |
| 12. Release order and scaffold | PASS | `coworld-release.yml`: build (`:159`) → certify (`:174`) → upload-policies (`:217`, explicitly "BEFORE upload-coworld") → upload-coworld (`:319`) → secret put (`:410`, "AFTER upload-coworld"); three workflows present; `docker_smoke.sh` 100755; `policies.json`: 4 policies — 2 × PLAYER_PROMPT with champion #2 carrying `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`, 2 × PLAYER_SCRIPTED (scout, bumper); I ran the exact three-name placeholder gate over the five files — **exit 0**. |
| 13. Viewer executes | PASS | wasm-viewer green at head **including** "Load the bundle in a real browser": `{"loaded":true,"ms":331,…}` against the replay docker-smoke produced; `needs: docker-smoke` (ci.yml); no continue-on-error; `data-replay-loaded` set in `static_replay.js:161` ('loaded' branch), `data-replay-error` in `showFailure()` (`:8-20`) — both the shell's own paths (file diffs vs starter show only the name substitution); playback opens at `gameStartTick` (`replay_runtime.nim:34-35`) and **every** seek clamps to `clamp(tick, replayStartTick(), replayMaxTick())` (`replays.nim:482`, plus `:521/:524/:555/:570`); `config.nims` has no MODULARIZE and the worker waits on `Module.onRuntimeInitialized` (`static_replay_worker.js:188`) — same starter, and the smoke's `loaded: true` is the executed proof. |
| 14. Chrome is the starter's | PASS | `client/chrome_common.js` **byte-identical** to `/workspace/starters/coworld-ctf/client/chrome_common.js` (sha256 equal, diff clean, pinned by test 35); I ran the repo's deriver against the real starter mount — `python3 tools/build_broadcast_page.py --starter /workspace/starters/coworld-ctf --check` → **"client/replay_broadcast.html matches the derivation", exit 0** — and test 36 pins the appended block to `client/minigrid_block.html`; transport rules: `--band/--topband/--hudscale` on `:root`, `#endcard { bottom: var(--band, 0px) }` + `#endcard.on` (`:858`) + `$('endcard').classList.remove('on')` on every seek (test 39); beats are labelled `<button class="beat-marker <kind> <lane>">` built by `mgBeat` (`:4580-4600`) that `CTX.send('s:'+tick)` on click, with CSS for **exactly** the 22 emitted kind×lane combinations (test 38 counts both directions); `#viewpanel`/zoom/minimap removed in markup, CSS, wiring and quoted-id forms (test 39 RemovedIds), `#povBadge` deliberately **restored** per the addendum (KeptIds, with the note in the test). |
| 15. Every drawn string fits | PASS | `--strict-text-bounds` on **both** smoke steps (bundle + fixture); bundle smoke reports `canvas text: 0 drawn` (board text is sprite-baked in the worker — not evidence, per the item), which is exactly why the worst-case renderer fixture exists: it loads the **shipped** index.html in an iframe, drives the real chrome with **four full-cap 140-rune emoji says at once** (`renderer_fixture.html:174-180`), a full-cap mission, fog, produce banner, all pip states and a lava endcard at 1280/640/**360** px, asserts its own strings full-length (`assertFullLength` — a shortened say fails the run), and its CI step is green: `174 drawn, 0 never inside, 0 ellipsized`. `never_inside == 0` on a covered check. |

**Addendum-specific commitments (the brief's list), each verified:**

- **num_agents 4 everywhere** — both variants' `game_config`, `certification.game_config`, `SMOKE_SEATS=4`, cert players `[scout,bumper,scout,bumper]`, two declared players both seated. PASS (see items 6/12).
- **Lane isolation** — every generator draw is `mix64(seed, taskIndex, salt)` (`sim_types.nim:302-324`; `tasks.nim` contains **zero** references to any lane index — grepped); `stepLane(lane, config, slot, tick, primitive, events)` takes no `SimServer` (`sim_state.nim:651-656`; `slot` is used only to tag emitted events). Tests: 46 (replace lane 1's whole stream ×2 replacements ×3 seeds → lanes 0/2/3 bit-identical per tick via `laneSnapshot` covering agent/grid/known/obstacles/subgoals/counters), 46b (lane alone == lane in four), 46c (observation reads only `lanes[i]`, no rival field), 47 (200 seeds × both variants × 5 phases: byte-identical grids/missions/rules/starts across lanes), 47b (identical plan stream → identical outcomes). PASS.
- **Deadline ladder** — 11000/6000/17000/11000 in both variants; passes the repo's own validators (`config.validate()` invoked inside test 51 for variants **and** cert fixture); worst case 631 ≤ 660 ≤ 720 asserted (tests 51 + 33). PASS.
- **Truthful fallback causes** — set at the point of failure, both attempts recorded, `transport_timeout` vs `parse_error` distinguished (decisions test 50 drives every enum value; enum spelling asserted). PASS.
- **Seek/clock contract** — single `mgScrubAxis()` with exactly five named consumers (test 52); caption a pure function of the playhead (`s.t - s.st`), recomputed every frame including `jumped`; ci.yml asserts 0 %/50 %/100 % pairwise distinct — green with distinct monotone readouts ending at TICK 720/720. PASS.
- **Feed rows drawn** — `feed_lines: 9` in the CI browser smoke (gate ≥ 4); phase row pushed on first drawn frame; `say` rows never dropped (test 54 + fixture). PASS.
- **Zero 404s / zero unknown-message-type warnings gated in CI** — the defect-gate step fails on either console line; none present; plus the static resolution test 55e and the wire round-trip tests. PASS.
- **360 px layout contract** — fixture asserts board/band/topband non-intersection and pip-caption non-overlap at 360 **and** 1280 (and 640), `never_inside == 0`; green. PASS.
- **GameVersion "2", fixtures re-recorded** — `sim_types.nim:22` `GameVersion* = "2"` with the prepend-only GV2 changelog line naming the addendum; `tests/replays/gauntlet-seed42.replay` re-recorded in the same commit (57 487 → 199 972 bytes) by the new `tools/record_fixture.sh`; test 32 (fixture version sweep) green in CI. PASS.
- **Champion prompts consistent with the six-turn cap** — `src/minigrid/llm.nim:209` "its own six turns" (the fourth re-pin, documented), `:227-228` "up to 24 actions"/"past 24 ticks"; `tools/ci/policies.json` champion #2 missionfirst: "you get six turns. If you have spent two and the target is still unseen…"; champion #1 cartographer names no turn count (as the addendum specifies). PASS.
- **One parallel batch per turn** — exactly **one** `curl.makeRequests(` call site in `decide.nim` (`:299`, asserted by decisions test 49 which counts call sites in the source), one request per still-open seat built in one loop (`:282-292`), retry again as a single batch; never a sequential per-seat loop. Sequential-calls finding: **none**. PASS.

## Fixer report audit

Not applicable: no `r2-fixes.md` exists (and no `r2-review.md`). The diff under judgment (`b19bc085..8a78a6bf`, 9 commits) was audited directly against the addendum instead — every commitment in the addendum's §Tests v2 list (13 amendments + tests 46–58) was located in the tree and cross-checked above. The five documented divergences in `docs/PORTING-MINIGRID.md` §"Divergences introduced by addendum v2" were each read and judged: all five are argued soundly (isolation-test scope under shared phase boundaries; retained-mode cells instead of static-band bakes with the fog-run family carrying the object budget; `_front_gun` site living in the page not core; 16 tinted cog chips instead of 32; the cert fixture's own reduced ladder — cert runs offline scripted under a 240 s wall clock, and test 33's inequality is asserted for the fixture too) and none falsifies a checklist item.

## Non-blocking observations

- Driver test 23 ("scout beats bumper") sweeps 60 seeds where v1 swept 100; each seed now plays 4 lanes and gained a per-lane fairness assertion, so coverage is comparable — but it is the one place the diff narrows an input range. Not a loosened assertion.
- `tools/ci/check_gameversion.sh` exists and understands the GV2 headline, but no workflow invokes it at head; the operative CI enforcement of "bump + re-record" is the fixture version sweep (test 32), which is green. If cross-branch GameVersion collisions matter, wire the script into ci.yml's PR path.
- The shared harness's 0 % scrub sample reads TICK 241/720 (not the axis start) because sampling races live playback; the gate is pairwise distinctness (met, monotone) and the fixture's `assertSeek` pins the exact `first + round(f × span)` mapping deterministically at five fractions, so the contract is covered — just don't read the 0 % absolute value as the axis origin when eyeballing logs.
- History carries the addendum work three times (9a5e94a / 4c803eb / 6d92683 with matching docs/viewer follow-ups) — the same content re-landed; tree state at head is consistent and CI is green on it. Cosmetic.
- The checklist's item 11 phrase "labels hidden under 640px" maps here to the starter's verbatim `.tiny` toggle at `boardW <= 620` plus the game's `.tiny` shrink rules — same behaviour the starter (and r1) shipped; substance satisfied.

BLOCKING: 0
