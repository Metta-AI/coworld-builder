blocking: 0

# r1 verdict — minigrid

Head: `b19bc085bbd70f63fcc6c4277b45ad71fad3d908` (== `main`, confirmed via `gh api repos/Metta-AI/cogame-minigrid/commits/main`)
Checklist: `prompts/30-review-loop.md` §ACCEPTANCE CHECKLIST (items 1–15, verbatim in the brief)
Independent read written before reading fixes: **yes** — the repo, the design note, the CI logs and the starter mount were read and my checklist notes formed before opening `r1-review.md`. `r1-fixes.md` was **not read at all**, per the brief; the fixer audit below is against the fix **commits**, not the fixer's report.
Review read: `runs/2026-08-28-minigrid/reviews/r1-review.md` (written against `d8f9e7c2`; 17 commits landed since — `git log --oneline d8f9e7c2..b19bc085`).

## Standing blocking findings

None. The review's single blocking finding is refuted at head (fixed), and my own independent checklist pass found no item failing at `b19bc085`.

## Refuted

### B1 — "manifest cannot be loaded by the pinned coworld CLI (`KeyError: 'image'`)" → REFUTED (fixed at head)
- The finding was correct at the review's sha `d8f9e7c2` and was fixed by `1187590` ("fix(manifest): B1 — move the image placeholder into game.runnable and use the CLI's player type").
- Evidence at head, verified three independent ways:
  1. `coworld_manifest_template.json`: `game.runnable.image == "{{MINIGRID_IMAGE}}"`, `game.runnable.source_url` present, `game.image` and `game.source_url` absent, `player[0].type == "player"` (read from the file at head).
  2. I ran the exact pinned loader locally: `pip install coworld==0.1.43; _load_template_manifest(template, "0.0.0", {"{{MINIGRID_IMAGE}}": "coworld-minigrid:latest"})` → **loads clean**: `minigrid coworld-minigrid:latest 2 variants`.
  3. The review's N10 gap (no CI step running the CLI validator) is also closed: `ci.yml:57-82` "Manifest loads under the pinned coworld CLI" (`coworld[auth]==0.1.43`), green in run 33213509695 (test-job log line 209: `manifest loads: minigrid coworld-minigrid:latest 2 variants`).

## Checklist pass (independent)

| item | status | evidence (path:line or run) |
|---|---|---|
| 1. CI green, no test loosened | PASS | `gh run list -w ci.yml --branch main`: run **33213509695**, `success`, on `main` at `b19bc08`; jobs test/docker-smoke/wasm-viewer all green, no `continue-on-error` in any workflow (grep: 0 hits). `git log -p --since="2026-08-28T19:07:00Z" -- tests/`: 5 commits, **one** deleted assertion total — `check m["game"]["image"]...` in `test_minigrid_manifest.nim`, replaced by 5 stronger checks asserting the shape the pinned CLI actually requires (`m["game"]["runnable"]["image"]`, absence of the two stray keys, `player[].type == "player"`). That is the expected corrected assertion and a **correction**, not a loosening (the old assertion pinned a shape `coworld==0.1.43` rejects). All other hunks are additive (`2e003ac`, `4dbec8e`, `d8f9e7c` add checks). No skip/xfail/tolerance anywhere in `tests/` (grep: 0). |
| 2. Replay re-derivation, viewer from same re-derivation | PASS | `tests/test_minigrid_replay.nim:68-88` records then re-derives for all four end reasons with `mismatchQuit = true` (per-tick hash divergence raises; one `writeHash` per tick at `test:26,44,50` and `server.nim` `recordHash()`); `:82-88` asserts tick/hash/endRule/reason/score equal. Viewer: `replay-viewer/minigrid_replay.nim:17` imports the same `minigrid/sim`; `:69-73` uses the shared `initReplayRuntime`; every frame is `buildReplayViewerPacket` over the re-simulated `SimServer` (`:54-57`) — no parallel recording. |
| 3. Static viewer | PASS | `coworld_manifest_template.json`: `game.replay_viewer == {"bundle": "static-replay-viewer"}`; `tools/build_replay_viewer.sh` present, mode **100755** (`git ls-files -s`), the path `coworld build` hard-codes as the hook; the shell/worker fetch only the replay URL handed in (`static_replay_worker.js:113` is the sole `fetch`; zero absolute `http(s)://` URLs in `replay_broadcast.html`). `/client/replay` exists only as the starter's local dev route (`server.nim:194`) and is never declared to the platform; `coworld-release.yml:211` actively rejects a pod viewer. |
| 4. Both name spaces | PASS | `sim_state.nim:771` `"you": seatAlias(0)` (= `Alpha`, `:128-137`); observation carries no real name/seed/score (read in full). Spectator side: `broadcast.nim` roster ships `name` + `alias`; `results.names[0]=="scout"`, `aliases[0]=="Alpha"` asserted (`test_minigrid_engine.nim:85-87`). Runtime evidence: wasm-viewer smoke scorebug string carries both `SCOUT` and `ALPHA` (run 33213509695, "Load the bundle" output). |
| 5. Degrade-never-hang, ≤ 720 s | PASS | Every wait bounded: `attempt1Ms` 6000 / `retryMs` 3000, each clamped to the remaining `turnBudgetMs` 9500 (`decide.nim:191-204`, the N8 fix `a1a8f57` — the 11.6 s over-budget turn the review found is gone: `deadlineMs = min(configured, remainingMs)`); `turnSpacingMs` 2600 rate floor; rolling-60 s rate guard at 28 (`decide.nim:119-126`); budget guard settles early (`:145-152`); wall-clock stop at top of loop (`server.nim:487-491`, 660 s → `deadline`); lobby bounded at `lobbyJoinTimeoutTicks` 2400 = 100 s, paced in wall clock; `gameOverTicks` hold capped at 30 s (`server.nim:628`). Worst case ≈ 660 + one turn ≤ 9.5 + hold 20 ≈ **690 s < 720 s** (60 % of 1200). `sim_config.nim:126-139` rejects sub-second deadlines and `attempt1Ms + retryMs > turnBudgetMs`. Single-seat pin honoured: one request per turn **through the batch machinery** (`decide.nim:214-223`, a batch of one), ≤ 2 per turn, ≤ 110/episode. |
| 6. `num_agents` + four SEAT-COUNT invariants | PASS | `num_agents: 1` in both variants' `game_config` and in `certification.game_config`; `len(certification.players) == len(certification.game_config.players) == 1` (read from the manifest at head). `tools/ci/docker_smoke.sh:106-151` enforces all four invariants (missing / non-positive-int / cert players / gc players) plus the independent `SMOKE_SEATS` cross-check, before any container starts. Docker-smoke log for run 33213509695: `grep -c 'SEAT-COUNT FAIL'` = **0**; `smoke OK: seats=1 … reason=complete`. |
| 7. Scripted baseline plays full episodes legally, tuned | PASS | `test_minigrid_engine.nim:47-87` runs a real all-scripted episode with the actual binaries, asserts `results.reason == "complete"` and the results identities; `test_minigrid_driver.nim:44-118` bounds every baseline action (≤ 12, enum verbs, `goto` in 0..12, dirs in enum) over 300 states for both `scout` and `bumper`, plus never-suicide and driver legality. Tuning: `tools/ci/baseline_tuning.json` + `ci.yml:192-193` re-runs `tune_baselines.nim --check` (test-job log: "baseline tuning matches the recorded sweep"). |
| 8. LLM reply handling | PASS | `directives.nim:41-80` `extractJsonObject` — outermost balanced `{…}`, string/escape-aware, prose- and fence-tolerant, first/last-brace fallback; retry exactly once (`decide.nim:188` `while open and attempt < 2`); second failure → `scoutFallback` (same proc as the baseline, `decide.nim:112-117`, asserted by driver test 20), recorded countably three ways: `fallback` chat record, `results.fallbackTurns`, and the `falling back` log phrase (attempt 1 says `will retry`, `decide.nim:245-249`). |
| 9. Rune-safe truncation | PASS | `sim_types.nim:228-246` `truncateRunes`/`sanitizeSay`/`sanitizeNote` via `runeSubStr`, never byte slices; applied to say/notes/policy/prompt/stopDetail/fallback detail/error text (incl. the player registrar since `c9f8c59`: `minigrid_player.nim:43-44`). Tests feed 4-byte emoji at the caps: `test_minigrid_driver.nim:174-184` (`runeLen == 140/300`, `validateUtf8() == -1`) and `test_minigrid_replay.nim:134-173` (strict-UTF-8 `replay_summary.py` over every field at cap). |
| 10. Manifest validates | PASS | `game.docs` = `{"readme": {"type","value"}, "pages": [3 × {"id","title","content":{"type","value"}}]}` and `game.protocols` carries **both** `player` and `global` as typed objects (read at head). Note, stated plainly: the content objects are `"type": "uri"`, not the checklist's literal `"type": "text"` — I judge this the admissible typed-object shape, on the evidence that the **pinned CLI's own pydantic model accepts it**: I ran `_load_template_manifest` locally (loads clean) and `ci.yml:57-82` runs it in CI (green). B1's blocker is fixed (see Refuted). |
| 11. Viewer legible at 360 px | PASS | `.plate-name { flex: 1 1 auto; min-width: 3.2em; overflow: hidden; text-overflow: ellipsis }` — `client/replay_broadcast.html:4114-4119`, asserted at `test_minigrid_viewer.nim:207-209`. Labels hide under the starter's `.tiny` toggle at `boardW <= 620` (< 640; starter verbatim, `:4058` equivalent, asserted `:216`): pip family names → tooltips, grip disabled, ribbon clamps; the alias now **stays** (shrink+ellipsis, `minigrid_block.html:220-228`, fix `c8abac4`). |
| 12. Release order and scaffold | PASS | `coworld-release.yml`: build (`:159`) → certify (`:173`, `--timeout-seconds 300`) → upload-policies (`:216`) → upload-coworld (`:314`) → secret put (`:410`). ci.yml's smoke builds its image in the same job (`:219-234`). All three workflows present; `docker_smoke.sh` 100755 and template-identical modulo the three substitutions (diffed); `policies.json`: 4 policies — 2 × `PLAYER_PROMPT` champions with #2 carrying `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`, 2 × `PLAYER_SCRIPTED` fillers (scout, bumper). The placeholder gate exits 0: I ran the exact grep over the five files — zero hits. |
| 13. Viewer executes | PASS | (a) `wasm-viewer` green in run 33213509695 **including** "Load the bundle in a real browser" (`{"loaded":true,"ms":322,…}`, soak `"0 / 267" → "192 / 267" → "240 / 267"`); `needs: docker-smoke` (`ci.yml:286`); no `continue-on-error`. (b) `data-replay-loaded` set in the `'loaded'` branch (`static_replay.js:161`), `data-replay-error` in `showFailure()` (`:8-20`), both the shell's own paths. (c) `replay_runtime.nim:34-35` sets `startTick = gameStartTick` and seeks there; every seek clamps to `replayStartTick()` (`replays.nim:464, 503-506, 537, 552`); the lobby is still re-simulated internally. (d) `config.nims` has no `MODULARIZE`/`EXPORT_NAME`; worker sets `Module.onRuntimeInitialized` (`static_replay_worker.js:188`) — one lineage (coworld-ctf), and the smoke's `loaded: true` is the executed proof; `-s ABORTING_MALLOC=1` present (`config.nims`). |
| 14. Chrome is the starter's | PASS | (a) `client/chrome_common.js` **byte-identical**: `cmp` against `/workspace/starters/coworld-ctf/client/chrome_common.js` clean, sha256 `7ace7287…72f7c`, pinned by test 35. (b) Strongest possible evidence: I ran the repo's own deriver against the real starter mount — `python3 tools/build_broadcast_page.py --starter /workspace/starters/coworld-ctf --check` → **"client/replay_broadcast.html matches the derivation"**; the page is the starter's bytes plus enumerated removals plus the appended block (239 281 B > starter's 234 070 B — grown, not rewritten); test 36 additionally pins the block region to `client/minigrid_block.html` (`4dbec8e`). (c) `relayout()` sets `--band`/`--topband`/`--hudscale` on `:root`; nothing in the block is `position: fixed` or anchored `bottom: var(--band` (asserted, test 39); `#endcard { bottom: var(--band, 0px) }` + `.on` + every seek removes it; beats are labelled `<button class="beat-marker <kind>" title aria-label>` that seek on click (`mgBeat`, `minigrid_block.html:280-300`) with CSS for exactly the seven emitted kinds and no others (test 38, both directions). The block uses its own `mgBeat` rather than `chrome_common.markBeat` — a named, deliberate anti-shadowing decision in the note (test 37 proves no alias collision), satisfying the item's substance. (d) `#viewpanel`/`#minimap`/`#zoom-*`/`#povBadge`/`#fpv-hp`/`#fpv-gear`/`#fpv-map*` appear nowhere (test 39 asserts markup, CSS, wiring and quoted-id forms; the deriver repoints the starter's wiring at detached nodes). |
| 15. Every drawn string fits | PASS | Both `ci.yml` smoke steps carry `--strict-text-bounds` (`:399`, `:442`). The bundle smoke reports `canvas text: 0 drawn` (board pixels render in a Worker OffscreenCanvas — not evidence, per the item), which is exactly why the repo ships the required worst-case renderer fixture: `tools/ci/renderer_fixture.html` loads the **shipped** `index.html` in an iframe, drives the real `MinigridChrome.frame/event` through the page's own `MG_CTX` with a say built to **exactly 140 runes** with 4-byte emoji on the boundary (`:48-58`, self-checked at `:243-246`), a full-cap mission, fog, produce banner, all pip states and a lava endcard, at 960/640/**360** px, probes `#fpv` is not `display:none`, **asserts its own strings came out full-length** (`assertFullLength`, `:134-149`, fails via `data-replay-error`), and transcribes wrapped runs as blocks (`b19bc08`). Its CI step reports `canvas text: 74 drawn, 0 never inside the canvas, 0 ellipsized (--strict-text-bounds)` (run 33213509695). `never_inside == 0` on a covered check. |

Single-seat rider: the per-turn budget arithmetic stays inside 720 s — see item 5; sequential-vs-batch does not apply and the one request still rides the batch path (`decide.nim:214-223`).

## Fixer report audit

`r1-fixes.md` was not read (per the brief). In its place, each review finding was audited directly against the code at head:

| finding | review filed as | status at head (my verification) |
|---|---|---|
| B1 manifest KeyError('image') | blocking (item 10) | **Fixed** (`1187590`): placeholder in `game.runnable`, `type: "player"`; pinned CLI loads clean locally and in CI |
| N1 `#fpv` never visible | non-blocking | Fixed (`4796dea`): block calls `panel.classList.add('on')` (`minigrid_block.html:422-423`); fixture fails if `display:none` at any width |
| N4 alias hidden at `.tiny` | non-blocking | Fixed (`c8abac4`): alias shrinks+ellipsises, no `display:none` (`minigrid_block.html:220-228`) |
| N7 invalid entry counted twice | non-blocking | Fixed (`b22f971`): `installPlan(…, directive.overCap, …)`; `repliesRepaired += directive.dropped` only (`decide.nim:277-290`) |
| N8 turn could reach 11.6 s | non-blocking | Fixed (`a1a8f57`): attempt deadline clamped to remaining `turnBudgetMs` (`decide.nim:191-204`) |
| N10 no CI CLI-validator step | non-blocking | Fixed (`2aebf85`): `ci.yml:57-82`, green |
| N11 no sweep `--check` in CI | non-blocking | Fixed (`5cae390`): `ci.yml:192-193`, green ("baseline tuning matches the recorded sweep") |
| N12 derived page unchecked | non-blocking | Fixed (`4dbec8e`): test 36 pins the block region; I also ran the full `--check` against the real starter — matches |
| N13 plan/fallback/budget replay-only | non-blocking | Fixed (`4f3dc37`): `server.nim:385-390` `pushControlEvents(record)` on the live path, same proc as playback |
| N16 registrar sends untruncated prompt | non-blocking | Fixed (`c9f8c59`): `minigrid_player.nim:43-44` truncates before the wire |
| N17 fixture lacked full-length assertion | non-blocking | Fixed (`3269c27`, `617e4b9`, `b19bc08`): `assertFullLength` + 140-rune self-check + wrapped-block transcription |
| N18 xland config bounds | non-blocking | Fixed (`2e003ac`): `sim_config.nim:166-174` validates `xlandRules == 3`, `4 ≤ xlandObjects ≤ 8`; test added |
| N2, N3, N5, N6, N9, N14, N15, minor notes | non-blocking | Still true at head (note-vs-code divergences and template behaviours); none ties to a checklist item — correctly filed non-blocking. Spot-checked: N15's cap is still 4096-runes-after-16 KiB-byte-cap (`llm.nim:187-200`, rune-safe throughout); N9's docker_smoke still doesn't gate on `fault` (template behaviour; this run reported `complete`). |

## Non-blocking observations (judge's own)

- `tests/test_minigrid_replay.nim:200-202`: the empty-fixtures fallback ends in `check fileExists(path) or true` — a tautology. Harmless today (`tests/replays/gauntlet-seed42.replay` exists, so `swept == 1` and the real assertions run), but the guard would not catch an emptied fixtures directory.
- The renderer fixture's origin clamp (`renderer_fixture.html`, `transcribe()`) still pulls a wholly off-frame run back toward the canvas before measuring, degrading (not defeating) `never_inside` sensitivity for absurdly mis-parked elements — the review's N17 second half; the length assertion now covers the failure mode that mattered.
- The mission ribbon and task pips overlay the board's top-left corner rather than the note's "left gutter" (review N3, still true at head). Spectator-facing polish, not a checklist item; the fixture proves the text stays in-frame at 360 px.

BLOCKING: 0
