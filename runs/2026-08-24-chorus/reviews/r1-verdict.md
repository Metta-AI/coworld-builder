blocking: 0

# r1 verdict — chorus

Head: `5e2dbe491b3e0ef2bfc955cae10cb7983dd1ff76` (main)   Checklist: `prompts/30-review-loop.md` §ACCEPTANCE CHECKLIST (items 1–14 + simultaneous-batch rule)   Independent read written before reading fixes: **yes** (repo, design note, starter diffs, CI run 32704049550 logs, and the checklist were all read and my notes formed before `r1-fixes.md` was opened; `r1-review.md` was read after my own pass of the code, per the brief)

Judged repo state: `/tmp/cogame-chorus` at `5e2dbe4` (fetched + checked out). Starter: `/workspace/starters/cogame-bullwhip` (read-only). CI evidence: run **32704049550** (`gh run list` → headSha `5e2dbe4…`, conclusion `success`, created 2026-08-24T07:59:21Z), full log pulled with `gh run view --log`.

## Standing blocking findings

**None.** Every reviewer finding is either fixed at the current head, resolved as a design-note error (the note was amended; the code was already the correct, starter-verbatim shape), or was never tied to a checklist item. My own independent checklist pass found no additional blocking findings.

## Refuted / resolved at head

### F1 — "retry-exhausted LLM fallback recorded as `scripted: false`" → FIXED AT HEAD (was true at the reviewed sha 8777d56)
- Evidence: `src/chorus/llm.nim:45-48` at `5e2dbe4` — `Decision` now carries `scripted*: bool` ("true when a baseline produced this bar … including the fallback after the retry is exhausted"); `llm.nim:208` — `scriptedAction` sets `result.scripted = true` and it is the sole producer of baseline bars, including the fallback loop at `llm.nim:615-618`; `src/chorus/server.nim:346` — `let wasScripted = decision.scripted` (the old guess `scripted[seat] != skNone or client.disabled` is gone, commit `80e5205`).
- Test: `tests/test_bot.nim:117-147` "a seat that fails both attempts is recorded as scripted" points an **enabled** client at `http://127.0.0.1:9` and asserts all four decisions and all four recorded `bar` events carry `scripted: true`. Green in run 32704049550 (debug and `-d:release`).
- Verdict: the finding was real at the review sha and is **not standing** at the head I judge. Counts zero.

### F2 — "zero-bar score is 15.0, not the 0.0 the design states" → REFUTED as a standing discrepancy
- Evidence: `src/chorus/sim.nim:303-304` (`if n < 2: return 0.5`) and `:324` (`max(0.0, 1.0 - 2.0*abs(raw-0.5))`) do produce `piece = 15.0` at `n = 0` — I confirm the reviewer's trace — but the design note at the current head documents exactly this: design.md:106-108 ("novelty takes its neutral `raw = 0.5` … giving `piece = 100 × 0.15 = 15.0`, `parts = [0, 0, 0, 1]` and **all-zero credits**") and design.md:230 ("the neutral-novelty floor `piece = 15.0`, all credits `0`"). Coworld-builder commit `3e8e957` amended the two casual `0.0` asides that contradicted the note's own `n < 2` rule (design.md:184). No checklist item names this value; `results_schema.piece: 0..100` holds; re-derivation reproduces it (`turn 0 … "piece": 15.0` in the smoke replay).

### F4 — "`client/player.html` opens a `/player` websocket, contradicting the design" → REFUTED at head
- Evidence: design.md at head, §Server (lines ~697-703): "`/client/player` is the seat-operator page kept from bullwhip verbatim: it opens `/player?slot=N&token=T` … and `playerUpgradeHandler` rejects any mismatch with **401**". The old sentence the reviewer quoted ("Neither `/client/` HTML route opens a player socket") was removed in `3e8e957`. The page is the starter's verbatim (checklist 14 requires keeping it); `server.nim:433-437` 401s any slot/token mismatch. No checklist item touched.

### F5 — "play deadline measured from before the connect wait" → REFUTED as a discrepancy; conduct verified bounded
- Evidence: `server.nim:249-250` (`gameStart = epochTime()` before the connect wait) and `:281-283` (`playDeadline = gameStart + timeoutSeconds * PlayBudgetFraction`) — true as traced, and design.md:303-310 at head states precisely this arrangement as deliberate ("a slow connect eats into the 720 s of play rather than extending the episode … worst case in wall clock … ≈ 801 s, ~400 s clear of the 1200 s kill"). Every wait is bounded (see checklist 5 below); the direction is conservative (bars given up to the scored, acceptable `deadline` ending). Not blocking.

### F3, F6, F7, F8, F9 — non-blocking in the review; all confirmed, none stands against a checklist item
- **F3**: the starter genuinely has no `client/chrome_common.js` (`ls /workspace/starters/cogame-bullwhip/client/` → `chrome.css global.html player.html renderer.js replay.html`), so item 14's literal diff target does not exist; see my item-14 pass below for the verification actually performed.
- **F6**: `makeEffects` in `chrome_common.js:161-198` switches on bullwhip's `week`/`order` kinds and is never called by chorus (`renderer.js` uses its own `makeChorusEffects`, `renderer.js:88`); keeping it verbatim is what item 14 demands. Confirmed, advisory only.
- **F7**: at ≤420 px only colour links a plate to a lane. Confirmed from `chrome.css:601-603` and `renderer.js:368-374` — but item 11's requirements are met (below). Advisory.
- **F8**: fixed at head by `5e2dbe4` — `tests/test_sim.nim:409-410` now plays the fixture episode to settlement and `:431` asserts `evEnd in kinds`, so the `end` event round-trips through the codec. Not standing.
- **F9**: `chorusMarkBeat`/`buildChorusScrub` (`renderer.js:703, 721`) instead of a `chrome_common.markBeat` — the starter has no `markBeat` either; the anti-shadowing naming is deliberate (design.md §Transport rules) and enforced by `tools/ci/chrome_check.mjs:40-48`, green in this run. Substance of 14(d) verified below. Not a finding.

## Checklist pass (independent)

| item | status | evidence (path:line or run) |
|---|---|---|
| 1 CI green, no test loosened | **pass** | Run **32704049550**, headSha `5e2dbe4…`, conclusion `success`; all three jobs and every step `success` (`gh run view --json jobs`), no `continue-on-error`. `git -C /tmp/cogame-chorus log -p -- tests/` over the run's whole history (a2e9334→5e2dbe4): tests added once in `147138b`, then only **tightened** — `80e5205` adds the fallback-provenance test and strengthens an assertion (`"", "", true` → `decisions[index].scripted`), `5e2dbe4` extends the codec test to cover `evEnd`. No deleted assertion, no widened tolerance, no skip/xfail, no removed file. |
| 2 Replay re-derivation, frame by frame, viewer uses it | **pass** | `sim.nim:645-689` `replayMatch`: `bar` events are the only source; `turn` events are checks — `turn`/`chord`/`piece`/4×`parts`/4×`credits` compared within `1e-4`, `ChorusError` on mismatch (`:660-675`). Tests: `test_sim.nim:385-402` (`frames.len == events.len+1`, final frame `tableStateJson` byte-equal to live), `:404-443` (codec round-trip for **every** kind incl. `evEnd`; tampered `piece` and `chord` both raise), `:445-457` (deadline end settles the replay). Viewer: `replay-viewer/chorus_replay.nim:37-39` builds `states` **only** from `replayMatch(config, events)`; `renderer.js` `attachReplay` draws `payload.states` — no parallel recording exists in the payload (`server.nim:169-182`). Real bytes: the 33-event smoke replay loaded in CI (`loaded: true`), i.e. `replayMatch` did not raise on a recorded episode. |
| 3 Static viewer | **pass** | `coworld_manifest_template.json:15-17` `"replay_viewer": {"bundle": "static-replay-viewer"}`; `tools/build_replay_viewer.sh` present, mode `100755` (`ls -l` → `-rwxr-xr-x`), `mkdir -p` of the output parent at `:22` before the containment check; the shell's only network call is `fetch()` on the `?replay=` URL (`static_replay.js:76`), assets relative (`assetBase: "./assets"`). No `/client/replay` pod path in the manifest — the pod route in `server.nim:533` mirrors the starter and is not declared as the viewer. |
| 4 Both name spaces | **pass** | `tableNames` (`sim.nim:122-133`) seeds anonymous cog aliases; `test_sim.nim:518-538` asserts each seat's alias is in both prompts and **none** of four policy names is; `resultsJson` emits policy names (`sim.nim:533`); the viewer maps aliases→policy names via `makeNameMap` (`renderer.js:1250`), baseline fillers keep aliases (`isBaselineFiller`). Player `final` frames carry aliases (`server.nim:208-217`). |
| 5 Degrade-never-hang, ≤60 % | **pass** | Every wait bounded: connect wait ≤ `playerConnectTimeoutSeconds` 180 s (`server.nim:252-258`); batch-spacing floor ≤ `min(minTurnSpacingMs, playDeadline − now)` (`:324-331`); LLM batch ≤ `llmTimeoutSeconds` per request × 2 attempts — **verified in curly's source at the pinned commit** (`nimby.lock: curly 1.1.1 a0f42baa…`): `makeRequests(curl, batch, timeout = 60)` sets `CURLOPT_TIMEOUT` per request (seconds, whole transfer), is `{.raises: [].}`, and returns a response-or-error per request; pacing ≤ `turnDelayMs`, itself capped by `sampleEpisode` at `PacingBudgetMs div bars` (`sim.nim:142-143`); flush 500 ms + grace 20 s then `quit(0)` (`server.nim:223, 237-239`). `playDeadline = gameStart + 0.6 × 1200 = 720 s` checked before every turn (`:299-307`), `endEarly()` between turns only, `reason = "deadline"`. Worst case per turn 2×30 s; `bars=8` → ≈483 s, `bars=10` → ≈604 s, both < 720 s. No unbounded loop or blocking read found in the game path. |
| 6 `num_agents` everywhere + smoke invariants | **pass** | `num_agents: 4` in all three variants (`coworld_manifest_template.json:374, 401, 428`) and `certification.game_config` (`:453`). `tools/ci/docker_smoke.sh:110-151` enforces all four invariants + the `SMOKE_SEATS` cross-check, each exiting via `SEAT-COUNT FAIL:`. **Grepped the full run-32704049550 log: 0 occurrences of `SEAT-COUNT FAIL`**; log shows `game=chorus seats=4 … "num_agents": 4`, `smoke OK: seats=4 … reason=complete`. `initSim` raises unless `players.len == 4` (`sim.nim:403-405`). |
| 7 Scripted baseline, full legal episodes, tuned not guessed | **pass** | `test_bot.nim:43-69`: seeds `[1,5,42,1234]` × both baselines, `check sim.reason == "complete"`, `turnsPlayed == bars`, every bar 16 tokens in `{-1} ∪ [0,13]`, `target == turn`, <2000 ms — green twice in run 32704049550. Tuning: the committed 200-seed measurement harness (`test_bot.nim:71-94`) pins the band `[40, 92]` and `pedal < arpeggio` ≥90 %, and the CI log **cites the measured output**: `arpeggio piece score over 200 seeds: mean 74.50, min 71.84, max 75.62; pedal scored lower on 200/200` — mid-band with wide margins. The design note derives the parameters against the metric to the digit (density 15/64 = 0.234 inside [0.20,0.55]; 9/16 sounding columns, none 4-voice; `Ra ≈ 0.84`), and I re-derived those figures from `llm.nim:164-201` and they match exactly. The parameters are demonstrably computed and measured, not guessed; the verifiable substance of the item is satisfied from the tree and cited CI evidence. |
| 8 LLM reply handling | **pass** (at head) | `extractJsonObject` tolerates leading/trailing prose (`llm.nim:401-414`); `parseSteps` accepts array/string forms, rest spellings, floats, numeric strings (`:500-535`); missing `target` → `t` (`:544-546`); retry **once** (`for attempt in 0 .. 1`, `:585`) with the hint appended (`:592-593`); fallback to `arpeggio` (`:615-618`) **recorded** via `Decision.scripted` → `server.nim:346` → `event.scripted` → replay (`sim.nim:506, 722`), asserted by `test_bot.nim:117-147`. Disabled client: no network, no retries (`:579-587`), asserted `test_bot.nim:96-114`. |
| 9 Rune-safe truncation | **pass** | `applyBar` cuts `say` at `runeSubStr(0, MaxSayLen)` and `notes` at `MaxNotesLen` (`sim.nim:489-497`); `cleanText` rune-cuts model text and captured errors (`llm.nim:475-482`, `MaxErrorLen=200` at `:610`); prompt cap 4000 runes (`server.nim:498-500`). Test: `test_sim.nim:355-382` feeds `"音"`×400/×900, asserts exact rune counts, `validateUtf8() == -1` on strings **and serialized event JSON bytes**, round-trips. `docker_smoke.sh:285-292` strict-UTF-8-parses the whole replay (default on), green. |
| 10 Manifest validates | **pass** | `game.docs` = `{"readme": {type,value}, "pages": [{id,title,content:{type,value}} ×2]}` (`coworld_manifest_template.json:261-284`); `game.protocols` carries both `player` (`:252-255`) and `global` (`:256-259`). `config_schema` `additionalProperties: false`, arrays all `minItems`/`maxItems` 4; `results_schema` matches `resultsJson`'s exact 14 keys. |
| 11 Legible at 360 px | **pass** | `.plate-name { … min-width: 3.2em; flex: 1 1 auto; }` inherited (`chrome.css:280-292`) and restated in the chorus block (`:533-534` of the appended CSS); `@media (max-width: 640px) { .plate-label { display: none; } }` (`:460-461`). Compact stage composition below 560 px (`renderer.js:46, 154-177`, `drawThumbnail`). CI smoke read the scorebug back intact at all three scrub positions. |
| 12 Release order and scaffold | **pass** | `coworld-release.yml`: Build manifest (:153) → Certify (:167) → **Upload the policies** (:206, comment "BEFORE upload-coworld") → Upload the Coworld (:304) → Put the Coworld secret (:342, "AFTER upload-coworld"). All three workflows present. `docker_smoke.sh` mode 100755; smoke runs against the image built in the same job (`ci.yml:176-185`). `policies.json`: 4 policies — champions `chorus-cantor` and `chorus-weaver` both `PLAYER_PROMPT` with materially different strategies, `chorus-weaver` carries `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"` (`:15`), fillers `chorus-arpeggio`/`chorus-pedal` `PLAYER_SCRIPTED`. Placeholder gate: I ran `grep -n '<slug>\|<IMAGE>\|<SEATS>'` over the five files myself → **no matches** (gate exits 0). |
| 13 Viewer executes | **pass** | `wasm-viewer` `needs: docker-smoke` (`ci.yml:212`); the `Load the bundle in a real browser` step **ran and succeeded** in run 32704049550: `{"loaded":true,"ms":289,"clock":"BAR 2 / 6 · C MIXOLYDIAN · 84 BPM · WAITING ON 3", …}`, `soak: 10s of playback kept advancing` (the `moved` gate compares clock/tick/scorebug across before/middle/after — the `null`s in the echo are the absent `#tick` selector only), scrub readouts differ at 0/50/100 % (`FINAL — PIECE 68.4` at 100 %). No `continue-on-error` in the job. Markers: `data-replay-loaded="true"` set at `renderer.js:1339` after the frame loop's first synchronous `renderer.draw` (`:1315-1337`); `data-replay-error="<message>"` set on every failure path (`static_replay.js:56`) and removed on retry (`:107, :134`). Same-starter contract: `config.nims` diffs from bullwhip's by output name, `EXPORT_NAME=ChorusReplayModule`, `_bw_*`→`_ch_*` only; `MODULARIZE=1` kept (`config.nims:38`) and the shell calls the factory `ChorusReplayModule()` (`static_replay.js:138`); no `Module.onRuntimeInitialized` anywhere. |
| 14 Chrome is the starter's | **pass** | The starter has **no** `client/chrome_common.js` (design note names this file as new-by-construction from the starter's `renderer.js`). I extracted all 15 transplanted functions from both files and hashed them: `assetUrl loadImages seatColor ellipsize hexToRgb rgba roundRect wrapLines escapeHtml isBaselineFiller makeNameMap applyNames clampName makeEffects bindFeedToggle` — **all byte-identical** to `cogame-bullwhip/client/renderer.js`; palette constants identical; the only non-starter lines are the IIFE, the export, and the banner-marked `relayout()` (`chrome_common.js:221-244`) — the named, minimal patch the design note records. `chrome.css`: `head -c 11964 | sha256sum` = `2bfa9443…` = the starter file's full hash; additions strictly appended under the banner; `chrome_check.mjs` enforces both in CI (step green). `replay_broadcast.html`: my diff against `cogame-bullwhip/client/replay.html` shows **only** title/wordmark/clock renames, `BullwhipRenderer`→`ChorusRenderer`, one inserted `chrome_common.js` script tag, and the banner-marked appended game block; no element removed (74-line starter page intact). Transport: (a) `relayout()` writes `--topband`/`--band`/`--hudscale` on `document.documentElement` (`chrome_common.js:237-240`), on load/resize, and `bindFeedToggle` dispatches `resize` (`:216`); (b) the two added chrome pieces are `#audio.tbtn` **inside** `.tbar` and the static flex `#chorus-legend` appended to `#transport` — nothing fixed-positioned in the band; (c) `#endscreen { bottom: var(--band, 0px); }` in the appended CSS; shown via `container.classList.toggle("show", !!show)` as `updateEndscreen`'s first live statement (`renderer.js:970`), matching the CSS rule `#endscreen.show` (`chrome.css:383`), and **every** seek path (scrub pointer events `:782-790`, beat-marker click `:713-716`, play-button restart) routes through `setIndex`, which calls `updateEndscreen` on every index change (`:1305-1308`) — there are no back/forward/keyboard seeks in this page or the starter's; (d) beats are labelled `<button type="button">`s with `title` + `aria-label` seeking on click (`:703-719`), and every emitted kind — `start`, `turn`, `bar`, `edit`, `end` — has a CSS rule in the appended block (`chrome.css` `.beat-marker.{bar,edit,turn,end,start}`), seat tint via the inherited `.seat0…seat4 { --tc: … }` (`:205-209`). Zoom/minimap: no `#viewpanel`, no `zoomAt/setZoom/attachMinimap` anywhere (grep clean); the starter has none and the board always fits the frame. |
| Simultaneous batch | **pass** | `decideAll` builds one `RequestBatch` and issues **one** `client.curl.makeRequests(batch, timeout)` per attempt for all open seats (`llm.nim:588-596`); `grep makeRequests src/` → exactly that line; scripted seats decided locally with no request. The batch runs outside the state lock on a snapshot (`server.nim:296-338`). No sequential per-seat calls exist. |

## Fixer report audit

| finding | fixer said | I verified | agrees |
|---|---|---|---|
| F1 | fixed in `80e5205` (Decision.scripted; server reads it; new test) | `llm.nim:45-48, 208`, `server.nim:346`, `test_bot.nim:117-147`; CI log shows the transport-failure path exercised and `[OK]`; smoke replay 24/24 `scripted: true` offline | yes |
| F2 | design note amended, code right | design.md:106-108, :230 at head document 15.0 (coworld-builder commit `3e8e957`); code trace and smoke replay confirm 15.0 | yes |
| F3 | verification note only | starter has no `chrome_common.js`; 15 functions hashed byte-identical | yes |
| F4 | design note amended, code is the starter's | design.md §Server at head describes `/client/player` opening `/player` with 401 rejection; `server.nim:433-437` | yes |
| F5 | design note amended, code conservative | design.md:303-310 at head states the arrangement + 801 s worst case; all waits bounded | yes |
| F6/F7/F9 | no change, justified | confirmed as above — none stands against a checklist item | yes |
| F8 | fixed in `5e2dbe4` | `test_sim.nim:409-410, 431`; tamper checks intact; green | yes |
| CND-1 (tuning harness) | carried to judge | ruled **verified in substance** (see item 7): committed 200-seed measurement harness + CI-cited output mean 74.50 min 71.84 max 75.62, pedal lower 200/200, + design-note derivations matching code to the digit | resolved |
| CND-2 (curly semantics) | couldn't read source | **settled**: fetched curly @ pinned `a0f42baa…` — `makeRequests(…, timeout = 60)` sets `CURLOPT_TIMEOUT` per request in seconds, `{.raises: [].}`, response-or-error per request; the batch wait is bounded | resolved |
| CND-3 (whisky timeout) | couldn't read source | not needed for any checklist item: the player's receive loop exits 0 on close/raise/none alike (`chorus_player.nim:56-84`), bounded by the game's own bounded lifecycle; smoke shows all four players exit 0 | resolved (immaterial) |
| CND-4 (phase-60 counting) | moot after F1 | agreed — the replay flag is now correct and the stdout line remains | yes |

The fixer's claim of a fast-forward push via the Git Data API checks out: `a2e9334 → 147138b → 8777d56 → 80e5205 → 5e2dbe4` is linear, and CI ran fresh on the head sha.

## Non-blocking observations (mine)

- The viewer smoke runs at a 1280×800 viewport (`viewer_smoke.mjs:321`), so the sub-560 px compact composition and the 360 px legibility posture are verified from the CSS/JS only, never executed in CI. Item 11 names only the CSS conditions, which hold; noting the gap for a future round.
- `results.json` is written with `"reason": ""` if the sim were somehow not done (`sim.nim:551`) — unreachable in practice since `finishEpisode` runs only after the loop exits on `done`, but the empty-string third value exists in code even though only `complete`/`deadline` have `settle` call sites.

## Verdict

The one blocking finding of r1 (F1) is fixed at `5e2dbe4` and covered by a new test; the review's remaining findings were non-blocking and are either fixed (F8), resolved by correcting the design note to the code's (correct, starter-faithful) behaviour (F2/F4/F5), or confirmed as advisory (F3/F6/F7/F9). My independent pass of all 14 checklist items plus the simultaneous-batch rule, against the tree at `5e2dbe4` and CI run 32704049550, found every item satisfied with the evidence cited above. Both of the reviewer's "could not determine" items that touch a checklist item are now settled (curly's timeout semantics from the pinned source; the baseline-tuning evidence from the committed 200-seed harness and its CI-logged measurements).

BLOCKING: 0
