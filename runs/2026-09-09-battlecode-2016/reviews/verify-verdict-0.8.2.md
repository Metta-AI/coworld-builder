blocking: 0

# Phase-60 verdict — battlecode-2016, final state (0.8.2)
Head: 95e1412048c1c490ab32a0e250e7c2036b4f41d2 (coworld-builder main) · cogame-battlecode main@6e89d0fe57cb12e564451a2d2dec6bc99bd7024d
Checklist: docs/SPEC.md §Definition of done (phase 60) / prompts/60-verify.md
Artifact adjudicated: runs/2026-09-09-battlecode-2016/VERIFY.md (third pass, against 0.8.2)
Reading order: SPEC §DoD → prompts/60-verify.md → VERIFY.md (the adjudicated artifact, per the brief), then all evidence below fetched/read by me from the platform APIs, GitHub, the committed artifacts, the replay bytes, the source tree, and the screenshots — which I read with my own eyes — **before** opening the subordinate files (reviews/verify-verdict.md, r3-duel-label-fixes.md, r2-endcard-fixes.md).

**Verdict: all eight definition-of-done items UPHELD at the current head. Zero blocking findings.** Five advisory notes, all transcription/evidencing nits in VERIFY.md with no verdict impact.

## Item-by-item (my own evidence)

### 1. ≥2 completed rounds after the fillers were set — UPHELD
My fetch (`GET /rounds?league_id=league_6cdded0f…&limit=50`): **33 rounds `completed`** (round 1 `round_5a5bf68d…` … round 33 `round_39a0402f…`, which completed after VERIFY's fetch — VERIFY's count of 32 was correct at its time, round 33 was then `pending`). Zero `failed`/`discarded`, every `error: null`. Filler registration precedes round 1: log.md:93 (fillers POST 13:24Z, response listing exactly the two filler UUIDs, "Set BEFORE any trigger-round") and log.md:96 (round 1 observed still `pending` with fillers registered). Rounds 30–33's episode-requests all carry `coworld_id: cow_26cf6929…` — ≥2 completed rounds under the new coworld specifically (4 available). TRUE.

### 2. Both champions ranked, fillers absent or Baseline — UPHELD
My fetch (`GET /divisions/div_1e064838…/leaderboard`, bare array): rank 1 `daveey-1` `battlecode-bc16-pullers:v1` score 1057.43, rounds_played 33, wins 26; rank 2 `daveey` `battlecode-bc16-bulwark:v1` score 976.61, rounds_played 33, wins 18; rank 3 `richard` `co-gas-battlecode-champion-richard:v5`, rounds_played 10. Both champions ranked with `rounds_played ≥ 1`; **no filler row at all** (`battlecode-bulwark`/`battlecode-greenhorn` absent — "absent" satisfies the disjunct). `richard` is a third real entrant, `is_filler: false` in his pairings (my fetch of `ereq_282bb51c…`), which the item does not disallow. Labels still `:v1`, per the rail decision. TRUE.

### 3. Latest round's episode request completed with a replay — UPHELD
Round 32 (`round_b2840ff5…`, the latest completed at VERIFY's fetch time): nested `GET /rounds/$R/episode-requests` → 3 rows (one per pairing), all `completed` with non-null `replay_url`s, all `coworld_id: cow_26cf6929…`. The champions' pairing `ereq_5a0c4611-0cc8-4a6b-8b50-43107ffa4954`: `status: "completed"`, `replay_url: …/c6230cca-01a9-4ba5-be7f-52b223f38baf.replay`, participants `daveey` (pos 0, bulwark) and `daveey-1` (pos 1, pullers), both `is_filler: false`, and **`coworld_version: "0.8.2"` literally in the response**. I also checked round 33 (latest at my fetch time): all 3 episode-requests `completed` with replays, all 0.8.2 — no regression. TRUE.

### 4. Replay bytes valid and show the game — UPHELD
My download of `c6230cca….replay`: `jq -e` strict parse ok; `protocol: cogame.battlecode.v1`; `.result.reason: "complete"`; `.result.fallbacks: [0,0]`; `.result.policy_kind: ["llm","llm"]`; `.result.decision_ms: [6448,6448]`; `.result.sheet_defaults_applied: [[],[]]` — both champion seats made real, non-fallback LLM doctrine decisions, and the endcard's two doctrine panels (read from the pixels, §8) show materially different 11-clause strategies. Event stream: **249 events** across 17 kinds (my histogram matches VERIFY's kind-for-kind: duel 22, infection 60, turned 72, zombie_wave 19, archon_lost 12, …). Three games, all `archons_destroyed`, lengths 1425/871/1005 (`game_end` events fetched by me), wins [2,1], scores matching the episode-request's `participant_scores` (462.67–236.33). The canonical coworld's manifest names `source_url: …/tree/6e89d0fe…` (PR #13's merge). TRUE.

### 5. Hosted game log clean — UPHELD
My elevated fetch of `ereq_5a0c4611…/artifacts/logs` (HTTP 200, 1903 bytes): `grep -E 'falling back|LLM provider is unavailable|cut off at max_tokens|rejected'` → **CLEAN**. The game container's `refused a seat-0 connection` line says "refused", which the pattern does not and should not match. Both seats registered `kind=llm`, `settled: complete`, `reason=complete games=3`. TRUE.

### 6. Public page uses the static replay path — UPHELD
My evidence, three sources: (i) raw HTML of `https://softmax.com/battlecode` (HTTP 200, ~1.04 MB) has no `<iframe>` — the documented client-rendered false negative; (ii) the same page's SSR payload `state.playlist[0]` carries a **featured match** with `coworldId: "cow_26cf6929-4ace-4d27-b4e6-da7edd5eb2ef"`, `coworldVersion: "0.8.2"` and a non-null `replayUrl` — featured match present, served under the new coworld (the match itself is from the coworld's Competition division — expected on a multi-variant MOD coworld's shared page); (iii) my own `POST /coworlds/replays/session` for the new cow_id + check-3 replay → `ready: true`, `viewer_url: …/v2/coworlds/replays/static/cow_26cf6929-4ace-4d27-b4e6-da7edd5eb2ef/sha256%3A2985d08f…8bf81/index.html?v=2#replay=…c6230cca….replay` — static route, fragment form, new cow_id **and** new manifest hash, not a `/client/replay` pod URL. `GET /coworlds` (bare array at my fetch time) confirms exactly one `canonical: true` battlecode row: `cow_26cf6929…`, `version: "0.8.2"`, `manifest_hash: sha256:2985d08f…8bf81`; 0.8.1 and 0.8.0 rows are `canonical: false`. TRUE. (Note 4 below on VERIFY's evidencing gap for the featured-match sub-condition.)

### 7. Certification declared the static bundle — UPHELD
Read myself from the committed `runs/2026-09-09-battlecode-2016/release-result.json` (already the 0.8.2 artifact: `cow_id: cow_26cf6929…`, `manifest_sha: sha256:2985d08f…8bf81`, `version: 0.8.2`, `ok: true`, `canonical: true`, `hosted_certification: "certified"`, `step_failed: null`, `errors: []`): `.certify.replay_liveness` = `"Replay liveness: skipped (static replay bundle declared; /client/replay and /replay not required)"` — the required string, present twice in `.certify.output_tail` alongside the 10-step pass transcript. Release run 34399197087 (`Coworld release`, Metta-AI/cogame-battlecode) checked via `gh`: `conclusion: success`, `headSha: 6e89d0fe…`; main CI 34391925052 likewise `success` at the same sha. TRUE.

### 8. Spectator judgment — viewer executed, then judged — UPHELD
**I formed this from the committed images and JSONs myself.**
(a) `loaded: true` — `viewer-check/viewer-smoke.json` (canonical run 34405727549, `conclusion: success`, checked via `gh`): `{"loaded":true,"ms":2466,…,"feed_lines":7}`, signals `{"data_replay_loaded":"true","data_replay_error":null,"bridge":["ready"],"bridge_ready":true,"bridge_error":[]}`, `failure: null`. Its `url` field carries the new cow_id + URL-encoded manifest sha and `#replay=` exactly check 3's replay — the same `src` check 6's session route returns.
(b) The replay advances — three distinct scrub readouts: 0% `2:07 GAME 1 OF 3 — CLOSEQUARTERS doctrines`, 50% `1:08 GAME 2 OF 3 — BOXY doctrines`, 100% `FINAL MATCH OVER doctrines`; the 50% map name matches `.result.games[1].map: "boxy"` in the replay bytes I fetched. Unattended 10 s soak: `round 1 → 193 → 241 / 3000`, `moved: true`, `page_errors: []`. The supplementary round-31 run 34404590215 (`success`) shows the same shape (`loaded:true`, TURTLE → VOLUTED → MATCH OVER).
(c) My spectator judgment from `viewer-smoke.png` (round 32, at match-over): the card is legible and shows this game. Headline band **drawn whole**: `CLAN ASH — DAVEEY`, with the boxed line `THE GAME ENDED ON ARCHONS DESTROYED IN GAME 3, ROUND 1005` — reconciling exactly with the replay's `game_end {game:2, round:1005}` — and `score 463 — 236 · 3 games played · 1005 rounds in the last one`. **No "Singularity" anywhere.** Two doctrine panels, both ending in complete sentences ("…walks its infected units away from its own archons" / "…walks its infected units at the enemy archons to die there") plus their mottos. Box score reconciles with `.result.games[2]` (133 units built, 2 dens destroyed, 3 neutrals activated for Ash; 94 built, 4 archons lost for Basil); it runs past the bottom edge — the documented, intended scroll behaviour of the E2 fix, not a defect. **The killfeed reads `TRADE — 1 attackers lost to 1, game 3, round 943`** — matching raw event `{"kind":"duel","game":2,"round":943,"lost":[1,1]}` — alongside `ARCHON DOWN` (rounds 1004, 954, 905), `OUTBREAK 3`, `WAVE — 16 zombies…`, and the game-3 result line = 7 beats = `feed_lines: 7`. **No "LAUNCHER" anywhere on the card.** The round-31 screenshot (`viewer-smoke-round31-tradecontrast.png`, my own read) shows `TRADE — 1 attackers lost to 1, game 3, round 555` fully legible and a second, panel-cropped `…DE — 1 attackers lost to 1, …ame 3, round 703`, headline `CLAN BASIL — DAVEEY-1` / `…GAME 3, ROUND 752` (reconciles with that replay's `game_end {game:2, round:752}` which I fetched), no LAUNCHER, no Singularity. Chrome is the starter's family: top scorebug strip with aliases + real player names + mottos + live scores, right-hand killfeed, bottom transport strip (rewind/step/play, `+25`, `spoilers` toggle, `round 1004 / 3000`, speed chips 1×–16×), scrubber with coloured momentum graph and chapter ticks. A spectator can tell who won (Clan Ash / daveey, 2–1, 463–236) and why (archons destroyed, game 3 round 1005, zombies doing most of the killing per the ARCHON DOWN lines). Not a different product. TRUE on all three sub-conditions.

## The four questions

**Q1 — the verifier's correction about label baking: CORRECT, verified independently at three levels.** (i) Source: `src/battlecode/broadcast.nim:151` — `proc beatsFor*(doc: ReplayDoc, frameOfGameRound: …)` computes the `duel` label at playback time from the raw event's `lost` array (`:364` `if isBc22 or isBc16: label = "TRADE — …"`); `broadcast` is imported by `replay-viewer/bc_replay.nim` (`beatsFor` called at :93), which `Dockerfile.replay-viewer` compiles to `bc_replay.wasm` for the static bundle; the replay writer `src/battlecode/replay.nim` contains no label strings at all. (ii) Bytes: my own greps — the 0.8.2 replays of rounds 30, 31 and 32 **and** the 0.8.1 replay of round 29 (`772f9b15…`, `coworld_id: cow_089d7551…`) all contain **zero** `LAUNCHER` and **zero** `TRADE`. The coordinator's expected grep difference cannot exist; the verifier was right to say so. (iii) Render: the screenshots produced by the 0.8.2 bundle (URLs carry the new cow_id + manifest sha) show `TRADE` lines that reconcile with specific raw `duel` events by exact game/round, and no `LAUNCHER`. The rendered evidence **does** establish the fix, and the verifier's corollary is the correct scope: the label depends only on `doc.year` and the bundle that renders it, so **every** replay rendered through the 0.8.2 bundle — 0.8.1-era replays included — now reads `TRADE`.

**Q2 — the third entrant: item 2 remains satisfied.** The item requires both champions ranked (they are, ranks 1–2, rounds_played 33 each) and fillers absent or Baseline (absent — no `battlecode-bulwark`/`battlecode-greenhorn` row exists). `richard` is neither a champion nor a filler; he is a real entrant (`is_filler: false`) in a `public: true` round-robin league, and nothing in SPEC item 2 caps the leaderboard at two rows or excludes third parties. His pairings also ran under 0.8.2 (my fetch). No finding.

**Q3 — the two discarded viewer-check runs: the cited evidence stands on its own, and the discards were legitimate — the opposite of shopping for a green.** I downloaded attempt 1's (34402317310) artifact myself: `loaded: true` but 0% and 50% scrubs **identical** (`2:56 GAME 1 OF 3 — VOLUTED` twice) and 100% still inside game 1 — a genuine sub-criterion-2 failure for that replay, on a CI-**green** run the verifier refused to cite (they judged the JSON, not the conclusion). Its soak advanced (`round 4 → 196 → 244`), showing playback works; and I verified the stated cause from round 30's replay bytes: game 0 runs 3000 of 4476 total rounds = **67.0%**, exactly as claimed. Attempt 2 (34402585742) is `conclusion: cancelled` — stuck `queued` in a repo-wide Actions backlog; it produced no evidence to discard. Retrying against a different round's replay is the prompt's own "different approach" rule, and the sub-criterion tests the viewer, not one replay. The two cited runs (34405727549, 34404590215) are both `success` with committed artifacts showing `loaded: true`, three distinct readouts, `failure: null`, and 0.8.2-bundle URLs — each sufficient alone.

**Q4 — the `:v1` rail decision's factual claims: both hold; no contradicting evidence.** (a) The ladder runs under the new coworld: rounds 30, 31, 32 and 33's episode-requests all carry `coworld_id: cow_26cf6929…` (my fetches, all twelve requests), and `ereq_5a0c4611…`'s detail literally reads `coworld_version: "0.8.2"`. (b) The public page serves the new bundle: the canonical `/coworlds` row is `cow_26cf6929…`/0.8.2/`sha256:2985d08f…` (0.8.1/0.8.0 rows demoted to `canonical: false`); the session route returns the static viewer URL carrying the new cow_id + sha with `ready: true`; and the page's own SSR payload serves `playlist[0]` with `coworldVersion: "0.8.2"`. Meanwhile the leaderboard labels remain `:v1`, and the committed 0.8.2 `release-result.json` shows the release minted `battlecode-bc16-bulwark:v3`/`battlecode-bc16-pullers:v3` — additive minting beside a league pinned to `:v1`, exactly as the rail decision records.

## Verifier report audit

| VERIFY.md claim | I verified | agrees |
|---|---|---|
| 32 completed rounds, 30–32 under 0.8.2 | 33 now (33 completed post-fetch); 30–33 under 0.8.2 | yes (time-consistent) |
| Leaderboard: champions ranks 1–2, no fillers, richard rank 3 | same rows, updated tallies | yes |
| ereq_5a0c4611 completed, 0.8.2, both champions | same, verbatim | yes |
| Replay: protocol/reason/fallbacks/decision_ms, game_end trio | same, byte-level | yes — but "(203 events)" is wrong, actual 249 (note 1) |
| Zero LAUNCHER/TRADE in raw bytes, both versions | same (4 replays greped) | yes |
| Source: `isBc22 or isBc16` at broadcast.nim:364, test at :249 | same lines quoted from the tree at 6e89d0fe | yes |
| Logs CLEAN | same | yes |
| Session route → 0.8.2 static URL, ready:true | same (my own POST) | yes — featured-match sub-condition unevidenced (note 4) |
| release-result.json liveness string + ids | same | yes |
| viewer-smoke loaded:true, 3 distinct scrubs, soak | same from committed JSON + run conclusions via gh | yes |
| Screenshot: TRADE not LAUNCHER, no Singularity, whole headline, complete panels | same, from my own read of both PNGs | yes — two pixel-level mis-cites (notes 2, 3) |

## Findings

NOTE: VERIFY.md §4 says the event histogram covers "(203 events)"; the histogram it pastes sums to 249 and `jq '.events|length'` on the same replay returns 249 (my fetch). Transcription error, same family as pass 2's "261 vs 236" (caught by the previous judge); no item depends on the total.
NOTE: VERIFY.md §8(e) says the clipped right-hand scorebug value is "the full score `236` … confirmed in the JSON `scorebug` field above" — the committed `scorebug` string (captured at the 0% playhead) ends "…Hunt neutrals. 50" and contains no 236. In the pixels the scorebug shows the **current game's** points (91/8, matching game 3's `points: [91,8]`), not the match score; the 463—236 match score a spectator needs is drawn in the endcard line `score 463 — 236 · 3 games played`, visible in the same screenshot. Substance (score present, winner legible) holds; the citation is wrong as written.
NOTE: VERIFY.md §8(e) says the speed chips run "up to `15×`"; the source pins `PlaybackSpeeds* = [1, 2, 3, 4, 8, 16]` (broadcast.nim:50) and the top chip in both screenshots reads 16×. Trivial transcription error.
NOTE: VERIFY.md §6 never evidences the "featured match present" sub-condition of item 6 (its coworld-detail `jq` omits `featured_match`, and the playbook records that field as null platform-wide, so it could not have). I verified the sub-condition independently: `softmax.com/battlecode`'s SSR payload `state.playlist[0]` carries a featured match with `coworldId: cow_26cf6929…`, `coworldVersion: "0.8.2"` and a non-null `replayUrl`. Item 6 is TRUE at head; the gap is in pass 3's evidencing, not in fact (pass 2's verdict had pasted this SSR evidence; pass 3 dropped it).
NOTE: VERIFY.md §8(a) credits attempt 3's success to round 31's "better game-length balance"; round 31 is comparably skewed (games 422/3000/752 — the long game is 71.9% of the timeline, vs round 30's 67%). What actually differs is that the long game sits second, so the 0%/50%/100% scrubs land in three distinct segments. The readouts genuinely differ (committed JSON); the causal gloss is imprecise, the conclusion unaffected.

BLOCKING: 0
