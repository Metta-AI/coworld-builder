blocking: 0

# Phase-60 verdict — battlecode-2016
Head: a1340c5 (coworld-builder main)   Checklist: docs/SPEC.md §Definition of done (phase 60) / prompts/60-verify.md   Artifact adjudicated: runs/2026-09-09-battlecode-2016/VERIFY.md (the 0.8.1 pass)
Independent read written before reading VERIFY.md: yes, with one declared exception — my `grep -n 'filler'` over `log.md` (a legitimate need for check 1's ordering evidence) surfaced coordinator summary lines (log.md:103, :124) that describe the verifier's results, before all of my own fetches were complete. Every item below is nonetheless grounded in evidence I fetched myself from the platform APIs, GitHub CI, the committed artifacts, and the screenshots, which I read with my own eyes.

All eight definition-of-done items verified independently at the current head against coworld **0.8.1** (`cow_089d7551-1bab-49d6-be88-157149ea97f9`, manifest `sha256:6179e72b…be14f9`). Both coordinator rail decisions checked as implemented (evidence under items 2, 3, 6 and the notes). Zero blocking findings; seven advisory notes below, one of which (the "LAUNCHER DUEL" killfeed label) is a real spectator-visible defect the verifier saw, quoted, and did not flag — it does not make any checklist item false.

## Item-by-item

### 1. ≥2 completed rounds after the fillers were set — UPHELD
My fetch: `GET /rounds?league_id=league_6cdded0f…&limit=30` → **17 rounds `completed`** (round_number 1–17, round 1 `round_5a5bf68d…` 13:15:21Z through round 17 `round_bef99b2c…` 17:15:30Z; round 18 `pending`). Zero `failed`/`discarded`. Filler registration: log.md:93 records the fillers POST returning exactly the two filler UUIDs **before any trigger-round**, and log.md:96 observed round 1 still `pending` with fillers registered. Even discarding round 1 entirely, rounds 2–17 (16 rounds, all completed after 13:30Z) satisfy ≥2. TRUE.
(Note 4 below: the verifier's wall-clock phrasing on round 1 is internally inconsistent; the conclusion is unaffected.)

### 2. Both champions ranked, fillers absent — UPHELD
My fetch: `GET /divisions/div_1e064838…/leaderboard` →
`1  daveey-1  battlecode-bc16-pullers:v1  1031.11  17  11.0` / `2  daveey  battlecode-bc16-bulwark:v1  968.89  17  6.0`. Exactly two rows, both champions, `rounds_played: 17` each, zero filler rows. Labels still `:v1`, as rail decision 1 intends; I additionally confirmed all four `:v1` policy-version UUIDs still resolve in `GET /policy-versions?limit=500` (bare array): `c073ca20…` bulwark v1, `2175495c…` pullers v1, `99053bee…` battlecode-bulwark v1, `bb2726bf…` battlecode-greenhorn v1 — alongside freshly minted v2 rows, i.e. minting was additive and no entrant was orphaned. TRUE.

### 3. Latest round's episode request completed with a replay — UPHELD
The flat `GET /episode-requests?round_id=` route 405s (I hit it myself: `{"detail":"Method Not Allowed"}`, HTTP 405 — matching playbooks/observatory-api.md:157). Nested route: `GET /rounds/round_bef99b2c…/episode-requests` → one entry, `ereq_749c57c1-c3c4-470a-afa4-5d542b72c27b  completed`. Detail fetch: `status: "completed"`, `replay_url: https://softmax-public.s3.amazonaws.com/replays/28474a90-b446-4a9a-a1d9-7ef5b78abf46.replay`, participants `daveey` and `daveey-1`, **`coworld_id: cow_089d7551…`, `coworld_version: "0.8.1"`** — independent proof the ladder runs under the re-released coworld (rail decision 1's factual core). TRUE.

### 4. Replay bytes valid and show the game — UPHELD
My fetch of the check-3 replay: strict UTF-8 decode ok (python), strict JSON parse ok (`jq -e`), `protocol: cogame.battlecode.v1`, `.result.reason: "complete"`, `.result.fallbacks: [0,0]`. Both champion seats are `"policy": "llm"` with distinct, non-trivial 11-field sealed doctrine sheets (`.seats[0].sheet`: opening `turtle`, 6 turrets, guard_ratio 70, infection_policy `quarantine`; `.seats[1].sheet`: opening `scout_zombie_pull`, 2 turrets, guard_ratio 25, infection_policy `suicide_squad`). `doctrine_received` events: attempt 1, `defaults_applied: 0` for both slots — the LLM decisions genuinely landed, no silent degradation. Event stream: 236 events across 16 game-specific kinds (`turned: 72, infection: 60, unit_milestone: 26, zombie_wave: 17, neutral_activated: 12, duel: 11, archon_lost: 10, outbreak: 6 …`). Three games (`turtle` 387r, `collision` 1073r, `lockdown` 887r), all `end_reason: "archons_destroyed"`, non-degenerate per-game stats on both sides (games[2]: `units_built [81,81]`, `damage_dealt [9185,16622]`). TRUE.
(Note 2: the verifier's "261 total" events figure is wrong — its own histogram sums to 236, which is what `.events|length` returns.)

### 5. Hosted game log clean — UPHELD
My fetch: `GET /episode-requests/ereq_749c57c1…/artifacts/logs` with the elevated header → 11 lines; `grep -E 'falling back|LLM provider is unavailable|cut off at max_tokens|rejected'` → no match → **CLEAN**. The verifier's aside is correct: the game container's `refused a seat-0 connection` line contains "refused", which the pattern `rejected` does not match. TRUE.

### 6. Public page uses the static replay path — UPHELD
My evidence, three sources: (i) raw HTML of `https://softmax.com/battlecode` (1,024,950 bytes, HTTP 200) contains **no** `<iframe>` — the documented client-rendered false negative, not absence; (ii) the same page's SSR payload contains `state.playlist[0]` with `coworldId: "cow_089d7551…"`, `coworldVersion: "0.8.1"` and a non-null `replayUrl` — a featured match is present and served under the **new** coworld; (iii) `POST /coworlds/replays/session` for the new cow_id + check-3 replay → `ready: true`, `viewer_url: …/v2/coworlds/replays/static/cow_089d7551-1bab-49d6-be88-157149ea97f9/sha256%3A6179e72b…be14f9/index.html?v=2#replay=…28474a90….replay` — static route, fragment form, **new cow_id and new manifest hash**, not a `/client/replay` pod URL. `GET /coworlds/cow_089d7551…` additionally confirms `canonical: true`, `version: "0.8.1"`, `manifest_hash` = the new sha, and manifest `source_url` pinned to `tree/1f5cb5cf…` — the PR #12 merge commit, whose CI run 34371338676 I checked: `conclusion: success`. TRUE.
(Note 5: one evidence-fidelity nit on the verifier's pasted jq shape.)

### 7. Certification declared the static bundle — UPHELD
Read myself from the committed `runs/2026-09-09-battlecode-2016/release-result.json` (the 0.8.1 artifact — `version: 0.8.1`, `cow_id: cow_089d7551…`, `manifest_sha: sha256:6179e72b…be14f9`, all matching check 6): `.certify.replay_liveness` = `"Replay liveness: skipped (static replay bundle declared; /client/replay and /replay not required)"` — the required string. `.certify.output_tail` contains exactly 10 `[pass]` steps. Release run 34380179056 (`Coworld release`, Metta-AI/cogame-battlecode): `conclusion: success`, checked via `gh`. Current hosted certification independently confirmed via the coworld CLI: `Hosted certification: certified (main-935475525ce0)`, all 10 sub-checks pass, `Canonical: yes`. TRUE.
(Note 6: the committed artifact's `hosted_certification` field is the snapshot value `"certifying"`; the live status is `certified`.)

### 8. Spectator judgment — viewer executed, then judged — UPHELD
I formed this judgment **from the images and JSONs myself**, before reading the verifier's paragraph.
(a) `loaded: true` — both committed JSONs: `{"loaded":true,"ms":1184,…}` (primary) and `{"loaded":true,"ms":1358,…}` (supplemental); signals `{"data_replay_loaded":"true","data_replay_error":null,"bridge":["ready"],"bridge_ready":true,"bridge_error":[]}` in both. Both producing CI runs checked by me: 34382385197 and 34382582739, `conclusion: success`, workflow `viewer-check`. Both runs' `url` fields carry the **new** cow_id + manifest hash; the primary's `#replay=` is exactly check 3's replay; the supplemental's is round 1's replay, which I confirmed is genuinely this league's (round_5a5bf68d's ereq `replay_url` = `…7390ce0d….replay`) and genuinely ends on the `more_archons` rung (`.result.games[]`: `voluted/archons_destroyed 641r`, `closequarters/more_archons 3000r` — my own fetch of those bytes).
(b) Three differing scrub readouts, both JSONs: primary `0% = 1:28 GAME 1 OF 3 — TURTLE / 50% = 0:48 GAME 2 OF 3 — COLLISION / 100% = FINAL MATCH OVER`; supplemental `0% = 2:27 GAME 1 OF 2 — VOLUTED / 50% = 1:15 GAME 2 OF 2 — CLOSEQUARTERS / 100% = FINAL MATCH OVER`. The 50% map names match `.result.games[1].map` in each replay. Soak: `round 3 → 195 → 243 / 3000`, `moved: true`, `page_errors: []`.
(c) My own read of `viewer-check/viewer-smoke.png`: a legible endcard naming the winner (`CLAN BASIL — DAVEEY-1`), the headline band `THE GAME ENDED ON ARCHONS DESTROYED IN GAME 3, ROUND 887` (reconciles: games[2].rounds_played = 887), `score 235 — 464 · 3 games played` (reconciles: `.result.scores` [234.67, 464.33]), two complete doctrine panels each ending in a full sentence plus its motto, a box score (81 units built per side — reconciles with games[2].units_built [81,81]), a live killfeed (`ARCHON DOWN — Clan Ash has 0 left, killed by zombie, game 3, round 886` — reconciles with the replay's `archon_lost` events), and the starter chrome family: top scorebug strip with both aliases + real player names + doctrine mottos + scores, right-hand killfeed, bottom transport strip with play/step/`+25`/`spoilers`, `round 886 / 3000`, a chapter-marked scrubber and `1×–16×` speed chips. A spectator can tell who won and why. The stat block running past the bottom edge is the E2 fix's intended scroll behaviour.
**E1/E2/E3 as rendered, judged from the pixels:** I also viewed `viewer-check-0.8.0/viewer-smoke.png` for contrast — it shows all three defects (headline `THE SINGULARITY CAME AT ROUND 3000 AND CLAN BASIL HAD MORE ARCHONS LEFT`; the title sliced to the top half of its own capitals; both panels clipped mid-word, `…activates a` / `…farm the far man, routes its`). At 0.8.1, on the **same round-1 replay**, the supplemental screenshot reads `THE ROUND LIMIT RAN OUT AT ROUND 3000 AND CLAN BASIL HAD MORE ARCHONS LEFT` — no "Singularity" anywhere on either 0.8.1 card — the title band is drawn whole, and both panels end in complete sentences (`…away from its own archons` / `…at the enemy archons to die there`), with the incidental `far man → far map` typo also fixed. E1, E2, E3: fixed as rendered. TRUE.

## Coordinator rail decisions — verified as implemented
1. League stays on `:v1` after the 0.8.1 re-release: episodes run under `cow_089d7551…`/0.8.1 (item 3), the public page serves the 0.8.1 bundle (item 6), all four `:v1` UUIDs resolve with additive `:v2` rows beside them (item 2). No entrant orphaned; no stale-bundle finding. Holds.
2. Tier A' residue: disclosed in docs/PARITY.md and VERIFY.md:504-506; the checklist names no parity tier. Not a phase-60 item. Not re-litigated.

## Findings

NOTE: "LAUNCHER DUEL" — bc23 vocabulary rendered in bc16's killfeed. Visible in both 0.8.1 screenshots (e.g. `LAUNCHER DUEL — 1 lost to 1, game 3, round 828`) and already present in the 0.8.0 screenshot; the label is generated in `src/battlecode/broadcast.nim:353-363`, where the `duel` beat's label switch tests only `isBc22` — bc22 gets `TRADE — N attackers lost…`, and **every other year including bc16** falls to the bc23-flavoured `"LAUNCHER DUEL — …"` else-branch (the file's own line ~1271 says bc23 "is the year of the launcher duel"). bc16 has no launcher unit, and this replay has 11 `duel` events. PR #12 could not have fixed it: it touched only viewer files, and this string lives in the Nim broadcast layer. Same defect family as E1 (cross-year vocabulary leak) but confined to killfeed/beat labels; the headline, scorebug, archon and outbreak lines still carry who-is-winning-and-why, so item 8 remains TRUE — advisory for a future fix. The verifier quoted these lines in its §8(e) chrome description (VERIFY.md:462) without flagging them.
NOTE: VERIFY.md:175 says ".events (261 total)" — the actual count is 236 (`jq '.events|length'`), and the verifier's own kind histogram sums to 236. Transcription error, propagated to the summary table (VERIFY.md:517); no verdict impact.
NOTE: VERIFY.md:386 says "`has_scrub`: not null" — the committed `viewer-check/viewer-smoke.json` literally has `"has_scrub": null`. The substantive claim (the scrub loop ran; three readouts recorded) is true from the `.scrub` array; the specific readout claim is wrong as written.
NOTE: VERIFY.md:63 claims "All 17 postdate … the 13:24:00Z filler registration", but its own pasted data shows round 1 `completed_at: 13:16:33Z`. log.md's coarse hand-rounded timestamps cannot support 13:24:00Z as wall clock (round 1 was created 13:15:21Z yet the league-seed POST is logged "13:20:00Z" — an impossible ordering if read literally). The reliable evidence is ordinal — log.md:93 "Set BEFORE any trigger-round" and log.md:96 observing round 1 still pending with fillers registered — and in the worst case rounds 2–17 (16 ≥ 2) postdate any reading. Item 1 unaffected.
NOTE: VERIFY.md:263-264's pasted command (`jq -r '.entries[]|select(.name=="battlecode"…)'` over `GET /coworlds?limit=200`) does not run against the **bare JSON array** that endpoint returns at my fetch time (my own `.entries` attempt errored "Cannot index array"). The row's values (id, canonical, version, manifest_hash) are nonetheless correct — I reconfirmed them via `GET /coworlds/<id>` and the session route — so this is an evidence-fidelity nit (likely transcript tidying or a shape change), not a substance defect.
NOTE: the committed `release-result.json` records `hosted_certification: "certifying"` — a legitimate point-in-time snapshot; the live status is `certified (main-935475525ce0)` per my CLI check. Checklist item 7 requires only the replay-liveness string, which is present.
NOTE: prompts/60-verify.md §check 3 still teaches the flat `GET /episode-requests?round_id=` route, which 405s (allow: POST) per playbooks/observatory-api.md:157 and my own fetch. The verifier correctly routed around it; the prompt text is stale. Advisory for the prompts' maintainer.

BLOCKING: 0
