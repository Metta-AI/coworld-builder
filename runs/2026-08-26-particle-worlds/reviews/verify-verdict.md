blocking: 0

# Phase-60 verdict — particle-worlds

Checklist: `docs/SPEC.md` §Definition of done (items 1–8) · Verifier evidence: `runs/2026-08-26-particle-worlds/VERIFY.md` (784 lines) · Rendered evidence: `runs/2026-08-26-particle-worlds/viewer-check/{viewer-smoke.json,viewer-smoke.png}` · Independent read written before adjudicating: yes. I re-fetched every live artifact myself (Observatory API, S3 replay bytes, hosted logs ×2 coworlds, public page, replay-session POST, gh run view) rather than accepting VERIFY.md's pasted output.

## Adjudication per check

### 1. ≥2 completed rounds after fillers — TRUE stands
Re-fetched `GET /rounds?league_id=league_2ae87c04…` now: round 3 `completed` 14:02:03Z, round 2 `completed` 13:47:30Z, round 1 `failed` with exactly the error VERIFY.md quotes verbatim (`Temporal RoundWorkflow failed before settling the round.`), round 4 still `pending` — so round 3 remains the latest *completed* round and the check-3–8 pinning to it is correct at adjudication time, as VERIFY.md's own 14:16:22Z re-poll note anticipated. "After fillers were set": I re-fetched `GET /leagues/…/filler-policies` (drifter:v2 `2239256d…`, beeline:v2 `5ad38c6d…` registered) and both counted rounds' episode details — round 2 (`ereq_8090183a…`) seats drifter:v2 at positions 2–3 `is_filler:true`, round 3 (`ereq_be89daa4…`) seats beeline:v2 at positions 2–3 `is_filler:true`. Fillers demonstrably *in* both counted rounds. One tension the verifier disclosed rather than hid: log.md's phase-50 line stamps registration 13:41:39Z while round 2 was *created* 13:40:21Z; but SPEC item 1 counts rounds **completed** after fillers were set (13:47:30Z and 14:02:03Z, both after either timestamp), and the seated `is_filler:true` participants settle the substance. Evidence inline, verdict supported.

### 2. Both champions ranked, fillers absent — TRUE stands
Re-fetched the leaderboard (bare array, as VERIFY.md's shape table records): exactly two rows — `1 daveey-1 particle-worlds-cipher:v2 1030.53… 2 2.0` and `2 daveey particle-worlds-swarm:v2 969.47… 2 0.0`. Byte-identical to VERIFY.md's paste. Both champions present with `rounds_played 2`; the two filler policies absent from the board; the replay roster names them `Baseline` / `Baseline (2)` (reproduced in my check-4 run below). Supported.

### 3. Latest round's episode completed with replay — TRUE stands
The flat `GET /episode-requests?round_id=` 405 is recorded with its body, and the nested-route substitution is the known platform quirk, correctly documented. Re-fetched `GET /episode-requests/ereq_be89daa4…` now: `status "completed"`, `replay_url …/1ae313cc-0f99-450d-aaf4-21bdc8688cc8.replay`, position 0 = daveey (swarm:v2, `is_filler:false`), position 1 = daveey-1 (cipher:v2, `is_filler:false`), positions 2–3 = beeline:v2 `is_filler:true`. Identical to the paste. Right round (3, latest completed), right league, right artifact. Supported.

### 4. Replay bytes valid and show the game — TRUE stands (design-declared substitute, legitimately used)
The substitute is genuinely design-declared: `design.md:1059` says verbatim "**The phase-60 substitute for SPEC §Definition of done check 4:**" followed by the exact `tools/replay_summary.py` command sequence VERIFY.md ran (design.md:1059–1072). I fetched the S3 bytes myself (130,673 bytes, `COWLDMPE` magic — matches VERIFY.md's `od` dump) and ran the tool at the repo head VERIFY.md names (`543c5a8`, confirmed): `strict UTF-8 JSON: ok`, `protocol particle-worlds/v1`, `results.reason complete`, `endRule full_time`, `roundsPlayed 4`. Directive-source table reproduced exactly: seats 0/1 (champions) 38+36 llm / 0 scripted / 2+4 fallback of 40 each — 92.5 % live LLM, no scripted directive on a champion seat; seats 2/3 scripted 40/40 (the filler, as designed). `fallbacks` 6, non-silent symbols 46. Champion notes are situational and game-specific (mark names, coordinates, role play per mode) — I spot-checked the round-1 spread notes and they match the paste. Scores `[0.524,0.904,0.73,0.502]` equal check 3's `participant_scores`. The `deadline` exception was not needed. Supported. (Trivial nit, not a defect: the tool prints `gameVersion 2` where design.md's *example* output shows `"1"`; gameVersion is not among the fields the design's requirement list names.)

### 5. Hosted log — TRUE stands on SPEC item 5's exception branch, which is properly documented
The verifier did not paper over the first branch: VERIFY.md states plainly it is NOT satisfied (14 matching lines) and claims the exception. I re-fetched the elevated logs and reproduced the count exactly — 14 matching lines in the `game` container, all either Bedrock 429 `Too many tokens per day` throttles or sidecar transport timeouts; zero `cut off at max_tokens`, zero `rejected`. The upstream attribution is real: the `bedrock-sidecar` container in the same log shows `ThrottlingException` / 429 returned by `bedrock-runtime.us-east-1.amazonaws.com` itself (5 throttle records, 76 of 81 calls 200 OK) — matching VERIFY.md's quoted WARNING record verbatim. The cross-check against another LLM coworld is documented **and true**: I re-fetched walker-waterworld's `ereq_62be0e80…` logs (a different coworld, `cow_36a12905…`, episode created 13:57:18Z — contemporaneous with particle-worlds round 3) and reproduced 120 matching lines including `LLM provider is unavailable` 503s. That is precisely the platform-wide-cause-checked-against-another-LLM-coworld branch SPEC item 5 licenses, and today's known platform-wide Bedrock 429 throttling corroborates it. The episode itself survived the throttle (all 4 rounds to full_time, 6/160 directives fallback). Supported.

### 6. Static replay path + featured match — TRUE stands
The raw-HTML iframe grep finding nothing was handled per the prompt's own fallback instruction (client-rendered page), the source used is recorded, and both sources check out live: I re-fetched `https://softmax.com/particle-worlds` (HTTP 200; zero `client/replay` occurrences) and found the SSR `playlist[0]` byte-equivalent to VERIFY.md's paste — featured match present, round 3, episode `ereq_be89daa4…`, matchup daveey-1 vs daveey. I re-ran the `POST /coworlds/replays/session` call and got the identical `viewer_url`: `…/v2/coworlds/replays/static/cow_039ad60d…/sha256%3Ac9ae68f3…/index.html?replay=<s3 url>&v=2`, `ready:true` — the static route keyed by the manifest sha, never a pod URL. The sha equals `release-result.json.manifest_sha` and `STATE.coworld.manifest_sha` (verified). Supported.

### 7. Certification declared the static bundle — TRUE stands
Read the committed `runs/2026-08-26-particle-worlds/release-result.json` myself: `.certify.replay_liveness` = `Replay liveness: skipped (static replay bundle declared; /client/replay and /replay not required)` — the required string. `version 0.1.2`, `cow_id`, `manifest_sha`, `canonical true`, `ok true` all consistent with STATE and check 6. Source (committed copy, not /tmp) is stated in VERIFY.md as the prompt requires. Supported.

### 8. Viewer executed + spectator judgment — TRUE stands
The run is real and correctly identified: `gh run view 32978130047` → workflow `viewer-check`, `success`, created 2026-08-26T14:05:38Z — 1 s after the verifier's logged dispatch timestamp, with the previous run 1h55m older, so the find-the-new-run attribution holds. `viewer-smoke.json.url` is the check-6 iframe src verbatim. Condition (a): `loaded:true`, `data_replay_loaded:"true"`, `data_replay_error:null`, `failure:null`, empty `console_tail` — verified in the committed json. Condition (b): the three scrub clocks differ and progress (`ROUND 1/4 · SPREAD` → `ROUND 3/4` countdown → `ROUND 4/4 · TAG · TURN 10/10`) — verified in the committed json. Condition (c): the judgment paragraph exists and I checked it against the png myself — the screenshot shows exactly what the paragraph claims: the starter's scorebug (four plates, real policy-owner names DAVEEY/BASELI…/DAVEE…/BASEL…, roles PURSUER/EVADER, centred 0:00 clock), the mark rail (`BONE 98% TEAL 44% AMBER 32% VIOLET 98%`), the radio strip (`RED D`, others em-dash), full transport strip with `DRAW 4888/4896` and speed chips, scrubber at 100 % with momentum strip. I recomputed the scorebug arithmetic: `(0.974+0.044+0.081)/3 = 0.366`, `(0.974+0.955+0.689)/3 = 0.872`, `(0.974+0.955+0.918)/3 = 0.949`, `(0.974+0.955+0.081)/3 = 0.670` — the four displayed numerals exactly; the picture and the replay bytes are the same episode. The chrome is the starter's (the cogame-gridlock rewrite failure mode is absent). The `feed_lines:0` explanation is correct at source: `templates/tools/ci/viewer_smoke.mjs:425` queries `#feed, .feed, #log` and the repo's page has only `id="killfeed"` (verified by grep in both repos) — a probe blind spot, not an empty-feed claim. Supported.

## Advisory observations — adjudicated, none falsifies a definition-of-done item

All three of the verifier's legibility observations are factually accurate (I verified each at source) and all three are correctly classed non-blocking:

1. **`LIVES LEAD` momentum label, single flat line** — confirmed at `client/replay_broadcast.html:1384` and `client/league_replayer.html:334` at head `543c5a8`, and visible in the png. This deviates from design.md §Readouts item 7 (retarget to four score series), but the DoD does not require design-readout conformance; item 8 requires loaded + advancing + a legible judgment, all of which hold, and the un-retargeted strip is *inherited starter chrome*, the opposite of the gridlock rewrite failure item 8 polices. Phase-30-class residue, not a DoD falsification.
2. **No beat markers on `#scrub` in the frame** — consistent with a seek-only smoke (beats accumulate from playback deltas). No DoD item requires beat markers. Not a falsification.
3. **No landmark discs on the tag-round board** — the frame is legible (scorebug, rails, sprites, bubbles all readable) and in `tag` the marks are design-declared inert; the rail/board tension is a legibility observation, correctly routed to the coordinator. The picture is not empty, static, or unreadable — item 8's actual bar. Not a falsification.

Also noted by the verifier and confirmed cosmetic: the 50 % scrub caption's mode label lags one round during the `:04 STARTING IN` countdown (`ROUND 3/4 · DECEIVE` where round 3 is crypto). It does not affect condition (b) — the three readouts differ regardless — and it was disclosed, not hidden.

## Conclusion

Every check has its command, fetched output excerpt, and verdict inline; every excerpt I re-fetched reproduced; no check was evaluated against a wrong round, league, or artifact (checks 3–8 correctly pin round 3, the latest completed round both at verification time and now); the two allowed non-fresh sources (committed release-result.json, the verifier-dispatched viewer-check artifact) are exactly the two the prompt permits; the check-4 substitution is design-declared at the cited lines and the tool run is shown; the check-5 exception branch is documented with a real, reproducible cross-check. Zero unproven or false checks.

BLOCKING: 0
