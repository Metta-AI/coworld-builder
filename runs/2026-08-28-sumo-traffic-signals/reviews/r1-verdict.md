blocking: 0

# r1 verdict — sumo-traffic-signals

Head: `e20601afffcf2e5b5ae7edc1341c2a50912634d9` (main)   Checklist: `prompts/30-review-loop.md` §ACCEPTANCE CHECKLIST (items 1–15 + one-parallel-batch)   Independent read written before reading fixes: yes (order followed: checklist → design note → repo/CI at head → own notes → r1-review.md → r1-fixes.md)

CI at head: run **33193230780**, conclusion **success**, head_sha `e20601a…`, jobs `test` / `docker-smoke` / `wasm-viewer` all success; the only skipped step is the PR-only GameVersion-collision check. The review was written against `54fd040`; sixteen fix commits (`033b3c7`…`e20601a`) landed after it, so every finding was re-verified at the current head, not at the reviewed sha.

## Standing blocking findings

None. Every reviewer finding is either fixed at head (verified below) or advisory against the checklist; my own independent pass found nothing that falsifies a checklist item.

## Refuted

### B1 — "Two assertions in `tests/` were removed or narrowed during this run" → REFUTED at head (fixed by `033b3c7`)
- The finding was accurate against `54fd040`: commit `d3fdc93` deleted `check sim.greenWaves >= 1` from the seed-42 fixture test and narrowed the chrome-alias shadowing guard to skip one-character aliases. Both hunks are restored at head, at full strength:
  - Alias guard: `tests/test_signals_viewer.nim:98-121` collects every alias again (`check "$" in aliases`), counts `var <alias> ` + `var <alias>=` declarations, requires exactly **0** for every alias and exactly **1** for the one enumerated exception `$`, which must sit inside the game block's own IIFE (`check block0.find("var $ ") > block0.find("(function () {")`). This is stricter than the pre-run presence check.
  - Wave assertion: it moves to a scenario that can carry it — new suite `coordination raises a green wave` (`tests/test_signals_engine.nim:117-158`) drives `wave EWG` orders with rising delays through the real `stepTick` and asserts `check sim.greenWaves >= 1` plus corridor `A`/`eastbound`. Head CI log: `coordinated corridor: tick 36/256 … waves 1` (printed in both debug and release passes).
  - The seed-42 fixture assertion itself is **not** restorable, and that is a fact, not a choice: the head CI log shows the cert mix on seed 42 produces `waves 0` (`cert fixture: tick 256/256 … spillback 15 gridlock 0 waves 0`) even after the wait-tick-at-spawn bug fix (`9c1d4b9`, `vehicles.nim`) that made waves physically possible — two uncoordinated scripted baselines never build an offset. The divergence from the design note is declared in the test file and the commit message. Note also that `check sim.greenWaves >= 1` never held at any green sha: the first green CI run is `9c9d29d` (runs at `311e76d`, `4531b9d`, `bdc1942`, `9c1d4b9`, `d3fdc93` all failed), by which point the assertion was already gone — so no green state of the suite was ever weakened. Every other `tests/` hunk since 13:21Z is a refactor or a strengthening; `grep -rn "skip\|xfail" tests/` finds no test-control directives.
- Verdict: zero. The finding was true when filed and is fixed at the current head.

## Checklist pass (independent)

| item | status | evidence (path:line or run) |
|---|---|---|
| 1 CI green, no test loosened | PASS | run 33193230780 success at `e20601a` (test/docker-smoke/wasm-viewer all ✓). `git log -p --since=2026-08-28T13:21Z -- tests/`: 12 commits read hunk-by-hunk; the one weakening (`d3fdc93`) is restored/strengthened by `033b3c7` (see B1); no skip/xfail/tolerance widening/test-file removal anywhere; `4531b9d`/`54fd040` hunks are refactors or stricter selectors |
| 2 replay re-derivation | PASS | `tests/test_signals_replay.nim:63-125` records + re-derives all five end rules, `hashMismatchTick == -1` at load and after seek-to-end; `:217-241` re-simulates from bytes alone. Viewer runs the SAME sim: `replay-viewer/signals_replay.nim` imports `signals/sim` via `config.nims` `switch("path", rootDir / "src")`; `replay_runtime.nim:32-51` re-simulates, no parallel recording |
| 3 static viewer | PASS | `coworld_manifest_template.json:9` `"replay_viewer": {"bundle": "static-replay-viewer"}` under `game`; `tools/build_replay_viewer.sh` mode 100755, asserted + invoked by path (`ci.yml:306-330`); worker fetches only the `?replay=` URL (`static_replay_worker.js:113`). `/client/replay` exists only as the starter's local dev route (`server.nim:42`), never declared to the platform; `coworld-release.yml:211` actively rejects a pod viewer |
| 4 both name spaces | PASS | observation builder uses `seatAlias`/ids only (`sim.nim:301-407`); real names ride `results.names` (`roster.nim:154`), join records (`server.nim:470-471`), and the viewer plates/rail/endcard (`page_sig_block` scorebug/endcard via `teamName`/`rosterName`); `showPlayerLabels: false` everywhere; board glyph alphabet closed to `A B C D G 1 2 3 4` (`rig_art.nim:164-198`, test 41 asserts `daveey` cannot be drawn) |
| 5 degrade-never-hang | PASS | attempt1 9 s / retry 4 s via `CURLOPT_TIMEOUT` (`decide.nim:245-264`); `turnBudgetMs` monotonic around the calls (`decide.nim:232-244`, clock starts after the bounded spacing sleep — N3 fix); rate guard 28/60 s (`:198-213`); budget guard (`:163-170`); lobby bounded by `lobbyJoinTimeoutTicks` (`server.nim:486-499`); engine stop at 660 s checked before every turn (`server.nim:556-560`); "never spin" break (`:583-584`); grace bounded by `gameOverTicks`; `wallClockBudgetSeconds ≤ 660 < 720 = 60 %·1200` asserted (`test_signals_engine.nim:257-266`, schema max 660). Only `while true` loops: local dev replay mode and the player pod's receive loop, neither on the episode path |
| 6 num_agents | PASS | `num_agents: 4` in both variants' `game_config` (manifest:217, 261) and `certification.game_config` (:309), absent at variant top level; `docker_smoke.sh:106-152` enforces all four SEAT-COUNT invariants + the independent `SMOKE_SEATS` cross-check; head docker-smoke log grepped: **zero** `SEAT-COUNT FAIL`, `smoke OK: seats=4 … reason=complete` |
| 7 scripted baseline full episodes | PASS | `test_signals_engine.nim:14-42`: real four-seat scripted episode → `reason == "complete"`, `throughput > 0`, artifacts written; `test_signals_driver.nim:40-66`: 200 worlds × both baselines × 4 slots, every order inside its bounds; tunables from the sweep (`tools/tune_baselines.nim --check` in `ci.yml:167`, `tools/ci/baseline_tuning.json`, `test_signals_tuning.nim`) |
| 8 LLM reply handling | PASS | `extractJsonObject` fence/prose-tolerant (`directives.nim:84-123`); exactly one retry as a second parallel batch (`decide.nim:236`, `attempt < 2`); fallback = the shared greedy proc, recorded (`fallbackRecord`, `decide.nim:65-73, 316-329`) and counted (`fallbackTurns` → results) |
| 9 rune-safe truncation | PASS | `truncateRunes` (`sim_types.nim:268`) on say/notes/policy/detail/stopDetail/prompt; new `truncateBytes` (byte cap on a rune boundary) for the 4096-byte provider read (`llm.nim:196-197`); tests feed 4-byte 🚦 at every cap and assert `validateUtf8() == -1` (`test_signals_driver.nim:216-256`, `test_signals_replay.nim:165-214`) |
| 10 manifest validates | PASS | `game.docs` = readme `{type,value}` + 3 pages `{id,title,content:{type,value}}` (manifest:20-51); `game.protocols` carries both `player` and `global` as objects (:10-19). (Advisory: the CLI-load CI step is `continue-on-error` and the installed CLI 0.1.42 rejects on `game.version` — pre-existing, phase-40 concern, outside item 10's named criteria) |
| 11 viewer legible at 360 px | PASS | `.plate .plate-name { flex: 1 1 auto; min-width: 3.2em; … }` (`replay_broadcast.html:2180-2186`; scoped through `.plate` to outrank the inherited `flex: 0 1 auto` rule — strictly stronger than the checklist's bare selector); labels hidden under `.tiny` (toggled at `boardW <= 620`, `:2116`; `.tiny .quad-tag`/`.served-label` display:none `:2387-2388`) |
| 12 release order and scaffold | PASS | `coworld-release.yml`: Build (:159) → Certify (:173, `--timeout-seconds 300`) → Upload policies (:216) → Upload coworld (:314) → Put secret (:410), one job, one run; ci.yml smoke builds its own image in-run; all three workflows present; both scripts 100755; `policies.json`: 4 distinct policies, 2 `PLAYER_PROMPT` champions + 2 scripted fillers, champion #2 = `"player": "ply_bac48eb1-662e-44f8-973d-f3e016dccf5d"`; the three-name placeholder grep exits 1 (no matches); surviving `<cow_id>/<sha>/<run_id>/<name>` are the sanctioned runtime values |
| 13 viewer executes | PASS | `wasm-viewer` `needs: docker-smoke` (`ci.yml:293`); at head, "Load the bundle in a real browser" ran and succeeded (no continue-on-error): `{"loaded":true,"ms":589,…}`, `soak: 10s of playback kept advancing ("1 / 256" -> "97 / 256" -> "121 / 256")` — 135 ticks of headroom after the N12 speed fix; `data-replay-loaded` set in the shell's own `'loaded'` branch (`static_replay.js:161`), `data-replay-error` in `showFailure()` (`:20`); no recorded lobby exists in this format (hashes written only inside the turn loop, `server.nim:574-575`), so tick 0 IS the game start and seeks clamp `[0, maxTick]` (`replays.nim:323-331`); `config.nims` has no MODULARIZE and the worker sets `Module.onRuntimeInitialized` (`static_replay_worker.js:188`) — both files diff against the starter as identifier renames only; `wasm_replay_smoke.cjs` ran the emitted module headless: `ok: … advanced 300 frames` |
| 14 chrome is the starter's | PASS | `chrome_common.js` **byte-identical** to `/workspace/starters/coworld-ctf/client/chrome_common.js` (diff clean, 40 022 bytes); `broadcast_core.js` differs by exactly one line (`CTF_WIRE`→`SIGNALS_WIRE`, :49); `replay_broadcast.html` **reproduces byte-for-byte** from the starter's page via `tools/fork_broadcast_page.py` + `tools/page_sig_block.html` (I ran the script against the starter mount and diffed clean) — the 132 KB-vs-234 KB size drop is entirely the recorded, design-note-listed removals (fpv/pov pipeline, #viewpanel, paintbot event routing/endcard/scorebug internals), not a rewrite; `relayout()` sets `--band`/`--topband`/`--hudscale` on `document.documentElement` (:2115-2121); `#endcard { top: var(--topband); bottom: var(--band, 0px) }` (:617-618), shows with `.on` (:629), every non-gameover frame removes it (:1535); beats are labelled `<button class="beat-marker <kind>">` that seek (`cityBeat`, :2440-2461) with CSS for exactly `{wave, spillback, gridlock, fallback, end}` (:2315-2319); `#viewpanel`/minimap/zoom-bar/fpv/povBadge removed — markup, CSS, wiring, ids (0 occurrences; `check_broadcast_page.py` enforces) |
| 15 every drawn string fits | PASS | `--strict-text-bounds` on both smoke steps (`ci.yml:400, 424`); real-replay run reports `canvas text: 0 drawn` (total 0 — covered nothing, correctly not relied on); the worst-case renderer fixture (`tools/ci/renderer_fixture.html`) loads the shipped `index.html` in an iframe, swaps only the wasm entry, drives a **verified 120-rune** say (self-asserted, `:305-308`) on all four seats at 360/620/900 px, asserts all four rendered **full-length** (`:242-246`), sets both markers, and its CI step reports `canvas text: 618 drawn, 0 never inside the canvas (0 draws crossed an edge), 48 ellipsized` — gated `never_inside == 0`; every `text-overflow: ellipsis` rule in the page is on a name label (`.plate-name`, `.pr-name`, endcard names), never a sentence (`.feed-row` is `white-space: nowrap; max-width: none`, no clipping) |
| one parallel batch | PASS | one `RequestBatch` for all open seats per attempt, one `curl.makeRequests` call (`decide.nim:247-264`); no per-seat call site exists; scripted seats never enter the batch |

## Fixer report audit

| finding | fixer said | I verified at head | agrees |
|---|---|---|---|
| B1 | fixed `033b3c7` | alias guard full-strength with exact-count exception; wave assertion end-to-end, CI prints `waves 1` | yes |
| N1 | fixed `a9a8c8c` | fixture SAY is 120 runes and self-asserted; says driven in their own frame past the MAX_FEED eviction; 4 full-length rows asserted; CI: 618 drawn / 0 never_inside | yes |
| N2 | fixed `69ba216` | `try/except CatchableError` around the play loop (`server.nim:585-597`), settles `fault`, exits 0; `docker_smoke.sh:307-317` raises `FAULT:` on `reason`/`endRule == "fault"` | yes |
| N3 | fixed `6ed6341` | `callsStart` taken after the spacing sleep (`decide.nim:224-232`); turn still bounded (spacing ≤ 12 s then calls ≤ 14 s) | yes |
| N5 | fixed `8731116` | "will retry" is attempt 1's alone (`decide.nim:295-304`); "falling back" logged once for genuine second failures | yes |
| N6 | fixed `d79ad59` | `truncateBytes` (`sim_types.nim:279`), used by `textOf` (`llm.nim:196-197`); test feeds 8000 🚦, asserts 4096 bytes / 1024 runes / valid UTF-8 | yes |
| N11 | fixed `1da1cbc` | gate-queue branch applies the same `movedLastTick` stop rule (`vehicles.nim:213-215`); `stops` never hashed, so re-derivation unaffected | yes |
| N12 | fixed `0712e1d` | `speedIndex = 0` (`replays.nim:213`); head soak shows 121/256 after 10 s (135 ticks headroom vs 12) | yes |
| N13 | fixed `419ff7a` | `variant` key in both variants + cert fixture + `config_schema` (manifest:92, 215, 259, 307); manifest test 33 pins it | yes |
| N14 | fixed `ef75566` | `waveSweepCells` placed by `buildBoardPacket` (`global.nim:315-318`); flash state not hashed; viewer test 41 pins band movement/coverage/expiry | yes |
| N15 | fixed `2dcb1ae` | `quadrantCornerGlyph(slot) == seatAlias(slot)[0]` (`rig_art.nim:198, 387`), `G` glyph added, alphabet still closed; test pins all four + `daveey` undrawable | yes |
| N18 | fixed `c951f2f` | `sim.waveCounts` counts waves; tally reads it; sim test pins per-corridor count and `sum == greenWaves` | yes |
| N19 | fixed `f15c2c7` | head CI clock readout: `THROUGH 69 / 260 PAR …` — par is on the clock | yes |
| N20 | fixed `ad80f42` | `axis = /^[0-9]/.test(corridor) ? 'column ' : 'row '` (`page_sig_block.html:486`); page still byte-reproducible from starter + block | yes |
| N21 | fixed `1d04957` | `refuseToStartDetail` predicate (`roster.nim:86-97`); server settles `fault` before `phase = Playing` (`server.nim:534-541`); never-connected seat still plays greedy; test 26 drives the real predicate | yes |
| N4/N7/N8/N9/N10/N16/N17/N22/N23/N24/N25/N26/N27 | won't fix / not a defect / NEEDS-DESIGN | each re-checked: none falsifies a named checklist item (details below) | yes |

## Rulings on the items left to the judge

- **N22 (`/client/replay` local route) — not blocking.** Item 3's substance is "the platform never gets a pod-served viewer": the manifest declares only the static bundle, the release workflow rejects a pod viewer, and the local route is the starter's own developer affordance, kept by the design note. The string existing in `server.nim` does not falsify the item.
- **N23 (zoom gestures survive `#viewpanel` removal) — not blocking.** Item 14(iv) names the artifact to remove — `#viewpanel`: "markup, CSS, wiring, test ids" — and all of it is gone, including `core.attachMinimap`. The surviving keyboard/wheel/pinch handlers are inherited-region starter code; deleting them would edit the inherited region beyond the note's removal list, the graver sin under item 14. Advisory.
- **N10 (`blockedByPhaseTicks` accumulates) — not blocking.** No checklist item covers it; the deviation makes the bounded, latched override more eager, never less. The fixer's NEEDS-DESIGN framing (hash-bearing counter; fix requires a re-tune) is accurate. Advisory for phase 40.
- **N16 (`.tiny` intersection labels baked) — not blocking.** Item 11 concerns the scorebug plate names and label hiding, both satisfied; item 15's gated number is 0. The design-note `.tiny` rule 3 half-implementation is a note divergence. Advisory.
- **N24 (viewer_smoke.mjs 30 lines behind the template) — not blocking.** Item 13 requires the smoke to run against the docker-smoke replay in a real browser and gate on load/advance/text-bounds; it does, and reported real values. The template drift is same-day lineage-selector fallbacks this page does not need. Advisory for phase 40.

## Non-blocking observations (mine, beyond the review)

- The `test` job's "Load the manifest under the installed coworld CLI" step is `continue-on-error: true` and, per the fixer's own NOTED section, the installed CLI rejects on `game.version`. Item 10's named criteria are tree-verified and met, but phase 40's upload runs the real CLI — resolve the `game.version` question before release.
- `decide.nim:316-324`: a seat whose two attempts both timed out reaches the final fallback loop with `cause = "parse_error"` (the disabled/llmOff/throttled branches don't match a timeout). The turn-level `fallback` records written inside the loop carry the correct `timeout` cause, so phase-60 counting still works; the attempt-2 record's cause label is merely imprecise. Cosmetic.
- The smoke replay contains no `wave` event (seed-42 cert mix, `waves 0`), so the shipped viewer never plays a real wave in CI; the wave display path is covered by the renderer fixture (banner/tally/beat) and the board sweep by unit test 41, and the wave physics end-to-end by engine test 25b. If a later round wants a real recorded wave in CI, the cert fixture would need a coordinated seat mix or a demand tuned for it.

BLOCKING: 0
