blocking: 1

# r3 verdict — coworld-builder (final round)
Head: ce52e34 (`r3: …`)   Working tree clean.   Fresh context; read the tree before `reviews/r2-verdict.md`'s findings were re-checked.

## Step 1 — the seven r2 findings

| # | r2 finding | fixed? | evidence |
|---|---|---|---|
| 1 | resume-race rule → "any new foreign `00 resume` nonce not there before the pull; rebase conflict → abort and exit", in 00-claim / AGENT / SPEC, wordings agreeing | **PARTIAL** | Executable step is correct: `prompts/00-claim.md:179-185` (conflict → `git rebase --abort` and exit; otherwise exit on "any `00 resume` line with a nonce other than yours that was not there before your pull", compared against the step-1 copy). `AGENT.md:26-29` and `docs/SPEC.md:39-42` say the same thing in the same terms (rebase, abort+exit on conflict, any foreign nonce new since the pull, "never the last line"). The three *primary* wordings agree. **But two stale copies of the old rule survive inside 00-claim.md itself**: the boxed "Rejected-push rule (claims and resumes alike)" at `prompts/00-claim.md:164-165` still says "a **last** `00 resume` line in `log.md` carrying a different `session=<nonce>` means that session owns the run → exit", and exit criterion (b) at `:241-242` still says "`log.md`'s last `00 resume` line carries another session's nonce". `AGENT.md:130` ("a newer `00 resume` nonce") is loose but not wrong. See New finding 2 (MINOR — the numbered step the agent executes is the correct one and explicitly says "do not read [the last line]"). |
| 2 | `PLAYER_SCRIPTED=1` gone outside reviews/ | TRUE | `grep -rn PLAYER_SCRIPTED --exclude-dir=.git --exclude-dir=reviews` → 14 hits, every one `=<baseline name>`/`=<name>`/`=<baseline>` or a JSON value: `prompts/20-build.md:29,50`, `prompts/10-design.md:70`, `playbooks/make-coworld.md:129,214-215`, `prompts/40-release.md:28,40-41,43`, `prompts/30-review-loop.md:125`, `docs/SPEC.md:148`, `templates/README.md:187`, `policies.json.example:21,28`. `grep -rn 'PLAYER_SCRIPTED=1'` → no hits. |
| 3 | progress-line rule refers to the previous session's resume line | TRUE | `prompts/00-claim.md:196-199`: "newer than the **previous session's** `00 resume` line (the last one before the line you appended in 5.0 — your own line is always the last one now)". Consistent with 5.0 step 2 writing the count in the same STATE write (`:174-176`). |
| 4 | 60-verify asset loop saves the files it later greps | TRUE | `prompts/60-verify.md:129-130` loop now writes `-o "/tmp/$(basename "$A")"`; `:127` globs `/tmp/*.js` (now populated by the loop — note the grep at `:127` runs before the loop at `:128-130`, see New finding 4, MINOR); `:136-137` grep `/tmp/static_replay.js`, which the loop writes whenever `static_replay.js` is among the index's assets. |
| 5 | announce limit 1800 | TRUE | `prompts/70-announce.md:31` "≤ 1800 characters (the hard limit in `templates/announce.md`)"; `templates/announce.md:10-12` "≤ 1800". No remaining `1000`. |
| 6 | SPEC §Repo layout lists all templates | TRUE | `docs/SPEC.md:235-237` lists ci.yml, coworld-release.yml, coworld-submit.yml, tools/ci/docker_smoke.sh, tools/ci/policies.json.example, run-task.md, blocked-subtask.md, announce.md, STATE.template.json, README.md — matches `ls templates templates/tools/ci`. |
| 7 | coworld-release.yml redacts streamed stderr; `redact` defined before use; yaml parses | TRUE | `redact` defined at `templates/coworld-release.yml:219-220`, first use `:233` (inside `unset_player`, called only later), streamed writes `:257` (`sys.stderr.write(redact(switch.stderr))`) and `:272` (`sys.stderr.write(redact(proc.stderr))`), recorded-failure strings `:263,277`. `yaml.safe_load` → OK (job `release`), all 11 `run:` blocks pass `bash -n`. |

## Step 2 — independent sweep

**(a) Duplicate run / repo in any path?**
- *Claim*: comment-first + 20 s re-read + yield (`00-claim.md:136-149`), non-forcing push with "another STATE for your idea → exit" (`:156-159,161-166`). Repo creation is in phase 20 after STATE exists, so a duplicate repo requires a duplicate run. No path found.
- *Resume (stale or ended)*: 5.0 nonce guard `:172-189` — pull, single STATE+log write, push; rejected push → rebase; conflict → abort+exit; any new foreign nonce → exit; then Asana `heartbeat_at` 20 s re-GET. Sound: the only way both sessions proceed is both pushes succeeding, which git serialises. No path found.
- *Blocked-resume*: step 3.3 (`:66-74`) pushes its "resumed after unblock" commit first, then enters 5.0 with the nonce guard explicitly (`:72`). Two heartbeats both seeing the completed human subtask both move the task to Running (idempotent section add) and both reach 5.0, where one loses. No duplicate phase work.
- *Skip*: (a) comment deduped `:115-116`, (b) `jq 'if index($g) …'` idempotent `:119-120`, (c) card deduped by title `:126-127`; re-GET at `:136-141` drops an idea concurrently skipped. Worst case: two cards if both heartbeats list the Fleet section in the same second — a duplicate *card*, not a run or repo.
- Residual (MINOR): the stale "last line" wording at `:164-165` could, if applied instead of 5.0.3, let a rebased loser re-push. See finding 2.

**(b) Can the queue wedge?** **Yes — one path (New finding 1, BLOCKING).** A run whose phase 80 Asana calls fail persistently (e.g. the idea task was deleted → 404 on complete; run task moved by a human; a permissions change) is left in *Running* with a `close-failed` comment and "let the next heartbeat retry phase 80" (`prompts/80-close.md:47-50`). It is exempt from the resume counter (`00-claim.md:223-228`, `SPEC.md:91-95`) and has no retry cap of its own. Every subsequent heartbeat sees it in *Running* with `session_ended_at ≥ heartbeat_at`, adopts it at step 2 (`00-claim.md:52-55`), retries 80, fails, ends — and step 4 (claim) is never reached because step 2 took the heartbeat. Concurrency 1 means no new idea is ever started until a human notices the comment. Other paths checked: SKIP continues to the next idea (`:132-133`); 2-Blocked cap is an intended idle (`:78-80`); every other phase has a bounded budget that ends in 90, which moves the task out of *Running* (`90-blocked.md:63-66`); Builder board unreachable → exit without creating (`:274-275`), self-healing.

**(c) Can a run loop forever without reaching 90?** Only via the same phase-80 path (finding 1). Bounds elsewhere: 10 → 3 designer rounds (`10-design.md:100-102`); 20 → 3 builder rounds (`20-build.md:143-146`); 30 → 4 rounds hard + 3 sub-agent retries per round (`30-review-loop.md:158-162`), `round = max(STATE.review_round,1)` (`:14`) with template `review_round: 0` (`STATE.template.json:10`); 40 → 3 dispatches (`40-release.md:125-128`); 50 → 3 per step, two failed triggers → 90 (`50-league.md:130-134`); 60 → 75 min wall clock + 3 per check (`60-verify.md:148-150,165-169`); 70 → 3 attempts (`70-announce.md:61-64`); resume counter → 90 at 3 markerless sessions (`00-claim.md:215-219`).

**(d) Can a secret print?** No path found. grep `echo|print|cat|log` × `TOKEN|_PAT|API_KEY|SECRET` over templates/prompts/fleet/agents/docs/AGENT.md/playbooks → only "is not set" errors (`coworld-release.yml:111,346`, `coworld-submit.yml:72`), presence notices without the value (`docker_smoke.sh:188,190`), and `printf '%s' "${ANTHROPIC_API_KEY}" > "$secret_file"` to a mode-600 tempfile that is deleted (`coworld-release.yml:349-354`). No `set -x` anywhere. `deploy.py --dry-run create` output: 0 hits for `ghp_|gho_|sk-ant-`, `"authorization_token": "<redacted>"` ×8. Prompts pass tokens by `$VAR` only (`00-claim.md:32,43`, `50-league.md:19`, `70-announce.md:22,39`); 90's scrub rule applies to CI log excerpts (`20-build.md:146-148`). Streamed CLI stderr now redacted (item 7).

**(e) Release/submit order and policy names.** Order in `coworld-release.yml`: Build `:147` → Certify `:161` (gated on `LIVENESS_MARKER`, `:69,172`) → Upload the policies `:200` → Upload the Coworld `:298` → Put the Coworld secret `:336` → Assemble `:358` → Enforce canonical `:492-502`. Matches SPEC §Phases row 40 and §Design pins ("policies before upload-coworld, secret put after"). Phase 40's triage table even names the two mis-orderings as failures to fix (`40-release.md:94-95`). Phase 50 submits `STATE.policies.champion1` (daveey, `50-league.md:52-59`) and `champion2` (daveey-1, `:66-78`) by label read from STATE with `jq`, never retyped; fillers resolved from `STATE.policies.fillers[]` by label match, champion rows excluded (`:91-101`), set **before** unpause+trigger (`:83`, `:102-105`). Names are the same `<name>:vN` labels phase 40 writes (`40-release.md:117-120`) from `release-result.json.policies[].{name,version}` (`coworld-release.yml:279-284`). Player ids `ply_44ae9048…`/`ply_bac48eb1…` match `fleet/cloud.md` §players and `policies.json.example:15`. No mismatch found.

**(f) Exit criteria vs SPEC §Phases.** Every prompt has `## Exit criterion`: 00 `:236-252` (a/b/c/c2/d) ↔ SPEC "task in Running, STATE written"; 10 `:88-92` ↔ "coordinator accepts the note against the checklist" (prompt: every checklist box checked in log.md); 20 `:90-103` ↔ "ci.yml green on main"; 30 `:145-150` ↔ "zero blocking findings (max 4 rounds; residue logged)"; 40 `:101-106` ↔ "Canonical: yes, hosted certification certified" (prompt adds the policy/secret checks SPEC §Design pins require); 50 `:115-119` ↔ "both champions entrants; round triggered"; 60 `:152-156` ↔ "definition-of-done all-true"; 70 `:50-52` ↔ "message posted, id in STATE"; 80 `:34-37` ↔ "—" (AGENT.md:59 "summaries, LEARNINGS entry, run Done, idea completed" — matches the prompt); 90 `:63-66` ↔ "run → Blocked, subtask → human, STATE.blocked". All present and consistent.

**(g) Mechanical.** `python3 -m py_compile fleet/bin/deploy.py` → OK. `python3 fleet/bin/deploy.py --dry-run create` → exit 0, ends "(dry run — nothing created, cloud.md untouched)". `yaml.safe_load` × 3 → OK (jobs `test,docker-smoke,wasm-viewer` / `release` / `submit`). `bash -n` on every `run:` block extracted with python: ci.yml 10/10, coworld-release.yml 11/11, coworld-submit.yml 5/5 OK. `bash -n templates/tools/ci/docker_smoke.sh` → OK.

## New findings

1. **BLOCKING (queue wedge)** — `prompts/80-close.md:47-50` + `prompts/00-claim.md:223-228` + `docs/SPEC.md:94-95`: a persistently failing close (idea task 404, run task moved, permissions) leaves the run in *Running* forever; step 2 adopts it on every heartbeat (`00-claim.md:52-55`), the phase-80 counter exemption means it never reaches 90, and step 4 never runs — no new idea is claimed while it sits there. *Fix (one line in 80-close.md, mirrored in 00-claim.md:223-228 and SPEC.md:94-95):* "After **3** `close-failed` heartbeats (count the `close-failed` comments / `80 close-failed` log lines), go to `prompts/90-blocked.md` with the exact Asana error — Blocked takes the task out of *Running* so the queue moves; the work is done, the human finishes the bookkeeping."

2. **MINOR** — `prompts/00-claim.md:164-165` (boxed rejected-push rule) and `:241-242` (exit criterion b) still adjudicate the resume race by "`log.md`'s **last** `00 resume` line", contradicting the corrected 5.0 step 3 at `:179-185`, `AGENT.md:26-29` and `SPEC.md:39-42`. *Fix:* at `:164-165` "on a **resume**, any `00 resume` line with a foreign nonce that was not there before your pull (or a rebase conflict) means that session owns the run → exit (step 5.0.3)"; at `:242` "… and `log.md` gained a foreign-nonce `00 resume` line, or the rebase conflicted, or the Asana …".

3. **MINOR** — `prompts/80-close.md:31` moves the run task to *Done* before completing the idea task; if the idea-task call then fails, the run task is no longer in *Running*, no heartbeat retries 80 (step 2 only scans *Running*), and the idea stays incomplete with no `close-failed` comment visible on the Builder board. *Fix:* reorder step 5 to "complete the idea task, then move the run task to *Done*" (the *Done* move is the last irreversible-looking step).

4. **MINOR** — `prompts/60-verify.md:127` greps `/tmp/*.js` for the wasm name **before** the loop at `:128-130` has fetched any `.js`; on a fresh session the glob is empty (literal `/tmp/*.js` → grep error). *Fix:* run the asset loop once over the `(src|href)` list first, then grep `/tmp/idx.html /tmp/*.js` for the `.wasm` and fetch it in a second pass.

BLOCKING: 1
